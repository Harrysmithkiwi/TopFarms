---
phase: 08-wizard-field-extensions
plan: 04
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/pages/onboarding/steps/SeekerStep1FarmType.tsx
  - src/pages/onboarding/steps/SeekerStep3Qualifications.tsx
  - src/pages/onboarding/steps/SeekerStep5LifeSituation.tsx
  - src/pages/onboarding/steps/SeekerStep7Complete.tsx
  - src/pages/onboarding/SeekerOnboarding.tsx
autonomous: true
requirements: [SONB-01, SONB-03, SONB-04, SONB-05, SONB-06, SONB-07]

must_haves:
  truths:
    - "Step 1 renders 6 sector chips (Dairy, Sheep & Beef, Cropping, Deer, Mixed, Other) in 2-column ChipSelector grid instead of 2 large cards"
    - "Step 3 includes NZ driver's licence chips (inline) and certification chips (inline: ATV, tractor, 4WD, first aid)"
    - "Step 5 includes minimum salary input, availability date input, notice period input"
    - "Step 5 housing sub-options use 6-item ChipSelector grid (2-column) instead of boolean checkboxes"
    - "Step 5 preferred regions uses 8-region multi-select ChipSelector (2-column) instead of single Select"
    - "Step 7 completion shows two-column layout with success checklist, CTAs, match loading state, and mini candidate preview"
    - "Seeker ChipSelector fields pre-populate from saved data when user re-opens wizard"
  artifacts:
    - path: "src/pages/onboarding/steps/SeekerStep1FarmType.tsx"
      provides: "6-sector ChipSelector grid"
      contains: "ChipSelector"
    - path: "src/pages/onboarding/steps/SeekerStep3Qualifications.tsx"
      provides: "Licence + certification chips"
      contains: "LICENCE_TYPE_OPTIONS"
    - path: "src/pages/onboarding/steps/SeekerStep5LifeSituation.tsx"
      provides: "Salary, availability, housing chips, region chips"
      contains: "HOUSING_SUB_OPTIONS"
    - path: "src/pages/onboarding/steps/SeekerStep7Complete.tsx"
      provides: "Two-column completion with match loading"
      contains: "calculating your matches"
    - path: "src/pages/onboarding/SeekerOnboarding.tsx"
      provides: "Shell passing new defaultValues + saving new fields"
      contains: "preferred_regions"
  key_links:
    - from: "src/pages/onboarding/steps/SeekerStep1FarmType.tsx"
      to: "src/types/domain.ts"
      via: "FARM_TYPE_OPTIONS import"
      pattern: "FARM_TYPE_OPTIONS"
    - from: "src/pages/onboarding/steps/SeekerStep5LifeSituation.tsx"
      to: "src/types/domain.ts"
      via: "HOUSING_SUB_OPTIONS, PREFERRED_REGION_OPTIONS imports"
      pattern: "HOUSING_SUB_OPTIONS"
    - from: "src/pages/onboarding/SeekerOnboarding.tsx"
      to: "src/types/domain.ts"
      via: "SeekerProfileData extended interface"
      pattern: "preferred_regions"
---

<objective>
Extend seeker onboarding steps 1, 3, 5, and rebuild step 7 with all SPEC v3.0 fields. Replace 2-card farm type selection with 6-option ChipSelector, add licence/certification chips, add salary/availability/housing sub-option chips/region multi-select, and rebuild completion screen as two-column layout with match loading state.

Purpose: Seeker onboarding captures the full SPEC field set. Covers SONB-01, SONB-03, SONB-04, SONB-05, SONB-06, SONB-07.
Output: 4 updated step components + updated seeker shell.
</objective>

<execution_context>
@/Users/harrysmith/.claude/get-shit-done/workflows/execute-plan.md
@/Users/harrysmith/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-wizard-field-extensions/08-CONTEXT.md
@.planning/phases/08-wizard-field-extensions/08-RESEARCH.md
@.planning/phases/08-wizard-field-extensions/08-UI-SPEC.md
@.planning/phases/08-wizard-field-extensions/08-01-SUMMARY.md

<interfaces>
From src/components/ui/ChipSelector.tsx:
```typescript
export interface ChipSelectorProps {
  options: ChipOption[]; value: string[]; onChange: (value: string[]) => void
  mode: 'single' | 'multi'; columns?: 1 | 2 | 3 | 'inline'; className?: string
}
```

From src/types/domain.ts (plan 01):
```typescript
export const FARM_TYPE_OPTIONS  // 6 farm types
export const LICENCE_TYPE_OPTIONS  // 4 NZ licence classes
export const CERTIFICATION_OPTIONS  // 6 certifications (ATV, tractor, 4WD, first aid, growsafe, chainsaw)
export const HOUSING_SUB_OPTIONS  // 6 options (Single, Couple working, etc.)
export const PREFERRED_REGION_OPTIONS  // 8 NZ regions

export interface SeekerProfileData {
  sector_pref?: string[]
  years_experience?: number
  shed_types_experienced?: ShedType[]
  herd_sizes_worked?: HerdSizeBucket[]
  dairynz_level?: DairyNZLevel
  region?: string
  open_to_relocate?: boolean
  accommodation_needed?: boolean
  housing_type_pref?: string
  pets?: { dogs?: boolean; cats?: boolean; other?: string }
  couples_seeking?: boolean
  family?: { has_children?: boolean; ages?: number[] }
  visa_status?: VisaStatus
  min_salary?: number
  availability_date?: string
  licence_types?: string[]
  certifications?: string[]
  housing_sub_options?: string[]
  preferred_regions?: string[]
  notice_period_text?: string
}
```

From src/pages/onboarding/steps/SeekerStep1FarmType.tsx:
```typescript
// Currently uses useState (NOT react-hook-form) — single-field step
// Keep useState pattern for simplicity per RESEARCH.md recommendation
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Steps 1, 3, 5 — Sector chips, licence/cert chips, salary/housing/region chips</name>
  <files>
    src/pages/onboarding/steps/SeekerStep1FarmType.tsx,
    src/pages/onboarding/steps/SeekerStep3Qualifications.tsx,
    src/pages/onboarding/steps/SeekerStep5LifeSituation.tsx,
    src/pages/onboarding/SeekerOnboarding.tsx
  </files>
  <read_first>
    src/pages/onboarding/steps/SeekerStep1FarmType.tsx,
    src/pages/onboarding/steps/SeekerStep3Qualifications.tsx,
    src/pages/onboarding/steps/SeekerStep5LifeSituation.tsx,
    src/pages/onboarding/SeekerOnboarding.tsx,
    src/components/ui/ChipSelector.tsx,
    src/components/ui/InfoBox.tsx,
    src/types/domain.ts
  </read_first>
  <action>
**SeekerStep1FarmType.tsx — SONB-01:**

1. Replace the 2-card layout with a ChipSelector. Keep the existing `useState` pattern (this step does NOT use react-hook-form, and that's fine for a single-field step).

2. Add import:
```typescript
import { ChipSelector } from '@/components/ui/ChipSelector'
import { FARM_TYPE_OPTIONS } from '@/types/domain'
```

3. Remove the `FARM_TYPES` constant (the 2-card definitions). Remove the large card button JSX.

4. Replace the card grid with ChipSelector:
```tsx
<form onSubmit={handleSubmit} className="space-y-6">
  <div>
    <h2 className="text-lg font-semibold" style={{ color: 'var(--color-ink)' }}>
      What type of farm work are you looking for?
    </h2>
    <p className="text-sm mt-1" style={{ color: 'var(--color-mid)' }}>
      Select all that apply — this helps us match you with relevant jobs
    </p>
  </div>

  <ChipSelector
    options={FARM_TYPE_OPTIONS}
    value={selectedTypes}
    onChange={setSelectedTypes}
    mode="multi"
    columns={2}
  />

  {selectedTypes.length === 0 && (
    <p className="text-[12px] font-body" style={{ color: 'var(--color-mid)' }}>
      Please select at least one farm type to continue
    </p>
  )}

  <div className="flex justify-end pt-2">
    <Button type="submit" variant="primary" size="md" disabled={selectedTypes.length === 0}>
      Continue
    </Button>
  </div>
</form>
```

5. Keep the same `onComplete({ sector_pref: selectedTypes })` call.

**SeekerStep3Qualifications.tsx — SONB-03:**

1. Read the current file. It currently has only a DairyNZ level select.

2. Add imports:
```typescript
import { ChipSelector } from '@/components/ui/ChipSelector'
import { LICENCE_TYPE_OPTIONS, CERTIFICATION_OPTIONS } from '@/types/domain'
```

3. Extend the zod schema (or add useState if the step uses useState):
```typescript
licence_types: z.array(z.string()).optional(),
certifications: z.array(z.string()).optional(),
```

4. Update defaultValues prop interface to accept `licence_types?: string[]` and `certifications?: string[]`.

5. Add two new ChipSelector sections after the existing DairyNZ level select:

NZ driver's licence (inline, multi):
```tsx
<div>
  <p className="font-body text-[13px] font-semibold text-ink mb-2">NZ driver's licence</p>
  <Controller control={control} name="licence_types" render={({ field }) => (
    <ChipSelector options={LICENCE_TYPE_OPTIONS} value={field.value ?? []} onChange={field.onChange} mode="multi" columns="inline" />
  )} />
</div>
```

Certifications (inline, multi):
```tsx
<div>
  <p className="font-body text-[13px] font-semibold text-ink mb-2">Certifications</p>
  <Controller control={control} name="certifications" render={({ field }) => (
    <ChipSelector options={CERTIFICATION_OPTIONS} value={field.value ?? []} onChange={field.onChange} mode="multi" columns="inline" />
  )} />
</div>
```

6. Pass `licence_types` and `certifications` in the `onComplete` call.

**SeekerStep5LifeSituation.tsx — SONB-04, SONB-05, SONB-06:**

1. Add imports:
```typescript
import { ChipSelector } from '@/components/ui/ChipSelector'
import { InfoBox } from '@/components/ui/InfoBox'
import { HOUSING_SUB_OPTIONS, PREFERRED_REGION_OPTIONS } from '@/types/domain'
```

2. Extend zod schema — replace boolean housing fields with chip arrays, add new fields:
```typescript
const schema = z.object({
  couples_seeking: z.boolean().optional(),
  partner_name: z.string().optional(),
  accommodation_needed: z.boolean().optional(),
  housing_sub_options: z.array(z.string()).optional(),
  preferred_regions: z.array(z.string()).optional(),
  min_salary: z.coerce.number().optional(),
  availability_date: z.string().optional(),
  notice_period_text: z.string().optional(),
})
```

Remove: `pets_dogs`, `family_has_children`, `vehicle_parking`, `region` from schema.

3. Update defaultValues prop to accept `housing_sub_options?: string[]`, `preferred_regions?: string[]`, `min_salary?: number`, `availability_date?: string`, `notice_period_text?: string`.

4. Update useForm defaultValues mapping:
```typescript
defaultValues: {
  couples_seeking: defaultValues?.couples_seeking ?? false,
  partner_name: '',
  accommodation_needed: defaultValues?.accommodation_needed ?? false,
  housing_sub_options: defaultValues?.housing_sub_options ?? [],
  preferred_regions: defaultValues?.preferred_regions ?? [],
  min_salary: defaultValues?.min_salary ?? undefined,
  availability_date: defaultValues?.availability_date ?? '',
  notice_period_text: defaultValues?.notice_period_text ?? '',
}
```

5. Replace the accommodation sub-section. Instead of 3 boolean checkboxes (pets_dogs, family_has_children, vehicle_parking), use a ChipSelector with HOUSING_SUB_OPTIONS:
```tsx
{accommodationOn && (
  <div className="space-y-3 pl-1">
    <p className="font-body text-[13px] font-semibold text-ink mb-2">Housing requirements</p>
    <Controller control={control} name="housing_sub_options" render={({ field }) => (
      <ChipSelector options={HOUSING_SUB_OPTIONS} value={field.value ?? []} onChange={field.onChange} mode="multi" columns={2} />
    )} />
  </div>
)}
```

6. Replace the single region Select with multi-select ChipSelector:
```tsx
<div>
  <p className="font-body text-[13px] font-semibold text-ink mb-2">Preferred regions</p>
  <p className="text-[12px] mb-2" style={{ color: 'var(--color-mid)' }}>Select all regions you'd work in</p>
  <Controller control={control} name="preferred_regions" render={({ field }) => (
    <ChipSelector options={PREFERRED_REGION_OPTIONS} value={field.value ?? []} onChange={field.onChange} mode="multi" columns={2} />
  )} />
</div>
```

7. Add new fields section — salary, availability, notice period (SONB-04):
```tsx
<div className="space-y-4">
  <Input label="Minimum salary expectation (NZD per annum)" type="number" placeholder="e.g. 55000" {...register('min_salary')} />

  <Input label="Available from" type="date" {...register('availability_date')} />

  <Controller control={control} name="notice_period_text" render={({ field }) => (
    <Select label="Notice period" placeholder="Select notice period" options={[
      { value: 'immediately', label: 'Available immediately' },
      { value: '1_week', label: '1 week' },
      { value: '2_weeks', label: '2 weeks' },
      { value: '1_month', label: '1 month' },
      { value: '2_months', label: '2+ months' },
    ]} value={field.value} onValueChange={field.onChange} />
  )} />

  <InfoBox variant="blue">
    Adding your availability helps employers plan — listings with dates get 30% more views
  </InfoBox>
</div>
```

8. Update the `onSubmit` function to map new fields:
```typescript
function onSubmit(data: FormData) {
  onComplete({
    couples_seeking: data.couples_seeking,
    accommodation_needed: data.accommodation_needed,
    housing_sub_options: data.housing_sub_options,
    preferred_regions: data.preferred_regions,
    min_salary: data.min_salary || undefined,
    availability_date: data.availability_date || undefined,
    notice_period_text: data.notice_period_text || undefined,
    // Keep region as first preferred region for backward compatibility
    region: data.preferred_regions?.[0] || undefined,
  })
}
```

Remove old mapping logic for `pets`, `family`.

**SeekerOnboarding.tsx shell:**

Read the file, then update it to:
1. Load new fields from DB: `licence_types`, `certifications`, `housing_sub_options`, `preferred_regions`, `notice_period_text` in the profile loading section.
2. Pass new fields as defaultValues to SeekerStep3Qualifications (licence_types, certifications) and SeekerStep5LifeSituation (housing_sub_options, preferred_regions, min_salary, availability_date, notice_period_text).
3. Include new fields in the upsert payload. If using spread like employer onboarding, this happens automatically. If explicit field listing, add the new fields.
  </action>
  <verify>
    <automated>cd /Users/harrysmith/dev/topfarms && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - SeekerStep1FarmType.tsx contains `import { ChipSelector }`
    - SeekerStep1FarmType.tsx contains `import { FARM_TYPE_OPTIONS }`
    - SeekerStep1FarmType.tsx contains `<ChipSelector` with `columns={2}`
    - SeekerStep1FarmType.tsx does NOT contain the old `FARM_TYPES` constant with `icon: '...'`
    - SeekerStep3Qualifications.tsx contains `LICENCE_TYPE_OPTIONS` import
    - SeekerStep3Qualifications.tsx contains `CERTIFICATION_OPTIONS` import
    - SeekerStep3Qualifications.tsx contains `<ChipSelector` for licence_types with `columns="inline"`
    - SeekerStep3Qualifications.tsx contains `<ChipSelector` for certifications with `columns="inline"`
    - SeekerStep5LifeSituation.tsx contains `HOUSING_SUB_OPTIONS` import
    - SeekerStep5LifeSituation.tsx contains `PREFERRED_REGION_OPTIONS` import
    - SeekerStep5LifeSituation.tsx does NOT contain `pets_dogs` or `family_has_children` or `vehicle_parking` in schema
    - SeekerStep5LifeSituation.tsx contains `housing_sub_options: z.array(z.string())`
    - SeekerStep5LifeSituation.tsx contains `preferred_regions: z.array(z.string())`
    - SeekerStep5LifeSituation.tsx contains `min_salary` input field
    - SeekerStep5LifeSituation.tsx contains `availability_date` input field
    - SeekerStep5LifeSituation.tsx contains `InfoBox variant="blue"` with "30% more views"
    - SeekerOnboarding.tsx contains `preferred_regions`
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>Step 1 renders 6-sector ChipSelector. Step 3 has licence + certification chips. Step 5 has salary, availability, notice period, housing sub-option chips, and multi-region chips. Shell loads and saves all new fields.</done>
</task>

<task type="auto">
  <name>Task 2: Step 7 — Two-column seeker completion screen</name>
  <files>
    src/pages/onboarding/steps/SeekerStep7Complete.tsx,
    src/pages/onboarding/SeekerOnboarding.tsx
  </files>
  <read_first>
    src/pages/onboarding/steps/SeekerStep7Complete.tsx,
    src/pages/onboarding/SeekerOnboarding.tsx,
    src/components/ui/Button.tsx
  </read_first>
  <action>
Rebuild `SeekerStep7Complete.tsx` to implement the SONB-07 two-column completion screen per CONTEXT.md decisions and UI-SPEC.

**Full component structure:**

```tsx
import { useNavigate } from 'react-router'
import { CheckCircle } from 'lucide-react'
import { Button } from '@/components/ui/Button'
import type { SeekerProfileData } from '@/types/domain'

interface SeekerStep7CompleteProps {
  profileData?: Partial<SeekerProfileData>
}

const CHECKLIST_ITEMS = [
  { label: 'Farm type preferences set', key: 'sector_pref' },
  { label: 'Experience added', key: 'years_experience' },
  { label: 'Qualifications complete', key: 'dairynz_level' },
  { label: 'Life situation details added', key: 'accommodation_needed' },
]

export function SeekerStep7Complete({ profileData }: SeekerStep7CompleteProps) {
  const navigate = useNavigate()

  function isItemComplete(key: string): boolean {
    if (!profileData) return false
    const val = profileData[key as keyof SeekerProfileData]
    if (Array.isArray(val)) return val.length > 0
    return val !== undefined && val !== null && val !== ''
  }

  return (
    <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
      {/* Left column — success + checklist + match loading + CTAs */}
      <div className="space-y-6">
        {/* Success icon + heading */}
        <div className="flex items-center gap-3">
          <div className="w-12 h-12 rounded-full flex items-center justify-center" style={{ backgroundColor: 'var(--color-hay-lt)' }}>
            <CheckCircle className="w-6 h-6" style={{ color: 'var(--color-fern)' }} />
          </div>
          <div>
            <h2 className="font-display text-2xl font-semibold" style={{ color: 'var(--color-soil)' }}>
              Your profile is ready!
            </h2>
            <p className="text-[16px] font-body mt-1" style={{ color: 'var(--color-mid)' }}>
              We're finding your best matches
            </p>
          </div>
        </div>

        {/* Profile completeness checklist */}
        <div className="space-y-3">
          {CHECKLIST_ITEMS.map((item) => {
            const complete = isItemComplete(item.key)
            return (
              <div key={item.key} className="flex items-center gap-3">
                <div className={`w-5 h-5 rounded-full flex items-center justify-center ${complete ? 'bg-fern' : 'bg-fog'}`}>
                  {complete && (
                    <svg className="w-3 h-3 text-white" viewBox="0 0 12 12" fill="none" stroke="currentColor" strokeWidth="2.5">
                      <path d="M2 6l3 3 5-5" />
                    </svg>
                  )}
                </div>
                <span className="text-[13px] font-body" style={{ color: complete ? 'var(--color-ink)' : 'var(--color-light)' }}>
                  {item.label}
                </span>
              </div>
            )
          })}
        </div>

        {/* Match pool loading state (Phase 11 wires real data) */}
        <div className="p-4 rounded-[10px] border border-fog bg-mist">
          <p className="text-[16px] font-semibold font-body" style={{ color: 'var(--color-ink)' }}>Your matches</p>
          <div className="flex items-center gap-2 mt-2">
            <div
              className="w-5 h-5 rounded-full border-[2px] border-t-transparent animate-spin"
              style={{ borderColor: 'var(--color-fern)', borderTopColor: 'transparent' }}
            />
            <p className="text-[13px] font-body" style={{ color: 'var(--color-mid)' }}>
              We're calculating your matches
            </p>
          </div>
        </div>

        {/* CTAs */}
        <div className="space-y-3">
          <Button variant="primary" size="lg" className="w-full" onClick={() => navigate('/jobs')}>
            Browse Jobs
          </Button>
          <Button variant="outline" size="lg" className="w-full" onClick={() => navigate('/onboarding/seeker')}>
            Edit Profile
          </Button>
        </div>
      </div>

      {/* Right column — mini candidate card preview */}
      <div className="hidden md:block">
        <div className="rounded-[14px] border border-fog overflow-hidden bg-white">
          <div className="p-4 space-y-3">
            <div className="flex items-center gap-3">
              <div className="w-10 h-10 rounded-full flex items-center justify-center" style={{ backgroundColor: 'var(--color-mist)' }}>
                <span className="text-[16px] font-semibold font-body" style={{ color: 'var(--color-moss)' }}>
                  {/* First initial */}
                  S
                </span>
              </div>
              <div>
                <p className="text-[16px] font-semibold font-body" style={{ color: 'var(--color-ink)' }}>Seeker Profile</p>
                <p className="text-[13px] font-body" style={{ color: 'var(--color-light)' }}>
                  {profileData?.region ?? 'Location not set'}
                </p>
              </div>
            </div>

            {/* Sector tags */}
            {profileData?.sector_pref && profileData.sector_pref.length > 0 && (
              <div className="flex flex-wrap gap-1.5">
                {profileData.sector_pref.map((s) => (
                  <span key={s} className="px-2 py-0.5 text-[11px] rounded-full border border-fog font-body" style={{ color: 'var(--color-mid)' }}>
                    {s}
                  </span>
                ))}
              </div>
            )}

            {/* Experience */}
            {profileData?.years_experience !== undefined && (
              <p className="text-[13px] font-body" style={{ color: 'var(--color-mid)' }}>
                {profileData.years_experience}+ years experience
              </p>
            )}

            {/* Accommodation badge */}
            {profileData?.accommodation_needed && (
              <span className="inline-block px-2 py-0.5 text-[11px] rounded-full border border-fern font-body" style={{ color: 'var(--color-fern)' }}>
                Needs accommodation
              </span>
            )}

            {/* Visa */}
            {profileData?.visa_status && (
              <p className="text-[12px] font-body" style={{ color: 'var(--color-light)' }}>
                {profileData.visa_status.replace(/_/g, ' ')}
              </p>
            )}
          </div>
        </div>
      </div>
    </div>
  )
}
```

**Update SeekerOnboarding.tsx:**

Pass `profileData` to `SeekerStep7Complete` in the render section. Find where step 7 is rendered (the completion step) and change from `<SeekerStep7Complete />` to:

```tsx
<SeekerStep7Complete profileData={{
  sector_pref: seekerData.sector_pref,
  years_experience: seekerData.years_experience,
  dairynz_level: seekerData.dairynz_level,
  accommodation_needed: seekerData.accommodation_needed,
  region: seekerData.preferred_regions?.[0] ?? seekerData.region,
  visa_status: seekerData.visa_status,
}} />
```

(Use whatever the shell's accumulated state variable name is — `seekerData`, `profileData`, etc.)

Also ensure the shell's navigate-on-complete logic doesn't bypass the completion screen. The completion screen handles its own navigation via CTAs. If the shell currently navigates away on the final step, remove that navigation and let the completion screen stay visible.
  </action>
  <verify>
    <automated>cd /Users/harrysmith/dev/topfarms && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - SeekerStep7Complete.tsx contains `grid grid-cols-1 md:grid-cols-2`
    - SeekerStep7Complete.tsx contains `Your profile is ready!`
    - SeekerStep7Complete.tsx contains `We're finding your best matches`
    - SeekerStep7Complete.tsx contains `Browse Jobs` button
    - SeekerStep7Complete.tsx contains `Edit Profile` button
    - SeekerStep7Complete.tsx contains `We're calculating your matches`
    - SeekerStep7Complete.tsx contains `animate-spin` for loading spinner
    - SeekerStep7Complete.tsx contains `hidden md:block` for right column
    - SeekerStep7Complete.tsx contains `Farm type preferences set` checklist item
    - SeekerStep7Complete.tsx contains `profileData?.sector_pref`
    - SeekerOnboarding.tsx passes `profileData` prop to SeekerStep7Complete
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>Step 7 completion screen renders two-column layout: left has success icon + checklist + match loading spinner + 2 CTAs (Browse Jobs, Edit Profile), right has mini candidate card preview. Single column on mobile. Match pool shows static "calculating" state for Phase 11 wiring.</done>
</task>

</tasks>

<verification>
- All seeker onboarding steps compile: `npx tsc --noEmit`
- Step 1 has 6-option ChipSelector (2-column) instead of 2 large cards
- Step 3 has licence chips (inline) + certification chips (inline)
- Step 5 has salary, availability date, notice period, housing sub-options ChipSelector (2-col), preferred regions ChipSelector (2-col)
- Step 7 has two-column completion with checklist + match loading + CTAs + candidate preview
- Shell passes new fields as defaultValues and saves them to Supabase
</verification>

<success_criteria>
- TypeScript compiles cleanly
- No boolean checkbox usage remains in step 5 housing sub-options
- Single Select for region replaced by multi-select ChipSelector
- All 6 SONB requirements addressed (SONB-02 deferred to Phase 11)
- Completion screen uses UI-SPEC copy exactly
</success_criteria>

<output>
After completion, create `.planning/phases/08-wizard-field-extensions/08-04-SUMMARY.md`
</output>
