---
phase: 08-wizard-field-extensions
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/014_ownership_type_array.sql
  - src/pages/onboarding/EmployerOnboarding.tsx
autonomous: true
requirements: [EONB-01]
gap_closure: true

must_haves:
  truths:
    - "Employer onboarding step 2 saves ownership_type as a text array to Supabase without errors"
    - "Re-opening the wizard after saving shows previously selected ownership chips pre-selected"
  artifacts:
    - path: "supabase/migrations/014_ownership_type_array.sql"
      provides: "ALTER COLUMN ownership_type from text to text[]"
      contains: "ALTER COLUMN ownership_type TYPE text"
    - path: "src/pages/onboarding/EmployerOnboarding.tsx"
      provides: "EmployerProfileData.ownership_type typed as string[]"
      contains: "ownership_type?: string[]"
  key_links:
    - from: "src/pages/onboarding/steps/Step2FarmDetails.tsx"
      to: "employer_profiles.ownership_type (DB)"
      via: "EmployerOnboarding.tsx upsert"
      pattern: "ownership_type.*data\\.ownership_type"
---

<objective>
Fix ownership_type column type mismatch that causes data loss on employer onboarding step 2.

Purpose: Step2FarmDetails uses a multi-select ChipSelector for ownership_type, which outputs string[]. But the DB column is text (not text[]) and EmployerProfileData types it as string (not string[]). This causes a Supabase runtime error when saving step 2.

Output: A new migration converting the column to text[], and an updated TypeScript interface matching the array type.
</objective>

<execution_context>
@/Users/harrysmith/.claude/get-shit-done/workflows/execute-plan.md
@/Users/harrysmith/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-wizard-field-extensions/08-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration to convert ownership_type from text to text[]</name>
  <files>supabase/migrations/014_ownership_type_array.sql</files>
  <read_first>
    supabase/migrations/001_initial_schema.sql
    supabase/migrations/013_phase8_wizard_fields.sql
  </read_first>
  <action>
Create a new SQL migration file `supabase/migrations/014_ownership_type_array.sql` with:

```sql
-- ============================================================
-- 014_ownership_type_array.sql
-- Fix ownership_type column: text -> text[] to support ChipSelector multi-select
-- ============================================================

ALTER TABLE public.employer_profiles
  ALTER COLUMN ownership_type TYPE text[]
  USING CASE
    WHEN ownership_type IS NULL THEN NULL
    ELSE ARRAY[ownership_type]
  END;
```

The USING clause converts any existing single text value (e.g., "Owner Operator") into a single-element array (e.g., {"Owner Operator"}), and preserves NULL as NULL. This matches the pattern used in 013 for the boolean-to-array accommodation migration.
  </action>
  <verify>
    <automated>grep -c "ALTER COLUMN ownership_type TYPE text" supabase/migrations/014_ownership_type_array.sql</automated>
  </verify>
  <acceptance_criteria>
    - File supabase/migrations/014_ownership_type_array.sql exists
    - File contains "ALTER COLUMN ownership_type TYPE text[]"
    - File contains "USING CASE" for safe conversion of existing text values
    - File contains "ARRAY[ownership_type]" to wrap existing values
  </acceptance_criteria>
  <done>Migration file exists that converts employer_profiles.ownership_type from text to text[] with backward-compatible USING clause</done>
</task>

<task type="auto">
  <name>Task 2: Update EmployerProfileData.ownership_type type from string to string[]</name>
  <files>src/pages/onboarding/EmployerOnboarding.tsx</files>
  <read_first>
    src/pages/onboarding/EmployerOnboarding.tsx
    src/pages/onboarding/steps/Step2FarmDetails.tsx
  </read_first>
  <action>
In `src/pages/onboarding/EmployerOnboarding.tsx`, find the `EmployerProfileData` interface and change:

```typescript
ownership_type?: string
```

to:

```typescript
ownership_type?: string[]
```

This is the only change needed in this file. The upsert payload already spreads `data.ownership_type` directly (line ~108), so it will correctly pass string[] to the now-text[] DB column. The load path reads from Supabase which will return string[] from a text[] column. Step2FarmDetails.tsx already handles backward compat via `Array.isArray()` check on defaultValues (line ~70), so existing profiles with the migrated single-element array will display correctly.

No changes to Step2FarmDetails.tsx are needed — it already outputs string[] and handles both string and string[] input via its defaultValues typing.
  </action>
  <verify>
    <automated>grep "ownership_type?: string\[\]" src/pages/onboarding/EmployerOnboarding.tsx && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - EmployerOnboarding.tsx contains `ownership_type?: string[]` (not `ownership_type?: string`)
    - `npx tsc --noEmit` exits with 0 errors
    - No other files modified — Step2FarmDetails.tsx already handles both string and string[] input
  </acceptance_criteria>
  <done>EmployerProfileData.ownership_type is typed as string[], TypeScript compiles cleanly, and the full save path (ChipSelector -> onComplete -> handleStepComplete -> Supabase upsert) sends string[] to a text[] column</done>
</task>

</tasks>

<verification>
1. `grep "ownership_type?: string\[\]" src/pages/onboarding/EmployerOnboarding.tsx` returns a match
2. `grep "ALTER COLUMN ownership_type TYPE text" supabase/migrations/014_ownership_type_array.sql` returns a match
3. `npx tsc --noEmit` exits cleanly with no errors
4. No references to `ownership_type?: string` (without brackets) remain in EmployerOnboarding.tsx
</verification>

<success_criteria>
- ownership_type DB column will be text[] after migration runs
- EmployerProfileData.ownership_type is string[]
- TypeScript compiles without errors
- Step 2 ChipSelector string[] output matches DB column type and TypeScript interface
- Existing single-value ownership_type data is preserved via ARRAY[ownership_type] USING clause
</success_criteria>

<output>
After completion, create `.planning/phases/08-wizard-field-extensions/08-05-SUMMARY.md`
</output>
