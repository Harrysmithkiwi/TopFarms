---
phase: 10-landing-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/components/landing/HeroSection.tsx
  - src/components/landing/CountersSection.tsx
  - src/components/landing/FeaturedListings.tsx
  - src/pages/Home.tsx
  - tests/landing-page.test.tsx
  - tests/setup.ts
autonomous: true
requirements: [LAND-01, LAND-02, LAND-06, LAND-10]

must_haves:
  truths:
    - "Hero headline animates in with staggered fadeUp on page load — each line appears sequentially with 150-200ms delay"
    - "Live counter strip shows a pulsing green Live dot badge"
    - "Featured job listing cards display a match score circle in the top-right corner"
    - "CTA fork card has 14px border radius and correct SPEC button text"
  artifacts:
    - path: "src/components/landing/HeroSection.tsx"
      provides: "Staggered hero animation + CTA fork radius fix"
      contains: "motion/react"
    - path: "src/components/landing/CountersSection.tsx"
      provides: "Live badge with pulsing dot"
      contains: "animate-pulse"
    - path: "src/components/landing/FeaturedListings.tsx"
      provides: "Match score circles on featured job cards"
      contains: "matchScore"
  key_links:
    - from: "package.json"
      to: "src/components/landing/HeroSection.tsx"
      via: "motion dependency"
      pattern: "motion/react"
---

<objective>
Install the motion library, add staggered fadeUp animation to the hero headline, add pulsing Live badge to the counter strip, add match score circles to featured listing cards, and fix the CTA fork border radius to 14px.

Purpose: These are modifications to existing landing page components that bring them to SPEC compliance — hero animation, live indicator, match scores, and CTA styling.
Output: Updated HeroSection, CountersSection, FeaturedListings with SPEC-compliant enhancements; motion library installed.
</objective>

<execution_context>
@/Users/harrysmith/.claude/get-shit-done/workflows/execute-plan.md
@/Users/harrysmith/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-landing-page/10-CONTEXT.md
@.planning/phases/10-landing-page/10-RESEARCH.md

@src/pages/Home.tsx
@src/components/landing/HeroSection.tsx
@src/components/landing/CountersSection.tsx
@src/components/landing/FeaturedListings.tsx
@tests/landing-page.test.tsx
@tests/setup.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install motion library and add hero stagger animation + CTA fork fix</name>
  <files>package.json, src/components/landing/HeroSection.tsx, tests/setup.ts</files>
  <read_first>
    - src/components/landing/HeroSection.tsx
    - package.json
    - tests/setup.ts
    - tests/landing-page.test.tsx
  </read_first>
  <action>
1. Run `npm install motion` to add the motion library.

2. In `src/components/landing/HeroSection.tsx`:
   - Add `import { motion } from 'motion/react'` at the top.
   - Define container variants: `{ hidden: {}, show: { transition: { staggerChildren: 0.18 } } }`.
   - Define line variants: `{ hidden: { opacity: 0, y: 24 }, show: { opacity: 1, y: 0, transition: { duration: 0.55, ease: [0.25, 0.46, 0.45, 0.94] } } }`.
   - Replace the static `<h1>` headline with `<motion.h1 variants={containerVariants} initial="hidden" animate="show">` wrapping each text line in `<motion.span variants={lineVariants} className="block">`.
   - The headline text lines should be: Line 1: "Where New Zealand's", Line 2: "Best Farms" (styled italic, hay color), Line 3: "Find Their Next Team". Match existing SPEC copy exactly — read the current h1 text first and preserve it.
   - Fix CTA fork wrapper: remove `rounded-2xl` from className, add `style={{ borderRadius: '14px' }}` (merge with existing style prop if present). Keep existing `borderColor` style.
   - Ensure CTA fork button text matches SPEC copy. Read current button text and preserve if already correct.

3. In `tests/setup.ts`: Add a motion/react mock IF the existing tests fail after motion install. The mock should strip motion-specific props (initial, animate, whileInView, variants, viewport, transition) and render the underlying HTML element:
   ```typescript
   vi.mock('motion/react', async () => {
     const React = await import('react')
     return {
       motion: new Proxy({}, {
         get: (_, tag) => React.forwardRef((props: any, ref: any) => {
           const { initial, animate, whileInView, variants, viewport, transition, whileHover, whileTap, ...rest } = props
           return React.createElement(tag as string, { ...rest, ref })
         })
       })
     }
   })
   ```
   Run `npx vitest tests/landing-page.test.tsx --run` after install to verify. Only add mock if tests fail.

4. Run `npx vitest tests/landing-page.test.tsx --run` to confirm existing tests still pass.
  </action>
  <verify>
    <automated>cd /Users/harrysmith/dev/topfarms && npx vitest tests/landing-page.test.tsx --run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "motion/react" src/components/landing/HeroSection.tsx` succeeds
    - `grep -q "staggerChildren" src/components/landing/HeroSection.tsx` succeeds
    - `grep -q "borderRadius.*14px" src/components/landing/HeroSection.tsx` succeeds
    - `grep -qv "rounded-2xl" src/components/landing/HeroSection.tsx` — no rounded-2xl on CTA fork wrapper
    - `grep -q '"motion"' package.json` succeeds
    - `npx vitest tests/landing-page.test.tsx --run` passes
  </acceptance_criteria>
  <done>Hero headline uses motion stagger animation with 180ms delay between lines; CTA fork has 14px border radius; motion library installed; tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add Live counter badge and featured listing match scores</name>
  <files>src/components/landing/CountersSection.tsx, src/components/landing/FeaturedListings.tsx, tests/landing-page.test.tsx</files>
  <read_first>
    - src/components/landing/CountersSection.tsx
    - src/components/landing/FeaturedListings.tsx
    - src/components/landing/HeroSection.tsx (for match score circle pattern reference)
    - tests/landing-page.test.tsx
  </read_first>
  <action>
1. In `src/components/landing/CountersSection.tsx`:
   - Add a "Live" badge element positioned centered above the counters grid (inside the section, before the counters).
   - Badge markup: `<div className="flex justify-center mb-4"><div className="inline-flex items-center gap-2 px-3 py-1.5 rounded-full border text-xs font-semibold tracking-widest uppercase" style={{ borderColor: 'rgba(122,175,63,0.3)', backgroundColor: 'rgba(122,175,63,0.08)', color: 'var(--color-meadow)' }}><span className="w-2 h-2 rounded-full animate-pulse" style={{ backgroundColor: 'var(--color-meadow)' }} />Live</div></div>`.
   - No motion library import needed — uses Tailwind `animate-pulse` only.

2. In `src/components/landing/FeaturedListings.tsx`:
   - Add a match score circle to each featured job card. Position it in the top-right corner of the card using `absolute top-3 right-3` positioning (card needs `relative` if not already).
   - Match score circle markup (reuse HeroSection pattern): `<div className="absolute top-3 right-3 w-12 h-12 rounded-full flex items-center justify-center text-sm font-bold" style={{ backgroundColor: 'rgba(122,175,63,0.2)', color: 'var(--color-meadow)', border: '2px solid var(--color-meadow)' }}>{score}%</div>`.
   - Use static mock scores: assign different scores per card (e.g., 94, 87, 91, 82). Define as a constant array `const MOCK_MATCH_SCORES = [94, 87, 91, 82]` and index by card position.
   - If the card currently has a tier badge in a similar position, shift tier badge down or remove it in favor of match score.

3. In `tests/landing-page.test.tsx`:
   - Add test: "CountersSection renders Live badge" — assert text "Live" is present and an element with `animate-pulse` class exists.
   - Add test: "FeaturedListings renders match score circles" — assert that percentage text (e.g., "94%") is rendered.

4. Run `npx vitest tests/landing-page.test.tsx --run`.
  </action>
  <verify>
    <automated>cd /Users/harrysmith/dev/topfarms && npx vitest tests/landing-page.test.tsx --run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "animate-pulse" src/components/landing/CountersSection.tsx` succeeds
    - `grep -q "Live" src/components/landing/CountersSection.tsx` succeeds
    - `grep -q "MOCK_MATCH_SCORES\|matchScore\|match.*score" src/components/landing/FeaturedListings.tsx` succeeds
    - `grep -q "rgba(122,175,63" src/components/landing/FeaturedListings.tsx` succeeds
    - `npx vitest tests/landing-page.test.tsx --run` passes
  </acceptance_criteria>
  <done>Counter strip shows pulsing green Live badge; featured listing cards show match score circles with mock percentages; tests pass.</done>
</task>

</tasks>

<verification>
- `npm ls motion` shows motion installed
- `npx vitest tests/landing-page.test.tsx --run` — all tests pass
- HeroSection imports from motion/react and uses stagger animation
- CTA fork uses borderRadius 14px inline style
- CountersSection shows Live badge with animate-pulse dot
- FeaturedListings cards show match score circles
</verification>

<success_criteria>
- Hero headline animates with staggered fadeUp (motion/react variants + staggerChildren: 0.18)
- Live counter badge renders with pulsing green dot
- Featured listing cards show match score circles
- CTA fork border radius is 14px
- All existing and new tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/10-landing-page/10-01-SUMMARY.md`
</output>
