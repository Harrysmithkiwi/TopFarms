---
phase: 10-landing-page
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/landing/AIMatchingSection.tsx
  - src/components/landing/FarmTypesStrip.tsx
  - src/components/landing/EmployerCTABand.tsx
  - src/pages/Home.tsx
  - tests/landing-page.test.tsx
autonomous: true
requirements: [LAND-04, LAND-05, LAND-07]

must_haves:
  truths:
    - "AI matching section renders a mock browser window with 4 feature bullet points and scroll-reveal animation"
    - "Farm types strip shows 5 sector cards (Dairy, Sheep & Beef, Horticulture, Viticulture, Arable) with listing counts"
    - "Employer CTA band shows mini dashboard preview with 4-point checklist and Post your first job button"
  artifacts:
    - path: "src/components/landing/AIMatchingSection.tsx"
      provides: "AI matching features section"
      contains: "mock browser window"
    - path: "src/components/landing/FarmTypesStrip.tsx"
      provides: "5 sector cards with counts"
      contains: "Dairy"
    - path: "src/components/landing/EmployerCTABand.tsx"
      provides: "Employer CTA with checklist"
      contains: "Post your first job"
  key_links:
    - from: "src/pages/Home.tsx"
      to: "src/components/landing/AIMatchingSection.tsx"
      via: "import and render"
      pattern: "AIMatchingSection"
    - from: "src/pages/Home.tsx"
      to: "src/components/landing/FarmTypesStrip.tsx"
      via: "import and render"
      pattern: "FarmTypesStrip"
    - from: "src/pages/Home.tsx"
      to: "src/components/landing/EmployerCTABand.tsx"
      via: "import and render"
      pattern: "EmployerCTABand"
---

<objective>
Create three new landing page sections: AI Matching (mock browser window with feature bullets), Farm Types Strip (5 sector cards), and Employer CTA Band (mini dashboard + checklist). Wire them into Home.tsx in the correct section order.

Purpose: These mid-funnel sections explain the product value (AI matching), showcase breadth (farm types), and convert employers (CTA band).
Output: Three new section components wired into Home.tsx.
</objective>

<execution_context>
@/Users/harrysmith/.claude/get-shit-done/workflows/execute-plan.md
@/Users/harrysmith/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-landing-page/10-CONTEXT.md
@.planning/phases/10-landing-page/10-RESEARCH.md

@src/pages/Home.tsx
@src/components/landing/HowItWorksSection.tsx (section pattern reference)
@src/components/landing/TestimonialsSection.tsx (dark section pattern reference)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AIMatchingSection, FarmTypesStrip, and EmployerCTABand components</name>
  <files>src/components/landing/AIMatchingSection.tsx, src/components/landing/FarmTypesStrip.tsx, src/components/landing/EmployerCTABand.tsx</files>
  <read_first>
    - src/components/landing/HowItWorksSection.tsx (eyebrow + heading + grid pattern)
    - src/components/landing/TestimonialsSection.tsx (dark section styling pattern)
    - src/components/landing/HeroSection.tsx (CTA button styling pattern)
    - src/pages/Home.tsx
  </read_first>
  <action>
1. Create `src/components/landing/AIMatchingSection.tsx`:
   - Import `motion` from `motion/react` for scroll-reveal animation.
   - Cream background section (`var(--color-cream)`), standard layout: `py-20 px-4`, `max-w-6xl mx-auto`.
   - Eyebrow badge: meadow line + "AI-Powered Matching" uppercase text (same pattern as HowItWorksSection).
   - Heading: "Smart Matching That Understands Agriculture" in soil color, `font-display font-bold text-4xl md:text-5xl`.
   - Two-column layout on desktop (`grid md:grid-cols-2 gap-12 items-center`):
     - Left: 4 feature bullet points with meadow-colored check icons (lucide `Check`):
       1. "Skills-based matching across dairy, livestock & horticulture"
       2. "Location and accommodation preference alignment"
       3. "Experience level and qualification verification"
       4. "Real-time match scoring updated as you complete your profile"
     - Right: Mock browser window with rounded-xl corners, shadow-2xl, fake traffic lights (red/yellow/green dots), address bar showing "topfarms.co.nz/match", and a content area showing a simplified match results preview (static decorative content — a heading "Your Top Matches" and 2-3 fake match rows with score badges).
   - Wrap the inner `max-w-6xl` div in `motion.div` with `initial={{ opacity: 0, y: 32 }}`, `whileInView={{ opacity: 1, y: 0 }}`, `viewport={{ once: true, amount: 0.15 }}`, `transition={{ duration: 0.55, ease: 'easeOut' }}`.
   - Export as named export `AIMatchingSection`.

2. Create `src/components/landing/FarmTypesStrip.tsx`:
   - White/fog background section (`var(--color-white)`), standard layout.
   - Eyebrow: "Farm Sectors" + heading: "Opportunities Across Every Sector".
   - 5 sector cards in a responsive grid: `grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-5 gap-4`.
   - Cards data (hardcoded): `[{ name: 'Dairy', count: 12, icon: '🐄' }, { name: 'Sheep & Beef', count: 8, icon: '🐑' }, { name: 'Horticulture', count: 5, icon: '🌱' }, { name: 'Viticulture', count: 3, icon: '🍇' }, { name: 'Arable', count: 4, icon: '🌾' }]`.
   - Each card: white bg, fog border, rounded-xl, p-6 text-center, sector name in soil bold, count in mid color ("12 listings"), hover shadow transition.
   - On mobile (below sm): horizontal scroll via `flex overflow-x-auto gap-4 snap-x` instead of grid. Use a wrapper div that switches layout: `className="flex overflow-x-auto snap-x sm:grid sm:grid-cols-3 lg:grid-cols-5 gap-4"` with each card having `snap-center min-w-[160px] sm:min-w-0`.
   - Wrap in motion.div scroll-reveal (same pattern as AIMatchingSection).
   - Export as named export `FarmTypesStrip`.

3. Create `src/components/landing/EmployerCTABand.tsx`:
   - Dark background: `var(--color-soil)`, cream text. Standard layout.
   - Two-column grid on desktop (`md:grid-cols-2 gap-12 items-center`):
     - Left column:
       - Eyebrow: "For Employers" in meadow.
       - Heading: "Find Your Next Team Member" in cream, font-display font-bold text-4xl.
       - 4-point checklist with meadow Check icons:
         1. "Post a job in under 5 minutes"
         2. "AI-matched candidates delivered to your dashboard"
         3. "Track applications and schedule interviews"
         4. "Only pay when you find the right person"
       - CTA button: "Post Your First Job" — meadow bg, soil text, rounded-full, px-8 py-3, font-semibold. Links to `/employer/onboarding`.
     - Right column: Mini dashboard mockup card — static decorative card with white bg, rounded-xl, shadow, showing a simplified dashboard preview (fake heading "Your Dashboard", 2-3 mini stat boxes, a fake applicant row). This is purely visual — not interactive.
   - Wrap in motion.div scroll-reveal.
   - Export as named export `EmployerCTABand`.
  </action>
  <verify>
    <automated>cd /Users/harrysmith/dev/topfarms && ls src/components/landing/AIMatchingSection.tsx src/components/landing/FarmTypesStrip.tsx src/components/landing/EmployerCTABand.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "AI-Powered Matching\|Smart Matching" src/components/landing/AIMatchingSection.tsx` succeeds
    - `grep -q "topfarms.co.nz/match" src/components/landing/AIMatchingSection.tsx` succeeds
    - `grep -q "Dairy" src/components/landing/FarmTypesStrip.tsx` succeeds
    - `grep -q "Viticulture" src/components/landing/FarmTypesStrip.tsx` succeeds
    - `grep -q "Post Your First Job" src/components/landing/EmployerCTABand.tsx` succeeds
    - `grep -q "whileInView" src/components/landing/AIMatchingSection.tsx` succeeds
  </acceptance_criteria>
  <done>All three new section components created with correct content, styling, and scroll-reveal animation.</done>
</task>

<task type="auto">
  <name>Task 2: Wire new sections into Home.tsx and add tests</name>
  <files>src/pages/Home.tsx, tests/landing-page.test.tsx</files>
  <read_first>
    - src/pages/Home.tsx
    - tests/landing-page.test.tsx
    - src/components/landing/AIMatchingSection.tsx
    - src/components/landing/FarmTypesStrip.tsx
    - src/components/landing/EmployerCTABand.tsx
  </read_first>
  <action>
1. Update `src/pages/Home.tsx`:
   - Add imports for the three new sections: `AIMatchingSection`, `FarmTypesStrip`, `EmployerCTABand`.
   - Insert sections in the SPEC order. After this task, Home.tsx section order MUST be:
     ```
     Nav
     HeroSection
     CountersSection
     AIMatchingSection        ← NEW (after counters, before how it works)
     HowItWorksSection
     FarmTypesStrip           ← NEW (after how it works, before featured listings)
     FeaturedListings
     EmployerCTABand          ← NEW (after featured listings, before testimonials)
     TestimonialsSection
     LandingFooter
     ```
   - Note: TrustedByStrip and FinalCTASection will be added by Plan 10-03 between TestimonialsSection and LandingFooter.

2. Update `tests/landing-page.test.tsx`:
   - Add test: "AIMatchingSection renders feature bullet points" — assert text containing "Skills-based matching" is present.
   - Add test: "FarmTypesStrip renders 5 sector cards" — assert "Dairy", "Sheep & Beef", "Horticulture", "Viticulture", "Arable" text present.
   - Add test: "EmployerCTABand renders CTA and checklist" — assert "Post Your First Job" text and "Post a job in under 5 minutes" text present.

3. Run `npx vitest tests/landing-page.test.tsx --run`.
  </action>
  <verify>
    <automated>cd /Users/harrysmith/dev/topfarms && npx vitest tests/landing-page.test.tsx --run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "AIMatchingSection" src/pages/Home.tsx` succeeds
    - `grep -q "FarmTypesStrip" src/pages/Home.tsx` succeeds
    - `grep -q "EmployerCTABand" src/pages/Home.tsx` succeeds
    - `npx vitest tests/landing-page.test.tsx --run` passes
  </acceptance_criteria>
  <done>Three new sections wired into Home.tsx in correct SPEC order; tests cover all three new sections and pass.</done>
</task>

</tasks>

<verification>
- All three new component files exist in src/components/landing/
- Home.tsx imports and renders all three in correct order
- `npx vitest tests/landing-page.test.tsx --run` — all tests pass
- Each section uses the project's established section pattern (max-w-6xl, py-20, CSS custom properties)
</verification>

<success_criteria>
- AIMatchingSection renders mock browser window with 4 feature bullets
- FarmTypesStrip renders 5 sector cards with hardcoded counts
- EmployerCTABand renders checklist + "Post Your First Job" CTA
- All three are in correct position in Home.tsx section order
- Tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/10-landing-page/10-02-SUMMARY.md`
</output>
