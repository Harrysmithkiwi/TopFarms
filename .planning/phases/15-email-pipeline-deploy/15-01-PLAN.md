---
phase: 15-email-pipeline-deploy
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/15-email-pipeline-deploy/15-01-SUMMARY.md
  - .planning/phases/15-email-pipeline-deploy/15-01-EVIDENCE/
autonomous: true
requirements:
  - MAIL-02
  - MAIL-01
  - DEPLOY-01
must_haves:
  truths:
    - "All 4 disk-only Edge Functions (notify-job-filled, send-followup-emails, acknowledge-placement-fee, create-placement-invoice) are listed by `mcp__supabase__list_edge_functions` with status ACTIVE"
    - "Every src/ callsite to `supabase.functions.invoke('<name>')` references a function present in the live deployed list (no undeployed-callee debt)"
    - "Resend dashboard / API confirms `topfarms.co.nz` (or configured sending domain) shows `status: verified`"
    - "All 4 deployed functions are confirmed BFIX-05-compliant (no `auth.getUser(token)` antipattern)"
  artifacts:
    - path: ".planning/phases/15-email-pipeline-deploy/15-01-EVIDENCE/list_edge_functions.json"
      provides: "Snapshot of `mcp__supabase__list_edge_functions` post-deploy showing all 9 functions"
      contains: "notify-job-filled, send-followup-emails, acknowledge-placement-fee, create-placement-invoice"
    - path: ".planning/phases/15-email-pipeline-deploy/15-01-EVIDENCE/resend_domains.json"
      provides: "Resend GET /domains response; primary MAIL-01 evidence (curl variant) — screenshot variant acceptable substitute committed under same dir"
      contains: '"status":"verified"'
    - path: ".planning/phases/15-email-pipeline-deploy/15-01-EVIDENCE/callsite_audit.txt"
      provides: "grep output of `supabase.functions.invoke` callsites in src/ alongside list_edge_functions diff = ∅"
    - path: ".planning/phases/15-email-pipeline-deploy/15-01-EVIDENCE/bfix05_audit.txt"
      provides: "grep audit confirming the 4 disk-only functions have NO `auth.getUser(token)` antipattern"
    - path: ".planning/phases/15-email-pipeline-deploy/15-01-SUMMARY.md"
      provides: "Phase 15-01 SUMMARY incl. PAT name (`topfarms-ci-deploy`) + creation date for rotation tracking, deploy outputs, dry-run quirk handling decision (Option A or B per RESEARCH.md §Pattern 3)"
  key_links:
    - from: "src/ callsites (`supabase.functions.invoke`)"
      to: "live Supabase Edge Functions registry"
      via: "callsite_audit.txt diff"
      pattern: "diff between grep src/ output and list_edge_functions = empty set"
    - from: "developer machine PAT (`topfarms-ci-deploy`)"
      to: "Supabase project `inlagtgpynemhipnqvty`"
      via: "supabase login + supabase functions deploy --use-api"
      pattern: "PAT scope = TopFarms project only; documented in SUMMARY for rotation"
---

<objective>
Deploy the 4 disk-only Edge Functions live (`notify-job-filled`, `send-followup-emails`, `acknowledge-placement-fee`, `create-placement-invoice`) via local Supabase CLI from the developer's machine, audit each for the BFIX-05 antipattern before deploy, capture Resend domain verification evidence, and record an undeployed-callee diff = ∅.

Purpose: MAIL-02 is silently failing in production right now (DB trigger on `on_job_filled` fires `pg_net` POST to a function that returns 404). This plan stops the prod silent-failure within hours by deploying manually, before the CI step is built in 15-03.

Output: 4 newly-deployed functions visible in `list_edge_functions`; evidence dir with deploy outputs, list_edge_functions snapshot, Resend domain status JSON, callsite audit, BFIX-05 audit; 15-01-SUMMARY.md with PAT rotation tracking + deploy log.
</objective>

<execution_context>
@/Users/harrysmith/.claude/get-shit-done/workflows/execute-plan.md
@/Users/harrysmith/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-email-pipeline-deploy/15-CONTEXT.md
@.planning/phases/15-email-pipeline-deploy/15-RESEARCH.md
@.planning/phases/15-email-pipeline-deploy/15-VALIDATION.md
@CLAUDE.md
@supabase/migrations/NAMING.md
@.planning/phases/13-email-notifications/13-01-SUMMARY.md
@.planning/phases/13-email-notifications/13-02-SUMMARY.md

<interfaces>
<!-- Function source signatures the executor needs to confirm pre-deploy. Each function uses Deno.serve + service-role-keyed Supabase client. None use verify_jwt: true gateway re-validation. -->

From supabase/functions/notify-job-filled/index.ts (Phase 13-01 ship):
- Entry: `Deno.serve(async (req) => { ... })`
- Reads body `{ job_id }`, fetches `applications` for that job in NOTIFY_STATUSES (`applied|review|interview|shortlisted|offered`), sends Resend email per applicant via `seeker_contacts.email`, returns `{ sent, failed, job_id }`.
- Auth: bearer Authorization header from pg_net trigger (service_role JWT). NOT calling `auth.getUser(token)`. (BFIX-05-compliant by inspection — confirm via grep in audit task.)

From supabase/functions/send-followup-emails/index.ts (v1.0 cron):
- Entry: `Deno.serve(...)`. Cron-driven Day 7 / 14 follow-ups.
- Auth: invoked by Supabase cron with service role.

From supabase/functions/acknowledge-placement-fee/index.ts and create-placement-invoice/index.ts (v1.0):
- Invoked from `src/pages/dashboard/employer/ApplicantDashboard.tsx:361, 423` via `supabase.functions.invoke(...)`.
- Auth: authenticated employer JWT.

Live deployed functions baseline (per RESEARCH.md §Code Examples cross-ref, 2026-05-01):
- generate-candidate-summary, generate-match-explanation, stripe-webhook, create-payment-intent, get-applicant-document-url
- Disk-only (this plan deploys): notify-job-filled, send-followup-emails, acknowledge-placement-fee, create-placement-invoice
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BFIX-05 antipattern audit + callsite cross-reference (pre-deploy guard)</name>
  <files>.planning/phases/15-email-pipeline-deploy/15-01-EVIDENCE/bfix05_audit.txt, .planning/phases/15-email-pipeline-deploy/15-01-EVIDENCE/callsite_audit.txt</files>
  <read_first>
    - supabase/functions/notify-job-filled/index.ts
    - supabase/functions/send-followup-emails/index.ts
    - supabase/functions/acknowledge-placement-fee/index.ts
    - supabase/functions/create-placement-invoice/index.ts
    - CLAUDE.md (§5 gateway-trust JWT pattern)
    - .planning/phases/15-email-pipeline-deploy/15-RESEARCH.md (§Pitfall 5 BFIX-05 antipattern, §Code Examples cross-reference)
  </read_first>
  <action>
    Create directory `.planning/phases/15-email-pipeline-deploy/15-01-EVIDENCE/` (mkdir -p).

    (1) BFIX-05 audit. Run from repo root:
        ```
        grep -nE "auth\.getUser\(|verify_jwt" supabase/functions/notify-job-filled/index.ts supabase/functions/send-followup-emails/index.ts supabase/functions/acknowledge-placement-fee/index.ts supabase/functions/create-placement-invoice/index.ts > .planning/phases/15-email-pipeline-deploy/15-01-EVIDENCE/bfix05_audit.txt 2>&1 || true
        ```
        Expected: file shows zero matches for `auth.getUser(` (no antipattern). `verify_jwt` matches in inline comments are acceptable. If ANY function calls `adminClient.auth.getUser(token)` with a service-role-keyed client, ABORT this task and surface the offending file+line — that function must be patched to gateway-trust + local atob decode (per CLAUDE.md §5) BEFORE proceeding to deploy. Do not deploy a known-broken pattern to production.

    (2) Callsite cross-reference. Run:
        ```
        grep -rnE "supabase\.functions\.invoke\(['\"]|/functions/v1/" src/ > .planning/phases/15-email-pipeline-deploy/15-01-EVIDENCE/callsite_audit.txt 2>&1 || true
        ```
        Append to the same file a comment block listing the 4 functions in scope for this plan + the 5 already-deployed (per RESEARCH.md §Code Examples). Confirm the diff is what RESEARCH.md predicted: src/ references {generate-candidate-summary, generate-match-explanation, acknowledge-placement-fee, create-placement-invoice, create-payment-intent, get-applicant-document-url}; pg_net trigger references notify-job-filled (not in src/); cron references send-followup-emails (not in src/). If a NEW function name appears in src/ that's not in either set, ABORT and surface for scope expansion.

    Append both file outputs into a single `bfix05_audit.txt` summary block at the bottom: "AUDIT RESULT: PASS — all 4 functions safe to deploy" or "AUDIT RESULT: FAIL — see line N".
  </action>
  <verify>
    <automated>test -f .planning/phases/15-email-pipeline-deploy/15-01-EVIDENCE/bfix05_audit.txt && test -f .planning/phases/15-email-pipeline-deploy/15-01-EVIDENCE/callsite_audit.txt && grep -q "AUDIT RESULT: PASS" .planning/phases/15-email-pipeline-deploy/15-01-EVIDENCE/bfix05_audit.txt</automated>
  </verify>
  <acceptance_criteria>
    - File `bfix05_audit.txt` exists and ends with literal string `AUDIT RESULT: PASS`
    - File `callsite_audit.txt` exists and contains at least the 6 invoke-strings present today: `acknowledge-placement-fee`, `create-placement-invoice`, `generate-candidate-summary`, `generate-match-explanation`, `create-payment-intent`, `get-applicant-document-url`
    - `grep -nE "auth\.getUser\(" supabase/functions/notify-job-filled/index.ts supabase/functions/send-followup-emails/index.ts supabase/functions/acknowledge-placement-fee/index.ts supabase/functions/create-placement-invoice/index.ts` returns zero matches
  </acceptance_criteria>
  <done>BFIX-05 antipattern absent from all 4 disk-only functions; callsite diff = ∅ confirmed; both evidence files exist on disk.</done>
</task>

<task type="auto">
  <name>Task 2: Manual deploy of 4 Edge Functions via local Supabase CLI</name>
  <files>.planning/phases/15-email-pipeline-deploy/15-01-EVIDENCE/deploy_log.txt, .planning/phases/15-email-pipeline-deploy/15-01-EVIDENCE/list_edge_functions.json</files>
  <read_first>
    - .planning/phases/15-email-pipeline-deploy/15-RESEARCH.md (§Code Examples "Manual deploy", §Pitfall 4 --use-api flag)
    - CLAUDE.md (§1 project ref `inlagtgpynemhipnqvty`)
    - .planning/phases/15-email-pipeline-deploy/15-01-EVIDENCE/bfix05_audit.txt (must show AUDIT RESULT: PASS before deploying)
  </read_first>
  <action>
    PRECONDITION: Task 1 produced `bfix05_audit.txt` with `AUDIT RESULT: PASS`. If not, do not proceed — re-run Task 1 or escalate.

    Confirm Supabase CLI installed: `supabase --version`. If absent, install via `brew install supabase/tap/supabase` (macOS — env says darwin) and re-check. Pin v2.95.4 or later expected per RESEARCH.md §Standard Stack; print the actual version into `deploy_log.txt`.

    PAT setup (one-time per machine, via developer's interactive shell):
      1. Open https://supabase.com/dashboard/account/tokens in a browser.
      2. Create a NEW token named `topfarms-ci-deploy` scoped to project `inlagtgpynemhipnqvty`.
      3. Copy creation date — record in 15-01-SUMMARY.md (Task 4).
      4. Authenticate via `supabase login` (paste the PAT when prompted). Note: this PAT will ALSO be added to GitHub Actions repo secrets in plan 15-03; do NOT discard it after `supabase login`.

    Link to project (idempotent):
      ```
      supabase link --project-ref inlagtgpynemhipnqvty
      ```

    Deploy all 4 disk-only functions in one command (per RESEARCH.md §Don't Hand-Roll — single command preferred over per-function loop):
      ```
      supabase functions deploy --use-api --project-ref inlagtgpynemhipnqvty 2>&1 | tee .planning/phases/15-email-pipeline-deploy/15-01-EVIDENCE/deploy_log.txt
      ```

      Note: `supabase functions deploy` with no name argument deploys ALL functions in `supabase/functions/` (since CLI v1.62.0). This will redeploy the 5 already-live functions too — that's idempotent and intentional (per RESEARCH.md §Open Questions #1 "deploy all every push" recommendation).

      `--use-api` flag is REQUIRED — server-side bundling, no Docker, fixes parallel-deploy race condition (RESEARCH.md §Pitfall 4).

    Capture post-deploy live function registry. Use the project-scoped MCP (read-only is fine — `list_edge_functions` is a read operation) and write the JSON output verbatim to `list_edge_functions.json`. Steps:
      1. Call `mcp__supabase__list_edge_functions({})` (no arg or `{}`).
      2. Pretty-print the JSON array result.
      3. Write to `.planning/phases/15-email-pipeline-deploy/15-01-EVIDENCE/list_edge_functions.json`.

    Cross-check the snapshot contains all 9 functions named: `notify-job-filled`, `send-followup-emails`, `acknowledge-placement-fee`, `create-placement-invoice`, `generate-candidate-summary`, `generate-match-explanation`, `stripe-webhook`, `create-payment-intent`, `get-applicant-document-url`. If any of the 4 newly-deployed are missing OR not status ACTIVE, retry the deploy for that specific function via `supabase functions deploy <name> --use-api --project-ref inlagtgpynemhipnqvty` and re-snapshot.
  </action>
  <verify>
    <automated>test -f .planning/phases/15-email-pipeline-deploy/15-01-EVIDENCE/list_edge_functions.json && grep -q "notify-job-filled" .planning/phases/15-email-pipeline-deploy/15-01-EVIDENCE/list_edge_functions.json && grep -q "send-followup-emails" .planning/phases/15-email-pipeline-deploy/15-01-EVIDENCE/list_edge_functions.json && grep -q "acknowledge-placement-fee" .planning/phases/15-email-pipeline-deploy/15-01-EVIDENCE/list_edge_functions.json && grep -q "create-placement-invoice" .planning/phases/15-email-pipeline-deploy/15-01-EVIDENCE/list_edge_functions.json</automated>
  </verify>
  <acceptance_criteria>
    - `deploy_log.txt` exists and contains the literal string `--use-api` in the captured command line
    - `deploy_log.txt` does NOT contain "Pulling image" or "Container starting" (would indicate Docker bundling — RESEARCH.md §Pitfall 4)
    - `list_edge_functions.json` contains substrings `"notify-job-filled"`, `"send-followup-emails"`, `"acknowledge-placement-fee"`, `"create-placement-invoice"` (exact slugs)
    - `list_edge_functions.json` shows status `ACTIVE` (or equivalent live indicator) for all 4 newly-deployed functions
  </acceptance_criteria>
  <done>4 previously-disk-only functions are live in production; `list_edge_functions.json` snapshot captured; deploy log captured.</done>
</task>

<task type="auto">
  <name>Task 3: Capture Resend domain verification evidence (MAIL-01 partial→satisfied evidence)</name>
  <files>.planning/phases/15-email-pipeline-deploy/15-01-EVIDENCE/resend_domains.json</files>
  <read_first>
    - .planning/phases/15-email-pipeline-deploy/15-CONTEXT.md (§Resend Verified status confirmation)
    - .planning/phases/15-email-pipeline-deploy/15-RESEARCH.md (§Code Examples "Resend evidence via curl")
    - .planning/phases/15-email-pipeline-deploy/15-VALIDATION.md (Manual-Only Verifications row for MAIL-01)
  </read_first>
  <action>
    This task captures the empirical evidence for MAIL-01 (Resend SPF/DKIM verified). Per CONTEXT.md, this phase confirms-and-captures only — does NOT redo DNS work.

    Path A (preferred — curl, structured JSON evidence):
      Confirm `RESEND_API_KEY` is available in the developer's local env (e.g. `~/.zshrc` export or 1Password). If not, surface and ask the user to provide before continuing.
      Run:
        ```
        curl -sX GET 'https://api.resend.com/domains' \
             -H "Authorization: Bearer $RESEND_API_KEY" \
             | jq '.' > .planning/phases/15-email-pipeline-deploy/15-01-EVIDENCE/resend_domains.json
        ```
      Confirm the response contains the topfarms.co.nz row (or whatever the live sending domain is per Phase 13-02 SUMMARY) with `"status": "verified"`. Acceptable values: `"verified"` only. Not acceptable: `"not_started"`, `"pending"`, `"partially_verified"`, `"partially_failed"`, `"failed"` (RESEARCH.md §Code Examples + §Pitfall 6).

      If status is NOT `verified` for the sending domain → ABORT this task and surface to user. The phase boundary in CONTEXT.md says "this phase confirms-and-captures only — does NOT redo DNS work"; if the domain is not verified, MAIL-01 cannot be marked satisfied and the user must address DNS records before proceeding (this is a human-action, NOT a Claude-automation task).

    Path B (fallback — screenshot, only if Resend API key not accessible):
      Visit https://resend.com/domains in a browser. Take a screenshot showing the SPF and DKIM rows for the sending domain with green `Verified` status. Save as `.planning/phases/15-email-pipeline-deploy/15-01-EVIDENCE/resend_domains.png`. Then write a JSON stub to `resend_domains.json` with shape:
        ```json
        {"evidence_type": "screenshot", "screenshot": "resend_domains.png", "captured_at": "<ISO8601>", "domain": "topfarms.co.nz", "status_observed": "verified"}
        ```
      Path A is strongly preferred for reproducibility. Only fall back to Path B if Path A is genuinely impossible.
  </action>
  <verify>
    <automated>test -f .planning/phases/15-email-pipeline-deploy/15-01-EVIDENCE/resend_domains.json && grep -q '"verified"' .planning/phases/15-email-pipeline-deploy/15-01-EVIDENCE/resend_domains.json</automated>
  </verify>
  <acceptance_criteria>
    - File `resend_domains.json` exists
    - File contains substring `"verified"` (the literal status value, not just any occurrence of that word)
    - File does NOT contain `"partially_verified"`, `"partially_failed"`, `"not_started"`, `"pending"`, or `"failed"` as the status value for the sending domain row
  </acceptance_criteria>
  <done>MAIL-01 evidence captured on disk; sending domain confirmed verified; ready for 13-VERIFICATION.md backfill in plan 15-04.</done>
</task>

<task type="auto">
  <name>Task 4: Write 15-01-SUMMARY.md (PAT rotation tracking, deploy outcome, dry-run quirk decision)</name>
  <files>.planning/phases/15-email-pipeline-deploy/15-01-SUMMARY.md</files>
  <read_first>
    - .planning/phases/14-bug-fixes/14-01-SUMMARY.md (frontmatter / shape reference)
    - .planning/phases/13-email-notifications/13-01-SUMMARY.md (Phase 13 SUMMARY shape)
    - .planning/phases/15-email-pipeline-deploy/15-01-EVIDENCE/deploy_log.txt
    - .planning/phases/15-email-pipeline-deploy/15-01-EVIDENCE/list_edge_functions.json
    - .planning/phases/15-email-pipeline-deploy/15-01-EVIDENCE/resend_domains.json
    - .planning/phases/15-email-pipeline-deploy/15-RESEARCH.md (§Pattern 3 Studio-applied migration handling)
  </read_first>
  <action>
    Create `.planning/phases/15-email-pipeline-deploy/15-01-SUMMARY.md` matching the shape of `14-01-SUMMARY.md` frontmatter (phase, plan, subsystem, tags, dependency_graph, tech_stack, key_files, decisions, metrics).

    REQUIRED frontmatter fields:
      phase: 15-email-pipeline-deploy
      plan: "01"
      subsystem: edge-function-deploy
      tags: [edge-function, deploy, mail-02, mail-01, deploy-01]
      dependency_graph:
        requires: []
        provides: [notify-job-filled-deployed, send-followup-emails-deployed, acknowledge-placement-fee-deployed, create-placement-invoice-deployed, resend-domain-verified-evidence]
        affects: [supabase-functions-registry]
      tech_stack:
        added: [supabase-cli-v2.95.4-or-later]
        patterns: [manual-deploy-via-cli, gateway-trust-jwt-confirmed-by-grep, callsite-diff-equals-empty-set]
      key_files:
        created:
          - .planning/phases/15-email-pipeline-deploy/15-01-EVIDENCE/bfix05_audit.txt
          - .planning/phases/15-email-pipeline-deploy/15-01-EVIDENCE/callsite_audit.txt
          - .planning/phases/15-email-pipeline-deploy/15-01-EVIDENCE/deploy_log.txt
          - .planning/phases/15-email-pipeline-deploy/15-01-EVIDENCE/list_edge_functions.json
          - .planning/phases/15-email-pipeline-deploy/15-01-EVIDENCE/resend_domains.json
        modified: []
      requirements_completed: [MAIL-02-deploy-side, DEPLOY-01-manual-baseline, MAIL-01-evidence]

    REQUIRED body sections:
      1. "## What Was Built" — bullet list referencing the 5 evidence files above and what each proves.
      2. "## PAT Rotation Tracking" — record:
         - PAT name: `topfarms-ci-deploy`
         - Created: <ISO date from Task 2>
         - Scope: TopFarms project only (ref `inlagtgpynemhipnqvty`)
         - Storage: developer machine `supabase login` keychain + (after plan 15-03) GitHub Actions repo secret `SUPABASE_ACCESS_TOKEN`
         - Rotation policy: manual annual review (per CONTEXT.md "no rotation policy this phase")
      3. "## Migration Registry Quirk Decision" — read RESEARCH.md §Pattern 3. Run `supabase db push --linked --dry-run` once from a developer machine; capture the output. Document one of:
         - **Option A chosen** — INSERT rows into `supabase_migrations.schema_migrations` for 016 + 017 via Supabase Studio SQL Editor (per CLAUDE.md §2 "preferred path for one-off DB writes"). Capture the SQL run + verification.
         - **Option B chosen** — trust idempotency (016 + 017 use `IF NOT EXISTS` / `CREATE OR REPLACE` everywhere). Document the dry-run output showing 016 + 017 listed as "pending" + a written acknowledgement that re-applying is a no-op for runtime artefacts. The CI step in 15-03 will inherit this decision.
         The Option A vs B choice is a real decision point — make it explicitly. If Option B, leave a note in the SUMMARY: "Next-touch person: see RESEARCH.md §Pattern 3 for context".
      4. "## Verification Notes for Plan 15-04" — point at evidence files for use in 13-VERIFICATION.md backfill (plan 15-04 will read this section).
      5. "## Atomic Commit Note" — per CLAUDE.md §4. Plan 15-01 produces a single commit: `feat(15-01): deploy 4 edge functions + capture mail-01 evidence`.

    Run `node /Users/harrysmith/.claude/get-shit-done/bin/gsd-tools.cjs frontmatter validate .planning/phases/15-email-pipeline-deploy/15-01-SUMMARY.md --schema summary` and ensure `valid: true` before completing.
  </action>
  <verify>
    <automated>test -f .planning/phases/15-email-pipeline-deploy/15-01-SUMMARY.md && grep -q "topfarms-ci-deploy" .planning/phases/15-email-pipeline-deploy/15-01-SUMMARY.md && grep -qE "Option (A|B) chosen" .planning/phases/15-email-pipeline-deploy/15-01-SUMMARY.md && node /Users/harrysmith/.claude/get-shit-done/bin/gsd-tools.cjs frontmatter validate .planning/phases/15-email-pipeline-deploy/15-01-SUMMARY.md --schema summary | grep -q '"valid": true'</automated>
  </verify>
  <acceptance_criteria>
    - File `15-01-SUMMARY.md` exists
    - Frontmatter validates against `summary` schema (gsd-tools returns `"valid": true`)
    - Body contains literal string `topfarms-ci-deploy`
    - Body contains exactly one of: `Option A chosen` or `Option B chosen` (the migration registry quirk decision, made explicitly)
    - `requirements_completed` frontmatter list includes `MAIL-02-deploy-side`, `DEPLOY-01-manual-baseline`, `MAIL-01-evidence`
  </acceptance_criteria>
  <done>15-01-SUMMARY.md committed with PAT rotation tracking, deploy outcome, and explicit migration registry quirk decision recorded.</done>
</task>

</tasks>

<verification>
- All 4 disk-only functions visible in `list_edge_functions.json` with status ACTIVE
- BFIX-05 antipattern absent from all 4 functions (grep returns zero matches for `auth.getUser(`)
- src/ callsites diff against deployed registry = ∅
- Resend sending domain status = `verified` (curl JSON or screenshot)
- 15-01-SUMMARY.md exists with valid frontmatter; PAT name + creation date documented; migration registry quirk Option A vs B decision recorded
</verification>

<success_criteria>
1. `mcp__supabase__list_edge_functions({})` returns all 4 newly-deployed function slugs (`notify-job-filled`, `send-followup-emails`, `acknowledge-placement-fee`, `create-placement-invoice`) with ACTIVE status
2. `grep -nE "auth\.getUser\(" supabase/functions/{notify-job-filled,send-followup-emails,acknowledge-placement-fee,create-placement-invoice}/index.ts` returns zero hits
3. Resend GET /domains response (or dashboard screenshot) shows the sending domain with `status: verified` for SPF + DKIM
4. `.planning/phases/15-email-pipeline-deploy/15-01-EVIDENCE/` contains: bfix05_audit.txt, callsite_audit.txt, deploy_log.txt, list_edge_functions.json, resend_domains.json
5. 15-01-SUMMARY.md frontmatter passes `gsd-tools frontmatter validate --schema summary`
</success_criteria>

<output>
After completion, create `.planning/phases/15-email-pipeline-deploy/15-01-SUMMARY.md` (Task 4).

Plan 15-02 will reference this plan's evidence dir for the trigger-fire MAIL-02 verification (depends on `notify-job-filled` being deployed live, captured in `list_edge_functions.json`).

Plan 15-03 will reference this plan's PAT (`topfarms-ci-deploy`) when seeding GitHub Actions repo secrets.

Plan 15-04 will quote `resend_domains.json` and `list_edge_functions.json` as MAIL-01 / DEPLOY-01 evidence in 13-VERIFICATION.md.
</output>
