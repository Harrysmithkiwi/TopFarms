---
phase: 10-landing-page
plan: 03
type: execute
wave: 2
depends_on: [10-01, 10-02]
files_modified:
  - src/components/landing/TestimonialsSection.tsx
  - src/components/landing/TrustedByStrip.tsx
  - src/components/landing/FinalCTASection.tsx
  - src/pages/Home.tsx
  - tests/landing-page.test.tsx
autonomous: true
requirements: [LAND-03, LAND-08, LAND-09]

must_haves:
  truths:
    - "Testimonials section includes 4 connected stat blocks (500+ Farms, 2000+ Workers, 48hr Avg Match Time, 95% Satisfaction)"
    - "Trusted-by strip shows 5-6 greyed-out farm brand name placeholders"
    - "Final CTA section shows centered headline with dual buttons (Find Farm Work + Post a Job)"
    - "Home.tsx renders all sections in the complete SPEC order from hero to footer"
  artifacts:
    - path: "src/components/landing/TestimonialsSection.tsx"
      provides: "4 social proof stat blocks integrated with testimonials"
      contains: "500+"
    - path: "src/components/landing/TrustedByStrip.tsx"
      provides: "Trusted-by farm brand strip"
      contains: "Fonterra"
    - path: "src/components/landing/FinalCTASection.tsx"
      provides: "Final CTA with dual buttons"
      contains: "Find Farm Work"
  key_links:
    - from: "src/pages/Home.tsx"
      to: "src/components/landing/TrustedByStrip.tsx"
      via: "import and render"
      pattern: "TrustedByStrip"
    - from: "src/pages/Home.tsx"
      to: "src/components/landing/FinalCTASection.tsx"
      via: "import and render"
      pattern: "FinalCTASection"
---

<objective>
Add 4 social proof stat blocks to the TestimonialsSection, create the Trusted-by strip and Final CTA section, and finalize the complete Home.tsx section ordering.

Purpose: These bottom-of-funnel sections provide social proof (stats + trusted brands) and a final conversion push (dual CTA) before the footer.
Output: Updated TestimonialsSection with stat blocks, two new section components, complete Home.tsx with all SPEC sections.
</objective>

<execution_context>
@/Users/harrysmith/.claude/get-shit-done/workflows/execute-plan.md
@/Users/harrysmith/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-landing-page/10-CONTEXT.md
@.planning/phases/10-landing-page/10-RESEARCH.md

@src/pages/Home.tsx
@src/components/landing/TestimonialsSection.tsx
@src/components/landing/HeroSection.tsx (CTA button styling reference for Final CTA)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add social proof stat blocks to TestimonialsSection</name>
  <files>src/components/landing/TestimonialsSection.tsx</files>
  <read_first>
    - src/components/landing/TestimonialsSection.tsx
    - src/components/landing/HeroSection.tsx (for visual pattern reference)
  </read_first>
  <action>
1. In `src/components/landing/TestimonialsSection.tsx`:
   - Add a stat blocks row above OR below the existing testimonial cards grid (within the same dark section).
   - 4 stat blocks in a horizontal row: `grid grid-cols-2 md:grid-cols-4 gap-6`.
   - Stat data (static/aspirational marketing numbers):
     - { value: "500+", label: "Farms" }
     - { value: "2,000+", label: "Workers" }
     - { value: "48hr", label: "Avg Match Time" }
     - { value: "95%", label: "Satisfaction" }
   - Each stat block: text-center, value in cream color `text-3xl md:text-4xl font-bold font-display`, label in mid/muted color `text-sm uppercase tracking-wider` below value.
   - Connected visually: add vertical divider borders between stats on desktop using `border-r` on all but last item (only visible on md+): `md:border-r` with `borderColor: 'rgba(255,255,255,0.12)'`.
   - Position the stat blocks row ABOVE the testimonials heading/cards with `mb-16` spacing.
   - Use the existing section's dark soil background — do NOT create a separate section wrapper.
  </action>
  <verify>
    <automated>cd /Users/harrysmith/dev/topfarms && grep -c "500+" src/components/landing/TestimonialsSection.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "500+" src/components/landing/TestimonialsSection.tsx` succeeds
    - `grep -q "2,000+" src/components/landing/TestimonialsSection.tsx` succeeds
    - `grep -q "48hr" src/components/landing/TestimonialsSection.tsx` succeeds
    - `grep -q "95%" src/components/landing/TestimonialsSection.tsx` succeeds
    - `grep -q "Avg Match Time\|Match Time" src/components/landing/TestimonialsSection.tsx` succeeds
  </acceptance_criteria>
  <done>TestimonialsSection includes 4 connected stat blocks with static marketing numbers in the dark section, visually separated by divider borders.</done>
</task>

<task type="auto">
  <name>Task 2: Create TrustedByStrip and FinalCTASection, finalize Home.tsx</name>
  <files>src/components/landing/TrustedByStrip.tsx, src/components/landing/FinalCTASection.tsx, src/pages/Home.tsx, tests/landing-page.test.tsx</files>
  <read_first>
    - src/pages/Home.tsx
    - src/components/landing/HeroSection.tsx (CTA button patterns)
    - src/components/landing/TestimonialsSection.tsx (dark section styling)
    - tests/landing-page.test.tsx
  </read_first>
  <action>
1. Create `src/components/landing/TrustedByStrip.tsx`:
   - Cream/light background (`var(--color-cream)`), `py-16 px-4`.
   - Centered heading: "Trusted by Leading New Zealand Farms" in mid color, `text-sm uppercase tracking-widest font-semibold mb-8 text-center`.
   - Horizontal row of 5-6 farm brand name placeholders: `flex flex-wrap justify-center items-center gap-8 md:gap-12`.
   - Brand names (placeholder): "Fonterra Sharemilkers", "Silver Fern Farms", "Greenfield Agriculture", "Highview Station", "Valley View Dairy".
   - Each name styled as greyed-out text logo: `text-lg md:text-xl font-bold` with `color: 'var(--color-mid)'` and `opacity: 0.5` to look like faded brand logos.
   - Wrap inner content in motion.div scroll-reveal: `initial={{ opacity: 0, y: 32 }}`, `whileInView={{ opacity: 1, y: 0 }}`, `viewport={{ once: true, amount: 0.15 }}`, `transition={{ duration: 0.55, ease: 'easeOut' }}`.
   - Import `motion` from `motion/react`.
   - Export as named export `TrustedByStrip`.

2. Create `src/components/landing/FinalCTASection.tsx`:
   - Dark background: `var(--color-soil-deep)` (or `var(--color-soil)` if soil-deep is not defined — check CSS vars), cream text.
   - `py-20 px-4`, `max-w-3xl mx-auto text-center`.
   - Heading: "Ready to Find Your Perfect Match?" in cream, `font-display font-bold text-4xl md:text-5xl mb-4`.
   - Subheading: "Whether you're looking for farm work or looking to hire, TopFarms connects you with the right people." in cream/muted, `text-lg mb-8` with `opacity: 0.8`.
   - Dual buttons row: `flex flex-col sm:flex-row justify-center gap-4`.
     - "Find Farm Work" button: meadow bg, soil text, rounded-full, px-8 py-3, font-semibold. Links to `/seeker/onboarding`.
     - "Post a Job" button: outline style — transparent bg, hay border, cream text, rounded-full, px-8 py-3, font-semibold. Links to `/employer/onboarding`.
   - Use `<Link>` from react-router-dom for navigation.
   - Wrap in motion.div scroll-reveal.
   - Export as named export `FinalCTASection`.

3. Update `src/pages/Home.tsx`:
   - Add imports for `TrustedByStrip` and `FinalCTASection`.
   - Final COMPLETE section order must be:
     ```
     Nav
     HeroSection
     CountersSection
     AIMatchingSection
     HowItWorksSection
     FarmTypesStrip
     FeaturedListings
     EmployerCTABand
     TestimonialsSection
     TrustedByStrip           ← NEW
     FinalCTASection          ← NEW
     LandingFooter
     ```

4. Update `tests/landing-page.test.tsx`:
   - Add test: "TestimonialsSection renders social proof stat blocks" — assert "500+" and "Satisfaction" text present.
   - Add test: "TrustedByStrip renders farm brand placeholders" — assert "Fonterra" or "Silver Fern" text present.
   - Add test: "FinalCTASection renders dual CTA buttons" — assert "Find Farm Work" and "Post a Job" text present.

5. Run `npx vitest tests/landing-page.test.tsx --run`.
  </action>
  <verify>
    <automated>cd /Users/harrysmith/dev/topfarms && npx vitest tests/landing-page.test.tsx --run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "TrustedByStrip" src/pages/Home.tsx` succeeds
    - `grep -q "FinalCTASection" src/pages/Home.tsx` succeeds
    - `grep -q "Fonterra" src/components/landing/TrustedByStrip.tsx` succeeds
    - `grep -q "Find Farm Work" src/components/landing/FinalCTASection.tsx` succeeds
    - `grep -q "Post a Job" src/components/landing/FinalCTASection.tsx` succeeds
    - `grep -q "motion/react" src/components/landing/TrustedByStrip.tsx` succeeds
    - `npx vitest tests/landing-page.test.tsx --run` passes
  </acceptance_criteria>
  <done>TrustedByStrip and FinalCTASection created; Home.tsx has all 11 sections in complete SPEC order; all tests pass.</done>
</task>

</tasks>

<verification>
- `npx vitest tests/landing-page.test.tsx --run` — all tests pass
- Home.tsx imports and renders all 11 sections in SPEC order
- TestimonialsSection has 4 stat blocks with correct values
- TrustedByStrip shows greyed farm brand names
- FinalCTASection shows dual CTA buttons
</verification>

<success_criteria>
- 4 stat blocks render in the testimonials dark section (500+ Farms, 2000+ Workers, 48hr Avg Match Time, 95% Satisfaction)
- Trusted-by strip renders 5 greyed-out farm brand name placeholders
- Final CTA renders "Find Farm Work" + "Post a Job" buttons on soil-deep background
- Home.tsx has complete SPEC section ordering (11 sections between Nav and Footer)
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/10-landing-page/10-03-SUMMARY.md`
</output>
