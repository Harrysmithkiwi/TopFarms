---
phase: 20.1-standalone-admin-login-gateway-account-bootstrap
plan: 04
subsystem: ui
tags: [react, react-router, vitest, lucide-react, sidebar, sign-out, auth]

# Dependency graph
requires:
  - phase: 20.1-03
    provides: AdminGate hybrid route at /admin (validates ProtectedRoute fall-through still drives logout redirect)
  - phase: 20-04
    provides: AdminSidebar footer-slot pattern (top-positioned escape hatch — mirror pattern, inverted position)
  - phase: 12-oauth-authentication
    provides: AuthContext.signOut() + onAuthStateChange clearing session → ProtectedRoute /login redirect
provides:
  - Sign Out button at the bottom of the consumer dashboard sidebar (employer + seeker)
  - vitest component coverage that click → signOut() once
  - Closure of CF-CODE-2 (Phase 20 carryforward) and the Phase 21 todo logged 2026-05-05
affects: [phase-20.1-05, phase-21]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "footer slot via mt-auto inside flex-col aside with min-h-[calc(100vh-56px)] (no aside restructure)"
    - "Sign Out via AuthContext only — no useNavigate; ProtectedRoute fall-through handles redirect"

key-files:
  created:
    - tests/sidebar-signout.test.tsx
  modified:
    - src/components/layout/Sidebar.tsx

key-decisions:
  - "Sign Out button sits at the bottom of Sidebar via mt-auto (parent already flex-col + min-h); no aside restructure required."
  - "onClick={() => signOut()} only — no explicit useNavigate('/login'); AuthContext.onAuthStateChange clears session → ProtectedRoute redirects automatically (RESEARCH Don't-hand-roll table)."
  - "Visible label is 'Sign out' (sentence case) consistent with site copy; matches the test regex /sign out/i regardless of capitalisation."
  - "lucide-react LogOut icon (size={18}) — same icon-size convention as the existing nav links."

patterns-established:
  - "Consumer sidebar footer slot: reuse the flex-col aside; pin button to bottom with mt-auto p-3 border-t. Mirror this for any future per-section sidebars."
  - "Atomic single-commit per plan (CLAUDE.md §4): production source + test land together in one commit even on a TDD plan."

requirements-completed: [CF-CODE-2]

# Metrics
duration: 2min
completed: 2026-05-05
---

# Phase 20.1 Plan 04: Sidebar Sign Out Summary

**Bottom-anchored Sign Out button shipped to the consumer dashboard sidebar via mt-auto footer slot; click calls AuthContext.signOut() exactly once, ProtectedRoute fall-through handles the /login redirect.**

## Performance

- **Duration:** ~2 min
- **Started:** 2026-05-05T03:17:08Z
- **Completed:** 2026-05-05T03:18:57Z
- **Tasks:** 2 (TDD RED + GREEN bundled in one atomic commit per CLAUDE.md §4)
- **Files modified:** 2 (1 source, 1 test)

## Accomplishments

- Sign Out button visible at the bottom of `/dashboard/employer` and `/dashboard/seeker` sidebars (single shared `Sidebar.tsx` renders for both roles).
- Click → `signOut()` from `useAuth()` exactly once; AuthContext clears session, ProtectedRoute redirects to `/login` automatically — no explicit `useNavigate`.
- Existing nav rendering preserved byte-for-byte (aside className unchanged, NavLink block untouched, responsive `hidden md:flex` behavior intact).
- `tests/sidebar-signout.test.tsx` ships as the regression guard (1 it block, asserts `signOutMock` called exactly once via `getByRole('button', { name: /sign out/i })`).
- Phase 21 todo (`2026-05-05-add-sign-out-button-to-dashboard-sidebar-footer.md`) moved to `.planning/todos/done/`.

## Task Commits

Single atomic commit per CLAUDE.md §4 — production source + RED-then-GREEN test in one landing:

1. **Task 1 + 2: Sidebar Sign Out (TDD RED → GREEN)** — `b4c6b4c` (feat)

**Plan metadata commit:** to be authored at the end of this run via `gsd-tools commit` (SUMMARY + STATE + ROADMAP + todo move).

## Files Created/Modified

- `src/components/layout/Sidebar.tsx` (+13/-1 net) — added `LogOut` to lucide import, destructured `signOut` from `useAuth()`, appended a `mt-auto p-3 border-t` footer `<div>` containing a `<button onClick={() => signOut()}>` after the existing `<nav>`. Aside className preserved verbatim.
- `tests/sidebar-signout.test.tsx` (new, 48 lines) — vi.mock(`@/hooks/useAuth`) + vi.mock(`@/lib/supabase`) (defensive — Sidebar's import chain doesn't reach supabase but the mock costs nothing); render Sidebar in MemoryRouter; click Sign Out; assert `signOutMock` called exactly once.
- `.planning/todos/pending/2026-05-05-add-sign-out-button-to-dashboard-sidebar-footer.md` → `.planning/todos/done/` (git mv).

## Decisions Made

- **Atomic single-commit landing of TDD RED + GREEN.** CLAUDE.md §4 mandates one commit per plan. Splitting into a RED commit (`test(20.1-04): add failing test`) followed by a GREEN commit (`feat(20.1-04): add Sign Out`) would have produced two commits for a 14-line behavioral change — out of policy. RED state was empirically observed pre-commit (test ran, failed with RTL `Unable to find an element by [role="button"][name="/sign out/i"]`) before the production edit, then re-ran post-edit (174 passed = 173 baseline + 1 new). Mirrors the Phase 20.1-01 atomic-TDD-bundle precedent.
- **Visible label "Sign out" (sentence case).** Matches the regex `/sign out/i` regardless. Sentence case is consistent with other site CTAs.
- **No aside restructure.** Existing aside is already `flex flex-col` with `min-h-[calc(100vh-56px)]`. Adding `mt-auto` to a sibling `<div>` after the `<nav>` is sufficient. Verified via the regression-guard grep (`hidden md:flex flex-col w-60 ... border-r` count=1 post-edit).
- **No `useNavigate` import.** AuthContext's `onAuthStateChange` clears session → ProtectedRoute fall-through redirects to `/login`. Verified via grep (`useNavigate` count=0 in Sidebar.tsx). Mirrors the Phase 20.1-03 AdminLoginPage pattern (RESEARCH Pitfall 9).

## Deviations from Plan

None — plan executed exactly as written. Imports updated (LogOut), destructure swapped (`{ role, signOut }`), footer JSX appended verbatim from RESEARCH §"Pitfall 5" AFTER snippet. No bugs found, no missing critical functionality, no blockers.

## Issues Encountered

None.

## Verification Evidence

- **RED state (pre-edit):** `pnpm test -- sidebar-signout` — Test Files 1 failed | 28 passed; tests 1 failed | 173 passed | 113 todo. Failure mode: `Unable to find an accessible element with the role "button" and name \`/sign out/i\`` (RTL `getElementError` from `@testing-library/dom/dist/config.js:37`). Confirms the failure is from the missing button, not from the mock plumbing.
- **GREEN state (post-edit):** `pnpm test -- sidebar-signout` — Test Files 29 passed | 7 skipped; tests 174 passed | 113 todo (287 total). Sidebar test 1/1 green; full vitest baseline preserved (173 → 174, +1 new only).
- **TypeScript:** `pnpm tsc --noEmit` exit 0 (no output).
- **Acceptance criteria grep sweep (all PASS):**
  - `LogOut,` import present in Sidebar.tsx (1)
  - `const { role, signOut } = useAuth()` destructure (1)
  - `mt-auto p-3 border-t` footer wrapper (1)
  - `<span>Sign out</span>` label (1)
  - `onClick={() => signOut()}` handler (1)
  - `<LogOut size={18} />` icon (1)
  - Existing aside className `hidden md:flex flex-col w-60 flex-shrink-0 min-h-[calc(100vh-56px)] border-r` preserved (1)
  - `useNavigate` count = 0 in Sidebar.tsx (regression guard for AuthContext-driven redirect)
  - tests/sidebar-signout.test.tsx: 1 `it(` block, 1 `toHaveBeenCalledTimes(1)`, 1 `getByRole('button', { name: /sign out/i })`
- **Atomic commit b4c6b4c file count:** 2 files (`src/components/layout/Sidebar.tsx`, `tests/sidebar-signout.test.tsx`); +61/-1 net.

## Next Phase Readiness

- Plan 05 (bootstrap UAT + role removal + ship) is the only remaining plan in Phase 20.1.
- All app-layer carryforwards (CF-AUTH-1, CF-AUTH-2, CF-AUTH-3, CF-CODE-1, CF-CODE-2) now closed with empirical evidence.
- Plan 5 is operator-action only: Studio Auth user create + Studio SQL admin role assign + UAT + old-role removal + 20.1-VERIFICATION.md + ROADMAP `[x]`.
- No blockers; no concerns.

## Self-Check

- Files exist:
  - `src/components/layout/Sidebar.tsx` — FOUND (verified post-edit, 84 lines)
  - `tests/sidebar-signout.test.tsx` — FOUND (48 lines, 1 it block)
  - `.planning/todos/done/2026-05-05-add-sign-out-button-to-dashboard-sidebar-footer.md` — FOUND (git mv staged)
  - `.planning/todos/pending/2026-05-05-add-sign-out-button-to-dashboard-sidebar-footer.md` — MOVED (no longer present)
- Commit `b4c6b4c` exists in `git log` (verified via `git show --stat`).
- ## Self-Check: PASSED

---
*Phase: 20.1-standalone-admin-login-gateway-account-bootstrap*
*Completed: 2026-05-05*
