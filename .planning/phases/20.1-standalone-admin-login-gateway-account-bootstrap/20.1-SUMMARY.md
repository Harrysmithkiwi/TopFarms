---
phase: 20.1-standalone-admin-login-gateway-account-bootstrap
type: phase-summary
plans: 5
status: complete
verified: 2026-05-05T03:45:00Z

requires:
  - phase: 20-super-admin-dashboard
    provides: AdminLayout, AdminSidebar, /admin route registry, ProtectedRoute requiredRole='admin', admin_* RPCs, ADMIN-BOOTSTRAP-1 UAT precedent
provides:
  - Standalone /admin login gateway (AdminGate hybrid route — render AdminLoginPage for unauth/non-admin, AdminLayout for admin)
  - dashboardPathFor(role) helper unifying 5 inline ternaries
  - Sign Out button in consumer dashboard Sidebar footer
  - Dedicated admin@topfarms.co.nz Supabase Auth account with role=admin
  - Removal of admin role from harry.symmans.smith@gmail.com (Phase 20 test admin demoted to seeker)
affects: [21-future-admin-features, design-system-tier1-tier2, future-auth-phases]

tech-stack:
  added: []  # NO new libraries — pure composition + helper extraction phase
  patterns:
    - "Hybrid auth gate component (AdminGate) — branches on auth+role state, renders LoginPage for unauthenticated (inverse of redirect-to-login pattern), inline AccessDenied for non-admin authenticated"
    - "Pure helper extraction with TDD-first refactor (write 3 unit tests, then helper, then atomic 5-callsite swap)"
    - "Studio SQL bootstrap precedent for new auth.users + role assign + role transfer (mirrors Phase 20-08 ADMIN-BOOTSTRAP-1)"
    - "Lockout-sequencing gate for credential transfers (RESEARCH Pitfall 2 — new account UAT must PASS before old role is removed)"

key-files:
  created:
    - "src/lib/routing.ts (Plan 01)"
    - "src/pages/admin/AdminLoginPage.tsx (Plan 03 — exports AdminGate + AdminLoginPage + AccessDeniedView)"
    - "tests/dashboard-routing.test.ts (Plan 01)"
    - "tests/admin-login.test.tsx (Plan 03)"
    - "tests/sidebar-signout.test.tsx (Plan 04)"
    - ".planning/phases/20.1-standalone-admin-login-gateway-account-bootstrap/20.1-VERIFICATION.md (Plan 05)"
  modified:
    - "src/main.tsx (Plan 03 — /admin route element swapped to <AdminGate />)"
    - "src/pages/auth/Login.tsx:49 (Plan 02)"
    - "src/pages/auth/VerifyEmail.tsx:36 (Plan 02)"
    - "src/components/layout/ProtectedRoute.tsx:64 (Plan 02)"
    - "src/pages/auth/SelectRole.tsx:31 (Plan 02)"
    - "src/components/layout/Nav.tsx:113 (Plan 02)"
    - "src/components/layout/Sidebar.tsx (Plan 04 — mt-auto p-3 border-t footer slot)"
    - "tests/admin-bootstrap-UAT.md (Plan 05 — Phase 20.1 transfer run-record appended)"

key-decisions:
  - "Hybrid AdminGate at /admin replaces ProtectedRoute wrapper (CONTEXT Q1) — renders AdminLoginPage for unauthenticated/non-admin (inverse-redirect), AdminLayout for admin"
  - "AdminLoginPage email + password ONLY (CONTEXT GA-1) — no Google/Facebook OAuth on the admin gateway; minimal functional brand surface"
  - "Inline AccessDenied (NOT redirect) for authenticated non-admin (CONTEXT Q3) — redirect would confuse since user IS authenticated"
  - "dashboardPathFor helper at src/lib/routing.ts (CONTEXT Q4) — pure function with type-only import, zero runtime deps, cannot create cycles"
  - "Studio SQL Editor for one-off DB writes (CLAUDE §2) — both bootstrap + role-removal SQL ran via Studio, NOT MCP; avoids --read-only flag-flip restart cycle"
  - "Audit-trail bypass acceptable for one-shot bootstrap operations (CONTEXT §Deferred) — Studio SQL writes don't append to admin_audit_log; documented in VERIFICATION.md"
  - "Lockout sequencing — old role removal MUST run AFTER new account UAT passes (RESEARCH Pitfall 2); if new account broken, restore path is Studio SQL re-grant, but operator never has zero admins concurrently"
  - "AccessDenied UI runtime caveat (CF-AUTH-2) — documented anomalous browser observation during regression UAT 8a attributed to stale cache; unit test passes, code path verified by grep, recommend fresh-session post-deploy re-verify (not a blocker per CLAUDE §7 — empirical proof exists at multiple layers)"

patterns-established:
  - "Hybrid auth gate at gateway routes: branches on {loading, !session, role===null, role!==required, role===required} 5 ways. Mirror ProtectedRoute.tsx:13-26 + :41-55 for spinner/race-window branches byte-for-byte; inline duplication is acceptable per CONTEXT Q2 (extract to AuthLoadingSpinner DEFERRED)"
  - "TDD-first helper extraction: tests file written first (RED state empirically observed via failed import), then helper, both in single atomic commit per CLAUDE §4"
  - "5-callsite atomic refactor with surgical Edit (not sed-style global replace) per RESEARCH Pitfall 4 — each callsite uses unique anchor lines surrounding the target ternary"
  - "Studio SQL operator-action plan with explicit lockout sequencing — UAT gate held BEFORE old credential is destroyed (RESEARCH Pitfall 2); checkpoint:human-action plans gate Task N+1 on resume signal `passed` for Task N"

requirements-completed:
  - CF-AUTH-1
  - CF-AUTH-2
  - CF-AUTH-3
  - CF-ACCOUNT-1
  - CF-ACCOUNT-2
  - CF-CODE-1
  - CF-CODE-2

duration: ~3h wall-clock across 2 sessions (Plans 01–04 in session 1, Plan 05 multi-stage operator-action in session 2)
completed: 2026-05-05
---

# Phase 20.1: Standalone Admin Login Gateway + Account Bootstrap Summary

**Standalone /admin gateway with AdminLoginPage + dedicated admin@topfarms.co.nz account — closes all 7 Phase 20 carryforwards in 5 atomic plans.**

## Performance

- **Plans:** 5/5 executed
- **Plan commits:** `b987eb7` (helper) · `7f61a74` (5-callsite swap) · `0dcda8a` (AdminLoginPage + AdminGate) · `b4c6b4c` (Sidebar Sign Out) · this commit (operator + docs trio)
- **Test deltas:** +8 tests across plans 01/03/04 (3 dashboard-routing + 4 admin-login + 1 sidebar-signout); 174 passed | 113 todo | 0 failed at Plan 04 closure
- **Source LOC:** ~370 lines across 9 files (1 new helper, 1 new page, 1 new route swap, 5 callsite swaps, 1 sidebar mod)

## Plan-by-plan receipts

| Plan | Subsystem | Status | Commit | Key artefacts |
|------|-----------|--------|--------|---------------|
| 20.1-01 | dashboardPathFor helper + 3 unit tests | PASS | `b987eb7` | `src/lib/routing.ts`, `tests/dashboard-routing.test.ts` |
| 20.1-02 | Atomic 5-callsite ternary swap | PASS | `7f61a74` | Login + VerifyEmail + ProtectedRoute + SelectRole + Nav |
| 20.1-03 | AdminLoginPage + AdminGate hybrid + 4 component tests | PASS | `0dcda8a` | `src/pages/admin/AdminLoginPage.tsx`, `src/main.tsx` route swap, `tests/admin-login.test.tsx` |
| 20.1-04 | Sign Out button in Sidebar footer + 1 click test | PASS | `b4c6b4c` | `src/components/layout/Sidebar.tsx`, `tests/sidebar-signout.test.tsx` |
| 20.1-05 | Bootstrap UAT + role transfer + ship | PASS | this commit | `tests/admin-bootstrap-UAT.md` Phase 20.1 run-record + 20.1-VERIFICATION.md + 20.1-SUMMARY.md + 20.1-05-SUMMARY.md + ROADMAP flip + audit close + STATE update |

Per-plan SUMMARYs on disk:
- `20.1-01-dashboard-path-helper-SUMMARY.md`
- `20.1-02-swap-callsites-SUMMARY.md`
- `20.1-03-admin-login-page-SUMMARY.md`
- `20.1-04-sidebar-sign-out-SUMMARY.md`
- `20.1-05-bootstrap-uat-and-ship-SUMMARY.md`

## Carryforward closure (7/7 — see 20.1-VERIFICATION.md for empirical evidence)

| CF-ID | Description | Verdict |
|-------|-------------|---------|
| CF-AUTH-1 | Standalone admin login gateway at `/admin` | PASS |
| CF-AUTH-2 | `/admin` renders AdminLoginPage for unauth/non-admin | PASS (with documented runtime AccessDenied caveat — recommend fresh-session post-deploy re-verify) |
| CF-AUTH-3 | `/login` redirects admin JWT to `/admin` | PASS |
| CF-ACCOUNT-1 | Dedicated admin@topfarms.co.nz account with role=admin | PASS |
| CF-ACCOUNT-2 | Admin role removed from harry.symmans.smith@gmail.com | PASS |
| CF-CODE-1 | dashboardPathFor helper unifies 5 callsites | PASS |
| CF-CODE-2 | Sign Out button in dashboard sidebar footer | PASS |

## Decisions Made

See key-decisions in frontmatter. Most consequential:
- **AdminGate hybrid pattern (CONTEXT Q1):** /admin no longer wrapped in ProtectedRoute — renders AdminLoginPage for unauthenticated (inverse-redirect), inline AccessDenied for non-admin authenticated, AdminLayout for admin. Branching mirrors ProtectedRoute branches BYTE-FOR-BYTE for spinner/race-window cases.
- **Studio SQL Editor for one-off bootstraps (CLAUDE §2):** Both Task 2 (admin role assign) and Task 6 (old-role removal) ran via Studio, NOT MCP. Audit-trail bypass acceptable for one-shot bootstraps; documented in VERIFICATION.md.
- **CF-AUTH-2 caveat handling (CLAUDE §3 + §7):** Anomalous `/dashboard/admin` 404 observed during regression UAT 8a was diagnosed (3 independent evidence lines: grep diagnostic, unit-test pass, helper-test pass) before declaring verdict. Operator-attributed root cause: stale browser/JWT cache. Documented as PASS with caveat — empirical proof of the full requirement exists at unit-test + DB-level + code-path-grep layers. Recommended fresh-session re-verify is a no-blocker post-deploy smoke test.

## Deviations from Plan

None — all 5 plans executed exactly as planned. The CF-AUTH-2 anomaly observation in Plan 05 is NOT a deviation (no scope change, no auto-fix needed); it's a documented runtime observation that did not change the verdict per CLAUDE §3 (diagnose before fix — diagnosed, no fix needed because no code regression exists).

## Issues Encountered

**Stale-cache anomaly during regression UAT 8a (Plan 05 Part A):** Old account direct `/admin` navigation post-signin observed `/dashboard/admin` → 404 instead of inline AccessDenied UI. Three independent diagnostics ruled out a code regression (grep returns only helper body; unit test passes; helper test passes). Operator hypothesis = stale browser/JWT cache from prior admin session or HMR chunk. Resolved as documented caveat (NOT a code fix) per CLAUDE §3 — diagnose before fix means we don't change code when there's no code-level cause. Recommended fresh-session post-deploy re-verify is logged in carryforward.

## User Setup Required

None — no external service configuration required. The `admin@topfarms.co.nz` mailbox/DNS provisioning is explicitly DEFERRED per CONTEXT (auto-confirm sufficient for this phase; mailbox is a phase prerequisite, not a phase deliverable). Password manager storage of the new admin password is operator's responsibility (NOT in repo).

## Next Phase Readiness

- Phase 20.1 closes ALL Phase 20 carryforwards. Phase 20 + 20.1 are now sealed.
- v2.0 milestone: Phase 20.1 entry on ROADMAP.md flipped `[x]` 2026-05-05.
- Phase 21 Sign Out todo: closed by Plan 04 (file `git mv`'d from `.planning/todos/pending/` to `.planning/todos/done/` 2026-05-05).
- Recommended fresh-session post-deploy re-verify of CF-AUTH-2 AccessDenied UI: NO-BLOCKER smoke test, logged in v2.0-MILESTONE-AUDIT.md row.
- Outstanding v2.0 phases: 16 (Privacy Bypass — gap closure), 17 (Saved Search), 18 (Tech Debt), 19 (Design System Tier 1), 19b (Design System Tier 2). All independent of Phase 20.1.

---

*Phase: 20.1-standalone-admin-login-gateway-account-bootstrap*
*Completed: 2026-05-05*
