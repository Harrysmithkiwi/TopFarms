---
phase: 20.1
slug: standalone-admin-login-gateway-account-bootstrap
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-05
---

# Phase 20.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest 3.1.1 + @testing-library/react 16.3.0 + jsdom |
| **Config file** | `vitest.config.ts` (extends `vite.config.ts`; `environment: 'jsdom'`, `globals: true`, setup `tests/setup.ts`, include `tests/**/*.test.{ts,tsx}`) |
| **Quick run command** | `pnpm test -- <file-pattern>` (e.g., `pnpm test -- dashboard-routing`) |
| **Full suite command** | `pnpm test` |
| **Estimated runtime** | ~30–60s full suite (Phase 20 baseline 166 passed / 113 todo); ~2–5s per file pattern |

---

## Sampling Rate

- **After every task commit:** Run `pnpm test -- <pattern>` for the file(s) touched (~2–5s)
- **After every plan wave:** Run `pnpm test` (full suite, ~30–60s)
- **Before `/gsd:verify-work`:** Full suite green + `pnpm tsc --noEmit` clean
- **Max feedback latency:** 60 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 20.1-01-01 | 01 | 1 | CF-CODE-1 | unit | `pnpm test -- dashboard-routing` | ❌ W0 | ⬜ pending |
| 20.1-01-02 | 01 | 1 | CF-CODE-1 | unit | `pnpm test -- dashboard-routing` | ❌ W0 | ⬜ pending |
| 20.1-02-01 | 02 | 2 | CF-CODE-1 (regression) | unit | `pnpm test -- admin-protected-route` | ✅ | ⬜ pending |
| 20.1-02-02 | 02 | 2 | CF-CODE-1 (regression) | full | `pnpm test` | ✅ | ⬜ pending |
| 20.1-03-01 | 03 | 3 | CF-AUTH-1, CF-AUTH-2 | component | `pnpm test -- admin-login` | ❌ W0 | ⬜ pending |
| 20.1-03-02 | 03 | 3 | CF-AUTH-1, CF-AUTH-2 | component | `pnpm test -- admin-login` | ❌ W0 | ⬜ pending |
| 20.1-03-03 | 03 | 3 | CF-AUTH-1, CF-AUTH-2 | component | `pnpm test -- admin-login` | ❌ W0 | ⬜ pending |
| 20.1-04-01 | 04 | 4 | CF-CODE-2 | component | `pnpm test -- sidebar-signout` | ❌ W0 | ⬜ pending |
| 20.1-04-02 | 04 | 4 | CF-CODE-2 | component | `pnpm test -- sidebar-signout` | ❌ W0 | ⬜ pending |
| 20.1-05-01 | 05 | 5 | CF-ACCOUNT-1 | manual UAT | Studio SQL verify + browser sign-in | ❌ W0 (UAT md) | ⬜ pending |
| 20.1-05-02 | 05 | 5 | CF-AUTH-1, CF-AUTH-2 (E2E) | manual UAT | Browser sign-in via `/admin` AdminLoginPage | ❌ W0 (UAT md) | ⬜ pending |
| 20.1-05-03 | 05 | 5 | CF-ACCOUNT-2 | manual UAT | Studio SQL verify + browser sign-in (old account) | ❌ W0 (UAT md) | ⬜ pending |
| 20.1-05-04 | 05 | 5 | CF-AUTH-3 | full + manual | `pnpm test` + Login.tsx redirect smoke | ✅ (full suite) | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*
*Plan IDs are illustrative; planner finalizes wave/task mapping in PLAN.md files.*

---

## Wave 0 Requirements

- [ ] `tests/dashboard-routing.test.ts` — covers CF-CODE-1 (3 unit assertions: employer/seeker/admin)
- [ ] `tests/admin-login.test.tsx` — covers CF-AUTH-1, CF-AUTH-2 (4 component assertions: loading / unauthenticated form / access-denied / admin renders AdminLayout)
- [ ] `tests/sidebar-signout.test.tsx` — covers CF-CODE-2 (1 component assertion: click calls `signOut` once)
- [ ] `tests/admin-bootstrap-2026-05-XX-UAT.md` (new run-record) OR a new `## Run YYYY-MM-DDTHH:MM:SSZ` section appended to existing `tests/admin-bootstrap-UAT.md` — covers CF-ACCOUNT-1, CF-ACCOUNT-2 (manual operator UAT)

No framework install needed — vitest 3.1.1, @testing-library/react 16.3.0, jsdom 29.0.0 already present.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| New `admin@topfarms.co.nz` Supabase Auth user exists with `role='admin'` and `is_active=true` | CF-ACCOUNT-1 | Studio Auth dashboard create-user is operator-only; no API surface in this phase | 1) Studio → Authentication → Add user `admin@topfarms.co.nz` + password. 2) Studio SQL Editor: paste INSERT/UPDATE from RESEARCH §"Studio SQL Bootstrap Sequence". 3) Read-only MCP `execute_sql`: `SELECT u.email, ur.role, ur.is_active FROM auth.users u JOIN public.user_roles ur ON ur.user_id=u.id WHERE u.email='admin@topfarms.co.nz';` → expect 1 row, role=admin, is_active=true |
| End-to-end sign-in via standalone `/admin` AdminLoginPage with new admin account renders AdminLayout | CF-AUTH-1, CF-AUTH-2 | Browser-only flow; no E2E framework in repo | 1) Sign out current session. 2) Navigate to `/admin`. 3) Confirm AdminLoginPage form renders (email + password ONLY, no OAuth buttons). 4) Sign in with `admin@topfarms.co.nz` + password. 5) Confirm AdminLayout renders, AdminSidebar visible, DailyBriefing or default landing renders. 6) Click into Employers/Seekers drawer to confirm row → ProfileDrawer flow still works. |
| Old account `harry.symmans.smith@gmail.com` shows AccessDenied at `/admin` | CF-ACCOUNT-2 | Browser-only flow; verifies role removal landed | 1) Studio SQL Editor: paste UPDATE from RESEARCH §"Studio SQL Bootstrap Sequence" Step 5. 2) Read-only MCP `execute_sql`: `SELECT u.email, ur.role FROM auth.users u JOIN public.user_roles ur ON ur.user_id=u.id WHERE u.email='harry.symmans.smith@gmail.com';` → expect role=seeker. 3) Sign out admin account. 4) Sign in with `harry.symmans.smith@gmail.com`. 5) Navigate to `/admin`. 6) Confirm "Access denied" inline state renders (NOT redirect to `/login`); confirm "Back to your dashboard" link target resolves to `/dashboard/seeker` via `dashboardPathFor`. |
| Existing `/login` flow still routes admin-role JWTs to `/admin` (no regression) | CF-AUTH-3 | Browser-only smoke after refactor — confirms `dashboardPathFor` swap preserves Login.tsx behavior shipped in commit `0e91ff2` | 1) Sign out. 2) Navigate to `/login`. 3) Sign in with `admin@topfarms.co.nz`. 4) Confirm post-login redirect lands at `/admin` and AdminLayout renders. |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify (Plan 5 is intentionally manual UAT — flagged in Manual-Only Verifications above)
- [ ] Wave 0 covers all MISSING references (`tests/dashboard-routing.test.ts`, `tests/admin-login.test.tsx`, `tests/sidebar-signout.test.tsx`, UAT run-record)
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
