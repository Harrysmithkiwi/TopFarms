---
phase: 18.1-pre-launch-hardening
plan: 03
subsystem: database

tags:
  - postgres
  - trigger
  - migration
  - vitest
  - static-source-test

# Dependency graph
requires:
  - phase: 18.1-00
    provides: tests/match-scores-cleanup-trigger.test.ts RED stub (5× it.todo())
  - phase: 13
    provides: jobs.status state machine + on_job_filled trigger pattern (017)
  - phase: 09
    provides: match_scores table + match_scores_job_id_idx (001) + compute_match_scores_batch (009)
provides:
  - "AFTER UPDATE OF status trigger on public.jobs that DELETEs orphaned match_scores rows on active -> non-active transitions"
  - "One-time backfill DELETE for stale match_scores rows accumulated pre-trigger"
  - "Static-source vitest guard against migration 027 deletion / shape drift (5 assertions)"
  - "Code-side closure of SC-5 (Phase 18.1 success criterion #5: stale match_scores rows referencing non-active jobs)"
affects:
  - 18.1-06-operator-uat-and-deploy (Wave 3 empirical UAT — UPDATE jobs status, observe match_scores DELETE; orphan-count MCP query)
  - 18.2-polish (removal of !inner workaround in SeekerStep7Complete.tsx, deferred per 18.1-CONTEXT)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "AFTER UPDATE OF column ON table — column-scoped trigger (only fires when the named column appears in SET clause), narrower than AFTER UPDATE-on-anything used by 017's on_job_filled. Reduces noise."
    - "OLD.status = literal AND NEW.status IS DISTINCT FROM literal — NULL-safe transition guard. Pattern source: 017_notify_job_filled_webhook.sql (which uses IS DISTINCT FROM for the NEW side too)."
    - "DO $verify$ block embedded in migration body asserts post-state at apply time; raises EXCEPTION on drift to roll back the entire BEGIN/COMMIT block. Pattern source: 022_fix_pg_net_http_post_signature.sql."

key-files:
  created:
    - "supabase/migrations/027_match_scores_cleanup_trigger.sql"
  modified:
    - "tests/match-scores-cleanup-trigger.test.ts"

key-decisions:
  - "Trigger name on_jobs_status_change_match_scores_cleanup chosen for distinctness from 017's on_job_filled. Postgres fires triggers alphabetically by name within the same TIMING/EVENT, so on_job_filled fires first, then this cleanup trigger — both inside the same xact."
  - "AFTER UPDATE OF status (column-scoped) preferred over plain AFTER UPDATE — narrower fire surface, only invoked when status appears in SET clause."
  - "Reactivation case (archived -> active) explicitly NOT handled by this trigger — match_scores get recomputed on demand by existing engine (compute_match_scores_batch in 009). Documented in trigger function comment."
  - "Failure mode rolls back the parent jobs.status UPDATE (acceptable per 18.1-CONTEXT). Surfaces broken cleanup at write time rather than letting debris accumulate silently."
  - "!inner workaround at SeekerStep7Complete.tsx left UNCHANGED. Removal deferred to Phase 18.2 polish per 18.1-CONTEXT §Deferred Ideas — defence-in-depth retained until empirical UAT confirms trigger is doing its job."

patterns-established:
  - "Column-scoped AFTER UPDATE trigger with NULL-safe IS DISTINCT FROM guard — reusable for any future state-machine-driven side effect."
  - "One-time backfill DELETE inside the same migration that adds the trigger — closes the historical-debris gap atomically with the going-forward fix."
  - "Embedded DO $verify$ block asserting trigger-attached + function-installed + zero-orphan-row-count — measurable post-apply state captured in the migration itself, not deferred to operator memory."

requirements-completed: []

# Metrics
duration: ~12min
completed: 2026-05-08
---

# Phase 18.1 Plan 03: match_scores cleanup trigger Summary

**AFTER UPDATE trigger on public.jobs deletes orphaned match_scores rows on active -> non-active transitions, with one-time backfill for pre-trigger debris and a 5-assertion static-source vitest guard.**

## Performance

- **Duration:** ~12 min
- **Started:** 2026-05-08T07:33:58Z (Wave 1 dispatch — STATE.md `last_updated`)
- **Completed:** 2026-05-08T07:45:09Z
- **Tasks:** 1 (TDD-bundled migration + test flip)
- **Files modified:** 2 (1 created + 1 flipped)

## Accomplishments

- Migration `027_match_scores_cleanup_trigger.sql` authored with trigger function `cleanup_match_scores_on_status_change()` (SECURITY DEFINER, search_path = public), `on_jobs_status_change_match_scores_cleanup` AFTER UPDATE OF status FOR EACH ROW trigger, one-time backfill DELETE for stale rows, and embedded `DO $verify$` post-state assertions.
- `tests/match-scores-cleanup-trigger.test.ts` flipped from 5× `it.todo()` to 5× `it()` — readFileSync + regex shape assertions passing GREEN.
- Atomic single commit per CLAUDE §4 covering migration + test.
- Code-side closure of SC-5 (Phase 18.1 #5 / orig #19). Runtime closure pending Studio apply + Wave 3 UAT (18.1-06).

## Task Commits

1. **Task 1: Author migration 027 + flip test GREEN** — `1d68769` (feat)

_Note: TDD bundled into single atomic commit per CLAUDE §4 + Wave 0 (6d6dfa3) + Wave 1 sibling precedent (Phase 17-01 1c6a0fc, Phase 20.1-04 b4c6b4c)._

## Files Created/Modified

- `supabase/migrations/027_match_scores_cleanup_trigger.sql` — NEW. 109 lines. Trigger function + trigger + backfill + DO $verify$ block.
- `tests/match-scores-cleanup-trigger.test.ts` — MODIFIED. Wave 0 stub flipped GREEN; 5 readFileSync + regex assertions pass.
- `.planning/phases/18.1-pre-launch-hardening/deferred-items.md` — NEW (out-of-scope discovery log; not in scoped commit, will be picked up by sibling 18.1-02 commit or final phase commit).

## Empirical Evidence

### Pre-flight: pg_constraint check (read-only MCP — operator/parent session)

Per 18.1-RESEARCH §Pitfall 4, we need to confirm nothing FK-references `match_scores` before authoring a DELETE-from-trigger. The check:

```sql
SELECT conname, conrelid::regclass AS referencing_table, confrelid::regclass AS referenced_table
  FROM pg_constraint
 WHERE contype = 'f'
   AND confrelid = 'public.match_scores'::regclass;
-- Expected: 0 rows.
```

**Status:** Pending operator/parent-session execution. The migration is authored on the assumption (load-bearing per RESEARCH) that match_scores is a leaf table — `001_initial_schema.sql:166-188` defines match_scores with FK columns OUT (job_id, seeker_id, employer_id) but no FKs IN; consistent with the leaf-table claim.

### Apply via Supabase Studio SQL Editor (operator action — CLAUDE §2)

The migration body is in `supabase/migrations/027_match_scores_cleanup_trigger.sql`. Operator paste-into-Studio path:

1. Open Supabase Studio for project `inlagtgpynemhipnqvty`.
2. SQL Editor → New query → paste the entire `BEGIN; ... COMMIT;` block.
3. Run. The embedded `DO $verify$` block raises a NOTICE on success or an EXCEPTION on drift (rolling the entire transaction back).

**Status:** Pending. Will be performed by the operator (likely batched with sibling 18.1-01's 025 + 18.1-02's 026 in the Wave 3 plan 18.1-06 operator-action window, or before that if convenient).

### Post-apply verification queries (read-only MCP — operator/parent session)

```sql
-- (a) Trigger attached
SELECT tgname, tgisinternal
  FROM pg_trigger
 WHERE tgrelid = 'public.jobs'::regclass
   AND tgname = 'on_jobs_status_change_match_scores_cleanup';
-- Expected: 1 row, tgisinternal=false.

-- (b) Function installed
SELECT proname, prosecdef
  FROM pg_proc p JOIN pg_namespace n ON n.oid = p.pronamespace
 WHERE n.nspname = 'public' AND p.proname = 'cleanup_match_scores_on_status_change';
-- Expected: 1 row, prosecdef=true.

-- (c) Zero orphans post-backfill
SELECT count(*) AS orphan_count
  FROM public.match_scores ms
  JOIN public.jobs j ON j.id = ms.job_id
 WHERE j.status IS DISTINCT FROM 'active';
-- Expected: 0.
```

**Status:** Pending Studio apply. Operator captures all 3 query results into the Wave 3 18.1-06 plan SUMMARY.

### Vitest output (executed; GREEN)

```
$ pnpm exec vitest run tests/match-scores-cleanup-trigger.test.ts
 RUN  v3.2.4 /Users/harrysmith/dev/topfarms

 ✓ tests/match-scores-cleanup-trigger.test.ts (5 tests) 2ms

 Test Files  1 passed (1)
      Tests  5 passed (5)
   Duration  881ms
```

5 passed | 0 todo | 0 fail. Acceptance criterion met.

### Full suite (executed; 1 sibling-attributed failure, 0 self-attributed)

```
$ pnpm test
 Test Files  1 failed | 37 passed | 9 skipped (47)
      Tests  242 passed | 121 todo (363)
```

The 1 failed file is `tests/mark-job-filled-rpc.test.ts` — sibling executor 18.1-02's in-progress JSX-in-`.ts` transform error (esbuild rejects JSX in .ts extension). **Out of scope for 18.1-03 per `<critical_constraints>`.** Logged to `.planning/phases/18.1-pre-launch-hardening/deferred-items.md` for sibling executor / verifier to address (sibling has since renamed to .tsx — visible in `git status` post-commit).

Suite excluding sibling's broken file: **`37 passed | 9 skipped | 242 passed | 121 todo | 0 failures`** — confirming 18.1-03's contribution is empirically GREEN. Verified via `pnpm exec vitest run --exclude tests/mark-job-filled-rpc.test.ts`.

### TypeScript check (executed; clean)

```
$ pnpm tsc --noEmit
EXIT=0
```

Zero type errors.

## Decisions Made

See `key-decisions` in frontmatter.

## Deviations from Plan

### Inadvertently included sibling-staged rename in atomic commit

**1. [Documenting, not auto-fixing] Pre-staged rename `tests/mark-job-filled-rpc.test.ts -> .tsx` landed in commit `1d68769`**
- **Found during:** post-commit `git show --stat` audit
- **Issue:** Sibling executor 18.1-02 had pre-staged a file rename (capital `R` in `git status` index column = rename staged) before this executor began. When this executor ran `git add` for its 2 scoped files and then committed, git's commit picked up everything in the index, including the pre-staged rename. The rename is a zero-content move (`100%` similarity in the rename header) — no sibling code shipped, but the rename did.
- **Severity:** LOW. The renamed file's content is the unfinished sibling state under a new name. No semantic drift introduced. The rename is necessary infrastructure for the sibling 18.1-02 executor's eventual GREEN flip (esbuild needs `.tsx` for JSX).
- **Per CLAUDE §3 + §4 + §8:** No `git reset` was attempted. History-rewriting commands are off-limits without explicit operator instruction. The atomic-commit-window precedent (BFIX-02/03 amended into 14-03 commit `e8f0882`) sanctions amendments within the same window when internal consistency requires it; this is mid-wave parallel work, not a hindsight amendment.
- **Recommended follow-up:** None at executor level. The sibling 18.1-02 executor's commit will reference the new `.tsx` filename without re-staging the rename (since it's already in tree). Verifier should confirm the sibling commit body accurately describes its content vs the rename being silently absorbed by 18.1-03.
- **Documented:** in this SUMMARY's Deviations section + commit body intentionally describes only 18.1-03's scope.

---

**Total deviations:** 1 documented (no auto-fixes; no scope creep)
**Impact on plan:** Zero functional impact on 18.1-03's contract (027 + GREEN test). Surfaces a parallel-wave file-discipline observation for the verifier.

## Issues Encountered

None at the executor's scope. Sibling 18.1-02's broken-test-transform is logged to `deferred-items.md` and is the sibling's lane.

## User Setup Required

**Studio SQL Editor migration apply** — operator runs the body of `supabase/migrations/027_match_scores_cleanup_trigger.sql` against project `inlagtgpynemhipnqvty` via Studio SQL Editor (CLAUDE §2). The embedded `DO $verify$` block makes apply self-asserting: success → NOTICE; drift → EXCEPTION + rollback.

**Post-apply MCP read-only verification** — three queries documented above (pg_trigger, pg_proc, orphan count). Captured by operator into Wave 3 plan 18.1-06 SUMMARY.

This step is bundled with siblings 18.1-01 (025) and 18.1-02 (026) for one Studio session covering all three Wave 1 migrations.

## Next Phase Readiness

- **Wave 1 disjoint-files acceptance:** 18.1-03's two files do not touch sibling executors' files — discipline preserved.
- **Wave 2 dependencies:** 18.1-04 (X-Webhook-Secret) and 18.1-05 (doc-types drift) are independent of this plan; no blockers.
- **Wave 3 dependencies:** 18.1-06 operator-action plan needs the Studio apply + post-apply MCP verification of all three Wave 1 migrations (025 + 026 + 027). The verification queries are captured here for batch execution.
- **!inner workaround removal:** intentionally NOT done in this phase per CONTEXT. Phase 18.2 polish item — depends on empirical UAT proving the trigger does its job in production.

## Self-Check: PASSED

- [x] `supabase/migrations/027_match_scores_cleanup_trigger.sql` exists (FOUND on disk; 109 lines)
- [x] `tests/match-scores-cleanup-trigger.test.ts` exists and is GREEN (FOUND; 5 passed | 0 todo | 0 fail)
- [x] Commit `1d68769` exists in git log (FOUND)
- [x] `.planning/phases/18.1-pre-launch-hardening/deferred-items.md` exists (FOUND)
- [x] Migration body contains `CREATE OR REPLACE FUNCTION public.cleanup_match_scores_on_status_change` (verified via grep)
- [x] Migration body contains `OLD.status = 'active' AND NEW.status IS DISTINCT FROM 'active'` (verified via grep)
- [x] Migration body contains `CREATE TRIGGER on_jobs_status_change_match_scores_cleanup` (verified via grep)
- [x] Migration body contains `AFTER UPDATE OF status ON public.jobs` (verified via grep)
- [x] Migration body contains backfill `DELETE FROM public.match_scores ms WHERE EXISTS` (verified via grep)
- [x] Migration body contains embedded `DO $verify$` block (verified via grep)
- [x] tsc clean (EXIT=0)
- [x] Scoped vitest GREEN (5 passed | 0 todo | 0 fail)
- [x] Full suite shows zero self-attributed failures (1 sibling-attributed failure logged out of scope)
- [x] `!inner` workaround in `src/pages/dashboard/seeker/SeekerStep7Complete.tsx` UNCHANGED (not in commit `1d68769`)
- [x] No destructive git operations attempted

---
*Phase: 18.1-pre-launch-hardening*
*Plan: 03*
*Completed: 2026-05-08*
