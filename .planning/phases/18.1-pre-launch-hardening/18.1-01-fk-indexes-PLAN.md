---
phase: 18.1-pre-launch-hardening
plan: 01
type: execute
wave: 1
depends_on:
  - "18.1-00"
files_modified:
  - supabase/migrations/025_phase_18_1_fk_indexes.sql
  - tests/fk-indexes.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Migration `025_phase_18_1_fk_indexes.sql` exists on disk with 15 `CREATE INDEX IF NOT EXISTS` statements"
    - "All 15 indexes follow the `<table>_<column>_idx` naming convention from `001_initial_schema.sql:140-188`"
    - "Migration applied via Supabase Studio SQL Editor (CLAUDE §2); 15 indexes visible via read-only MCP `pg_indexes` query"
    - "`tests/fk-indexes.test.ts` flips from RED-todo to GREEN with all 16 assertions passing"
    - "SC-6 (orig #10a — 15× unindexed_foreign_keys advisor lints) is closed at code-side; runtime-side closure verified at Wave 3 by operator MCP query"
  artifacts:
    - path: supabase/migrations/025_phase_18_1_fk_indexes.sql
      provides: "Idempotent FK index migration covering 15 unindexed FKs across 8 tables"
      contains: "CREATE INDEX IF NOT EXISTS"
    - path: tests/fk-indexes.test.ts
      provides: "Static-source guard against migration 025 deletion or shape drift"
      contains: "describe('FK index migration 025"
  key_links:
    - from: "supabase/migrations/025_phase_18_1_fk_indexes.sql"
      to: "Live Supabase project `inlagtgpynemhipnqvty` pg_indexes"
      via: "Studio SQL Editor paste-and-run (CLAUDE §2)"
      pattern: "<table>_<col>_idx"
    - from: "tests/fk-indexes.test.ts"
      to: "supabase/migrations/025_phase_18_1_fk_indexes.sql"
      via: "readFileSync + 16 regex assertions (1 count + 15 per-index)"
      pattern: "readFileSync.*025_phase_18_1_fk_indexes"
---

<objective>
Create migration `025_phase_18_1_fk_indexes.sql` with 15 `CREATE INDEX IF NOT EXISTS` statements covering the 15 unindexed_foreign_keys advisor lints surfaced via `mcp__supabase__get_advisors --type=performance`. Apply via Supabase Studio SQL Editor per CLAUDE §2. Flip `tests/fk-indexes.test.ts` from RED-todo to GREEN.

Purpose: Closes SC-6 (Phase 18.1 success criterion #6 — "14× unindexed_foreign_keys advisor lints addressed"; live count is 15 — see 18.1-RESEARCH §Pitfall 6 for the count drift between ROADMAP §"Phase 18.1" SC-6 wording (14) and live advisor (15)). FK indexes have material query-time impact pre-launch; landing them avoids a slow-query class once production traffic arrives.

Output:
- 1 new migration file (paste-ready body in `<action>`)
- 1 test file (Wave 0 stub) flipped GREEN
- Migration applied to live project `inlagtgpynemhipnqvty` via Studio SQL Editor
- Read-only MCP `pg_indexes` query confirms 15 expected index names
- One atomic commit per CLAUDE §4

Constraints (CLAUDE §4 + §8 — surface to executor):
- One atomic commit at the end. DO NOT amend prior commits.
- DO NOT run history-rewriting commands (`git reset --hard`, `git rebase`, `git push --force`, `git checkout --` over uncommitted work, `git branch -D`, `git clean -f`) under ANY circumstances unless operator explicitly types the command in chat. Surface a `STOP` notice if a situation appears to call for a reset (precedent: §8 Phase 17 Wave A reset incident).
- DO NOT use MCP `apply_migration` while `--read-only` is ON (CLAUDE §2 sub-finding). Use Studio SQL Editor.
- Studio-applied migrations don't write `supabase_migrations.schema_migrations` rows — verify via `pg_indexes`, NOT `list_migrations` (CLAUDE §2 sub-finding; see Phase 17-01 + 20-02 + 20-08 precedent).
</objective>

<execution_context>
@/Users/harrysmith/.claude/get-shit-done/workflows/execute-plan.md
@/Users/harrysmith/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/18.1-pre-launch-hardening/18.1-CONTEXT.md
@.planning/phases/18.1-pre-launch-hardening/18.1-RESEARCH.md
@.planning/phases/18.1-pre-launch-hardening/18.1-VALIDATION.md
@CLAUDE.md
@supabase/migrations/001_initial_schema.sql
@tests/fk-indexes.test.ts

<interfaces>
<!-- Index naming precedent (extract from 001_initial_schema.sql lines 140-188): -->
-- jobs_employer_id_idx           ON public.jobs(employer_id)
-- match_scores_seeker_id_idx     ON public.match_scores(seeker_id)
-- match_scores_job_id_idx        ON public.match_scores(job_id)
-- applications_seeker_id_idx     ON public.applications(seeker_id)
-- applications_job_id_idx        ON public.applications(job_id)
-- ... etc.
-- Pattern: <table>_<column>_idx, all in public schema.

<!-- 15 expected indexes (advisor output, captured 2026-05-07): -->
-- admin_notes(admin_id)            → admin_notes_admin_id_idx
-- job_skills(skill_id)             → job_skills_skill_id_idx
-- listing_fees(employer_id)        → listing_fees_employer_id_idx
-- listing_fees(job_id)             → listing_fees_job_id_idx
-- message_threads(employer_id)     → message_threads_employer_id_idx
-- message_threads(job_id)          → message_threads_job_id_idx
-- message_threads(seeker_id)       → message_threads_seeker_id_idx
-- messages(sender_id)              → messages_sender_id_idx
-- messages(thread_id)              → messages_thread_id_idx
-- placement_fees(application_id)   → placement_fees_application_id_idx
-- placement_fees(employer_id)      → placement_fees_employer_id_idx
-- placement_fees(job_id)           → placement_fees_job_id_idx
-- placement_fees(seeker_id)        → placement_fees_seeker_id_idx
-- saved_jobs(job_id)               → saved_jobs_job_id_idx
-- seeker_skills(skill_id)          → seeker_skills_skill_id_idx
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author migration 025 + flip fk-indexes test GREEN</name>
  <files>
    supabase/migrations/025_phase_18_1_fk_indexes.sql
    tests/fk-indexes.test.ts
  </files>
  <read_first>
    - .planning/phases/18.1-pre-launch-hardening/18.1-RESEARCH.md (§"Pattern 5: 15× FK index migration" + §"Code Examples" Item #5 — paste-ready bodies)
    - .planning/phases/18.1-pre-launch-hardening/18.1-CONTEXT.md (§"Item #6 / orig #10a — 15× unindexed_foreign_keys advisor lints" — locked decision)
    - tests/fk-indexes.test.ts (Wave 0 stub from plan 18.1-00 — see what `it.todo` titles to match)
    - supabase/migrations/001_initial_schema.sql (lines 140-188 — naming convention reference)
    - CLAUDE.md §2 (Studio SQL Editor preference)
  </read_first>
  <action>
**Step 1 — Create migration `supabase/migrations/025_phase_18_1_fk_indexes.sql`** (paste-ready body, 15× CREATE INDEX IF NOT EXISTS):

```sql
-- ============================================================
-- 025_phase_18_1_fk_indexes.sql
-- TopFarms — Phase 18.1 #6 / orig #10a
--
-- Closes 15× unindexed_foreign_keys advisor lints surfaced via
-- mcp__supabase__get_advisors --type=performance (captured 2026-05-07).
--
-- Naming: <table>_<column>_idx per 001_initial_schema.sql:140-188 precedent.
-- Idempotent: CREATE INDEX IF NOT EXISTS — re-running this migration is safe.
-- Plain CREATE INDEX (NOT CONCURRENTLY) — Studio SQL Editor wraps multi-statement
-- runs in an implicit transaction; CREATE INDEX CONCURRENTLY cannot run inside
-- a transaction. Pre-launch tables are tiny (admin_notes, placement_fees,
-- message_threads, messages, listing_fees mostly empty or single-digit row counts);
-- index build is millisecond-scale on these.
--
-- Apply path: Supabase Studio SQL Editor for project ref inlagtgpynemhipnqvty
-- (CLAUDE §2). Verify post-apply via read-only MCP pg_indexes query — Studio
-- applies don't write supabase_migrations.schema_migrations rows.
-- ============================================================

BEGIN;

-- admin_notes (1 FK)
CREATE INDEX IF NOT EXISTS admin_notes_admin_id_idx        ON public.admin_notes        (admin_id);

-- job_skills (1 FK)
CREATE INDEX IF NOT EXISTS job_skills_skill_id_idx         ON public.job_skills         (skill_id);

-- listing_fees (2 FKs)
CREATE INDEX IF NOT EXISTS listing_fees_employer_id_idx    ON public.listing_fees       (employer_id);
CREATE INDEX IF NOT EXISTS listing_fees_job_id_idx         ON public.listing_fees       (job_id);

-- message_threads (3 FKs)
CREATE INDEX IF NOT EXISTS message_threads_employer_id_idx ON public.message_threads    (employer_id);
CREATE INDEX IF NOT EXISTS message_threads_job_id_idx      ON public.message_threads    (job_id);
CREATE INDEX IF NOT EXISTS message_threads_seeker_id_idx   ON public.message_threads    (seeker_id);

-- messages (2 FKs)
CREATE INDEX IF NOT EXISTS messages_sender_id_idx          ON public.messages           (sender_id);
CREATE INDEX IF NOT EXISTS messages_thread_id_idx          ON public.messages           (thread_id);

-- placement_fees (4 FKs)
CREATE INDEX IF NOT EXISTS placement_fees_application_id_idx ON public.placement_fees   (application_id);
CREATE INDEX IF NOT EXISTS placement_fees_employer_id_idx    ON public.placement_fees   (employer_id);
CREATE INDEX IF NOT EXISTS placement_fees_job_id_idx         ON public.placement_fees   (job_id);
CREATE INDEX IF NOT EXISTS placement_fees_seeker_id_idx      ON public.placement_fees   (seeker_id);

-- saved_jobs (1 FK)
CREATE INDEX IF NOT EXISTS saved_jobs_job_id_idx           ON public.saved_jobs         (job_id);

-- seeker_skills (1 FK)
CREATE INDEX IF NOT EXISTS seeker_skills_skill_id_idx      ON public.seeker_skills      (skill_id);

COMMIT;

-- Post-verify (run as separate read-only MCP query after Studio apply):
--   SELECT indexname FROM pg_indexes
--    WHERE schemaname = 'public'
--      AND indexname IN (
--        'admin_notes_admin_id_idx','job_skills_skill_id_idx',
--        'listing_fees_employer_id_idx','listing_fees_job_id_idx',
--        'message_threads_employer_id_idx','message_threads_job_id_idx','message_threads_seeker_id_idx',
--        'messages_sender_id_idx','messages_thread_id_idx',
--        'placement_fees_application_id_idx','placement_fees_employer_id_idx',
--        'placement_fees_job_id_idx','placement_fees_seeker_id_idx',
--        'saved_jobs_job_id_idx','seeker_skills_skill_id_idx'
--      )
--    ORDER BY indexname;
--   -- Expect 15 rows.
```

**Step 2 — Apply migration via Supabase Studio SQL Editor.**

CLAUDE §2: Studio SQL Editor is the preferred path for one-off DB writes. Project ref `inlagtgpynemhipnqvty`. The `--read-only` MCP flag is ON by default and a flip cycle requires a Claude Code restart, which is unnecessary here.

1. Open Supabase Studio for project `inlagtgpynemhipnqvty`.
2. Navigate: SQL Editor → New query.
3. Paste the entire `BEGIN; ... COMMIT;` block from Step 1 (15× CREATE INDEX statements).
4. Click Run. Studio shows "Success" with no rows returned (DDL doesn't return rows).
5. Capture the run timestamp + Studio output for the SUMMARY (CLAUDE §3 — show the SQL body and confirm it landed before any next step).

**Step 3 — Verify via read-only MCP `pg_indexes` query.**

Run via Supabase MCP `execute_sql` (read-only — no flag flip needed):

```sql
SELECT indexname FROM pg_indexes
 WHERE schemaname = 'public'
   AND indexname IN (
     'admin_notes_admin_id_idx','job_skills_skill_id_idx',
     'listing_fees_employer_id_idx','listing_fees_job_id_idx',
     'message_threads_employer_id_idx','message_threads_job_id_idx','message_threads_seeker_id_idx',
     'messages_sender_id_idx','messages_thread_id_idx',
     'placement_fees_application_id_idx','placement_fees_employer_id_idx',
     'placement_fees_job_id_idx','placement_fees_seeker_id_idx',
     'saved_jobs_job_id_idx','seeker_skills_skill_id_idx'
   )
 ORDER BY indexname;
```

Expect 15 rows. If fewer, capture the actual count and which indexes are missing — diagnose before retry (CLAUDE §3).

**Step 4 — Flip `tests/fk-indexes.test.ts` from RED-todo to GREEN.** Replace Wave 0 stubs with the full body skeleton from 18.1-RESEARCH §"Code Examples" Item #5 (paste-ready):

```typescript
// Phase 18.1 #6 — 15× FK index migration shape guard.
//
// DB-level verification runs post-apply via read-only MCP pg_indexes query
// (operator action in plan SUMMARY at Wave 3). This test guards 025's migration
// body — 15 CREATE INDEX IF NOT EXISTS lines, naming pattern <table>_<col>_idx.

import { describe, it, expect } from 'vitest'
import { readFileSync } from 'node:fs'
import { resolve } from 'node:path'

const SQL_PATH = resolve(__dirname, '..', 'supabase/migrations/025_phase_18_1_fk_indexes.sql')

describe('FK index migration 025', () => {
  const sql = readFileSync(SQL_PATH, 'utf8')

  it('contains 15 CREATE INDEX IF NOT EXISTS statements', () => {
    const matches = sql.match(/CREATE\s+INDEX\s+IF\s+NOT\s+EXISTS/g) ?? []
    expect(matches).toHaveLength(15)
  })

  // 15 expected indexes by table and column
  const EXPECTED: Array<[string, string]> = [
    ['admin_notes', 'admin_id'],
    ['job_skills', 'skill_id'],
    ['listing_fees', 'employer_id'], ['listing_fees', 'job_id'],
    ['message_threads', 'employer_id'], ['message_threads', 'job_id'], ['message_threads', 'seeker_id'],
    ['messages', 'sender_id'], ['messages', 'thread_id'],
    ['placement_fees', 'application_id'], ['placement_fees', 'employer_id'],
    ['placement_fees', 'job_id'],         ['placement_fees', 'seeker_id'],
    ['saved_jobs', 'job_id'],
    ['seeker_skills', 'skill_id'],
  ]

  it.each(EXPECTED)('declares index on public.%s(%s)', (table, col) => {
    const indexName = `${table}_${col}_idx`
    expect(sql).toMatch(new RegExp(`CREATE\\s+INDEX\\s+IF\\s+NOT\\s+EXISTS\\s+${indexName}\\s+ON\\s+public\\.${table}\\s*\\(${col}\\)`))
  })
})
```

**Step 5 — Run vitest, confirm GREEN.**

```
pnpm test -- tests/fk-indexes.test.ts
```

Expect 16 assertions pass (1 count + 15 it.each). Then run `pnpm test` (full suite) — expect zero new failures.

**Step 6 — Atomic commit per CLAUDE §4:**

```
git add supabase/migrations/025_phase_18_1_fk_indexes.sql tests/fk-indexes.test.ts
git commit -m "feat(18.1): add 15× FK indexes migration 025 + GREEN drift test"
```

Capture the commit SHA + Studio apply timestamp + MCP verify count for the SUMMARY.

DO NOT touch any other files. DO NOT modify migrations 026, 027, 028 (those are other plans' scope). DO NOT modify `MarkFilledModal.tsx`, Edge fns, or the doc-types files.
  </action>
  <verify>
<automated>pnpm test -- tests/fk-indexes.test.ts</automated>
Expect: 16 tests pass (1 count assertion + 15 per-index it.each), 0 todo, 0 fail. Then run `pnpm test` full suite — expect zero new failures.

Manual MCP verify (read-only — no flag flip):
```sql
SELECT count(*) FROM pg_indexes
 WHERE schemaname = 'public'
   AND indexname IN (<15 names from Step 3>);
```
Expect: count=15.
  </verify>
  <acceptance_criteria>
    - File `supabase/migrations/025_phase_18_1_fk_indexes.sql` exists; `grep -c "CREATE INDEX IF NOT EXISTS" supabase/migrations/025_phase_18_1_fk_indexes.sql` returns 15
    - File contains literal `BEGIN;` + `COMMIT;` wrapping the 15 statements
    - File contains all 15 expected `<table>_<col>_idx` index names: `admin_notes_admin_id_idx`, `job_skills_skill_id_idx`, `listing_fees_employer_id_idx`, `listing_fees_job_id_idx`, `message_threads_employer_id_idx`, `message_threads_job_id_idx`, `message_threads_seeker_id_idx`, `messages_sender_id_idx`, `messages_thread_id_idx`, `placement_fees_application_id_idx`, `placement_fees_employer_id_idx`, `placement_fees_job_id_idx`, `placement_fees_seeker_id_idx`, `saved_jobs_job_id_idx`, `seeker_skills_skill_id_idx`
    - `tests/fk-indexes.test.ts` no longer contains `it.todo` (all stubs flipped to real `it(`/`it.each(` bodies)
    - `pnpm test -- tests/fk-indexes.test.ts` exits 0 with `16 passed`
    - `pnpm test` full suite exits 0 with no new failures
    - MCP read-only `SELECT count(*) FROM pg_indexes WHERE indexname IN (...)` returns 15
    - Single atomic commit lands; `git log --oneline -1` shows `feat(18.1): add 15× FK indexes` (or equivalent); commit touches exactly 2 files
    - SUMMARY captures: commit SHA, Studio apply timestamp + output, MCP verify count
  </acceptance_criteria>
  <done>Migration 025 created, applied via Studio, GREEN test guards future drift, MCP confirms 15 indexes live; one atomic commit.</done>
</task>

</tasks>

<verification>
- `tests/fk-indexes.test.ts` is GREEN (16 passed); zero todo remain in this file
- Migration applied empirically — read-only MCP `pg_indexes` returns 15 expected names
- File `025_phase_18_1_fk_indexes.sql` exists on disk; sequence-prefix continues correctly (024 → 025)
- ROADMAP §"Phase 18.1" SC-6 wording says "14×" but live count is 15 (per 18.1-RESEARCH §Pitfall 6); reconciliation deferred to phase verifier (NOT this plan's scope)
</verification>

<success_criteria>
- Migration 025 created on disk + applied to live project via Studio
- `tests/fk-indexes.test.ts` flipped GREEN with 16 assertions
- MCP read-only verification returns 15 expected index names
- Single atomic commit per CLAUDE §4
- ZERO production source code touched (only the migration + the test)
- SUMMARY captures empirical evidence per CLAUDE §3 (Studio output + MCP query result)
</success_criteria>

<output>
After completion, create `.planning/phases/18.1-pre-launch-hardening/18.1-01-fk-indexes-SUMMARY.md` capturing:
- Atomic commit SHA
- Studio apply timestamp + paste of Studio's success output
- MCP `pg_indexes` query result (15 rows expected, paste actual rows)
- Vitest output: `16 passed | 0 todo | 0 fail` for `tests/fk-indexes.test.ts`
- ROADMAP SC-6 wording note: "14× was the original wording; live advisor count is 15. Material implication: 1 extra index landed vs ROADMAP estimate. Reconciliation deferred to phase verifier per 18.1-RESEARCH §Pitfall 6."
</output>
