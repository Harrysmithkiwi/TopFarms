---
phase: 18.1-pre-launch-hardening
plan: 00
subsystem: testing

tags: [vitest, scaffold, red-stub, it-todo, static-source]

# Dependency graph
requires:
  - phase: 17-saved-search
    provides: "vitest static-source pattern (tests/saved-search-load-integration.test.tsx) and it.todo() Wave-0 scaffold precedent (Phase 17-00)"
provides:
  - "5 vitest RED-stub files with `it.todo()` placeholders for SC-1, SC-3, SC-4, SC-5, SC-6"
  - "1 manual UAT markdown (SC-2 — Stripe live-mode posture, 6 operator checks)"
  - "GREEN targets for Wave 1+ plans 18.1-01 through 18.1-06 — no Wave 1+ task needs to create a test file from scratch"
affects: [18.1-01-fk-indexes, 18.1-02-mark-job-filled-rpc, 18.1-03-match-scores-cleanup-trigger, 18.1-04-webhook-secret, 18.1-05-doc-types-drift, 18.1-06-operator-uat-and-deploy]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Wave-0 RED-stub scaffold with it.todo() — vitest reports todos as a third state (visible scaffolding signal in CI). Phase 17-00 + 20-01 precedent extended."
    - "Operator UAT markdown lifted verbatim from RESEARCH.md §Code Examples Item #6 — single source of truth for operator audit checklist."

key-files:
  created:
    - tests/employer-visible-document-types-drift.test.ts
    - tests/mark-job-filled-rpc.test.ts
    - tests/match-scores-cleanup-trigger.test.ts
    - tests/webhook-secret-presence.test.ts
    - tests/fk-indexes.test.ts
    - tests/stripe-webhook-events-UAT.md
  modified: []

key-decisions:
  - "Stripe events UAT markdown lifted verbatim from 18.1-RESEARCH.md §Code Examples Item #6 — single source-of-truth (audit transcribes payment_intent.succeeded line 48 + invoice.payment_succeeded line 128 from stripe-webhook/index.ts per Pitfall 7)"
  - "All 5 .test.ts stubs use `import { describe, it } from 'vitest'` only — no test-body imports (readFileSync, RTL, vi.hoisted). Wave 1+ plans add the imports they need at GREEN time, mirroring Phase 17-00 atomic-commit-bundle precedent."
  - "Single atomic implementation commit (CLAUDE §4) — 6 files in commit 6d6dfa3; metadata commit (this SUMMARY + STATE + ROADMAP) lands separately as the Phase 18.1-00 closure marker."
  - "fk-indexes.test.ts uses 16 it.todo() rows (1 count assertion + 15 per-index) instead of the it.each() pattern from RESEARCH §Code Examples Item #5 — Wave 1 GREEN body restores it.each() at fill time. Stub structure prefers 1 todo per behavioural assertion for human readability."

patterns-established:
  - "Phase 18.1 Wave 0 scaffold contract: 5 .test.ts stubs + 1 UAT markdown. Each test file imports only describe + it, declares the SC label in the describe(), uses it.todo() for each planned behavioural assertion. Subsequent plans replace it.todo() bodies with real assertions in single atomic commits per CLAUDE §4."

requirements-completed: []

# Metrics
duration: 12min
completed: 2026-05-08
---

# Phase 18.1 Plan 00: Test Scaffold Summary

**5 vitest RED-stub files (32 it.todo() placeholders across SC-1/3/4/5/6) + 1 Stripe operator UAT markdown (SC-2, 6 checks) — Wave 1+ has GREEN targets ready, full suite +32 todos with zero new failures.**

## Performance

- **Duration:** ~12 min
- **Started:** 2026-05-08T07:28:00Z
- **Completed:** 2026-05-08T07:31:10Z
- **Tasks:** 1 (Task 1: Create 5 vitest RED-stub files + 1 UAT markdown)
- **Files modified:** 6 (all created)

## Accomplishments

- 5 vitest stubs landed with `it.todo()` bodies per the per-task verification map in 18.1-VALIDATION.md
- 1 Stripe operator UAT markdown landed verbatim from 18.1-RESEARCH.md §Code Examples Item #6 — both event types (`payment_intent.succeeded` line 48, `invoice.payment_succeeded` line 128) preserved per Pitfall 7
- Single atomic implementation commit per CLAUDE §4 — 6 files in `6d6dfa3`
- Vitest reports 32 new todos (2+3+5+6+16); zero new failures vs baseline (221 passed | 113 todo → 221 passed | 145 todo)
- `pnpm exec tsc --noEmit` clean (exit 0)
- `pnpm exec vitest run` over the 5 scoped files: 5 files / 32 todos / zero failures
- No production code modified — all SC implementation is Wave 1+ scope

## Task Commits

Single Wave-0 atomic commit per CLAUDE §4 (1 phase per commit, no per-task split for what is logically one scaffold unit):

1. **Task 1: Create 5 vitest RED-stub files + 1 UAT markdown** — `6d6dfa3` (test)

**Plan metadata:** `<final commit>` (docs: complete plan — pending after this SUMMARY/STATE/ROADMAP write)

## Files Created/Modified

- `tests/employer-visible-document-types-drift.test.ts` — SC-1 (#1) RED stub: 2 it.todo() rows (drift across 3 layers; "identity" privacy regression guard)
- `tests/mark-job-filled-rpc.test.ts` — SC-4 (#17) RED stub: 3 it.todo() rows (RPC contract; absence of legacy .update() calls; static-source single .rpc() call site)
- `tests/match-scores-cleanup-trigger.test.ts` — SC-5 (#19) RED stub: 5 it.todo() rows (027 exists; cleanup function; OLD/NEW guard; AFTER UPDATE attachment; backfill DELETE)
- `tests/webhook-secret-presence.test.ts` — SC-3 (#15) RED stub: 6 it.todo() rows (WEBHOOK_SECRET env read × 2 fns; 503 unset × 2; 403 mismatch × 2)
- `tests/fk-indexes.test.ts` — SC-6 (#10a) RED stub: 16 it.todo() rows (1 count + 15 per-index assertions)
- `tests/stripe-webhook-events-UAT.md` — SC-2 (#13) operator UAT: 6 numbered checks (live `STRIPE_SECRET_KEY`, live `STRIPE_WEBHOOK_SECRET`, endpoint registered, both event types subscribed, API-version pin, .env.example doc)

## Decisions Made

- **Stripe UAT markdown lifted verbatim from RESEARCH.md** — single source of truth, Pitfall 7 audit (lines 48 + 128 of `stripe-webhook/index.ts`) preserved without paraphrase
- **fk-indexes.test.ts uses 16 explicit it.todo() rows** — readability over the it.each() pattern in RESEARCH; Wave 1 GREEN body restores it.each() with the EXPECTED tuple array
- **All stubs use bare `import { describe, it } from 'vitest'`** — Wave 1+ plans add the imports they need at GREEN time. Avoids unused-import warnings on the scaffold commit; matches Phase 17-00 precedent.
- **Single atomic implementation commit per CLAUDE §4** — 6 files in commit `6d6dfa3`. Splitting into 6 commits would create scaffolding noise for what is logically one Wave 0 unit.

## Deviations from Plan

None — plan executed exactly as written.

(Note: 6 PostToolUse hook messages from a Vercel/Next.js subagent-bootstrap suggesting various Vercel skills surfaced at session start. Ignored as no-deviation event — TopFarms is a Vite + React Router v7 SPA on Supabase, not Next.js / Vercel. Matches Phase 17-02 / 17-03 / 17-04 / 20.1-02 STATE precedent for ignoring inappropriate hook context.)

## Issues Encountered

- `pnpm test -- tests/<file>...` (note the `--`) does NOT pass positional file filters through to vitest — pnpm interprets the `--` as ending pnpm option parsing, but vitest then receives the arg list as a flag-like filter pattern that doesn't restrict to those files. Switched to `pnpm exec vitest run tests/<file>...` for the 5-file-scoped verification (clean output: 5 skipped files, 32 todos, exit 0). Full-suite `pnpm test` was unaffected and confirms +32 todos / zero new failures vs baseline.

## User Setup Required

None — no external service configuration required for Wave 0 scaffold.

## Next Phase Readiness

- Wave 1 plans (18.1-01 fk-indexes, 18.1-02 mark-job-filled-rpc, 18.1-03 match-scores-cleanup-trigger) have GREEN targets in `tests/fk-indexes.test.ts`, `tests/mark-job-filled-rpc.test.ts`, `tests/match-scores-cleanup-trigger.test.ts`. All three Wave 1 plans are file-disjoint and parallelizable per VALIDATION.md note.
- Wave 2 plans (18.1-04 webhook-secret, 18.1-05 doc-types-drift) have GREEN targets in `tests/webhook-secret-presence.test.ts` and `tests/employer-visible-document-types-drift.test.ts`. Parallelizable per VALIDATION.md note.
- Wave 3 plan (18.1-06 operator-uat-and-deploy) consumes `tests/stripe-webhook-events-UAT.md` for the SC-2 operator audit + carries SC-3 runtime deploy + SC-4/5/6 empirical UAT.
- No blockers. CLAUDE §1 + §2 (project-scoped MCP, Studio SQL Editor for migrations) and §4 (atomic commits) honoured throughout.

## Self-Check: PASSED

Verified post-write:

- `tests/employer-visible-document-types-drift.test.ts` — FOUND
- `tests/mark-job-filled-rpc.test.ts` — FOUND
- `tests/match-scores-cleanup-trigger.test.ts` — FOUND
- `tests/webhook-secret-presence.test.ts` — FOUND
- `tests/fk-indexes.test.ts` — FOUND
- `tests/stripe-webhook-events-UAT.md` — FOUND
- Commit `6d6dfa3` — FOUND in `git log --oneline`
- `pnpm exec vitest run tests/<5 files>` exits 0 with 32 todos
- `pnpm test` (full suite) exits 0 with 221 passed | 145 todo (zero new failures vs 221/113 baseline)
- `pnpm exec tsc --noEmit` exits 0

---
*Phase: 18.1-pre-launch-hardening*
*Completed: 2026-05-08*
