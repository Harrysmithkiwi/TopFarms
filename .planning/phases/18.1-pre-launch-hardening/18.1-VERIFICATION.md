---
phase: 18.1
status: PARTIAL
verified_at: 2026-05-10T17:58:00+12:00
verifier: gsd-executor (operator-action plan 18.1-06 execution)
nyquist_compliant: true
sc_summary:
  SC-1: PASS
  SC-2: PARTIAL
  SC-3: PASS
  SC-4: PASS
  SC-5: PASS
  SC-6: PASS
---

# Phase 18.1 — Verification Record

**Phase:** 18.1 Pre-Launch Hardening
**Verification type:** Operator-action UAT + read-only MCP sweeps + static-source tests
**Overall status:** PARTIAL — 5/6 SCs PASS, SC-2 PARTIAL (Stripe test-mode per PEND-01)

---

## SC-1: EMPLOYER_VISIBLE_DOCUMENT_TYPES drift guard

**Verdict:** PASS

**Evidence:**

- `tests/employer-visible-document-types-drift.test.ts` GREEN — 2 assertions; plan 18.1-05 commit `4d5eda7`
- Sorted-set equality confirmed across 3 layers: TS canonical (`src/types/domain.ts:200`), Edge fn whitelist (`supabase/functions/get-applicant-document-url/index.ts:43`), SQL RLS policy (`supabase/migrations/020_seeker_documents_employer_policy.sql:35`)
- `'identity'` NOT in canonical list — BFIX-02 / PRIV-02 privacy regression guard passes
- Test runtime: 8ms (pure-Node; no jsdom; well under 50ms target)
- Full suite baseline preserved: 247 passed | 119 todo | 0 failures post-commit

**Code closure:** complete (static-source guard regression-protects all 3 layers). **Runtime closure:** N/A — drift is statically detected at test time; no operator runtime action needed.

---

## SC-2: Stripe production-mode posture

**Verdict:** PARTIAL

**Evidence (6/6 configuration checks PASS — in sandbox/test mode):**

- Check 1: `STRIPE_SECRET_KEY` set in Supabase Edge fn secrets (confirmed via `supabase secrets list`). Value is test-mode key (starts `sk_test_`). Operator confirmed.
- Check 2: `STRIPE_WEBHOOK_SECRET` set in Supabase Edge fn secrets (confirmed via `supabase secrets list`). Value starts `whsec_`. Operator confirmed.
- Check 3: Stripe Dashboard (test mode) → webhook endpoint `https://inlagtgpynemhipnqvty.supabase.co/functions/v1/stripe-webhook` is registered and ENABLED. Operator confirmed.
- Check 4: Endpoint subscribed to both required event types: `payment_intent.succeeded` (stripe-webhook/index.ts:48) and `invoice.payment_succeeded` (stripe-webhook/index.ts:128). 6 events total subscribed. Operator confirmed.
- Check 5: Stripe API version `2024-06-20` (code pin at `stripe-webhook/index.ts:27`) matches endpoint API version. Operator confirmed.
- Check 6: `.env.example` documents both `STRIPE_SECRET_KEY` and `STRIPE_WEBHOOK_SECRET` with placeholder values. No change needed (verified 2026-05-07). Operator confirmed.

**Why PARTIAL:** All 6 configuration checks pass, but keys are in sandbox/test mode (not live mode). Per CLAUDE §7 partial-close discipline: configuration is correct and complete for current mode. The gap is the live-mode key tier, which is PEND-01 (pre-launch live-mode swap — documented operator action before first real charge). This is not a code gap or a misconfiguration; it is an intentional operational state during development.

**Code closure:** complete (stripe-webhook function source correct; event handling correct). **Runtime closure:** PARTIAL — sandbox/test mode is correct for current development phase. **Carryforward:** PEND-01 pre-launch Stripe live-mode key swap (see v2.0-MILESTONE-AUDIT.md).

---

## SC-3: X-Webhook-Secret defence-in-depth

**Verdict:** PASS

**Evidence:**

**Code side:**
- `tests/webhook-secret-presence.test.ts` GREEN — 6 assertions (3 per fn × 2 fns); plan 18.1-04 commit `35e1cef`
- Migration 028 applied via Studio; `handle_job_filled` body contains X-Webhook-Secret
- MCP query: `position('X-Webhook-Secret' IN pg_get_functiondef('public.handle_job_filled'::regproc))` → `pos=988` (pos > 0 ✓)

**Deploy side:**
- Operator: `supabase secrets set WEBHOOK_SECRET=<64-hex-chars> --project-ref inlagtgpynemhipnqvty` — confirmed via `secrets list | grep WEBHOOK_SECRET`
- DB-level: Vault approach used via migration 029 (ALTER DATABASE blocked in Studio with permission denied; `vault.create_secret('WEBHOOK_SECRET', ...)` used instead). MCP confirmed: `SELECT name, length(decrypted_secret) FROM vault.decrypted_secrets WHERE name='WEBHOOK_SECRET'` → `secret_len=64` ✓
- `current_setting('app.webhook_secret', true)` returns NULL (expected — Vault is source of truth for this deployment; `handle_job_filled` reads from vault.decrypted_secrets directly)
- Both Edge fns redeployed: `supabase functions deploy notify-job-filled send-followup-emails --project-ref inlagtgpynemhipnqvty`

**Runtime UAT (synthetic curl — 6/6):**
- notify-job-filled + correct header → HTTP/2 200 ✓
- notify-job-filled + wrong header → HTTP/2 403 ✓
- notify-job-filled + missing header → HTTP/2 403 ✓
- send-followup-emails + correct header → HTTP/2 200 ✓
- send-followup-emails + wrong header → HTTP/2 403 ✓
- send-followup-emails + missing header → HTTP/2 403 ✓

**Deviation note:** Plan specified `ALTER DATABASE postgres SET app.webhook_secret = '<hex>'` as the DB-level config path. Studio returned "permission denied" on that DDL. Vault approach used instead (migration 029: `vault.create_secret`). `handle_job_filled` reads from `vault.decrypted_secrets` rather than `current_setting`. Synthetic curl UAT empirically confirms the full chain works end-to-end regardless of the config path.

**Code closure:** complete. **Runtime closure:** complete (6/6 synthetic curl + Vault row + pg_get_functiondef pos > 0).

---

## SC-4: mark_job_filled RPC atomicity

**Verdict:** PASS

**Evidence:**

**Code side:**
- Migration `026_mark_job_filled_rpc.sql` on disk (commit `5c164c2`); `tests/mark-job-filled-rpc.test.tsx` GREEN 3/3
- `MarkFilledModal.handleConfirm` refactored to single `.rpc('mark_job_filled', ...)` call

**MCP corroboration:**
- `SELECT proname, prosecdef FROM pg_proc ... WHERE proname='mark_job_filled'` → `proname=mark_job_filled, prosecdef=true` ✓
- RPC exists in public schema with SECURITY DEFINER confirmed

**Runtime UAT:**
- Operator completed mark-filled flow on job `b00254c7` ("UAT Farm Assistant — Applied") via "Hired externally" path (p_applicant_id=null)
- MCP: `SELECT status FROM public.jobs WHERE id='b00254c7-b9e0-4865-80fb-756d570d9a66'` → `status='filled'` ✓
- No orphan-hired-application: hired-externally path leaves applications in prior state (expected — RPC only sets status='hired' when p_applicant_id is non-null)
- notify-job-filled webhook log: 200 response observed post mark-filled (X-Webhook-Secret passed)

**Note on operator resume signal:** Operator resume signal referenced job_id `6c867c1a`. MCP verification shows that job remains `active` with `match_scores=3` — it was not the UAT job. The actual UAT was on `b00254c7` (identified via `SELECT * FROM jobs WHERE status='filled'`). Resume signal contained a transcription error; the underlying UAT executed correctly.

**Code closure:** complete. **Runtime closure:** complete (RPC prosecdef=true + jobs.status='filled' post-UAT).

---

## SC-5: match_scores cleanup trigger

**Verdict:** PASS

**Evidence:**

**Code side:**
- Migration `027_match_scores_cleanup_trigger.sql` on disk (commit `1d68769`); `tests/match-scores-cleanup-trigger.test.ts` GREEN 5/5
- Trigger: `on_jobs_status_change_match_scores_cleanup` AFTER UPDATE OF status ON public.jobs FOR EACH ROW

**MCP corroboration:**
- `SELECT proname, prosecdef FROM pg_proc ... WHERE proname='cleanup_match_scores_on_status_change'` → `proname=cleanup_match_scores_on_status_change, prosecdef=true` ✓
- `SELECT tgname FROM pg_trigger WHERE tgname='on_jobs_status_change_match_scores_cleanup' AND NOT tgisinternal` → `tgname=on_jobs_status_change_match_scores_cleanup` ✓

**Runtime UAT (empirical trigger fire):**
- UAT job: `b00254c7` ("UAT Farm Assistant — Applied")
- Pre-transition: match_scores rows existed for this job (operator-confirmed)
- Post-transition (after mark-filled, jobs.status→'filled'): MCP `SELECT count(*) FROM public.match_scores WHERE job_id='b00254c7-b9e0-4865-80fb-756d570d9a66'` → `scores_remaining=0` ✓
- Trigger fired and deleted all match_scores rows in the same transaction as the jobs.status UPDATE

**Code closure:** complete. **Runtime closure:** complete (trigger exists in pg_trigger + match_scores=0 post-transition empirically confirmed).

---

## SC-6: FK indexes (15 unindexed_foreign_keys advisor lints)

**Verdict:** PASS

**Evidence:**

**Code side:**
- Migration `025_phase_18_1_fk_indexes.sql` on disk (commit `91f7197`); `tests/fk-indexes.test.ts` GREEN 16/16
- 15× `CREATE INDEX IF NOT EXISTS` covering 8 tables; idempotent

**MCP corroboration (all 15 indexes confirmed in pg_indexes):**

```
SELECT indexname FROM pg_indexes
 WHERE schemaname = 'public'
   AND indexname IN (
     'admin_notes_admin_id_idx','job_skills_skill_id_idx',
     'listing_fees_employer_id_idx','listing_fees_job_id_idx',
     'message_threads_employer_id_idx','message_threads_job_id_idx','message_threads_seeker_id_idx',
     'messages_sender_id_idx','messages_thread_id_idx',
     'placement_fees_application_id_idx','placement_fees_employer_id_idx',
     'placement_fees_job_id_idx','placement_fees_seeker_id_idx',
     'saved_jobs_job_id_idx','seeker_skills_skill_id_idx'
   )
 ORDER BY indexname;
```

Result: 15 rows returned (all 15 index names present):
1. admin_notes_admin_id_idx
2. job_skills_skill_id_idx
3. listing_fees_employer_id_idx
4. listing_fees_job_id_idx
5. message_threads_employer_id_idx
6. message_threads_job_id_idx
7. message_threads_seeker_id_idx
8. messages_sender_id_idx
9. messages_thread_id_idx
10. placement_fees_application_id_idx
11. placement_fees_employer_id_idx
12. placement_fees_job_id_idx
13. placement_fees_seeker_id_idx
14. saved_jobs_job_id_idx
15. seeker_skills_skill_id_idx

**ROADMAP wording note:** ROADMAP §Phase 18.1 SC-6 said "14× unindexed_foreign_keys advisor lints addressed". Live Supabase advisor count (captured 2026-05-07) returned 15. One extra index (`saved_jobs_job_id_idx`) was present in the advisor output vs the ROADMAP estimate. The migration was written for the actual live count (15); ROADMAP wording updated to `15×` in this closure commit.

**Code closure:** complete. **Runtime closure:** complete (15/15 indexes confirmed in pg_indexes).

---

## Phase Verdict

**PARTIAL** — 5/6 SCs have full empirical evidence (PASS). SC-2 is PARTIAL: Stripe configuration is correct and all 6 checks pass, but in sandbox/test mode (not live mode). Live-mode key swap is PEND-01 (pre-launch operator action, no code changes needed). Per CLAUDE §7 partial-close discipline: ROADMAP Phase 18.1 row stays `[ ]` with partial-close annotation; carryforward row added to v2.0-MILESTONE-AUDIT.md.

**Next step:** `/gsd:verify-work` reads this file as the canonical evidence ledger for Phase 18.1.
