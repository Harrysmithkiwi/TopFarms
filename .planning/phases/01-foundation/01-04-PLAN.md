---
phase: 01-foundation
plan: 04
type: execute
wave: 3
depends_on: [01-02, 01-03]
files_modified:
  - src/main.tsx
  - src/App.tsx
  - src/components/layout/ProtectedRoute.tsx
  - src/components/layout/Nav.tsx
  - src/components/layout/Sidebar.tsx
  - src/components/layout/DashboardLayout.tsx
  - src/pages/Home.tsx
  - src/pages/dashboard/EmployerDashboard.tsx
  - src/pages/dashboard/SeekerDashboard.tsx
autonomous: true
requirements: [AUTH-05]

must_haves:
  truths:
    - "After login, an employer is routed to /dashboard/employer and a seeker to /dashboard/seeker"
    - "An unauthenticated user accessing /dashboard/* is redirected to /login"
    - "A seeker accessing /dashboard/employer is redirected to /dashboard/seeker (and vice versa)"
    - "The navigation bar shows the TopFarms logo, nav links, and user state (logged in/out)"
    - "The dashboard shell shows a sidebar on desktop and hamburger menu on mobile (below 768px)"
    - "Employer dashboard shows onboarding prompt: 'Complete your farm profile to start posting jobs'"
    - "Seeker dashboard shows onboarding prompt: 'Complete your profile to start matching with jobs'"
  artifacts:
    - path: "src/components/layout/ProtectedRoute.tsx"
      provides: "Route guard checking auth + optional role"
      exports: ["ProtectedRoute"]
    - path: "src/components/layout/Nav.tsx"
      provides: "Top navbar with soil bg, logo, auth-aware user menu"
      exports: ["Nav"]
    - path: "src/components/layout/Sidebar.tsx"
      provides: "Left sidebar navigation for dashboard"
      exports: ["Sidebar"]
    - path: "src/components/layout/DashboardLayout.tsx"
      provides: "Dashboard shell wrapping Nav + Sidebar + content area"
      exports: ["DashboardLayout"]
    - path: "src/pages/dashboard/EmployerDashboard.tsx"
      provides: "Employer dashboard with onboarding prompt"
      exports: ["EmployerDashboard"]
    - path: "src/pages/dashboard/SeekerDashboard.tsx"
      provides: "Seeker dashboard with onboarding prompt"
      exports: ["SeekerDashboard"]
    - path: "src/main.tsx"
      provides: "Router configuration with all routes"
      exports: ["default"]
  key_links:
    - from: "src/components/layout/ProtectedRoute.tsx"
      to: "src/hooks/useAuth.ts"
      via: "useAuth hook for session and role"
      pattern: "useAuth\\(\\)"
    - from: "src/main.tsx"
      to: "src/pages/auth/*.tsx"
      via: "route definitions"
      pattern: "path:.*/(login|signup|auth)"
    - from: "src/main.tsx"
      to: "src/components/layout/ProtectedRoute.tsx"
      via: "route wrapping"
      pattern: "ProtectedRoute"
    - from: "src/components/layout/Nav.tsx"
      to: "src/hooks/useAuth.ts"
      via: "auth state for user menu"
      pattern: "useAuth\\(\\)"
---

<objective>
Wire the complete routing system — protected routes with role-based access, navigation components, and dashboard shells with onboarding prompts.

Purpose: This is the final plan that makes the entire Phase 1 foundation usable. Users can sign up, verify email, log in, and land on the correct dashboard with navigation. This validates the full auth → role → routing → dashboard flow end-to-end.

Output: Working router with all Phase 1 routes, ProtectedRoute guard, Nav + Sidebar + DashboardLayout, and employer/seeker dashboard shells.
</objective>

<execution_context>
@/Users/harrysmith/.claude/get-shit-done/workflows/execute-plan.md
@/Users/harrysmith/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
@SPEC.md

<interfaces>
<!-- From Plan 02 — design system components -->
From src/components/ui/Button.tsx:
```typescript
export function Button({ variant, size, className, ...props }: ButtonProps): JSX.Element
```

From src/components/ui/Card.tsx:
```typescript
export function Card({ className, children, hover }: CardProps): JSX.Element
```

From src/components/ui/ProgressBar.tsx:
```typescript
export function ProgressBar({ progress, className }: ProgressBarProps): JSX.Element
```

<!-- From Plan 03 — auth hook and pages -->
From src/hooks/useAuth.ts:
```typescript
export function useAuth(): {
  session: Session | null
  role: UserRole | null
  loading: boolean
  signUpWithRole: (email: string, password: string, role: 'employer' | 'seeker') => Promise<...>
  signIn: (email: string, password: string) => Promise<...>
  signOut: () => Promise<void>
  resetPassword: (email: string) => Promise<...>
  updatePassword: (newPassword: string) => Promise<...>
}
```

From src/pages/auth/*:
```typescript
export function SignUp(): JSX.Element
export function Login(): JSX.Element
export function VerifyEmail(): JSX.Element
export function ForgotPassword(): JSX.Element
export function ResetPassword(): JSX.Element
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Nav, Sidebar, DashboardLayout, and ProtectedRoute components</name>
  <files>src/components/layout/ProtectedRoute.tsx, src/components/layout/Nav.tsx, src/components/layout/Sidebar.tsx, src/components/layout/DashboardLayout.tsx</files>
  <action>
    **ProtectedRoute.tsx** — route guard:
    - Props: `children: ReactNode`, `requiredRole?: 'employer' | 'seeker'`
    - Uses `useAuth()` hook
    - If `loading`: render a loading skeleton (centered spinner or pulsing placeholder)
    - If `!session`: `<Navigate to="/login" replace />`
    - If `requiredRole && role !== requiredRole`: `<Navigate to={'/dashboard/' + role} replace />`
    - Otherwise: render `{children}`
    - Import Navigate from `react-router`

    **Nav.tsx** — top navigation per SPEC Section 4.3 + user decisions:
    - Sticky top, height 56px, `bg-soil` background
    - Left: TopFarms logo — "TopFarms" in Fraunces font (text-[20px] font-semibold text-cream), with a leaf emoji or meadow-coloured accent
    - Center/right (authenticated): nav links in DM Sans 12px 600, cream at 50% opacity, active link has full cream text + `rgba(255,255,255,0.08)` pill background
    - Right (authenticated): user avatar circle (first letter of email) with dropdown (Profile, Settings, Sign Out)
    - Right (unauthenticated): "Log in" and "Sign up" buttons
    - Mobile (below 768px): hamburger icon that toggles a slide-out menu (use React state, not a separate mobile nav component)
    - Bottom border: `rgba(255,255,255,0.06)` subtle line
    - The Nav links should be role-aware:
      - Employer: Dashboard, My Listings, Applications, Settings
      - Seeker: Find Work, My Applications, My Profile, Settings
      - Unauthenticated: Find Work, For Employers, Pricing

    **Sidebar.tsx** — left sidebar for dashboard:
    - Width: 240px on desktop, hidden on mobile (hamburger nav replaces it)
    - `bg-white` background, `border-r border-fog`
    - Navigation items: icon + label, `text-mid` default, `text-moss bg-moss/5 font-semibold` active state
    - Employer items: Dashboard, My Listings, Applications, Farm Profile, Settings
    - Seeker items: Dashboard, Find Work, My Applications, My Profile, Settings
    - Use `lucide-react` icons: LayoutDashboard, Briefcase, Users, Building2, Settings, Search, FileText, User
    - Items are NavLink from react-router with active state detection

    **DashboardLayout.tsx** — wraps dashboard pages:
    - Renders `<Nav />` at top
    - Below nav: flex row with `<Sidebar />` (left) and main content area (right, flex-1, p-6)
    - Sidebar hidden below 768px (mobile gets nav hamburger menu instead)
    - Content area: `max-w-[1200px]` centered, `bg-cream` background
  </action>
  <verify>
    Run `npx tsc --noEmit` — no TypeScript errors.
    Verify ProtectedRoute uses useAuth hook and Navigate from react-router.
    Verify Nav renders at 56px height with soil background.
  </verify>
  <done>All layout components built: ProtectedRoute guards auth + role, Nav shows auth-aware UI at 56px, Sidebar shows role-specific navigation, DashboardLayout composes them into the dashboard shell.</done>
</task>

<task type="auto">
  <name>Task 2: Create dashboard pages and wire complete router</name>
  <files>src/pages/Home.tsx, src/pages/dashboard/EmployerDashboard.tsx, src/pages/dashboard/SeekerDashboard.tsx, src/main.tsx, src/App.tsx</files>
  <action>
    **Home.tsx** (`/`):
    - Minimal placeholder — "TopFarms" heading with "Find your next farm role" subtitle
    - Two CTA buttons: "I'm a Farm Worker" (links to /signup) and "I'm a Farm Employer" (links to /signup)
    - This is a skeleton — the full landing page is Phase 6
    - Use Nav component at the top

    **EmployerDashboard.tsx** (`/dashboard/employer`):
    - Wrapped in DashboardLayout
    - Page title: "Welcome to TopFarms" (Fraunces heading)
    - Onboarding prompt card (use Card component):
      - Icon or illustration placeholder
      - Heading: "Complete your farm profile to start posting jobs"
      - Subtitle: "Set up your farm details, verification, and team culture to attract the best candidates"
      - Progress indicator: ProgressBar at 0% with "0 of 8 steps completed"
      - CTA: Button (primary) "Get Started" — links to /onboarding/employer (placeholder route for now)
    - Below: empty state cards for "Recent Applications", "Active Listings", "Quick Stats" — all showing placeholder/empty content

    **SeekerDashboard.tsx** (`/dashboard/seeker`):
    - Wrapped in DashboardLayout
    - Page title: "Welcome to TopFarms"
    - Onboarding prompt card:
      - Heading: "Complete your profile to start matching with jobs"
      - Subtitle: "Tell us about your experience, skills, and what you're looking for to get matched with the best roles"
      - Progress indicator: ProgressBar at 0%
      - CTA: Button (primary) "Get Started" — links to /onboarding/seeker (placeholder)
    - Below: empty state cards for "Recommended Jobs", "My Applications", "Profile Strength"

    **main.tsx** — complete router setup:
    ```
    Routes:
    - / → Home
    - /login → Login
    - /signup → SignUp
    - /auth/verify → VerifyEmail
    - /forgot-password → ForgotPassword
    - /auth/reset → ResetPassword
    - /dashboard/employer → ProtectedRoute(requiredRole="employer") → EmployerDashboard
    - /dashboard/seeker → ProtectedRoute(requiredRole="seeker") → SeekerDashboard
    - /onboarding/employer → ProtectedRoute(requiredRole="employer") → placeholder
    - /onboarding/seeker → ProtectedRoute(requiredRole="seeker") → placeholder
    - /jobs → placeholder (Phase 3)
    - /jobs/:id → placeholder (Phase 3)
    ```
    Use `createBrowserRouter` and `RouterProvider` from `react-router`.
    Add `<Toaster />` from sonner in the root layout.

    **App.tsx** — update to use RouterProvider instead of static content.

    CRITICAL: After login, the useAuth hook's onAuthStateChange fires and sets the role. The Login page should check role after successful signIn and navigate to `/dashboard/${role}`. Update Login.tsx to:
    1. After successful signIn, wait for role to be loaded
    2. Navigate to `/dashboard/${role}`

    Similarly, VerifyEmail.tsx should navigate to `/dashboard/${role}` after processing the email verification token.
  </action>
  <verify>
    Run `npm run dev` and verify:
    1. Home page renders at / with TopFarms heading and two CTA buttons
    2. /login renders the login form in split-screen layout
    3. /signup renders role selection then form
    4. /dashboard/employer redirects to /login when unauthenticated
    5. Nav shows correct links for unauthenticated state
    Run `npx tsc --noEmit` — no TypeScript errors.
  </verify>
  <done>Complete routing is wired. Unauthenticated users are redirected to login. Authenticated users land on role-appropriate dashboards with onboarding prompts. Nav and Sidebar show role-specific navigation. The full Phase 1 auth flow works end-to-end.</done>
</task>

</tasks>

<verification>
1. Navigate to / — Home page with TopFarms heading and dual CTAs
2. Navigate to /login — split-screen login form
3. Navigate to /signup — role selection (Employer/Seeker cards) then signup form
4. Navigate to /dashboard/employer unauthenticated — redirects to /login
5. Navigate to /dashboard/seeker unauthenticated — redirects to /login
6. After login as employer — lands on /dashboard/employer with onboarding prompt
7. After login as seeker — lands on /dashboard/seeker with onboarding prompt
8. Employer on /dashboard/seeker — redirected to /dashboard/employer
9. Nav shows hamburger menu below 768px viewport width
10. Sidebar shows role-specific navigation items
11. `npx tsc --noEmit` passes
</verification>

<success_criteria>
- The full auth → role → dashboard flow works: signup with role → email verify → login → correct dashboard
- ProtectedRoute guards prevent unauthorized access and enforce role-based routing
- Navigation is auth-aware and role-aware
- Dashboard shells display onboarding prompts per the user's design decisions
- Mobile responsive: hamburger nav replaces sidebar below 768px
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
