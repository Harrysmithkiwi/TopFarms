---
phase: 18.2-code-quality-ux-polish
plan: "03"
subsystem: database
tags: [postgres, rls, auth, performance, supabase]

requires:
  - phase: 18.2-02-ui-components
    provides: stable test suite before DB migration; MarkFilledModal + RPC work that 031 indirectly supports

provides:
  - "Migration 031: all 37 RLS policies rewritten from bare auth.uid() to (select auth.uid()) — auth_rls_initplan advisor warnings eliminated"
  - "AuthContext.tsx loadRole timing instrumentation for AUTH-FIX-02 diagnostic"
  - "REQUIREMENTS.md AUTH-FIX-02 entry updated with Phase 18.2 SC-7 diagnostic guide"
  - "NAMING.md lookup table updated with migrations 030 and 031 Studio-applied entries (Task 4, post-operator)"

affects:
  - "18.2-04 and later plans — RLS rewrite is live in production after operator applies 031"
  - "Future auth debugging — AUTH-FIX-02 timing output available in browser DevTools"

tech-stack:
  added: []
  patterns:
    - "(select auth.uid()) form for all RLS USING/WITH CHECK clauses — prevents Postgres initplan re-evaluation per row"
    - "console.time/timeEnd around DB queries for temporary diagnostic instrumentation (remove once root cause confirmed)"

key-files:
  created:
    - "supabase/migrations/031_rls_initplan_performance.sql"
  modified:
    - "src/contexts/AuthContext.tsx"
    - ".planning/REQUIREMENTS.md"
    - "supabase/migrations/NAMING.md"

key-decisions:
  - "DROP POLICY IF EXISTS + CREATE POLICY pattern (not ALTER POLICY) — ALTER POLICY cannot change USING expressions in Postgres"
  - "Performance-only RLS rewrite: (select auth.uid()) subquery prevents initplan re-evaluation; logically identical to bare auth.uid() in all cases"
  - "Instrumentation scoped to loadRole inner DB query only — NOT loadRoleWithTimeout outer race wrapper; timing measures pure DB round-trip"
  - "Migration 031 applied via Supabase Studio SQL Editor per CLAUDE.md §2 (MCP --read-only default; no restart cycle)"

requirements-completed: []

duration: ~25min (Tasks 1-2 autonomous; Task 3 operator checkpoint; Task 4 post-operator)
completed: 2026-05-10
---

# Phase 18.2 Plan 03: RLS Migration and AUTH-FIX-02 Diagnostic Summary

**37-policy auth_rls_initplan sweep in migration 031 (bare auth.uid() → (select auth.uid())) plus AUTH-FIX-02 timing instrumentation in AuthContext.tsx loadRole**

## Performance

- **Duration:** ~25 min (Tasks 1-2; Task 3 operator-gated; Task 4 post-operator)
- **Started:** 2026-05-10T21:00:00Z
- **Completed:** 2026-05-10 (Tasks 1-2 complete; Task 3 awaiting operator; Task 4 post-Task-3)
- **Tasks completed:** 2/4 autonomous (Task 3 = operator checkpoint; Task 4 = post-operator NAMING.md update)
- **Files modified:** 4

## Accomplishments

- SC-13: Migration 031 written on disk with 37 DROP/CREATE policy pairs covering all tables. 80 `(select auth.uid())` occurrences. Zero bare `auth.uid()` remaining. Balanced DROP/CREATE counts. Begins with `BEGIN;`, ends with `COMMIT;`
- SC-7: `loadRole` in `AuthContext.tsx` instrumented with `console.time('[AUTH-FIX-02] loadRole:db-query')` / `console.timeEnd(...)` wrapping only the Supabase DB query (not the outer 3s timeout race). REQUIREMENTS.md AUTH-FIX-02 entry updated with Phase 18.2 diagnostic instructions and timing interpretation guide
- vitest suite: 260 passed | 0 failures after both changes

## Task Commits

Each task was committed atomically:

1. **Task 1: SC-13 — Write migration 031** - `4bb2082` (feat)
2. **Task 2: SC-7 — AUTH-FIX-02 loadRole instrumentation + REQUIREMENTS.md** - `064ad3a` (feat)
3. **Task 3: SC-13 operator — Apply migrations 030 + 031 via Supabase Studio** - AWAITING OPERATOR
4. **Task 4: SC-13 post-apply — NAMING.md lookup table update** - PENDING (post Task 3)

## Files Created/Modified

- `supabase/migrations/031_rls_initplan_performance.sql` — 37-policy RLS rewrite from bare `auth.uid()` to `(select auth.uid())` across all public schema tables. Tables: user_roles, employer_profiles, seeker_profiles, seeker_contacts, skills, jobs, job_skills, seeker_skills, match_scores, applications (×4 policies incl. 009 update policies), listing_fees, placement_fees, message_threads, messages, employer_verifications, saved_jobs, seeker_documents (×5), saved_searches (×4)
- `src/contexts/AuthContext.tsx` — `loadRole` function: 3-line addition (comment + console.time + console.timeEnd) wrapping DB query only
- `.planning/REQUIREMENTS.md` — AUTH-FIX-02 entry extended with Phase 18.2 SC-7 diagnostic instructions

## Decisions Made

- DROP POLICY IF EXISTS + CREATE POLICY (not ALTER POLICY) — ALTER POLICY cannot change USING expressions in Postgres; DROP/CREATE is the only path
- Tables covered include all sources of `auth.uid()` found across migrations 002, 005, 009, 015, 019, 020, 024 — comprehensive sweep not just 002
- Instrumentation in `loadRole` only (inner DB query), not `loadRoleWithTimeout` (outer race wrapper) — measures pure DB round-trip to isolate whether latency is at DB layer or Lock/promise layer
- Migration 031 applied via Studio (per CLAUDE.md §2) — MCP is `--read-only` by default; Studio is the preferred path for one-off DB writes

## Deviations from Plan

None — plan executed exactly as written. Policy names and expressions were read from source migration files before writing 031, as the plan required.

## Issues Encountered

None.

## User Setup Required

**Operator action required at Task 3 (blocking checkpoint):**

1. Apply `supabase/migrations/030_rls_get_applicants_for_job_rpc.sql` via Supabase Studio SQL Editor (Project `inlagtgpynemhipnqvty`) — if not already applied in plan 18.2-02 operator step
2. Apply `supabase/migrations/031_rls_initplan_performance.sql` via Supabase Studio SQL Editor
3. Verify via pg_policies query — expect 0 rows with bare `auth.uid()`
4. Verify no functional regressions (seeker + employer dashboard loads)
5. Resume with "applied — [count] compliant policies confirmed; no functional regressions"

After operator confirmation, Task 4 (NAMING.md update) completes the plan.

## Next Phase Readiness

- SC-13 (RLS initplan sweep) complete once operator applies 031; pg_policies verification closes the advisor warning loop
- SC-7 (AUTH-FIX-02 diagnostic) complete; timing data will be available in browser DevTools on next local run
- No blockers for 18.2-04 or subsequent plans

---
*Phase: 18.2-code-quality-ux-polish*
*Completed: 2026-05-10 (partial — awaiting operator Task 3)*
