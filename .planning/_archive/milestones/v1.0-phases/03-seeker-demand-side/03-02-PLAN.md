---
phase: 03-seeker-demand-side
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/pages/onboarding/SeekerOnboarding.tsx
  - src/pages/onboarding/steps/SeekerStep1FarmType.tsx
  - src/pages/onboarding/steps/SeekerStep2Experience.tsx
  - src/pages/onboarding/steps/SeekerStep3Qualifications.tsx
  - src/pages/onboarding/steps/SeekerStep4Skills.tsx
  - src/pages/onboarding/steps/SeekerStep5LifeSituation.tsx
  - src/pages/onboarding/steps/SeekerStep6Visa.tsx
  - src/pages/onboarding/steps/SeekerStep7Complete.tsx
autonomous: true
requirements: [SONB-01, SONB-02, SONB-03, SONB-04, SONB-05, SONB-06, SONB-07, SONB-08]

must_haves:
  truths:
    - "A seeker can advance through all 7 onboarding steps"
    - "Farm type preference is saved to seeker_profiles.sector_pref"
    - "Experience data (years, shed types, herd sizes) is saved to seeker_profiles"
    - "Skills are saved to seeker_skills table with proficiency levels"
    - "Life situation (couples, accommodation, region) is saved"
    - "Visa status and DairyNZ level are saved"
    - "Wizard resumes from last saved step on return visit"
    - "Completion redirects to /jobs with success toast and pre-set filters"
  artifacts:
    - path: "src/pages/onboarding/SeekerOnboarding.tsx"
      provides: "Wizard shell with Supabase persistence"
      exports: ["SeekerOnboarding"]
    - path: "src/pages/onboarding/steps/SeekerStep1FarmType.tsx"
      provides: "Farm type selection step"
    - path: "src/pages/onboarding/steps/SeekerStep4Skills.tsx"
      provides: "Skills self-assessment using SkillsPicker"
  key_links:
    - from: "SeekerOnboarding.tsx"
      to: "supabase.from('seeker_profiles').upsert"
      via: "handleStepComplete callback"
      pattern: "supabase.*from.*seeker_profiles.*upsert"
    - from: "SeekerStep4Skills.tsx"
      to: "supabase.from('seeker_skills')"
      via: "delete+insert pattern"
      pattern: "from.*seeker_skills.*delete|from.*seeker_skills.*insert"
---

<objective>
Build the seeker onboarding wizard — 7 steps mirroring the employer onboarding pattern exactly.

Purpose: Seekers must complete their profile before they can search with match scores or apply to jobs. The wizard captures all agriculture-specific data (shed types, herd sizes, DairyNZ, visa) that powers the matching engine.

Output: SeekerOnboarding shell + 7 step components.
</objective>

<execution_context>
@/Users/harrysmith/.claude/get-shit-done/workflows/execute-plan.md
@/Users/harrysmith/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-seeker-demand-side/03-CONTEXT.md
@.planning/phases/03-seeker-demand-side/03-RESEARCH.md
@.planning/phases/03-seeker-demand-side/03-01-SUMMARY.md

<interfaces>
<!-- Wizard pattern to follow exactly -->
From src/pages/onboarding/EmployerOnboarding.tsx:
```typescript
export function EmployerOnboarding() {
  const { session } = useAuth()
  const wizard = useWizard({ totalSteps: TOTAL_STEPS, initialStep })
  // Load profile on mount, resume from saved step
  // handleStepComplete(stepData, stepIndex) — upserts to supabase
}
```

From src/hooks/useWizard.ts:
```typescript
interface UseWizardReturn {
  currentStep: number; totalSteps: number; isFirstStep: boolean; isLastStep: boolean;
  goToStep: (step: number) => void; nextStep: () => void; prevStep: () => void; progress: number;
}
export function useWizard({ totalSteps, initialStep }: UseWizardOptions): UseWizardReturn
```

From src/components/ui/StepIndicator.tsx:
```typescript
<StepIndicator currentStep={currentStep} totalSteps={TOTAL_STEPS} labels={STEP_LABELS} />
```

From src/components/ui/SkillsPicker.tsx:
```typescript
// requirementMode=false for seeker proficiency (basic/intermediate/advanced)
<SkillsPicker selectedSkills={skills} onChange={setSkills} sector={sectorFilter} />
```

From src/types/domain.ts (after Plan 01):
```typescript
export type DairyNZLevel = 'none' | 'level_1' | 'level_2' | 'level_3' | 'level_4'
export type VisaStatus = 'nz_citizen' | 'permanent_resident' | 'working_holiday' | 'student' | 'needs_sponsorship'
export type HerdSizeBucket = '<200' | '200-500' | '500-1000' | '1000+'
export type ShedType = 'rotary' | 'herringbone' | 'other'
export interface SeekerProfileData { ... }
export const DAIRYNZ_LEVELS, VISA_OPTIONS, HERD_SIZE_BUCKETS, SHED_TYPES
```

From src/lib/constants.ts:
```typescript
export const NZ_REGIONS = ['Northland', 'Auckland', 'Waikato', ...] as const
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SeekerOnboarding wizard shell + steps 1-3</name>
  <files>
    src/pages/onboarding/SeekerOnboarding.tsx,
    src/pages/onboarding/steps/SeekerStep1FarmType.tsx,
    src/pages/onboarding/steps/SeekerStep2Experience.tsx,
    src/pages/onboarding/steps/SeekerStep3Qualifications.tsx
  </files>
  <read_first>
    - src/pages/onboarding/EmployerOnboarding.tsx
    - src/pages/onboarding/steps/Step1FarmType.tsx
    - src/hooks/useWizard.ts
    - src/components/ui/StepIndicator.tsx
    - src/types/domain.ts
    - src/lib/constants.ts
  </read_first>
  <action>
**SeekerOnboarding.tsx** — Mirror EmployerOnboarding.tsx structure exactly:
- Import `useWizard`, `StepIndicator`, `DashboardLayout`, `useAuth`, `supabase`
- STEP_LABELS = ['Farm Type', 'Experience', 'Qualifications', 'Skills', 'Life Situation', 'Visa', 'Complete']
- TOTAL_STEPS = 7
- State: `profileData: Partial<SeekerProfileData>`, `loading`, `saving`, `initialStep`
- `useEffect` on mount: load from `supabase.from('seeker_profiles').select('*').eq('user_id', session.user.id).single()`. Handle PGRST116 as new user. Resume step from `data.onboarding_step`.
- `handleStepComplete(stepData, stepIndex)`: merge stepData into profileData, upsert to seeker_profiles with `user_id`, `onboarding_step: stepIndex + 1`. On last step (index 6), also set `onboarding_complete: true`.
- On completion (last step), navigate to `/jobs?sector=${profileData.sector_pref?.[0] || ''}&region=${profileData.region || ''}` with `toast.success('Profile complete! Here are jobs matching your skills')`.
- Render: DashboardLayout > max-w-2xl > header "Set up your job seeker profile" > StepIndicator > white card with step content.
- Each step rendered conditionally: `{currentStep === 0 && <SeekerStep1FarmType onComplete={...} defaultValues={...} />}`

**SeekerStep1FarmType.tsx** — Farm type preference:
- Props: `{ onComplete: (data: Partial<SeekerProfileData>) => void; defaultValues?: { sector_pref?: string[] } }`
- Two clickable cards: "Dairy Cattle" (value: 'dairy') and "Sheep & Beef" (value: 'sheep_beef'). Allow multi-select (both can be selected). Store as `sector_pref` array.
- Style: Use same card-selector pattern as employer Step1FarmType. Selected card has moss border + green-lt background.
- "Continue" button at bottom, disabled if no selection.
- On submit: `onComplete({ sector_pref: selectedTypes })`

**SeekerStep2Experience.tsx** — Experience capture:
- Props: `{ onComplete, onBack, defaultValues }` with years_experience, shed_types_experienced, herd_sizes_worked
- Total years: `<Input type="number" label="Years of farm experience" min={0} max={50} />` with react-hook-form register.
- Shed types experienced: Multi-select checkboxes using the `Checkbox` component. Labels from `SHED_TYPES` constant. Store as `shed_types_experienced: ShedType[]`.
- Herd sizes worked with: Multi-select checkboxes using `Checkbox`. Labels from `HERD_SIZE_BUCKETS` constant. Store as `herd_sizes_worked: HerdSizeBucket[]`.
- Buttons: Back (calls onBack), Continue (calls onComplete with form data).

**SeekerStep3Qualifications.tsx** — DairyNZ qualification:
- Props: `{ onComplete, onBack, defaultValues }` with dairynz_level
- Single `<Select>` dropdown with options from `DAIRYNZ_LEVELS` constant.
- Below each option, show the description as helper text (or use a tooltip icon next to the select with an InfoBox explaining levels).
- Store as `dairynz_level: DairyNZLevel`.
- Buttons: Back, Continue.
  </action>
  <verify>
    <automated>ls src/pages/onboarding/SeekerOnboarding.tsx src/pages/onboarding/steps/SeekerStep1FarmType.tsx src/pages/onboarding/steps/SeekerStep2Experience.tsx src/pages/onboarding/steps/SeekerStep3Qualifications.tsx && grep -l "onComplete" src/pages/onboarding/steps/SeekerStep*.tsx | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - SeekerOnboarding.tsx contains `useWizard({ totalSteps: 7` (or TOTAL_STEPS = 7)
    - SeekerOnboarding.tsx contains `supabase.from('seeker_profiles').upsert(`
    - SeekerOnboarding.tsx contains `PGRST116`
    - SeekerOnboarding.tsx contains `onboarding_complete: true`
    - SeekerOnboarding.tsx contains `navigate('/jobs`
    - SeekerOnboarding.tsx contains `toast.success`
    - SeekerStep1FarmType.tsx contains `sector_pref`
    - SeekerStep2Experience.tsx contains `years_experience` and `shed_types_experienced` and `herd_sizes_worked`
    - SeekerStep3Qualifications.tsx contains `dairynz_level` and `DAIRYNZ_LEVELS`
  </acceptance_criteria>
  <done>Wizard shell loads/resumes seeker profile, renders StepIndicator, and delegates to step components. Steps 1-3 capture farm type, experience, and DairyNZ qualification with correct data shapes.</done>
</task>

<task type="auto">
  <name>Task 2: Seeker onboarding steps 4-7</name>
  <files>
    src/pages/onboarding/steps/SeekerStep4Skills.tsx,
    src/pages/onboarding/steps/SeekerStep5LifeSituation.tsx,
    src/pages/onboarding/steps/SeekerStep6Visa.tsx,
    src/pages/onboarding/steps/SeekerStep7Complete.tsx
  </files>
  <read_first>
    - src/components/ui/SkillsPicker.tsx
    - src/components/ui/Toggle.tsx
    - src/components/ui/Checkbox.tsx
    - src/components/ui/Select.tsx
    - src/components/ui/Input.tsx
    - src/pages/onboarding/steps/Step4Accommodation.tsx
    - src/pages/onboarding/steps/Step8Complete.tsx (for employer completion pattern, file may be named differently)
    - src/types/domain.ts
    - src/lib/constants.ts
  </read_first>
  <action>
**SeekerStep4Skills.tsx** — Skills self-assessment:
- Props: `{ onComplete, onBack, defaultValues, seekerId }` — seekerId needed for delete+insert to seeker_skills.
- Render `<SkillsPicker>` with `requirementMode={false}` (proficiency mode: basic/intermediate/advanced). Filter by sector based on `sector_pref` from wizard state (pass as prop or default to 'both').
- State: `selectedSkills: SelectedSkill[]` initialized from defaultValues or loaded from `seeker_skills` table on mount.
- On mount: If seekerId exists, load existing skills from `supabase.from('seeker_skills').select('skill_id, proficiency').eq('seeker_id', seekerId)` and map to SelectedSkill format.
- On submit: Use delete+insert pattern (same as JobStep3Skills):
  1. `await supabase.from('seeker_skills').delete().eq('seeker_id', seekerId)`
  2. `await supabase.from('seeker_skills').insert(selectedSkills.map(s => ({ seeker_id: seekerId, skill_id: s.skill_id, proficiency: s.proficiency })))`
  3. Call `onComplete({})` — skills saved directly, no profile upsert needed.
- NOTE: The wizard shell needs to provide the seeker profile ID. Look up from `seeker_profiles` query on mount. If no profile exists yet (step 0), the shell creates the profile row on step 0 completion, then passes the ID to subsequent steps. Modify SeekerOnboarding.tsx to track `seekerProfileId` in state, setting it from the initial load or from the first upsert response.

**SeekerStep5LifeSituation.tsx** — Life situation:
- Props: `{ onComplete, onBack, defaultValues }` with couples_seeking, accommodation_needed, pets, family, region
- Layout with grouped sections:
  1. **Couples section:** Toggle "Seeking work as a couple?" — if true, no partner name needed (SPEC doesn't require it, but CONTEXT says "if yes: partner name"). Add an optional `<Input label="Partner's name (optional)" />` below the toggle when enabled. Store partner name in `family` jsonb or a separate text field.
  2. **Accommodation section:** Toggle "Need on-farm accommodation?" — if true, show sub-options as checkboxes:
     - "Pets" checkbox (sets `pets: { dogs: true }` — simplified)
     - "Children/Family" checkbox (sets `family: { has_children: true }`)
     - "Vehicle parking needed" checkbox (store in accommodation preferences)
  3. **Region preference:** `<Select>` with options from `NZ_REGIONS` constant. Single select (per research recommendation: store as single text, search page handles multi-region via URL params). Store as `region`.
- On submit: `onComplete({ couples_seeking, accommodation_needed, pets, family, region })`

**SeekerStep6Visa.tsx** — Visa status:
- Props: `{ onComplete, onBack, defaultValues }` with visa_status
- Single `<Select>` dropdown with options from `VISA_OPTIONS` constant (5 options).
- Below the select, show an InfoBox (variant="blue") explaining: "Your visa status helps us match you with jobs that offer the right sponsorship. This information is only shared with employers when you apply."
- Store as `visa_status: VisaStatus`.
- Buttons: Back, Complete Profile (this is the last data-collection step).
- On submit: `onComplete({ visa_status })`

**SeekerStep7Complete.tsx** — Completion screen:
- No props needed (no onComplete since wizard shell handles navigation on step 6 completion).
- Actually: This step fires automatically. The wizard shell's `handleStepComplete` for step 5 (0-indexed) sets `onboarding_complete: true` and navigates to `/jobs`. So step 7 (index 6) should be the final informational/success screen shown briefly before redirect.
- On mount: `useEffect` fires `toast.success('Profile complete! Here are jobs matching your skills')`. The wizard shell should navigate away after the step 6 complete, so this screen may not actually render. But include it for completeness:
  - Show success message: "Your profile is ready!" with a green checkmark icon
  - "Finding your best matches..." subtext
  - The redirect happens from the shell's handleStepComplete for step 6.
  </action>
  <verify>
    <automated>ls src/pages/onboarding/steps/SeekerStep4Skills.tsx src/pages/onboarding/steps/SeekerStep5LifeSituation.tsx src/pages/onboarding/steps/SeekerStep6Visa.tsx src/pages/onboarding/steps/SeekerStep7Complete.tsx && grep -l "SkillsPicker" src/pages/onboarding/steps/SeekerStep4Skills.tsx</automated>
  </verify>
  <acceptance_criteria>
    - SeekerStep4Skills.tsx contains `SkillsPicker` import and renders it
    - SeekerStep4Skills.tsx contains `from('seeker_skills').delete()` (delete+insert pattern)
    - SeekerStep4Skills.tsx contains `from('seeker_skills').insert(`
    - SeekerStep5LifeSituation.tsx contains `couples_seeking` and `accommodation_needed`
    - SeekerStep5LifeSituation.tsx contains `NZ_REGIONS` import
    - SeekerStep6Visa.tsx contains `VISA_OPTIONS` import
    - SeekerStep6Visa.tsx contains `visa_status`
    - SeekerStep7Complete.tsx exists and contains success messaging
    - All 4 files export a named function component
  </acceptance_criteria>
  <done>Steps 4-7 implemented: skills with SkillsPicker and delete+insert, life situation with toggles and region select, visa with dropdown and info text, completion screen. All data persisted to seeker_profiles and seeker_skills tables.</done>
</task>

</tasks>

<verification>
- Wizard shell mirrors EmployerOnboarding exactly: useWizard, StepIndicator, upsert per step, PGRST116 resume
- All 7 steps have onComplete callbacks that fire upsert
- Skills use delete+insert pattern from Phase 2 (JobStep3Skills)
- Post-onboarding redirects to /jobs with profile-based filter params and success toast
- SkillsPicker used in proficiency mode (requirementMode=false)
</verification>

<success_criteria>
- 8 new files exist (1 shell + 7 steps)
- `npm run build` passes
- Wizard data flows: step selection -> onComplete -> upsert -> nextStep -> ... -> navigate to /jobs
</success_criteria>

<output>
After completion, create `.planning/phases/03-seeker-demand-side/03-02-SUMMARY.md`
</output>
