---
phase: 20.1-standalone-admin-login-gateway-account-bootstrap
plan: 05
type: execute
wave: 5
depends_on:
  - "20.1-04"
files_modified:
  - tests/admin-bootstrap-UAT.md
  - .planning/phases/20.1-standalone-admin-login-gateway-account-bootstrap/20.1-VERIFICATION.md
  - .planning/phases/20.1-standalone-admin-login-gateway-account-bootstrap/20.1-SUMMARY.md
  - .planning/ROADMAP.md
  - .planning/v2.0-MILESTONE-AUDIT.md
  - .planning/STATE.md
  - .planning/todos/pending/2026-05-05-add-sign-out-button-to-dashboard-sidebar-footer.md
autonomous: false
requirements:
  - CF-ACCOUNT-1
  - CF-ACCOUNT-2
  - CF-AUTH-1
  - CF-AUTH-2
  - CF-AUTH-3
must_haves:
  truths:
    - "Supabase Auth user `admin@topfarms.co.nz` exists with `role='admin'`, `is_active=true`"
    - "New admin account UAT PASS: signed in via standalone /admin AdminLoginPage; AdminLayout rendered; ProfileDrawer smoke (employer row click) works"
    - "Old account `harry.symmans.smith@gmail.com` has `role='seeker'`, `is_active=true` (admin role removed)"
    - "Old account regression UAT: navigates to /admin → sees inline 'Access denied' state; 'Back to your dashboard' link points to /dashboard/seeker"
    - "Existing /login flow still routes admin-role JWT to /admin (CF-AUTH-3 regression smoke pass)"
    - "20.1-VERIFICATION.md exists, enumerates each CF-ID with empirical evidence (per CLAUDE §7)"
    - "ROADMAP.md Phase 20.1 row flipped [x] ONLY if every CF-ID empirically closed; otherwise partial-close note + carryforward to v2.0-MILESTONE-AUDIT.md"
  artifacts:
    - path: "tests/admin-bootstrap-UAT.md"
      provides: "Run-record of operator UAT (Phase 20.1 transfer section appended)"
      contains: "Phase 20.1 admin transfer"
    - path: ".planning/phases/20.1-standalone-admin-login-gateway-account-bootstrap/20.1-VERIFICATION.md"
      provides: "Phase 20.1 verification with per-CF-ID evidence pointers"
      contains: "CF-AUTH-1"
    - path: ".planning/phases/20.1-standalone-admin-login-gateway-account-bootstrap/20.1-SUMMARY.md"
      provides: "Phase 20.1 closure summary"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 20.1 status flip"
      contains: "Phase 20.1"
  key_links:
    - from: "Studio Auth dashboard create-user (operator action)"
      to: "auth.users (Supabase)"
      via: "Studio UI: Authentication → Add user"
      pattern: "admin@topfarms.co.nz"
    - from: "Studio SQL Editor (operator action)"
      to: "public.user_roles"
      via: "INSERT/UPDATE SQL block from RESEARCH §Studio SQL Bootstrap Sequence"
      pattern: "INSERT INTO public.user_roles"
    - from: "Browser UAT (operator)"
      to: "/admin AdminLoginPage rendered + AdminLayout post-signin"
      via: "Manual sign-out + sign-in cycle"
      pattern: "Run YYYY-MM-DDTHH:MM:SSZ"
---

<objective>
Operator-action plan that closes Phase 20.1. Bootstraps `admin@topfarms.co.nz`, runs end-to-end UAT through the standalone `/admin` gateway built in Plan 03, removes admin role from `harry.symmans.smith@gmail.com`, then writes the Phase 20.1 verification + summary docs and flips the ROADMAP entry.

Purpose: Closes CF-ACCOUNT-1 (dedicated admin account exists), CF-ACCOUNT-2 (old account's admin role removed), and provides empirical proof for CF-AUTH-1, CF-AUTH-2, CF-AUTH-3 (the gateway works end-to-end against real Supabase Auth, not just mocked tests).

**CRITICAL — Lockout Sequencing (RESEARCH Pitfall 2):** Old role removal MUST run AFTER new account UAT passes. If the new account is broken (typo, wrong password, mailbox unreachable) and the old role is removed first, the operator locks themselves out of admin entirely. This plan sequences operator steps in strict order with explicit gates: Task 4 must PASS before Task 6 runs.

Output:
- `tests/admin-bootstrap-UAT.md` — new `## Run` section appended (Phase 20.1 transfer log)
- `.planning/phases/20.1-standalone-admin-login-gateway-account-bootstrap/20.1-VERIFICATION.md` — per-CF-ID empirical evidence
- `.planning/phases/20.1-standalone-admin-login-gateway-account-bootstrap/20.1-SUMMARY.md` — closure summary
- `.planning/ROADMAP.md` — Phase 20.1 row updated (flipped `[x]` only if all CFs empirically closed)
- `.planning/v2.0-MILESTONE-AUDIT.md` — Phase 20.1 carryforward row updated
- `.planning/STATE.md` — current position updated to Phase 20.1 complete
- `.planning/todos/pending/2026-05-05-add-sign-out-button-to-dashboard-sidebar-footer.md` — moved to `done/` or annotated closed

Constraints:
- All DB writes via Supabase Studio SQL Editor per CLAUDE §2 (NOT MCP `apply_migration` while `--read-only ON`).
- Verification SELECTs via read-only MCP — no flag-flip required.
- Audit-log gap (Studio SQL bypasses `admin_audit_log`) is acceptable for one-shot bootstrap; document in SUMMARY.
- Final ROADMAP flip honors CLAUDE §7 partial-close discipline — if any CF-ID lacks empirical proof, do NOT flip [x]; capture partial-close + carryforward.
</objective>

<execution_context>
@/Users/harrysmith/.claude/get-shit-done/workflows/execute-plan.md
@/Users/harrysmith/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/20.1-standalone-admin-login-gateway-account-bootstrap/20.1-CONTEXT.md
@.planning/phases/20.1-standalone-admin-login-gateway-account-bootstrap/20.1-RESEARCH.md
@.planning/phases/20.1-standalone-admin-login-gateway-account-bootstrap/20.1-VALIDATION.md
@.planning/phases/20.1-standalone-admin-login-gateway-account-bootstrap/20.1-04-SUMMARY.md
@.planning/phases/20-super-admin-dashboard/20-VERIFICATION.md
@CLAUDE.md
@tests/admin-bootstrap-UAT.md

<interfaces>
Studio SQL bootstrap (verbatim from RESEARCH §"Studio SQL Bootstrap Sequence"):

```sql
-- Step 2 (after Studio Auth → Add user admin@topfarms.co.nz)
INSERT INTO public.user_roles (user_id, role, is_active)
VALUES (
  (SELECT id FROM auth.users WHERE email = 'admin@topfarms.co.nz'),
  'admin',
  true
)
ON CONFLICT (user_id) DO UPDATE SET role = 'admin', is_active = true;

-- Step 3 (read-only verification via MCP):
SELECT u.email, ur.role, ur.is_active
FROM auth.users u
JOIN public.user_roles ur ON ur.user_id = u.id
WHERE u.email = 'admin@topfarms.co.nz';

-- Step 5 (ONLY after Step 4 UAT pass):
UPDATE public.user_roles
SET role = 'seeker', is_active = true
WHERE user_id = (SELECT id FROM auth.users WHERE email = 'harry.symmans.smith@gmail.com');

-- Step 6 (read-only verification):
SELECT u.email, ur.role
FROM auth.users u
JOIN public.user_roles ur ON ur.user_id = u.id
WHERE u.email = 'harry.symmans.smith@gmail.com';
```

Run-record format (verbatim from RESEARCH §"Studio SQL Operator-Action Precedent"):

```markdown
## Run YYYY-MM-DDTHH:MM:SSZ
- Studio Auth user created: admin@topfarms.co.nz (auth.users.id: ____)
- Studio SQL admin role assignment result: ____ row(s) affected
- Read-only verification: role=admin, is_active=true (yes/no)
- UAT: signed out + signed in via /admin AdminLoginPage: yes/no
- /admin URL final: /admin (yes/no)
- AdminLayout renders: yes/no
- ProfileDrawer opens on row click: yes/no
- Studio SQL old-account role removal result: ____ row(s) affected
- Read-only verification (old account): role=seeker (yes/no)
- Old account /admin shows AccessDenied: yes/no
```

CLAUDE §7 partial-close template (when not all CFs are empirically closed):
- Do NOT flip ROADMAP `[x]`
- Add partial-close note explaining what's done vs what remains
- Append to v2.0-MILESTONE-AUDIT.md "Phase 20.1 Carryforward" row with effort estimate
- Counter-signal: when EVERY CF empirically PASS, flipping is correct
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Operator — Create admin@topfarms.co.nz Supabase Auth user</name>
  <files>(no repo files modified — Supabase Studio Auth dashboard action only)</files>
  <read_first>
    - .planning/phases/20.1-standalone-admin-login-gateway-account-bootstrap/20.1-CONTEXT.md (§"Account bootstrap (GA-2)")
    - .planning/phases/20.1-standalone-admin-login-gateway-account-bootstrap/20.1-RESEARCH.md (§"Pitfall 8: --read-only MCP flag")
    - CLAUDE.md §2 (Studio SQL preference)
  </read_first>
  <action>
**CHECKPOINT — operator action required.** Plans 01-04 are merged. Standalone /admin AdminLoginPage gateway is live in the codebase but no dedicated `admin@topfarms.co.nz` account exists yet. Old test admin (`harry.symmans.smith@gmail.com`) still holds role='admin'.

Operator (Harry) MUST perform this in Supabase Studio (NOT via MCP). Studio Auth dashboard create-user is operator-only — no API surface for it in this phase, so this remains a `checkpoint:human-action` per the small set of legitimate human-only steps:

1. Open Supabase Studio for project ref `inlagtgpynemhipnqvty` (TopFarms).
2. Navigate: **Authentication → Users → Add user** (button top-right).
3. Choose **"Create new user"** with these inputs:
   - Email: `admin@topfarms.co.nz`
   - Password: choose a strong password; store in operator's password manager labelled `admin@topfarms.co.nz — TopFarms Phase 20.1`.
   - **Auto Confirm User: YES** (skip email-verification step — phase ASSUMES Harry receives email at this address; mailbox provisioning is a phase prerequisite per CONTEXT §Deferred).
4. Click **Create user**. Confirm Studio shows the new row in the Users table.
5. Copy the new `auth.users.id` UUID into operator notes — Task 5 will record it in the run-record.

Do NOT run any SQL yet. Do NOT sign in as this user yet.

**Resume signal:** Operator types `created: <UUID>` when the Studio Auth user creation is complete and confirmed in the Users table. If Studio operation fails, type `failed: <reason>` so we can diagnose before continuing (per CLAUDE §3).
  </action>
  <verify>
**Post-checkpoint verification (executor agent runs after operator types `created:`):**

Use project-scoped Supabase MCP (read-only OK) to confirm the new auth.users row exists:

```sql
SELECT id, email, email_confirmed_at, created_at
FROM auth.users
WHERE email = 'admin@topfarms.co.nz';
```

Expect exactly 1 row with `email_confirmed_at` populated (auto-confirm) and `created_at` recent. If 0 rows or `email_confirmed_at` null, signal failure to operator before proceeding.
  </verify>
  <acceptance_criteria>
    - Operator confirms `admin@topfarms.co.nz` appears in Studio's Authentication → Users list
    - Operator captures the new auth.users.id UUID for the run-record (Task 5 will record it verbatim)
    - MCP read-only SELECT returns exactly 1 row matching `email='admin@topfarms.co.nz'`
    - `email_confirmed_at` is non-null (auto-confirm landed)
    - No password is committed to the repo or any planning doc; lives only in operator's password manager
  </acceptance_criteria>
  <done>New auth.users row exists for admin@topfarms.co.nz (no role yet — that's Task 2). UUID captured.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Operator — Studio SQL: assign admin role to new user</name>
  <files>(no repo files modified — Studio SQL Editor action only; modifies public.user_roles in Supabase)</files>
  <read_first>
    - .planning/phases/20.1-standalone-admin-login-gateway-account-bootstrap/20.1-RESEARCH.md (§"Studio SQL Bootstrap Sequence" — Step 2 SQL block verbatim)
    - .planning/phases/20.1-standalone-admin-login-gateway-account-bootstrap/20.1-CONTEXT.md (§"Account bootstrap (GA-2)")
    - CLAUDE.md §2 (`--read-only ON` MCP; Studio SQL Editor for one-off writes)
  </read_first>
  <action>
**CHECKPOINT — operator action required.** auth.users row for admin@topfarms.co.nz exists from Task 1. Now assign the admin role.

Operator runs this in **Supabase Studio → SQL Editor** (NOT MCP — per CLAUDE §2, MCP `--read-only` flag-flip is avoided by using Studio Editor for one-off writes):

1. Open Studio SQL Editor for project ref `inlagtgpynemhipnqvty`.
2. Paste this SQL block VERBATIM (do NOT alter the email string, do NOT alter the role string):

```sql
INSERT INTO public.user_roles (user_id, role, is_active)
VALUES (
  (SELECT id FROM auth.users WHERE email = 'admin@topfarms.co.nz'),
  'admin',
  true
)
ON CONFLICT (user_id) DO UPDATE SET role = 'admin', is_active = true;
```

3. Click **Run**. Studio reports `INSERT 0 1` (or `UPDATE 0 1` if the `handle_new_user` trigger pre-created a row).
4. Capture the row-count Studio reports for the run-record.
5. **DO NOT proceed to Task 3 until Studio confirms success.** If Studio reports an error, diagnose before retrying (CLAUDE §3) — likely cause: typo in email, sequence-prefix migration drift, or `handle_new_user` trigger created a row already with `role='seeker'` (then ON CONFLICT path runs UPDATE — also fine).

**Resume signal:** Operator types `assigned: <row-count>` (e.g., `assigned: 1`) when Studio confirms success. If failed, type `failed: <Studio error message>`.
  </action>
  <verify>
**Post-checkpoint verification (executor agent runs after operator types `assigned:`):** Task 3 below performs the explicit MCP read-only SELECT to confirm role landed. The verification of Task 2 IS Task 3.
  </verify>
  <acceptance_criteria>
    - Studio reports the SQL block ran successfully (1 row affected)
    - Row count captured for run-record
    - No edit to MCP `--read-only` flag occurred (CLAUDE §2 — Studio Editor only; flag stays ON)
    - No transient errors reported in Studio output
  </acceptance_criteria>
  <done>public.user_roles row for admin@topfarms.co.nz exists with role='admin', is_active=true.</done>
</task>

<task type="auto">
  <name>Task 3: Read-only MCP — verify new admin row</name>
  <files>(no repo files modified — read-only MCP query against public.user_roles)</files>
  <read_first>
    - .planning/phases/20.1-standalone-admin-login-gateway-account-bootstrap/20.1-RESEARCH.md (§"Studio SQL Bootstrap Sequence" — Step 3 SELECT)
    - CLAUDE.md §1 (project-scoped MCP, ref `inlagtgpynemhipnqvty`)
    - CLAUDE.md §2 (read-only OK for SELECTs; flag-flip not required)
  </read_first>
  <action>
Use the project-scoped Supabase MCP (`--read-only ON`) to verify the new admin row landed correctly. Execute via the `execute_sql` MCP tool:

```sql
SELECT u.email, ur.role, ur.is_active, ur.created_at
FROM auth.users u
JOIN public.user_roles ur ON ur.user_id = u.id
WHERE u.email = 'admin@topfarms.co.nz';
```

**Expected:** Exactly 1 row returned with `role='admin'`, `is_active=true`. The `created_at` timestamp should be within the last few minutes.

**If 0 rows:** Diagnose before continuing (CLAUDE §3). Likely causes:
- Email typo in Task 1 or Task 2 SQL.
- Studio SQL Editor pasted into wrong project (verify project ref via Studio URL).
- `handle_new_user` trigger interaction edge case.

**If `role='seeker'` instead of `'admin'`:** The ON CONFLICT branch ran but didn't UPDATE — re-check Studio SQL ran successfully in Task 2.

**If `is_active=false`:** Re-run Task 2 SQL.

**Capture the SELECT response (email, role, is_active, created_at) for the run-record in Task 5.**

Do NOT proceed to Task 4 (browser UAT) until this verification returns the expected single admin row.
  </action>
  <verify>
    <automated>echo "Manual MCP read-only SELECT — operator records JSON response in Task 5 run-record (cannot automate from CI; gate is the JSON output captured by executor agent)"</automated>
  </verify>
  <acceptance_criteria>
    - MCP `execute_sql` SELECT returns exactly 1 row
    - Row fields: `email='admin@topfarms.co.nz'`, `role='admin'`, `is_active=true`
    - `created_at` recent (within current session)
    - JSON response captured by executor agent for run-record
  </acceptance_criteria>
  <done>New admin row empirically confirmed via read-only MCP. Safe to proceed to UAT.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Operator UAT — sign in via /admin AdminLoginPage with new account</name>
  <files>(no repo files modified — browser UAT only; outcomes recorded in Task 5 run-record)</files>
  <read_first>
    - .planning/phases/20.1-standalone-admin-login-gateway-account-bootstrap/20.1-VALIDATION.md (§"Manual-Only Verifications" — UAT step list)
    - .planning/phases/20.1-standalone-admin-login-gateway-account-bootstrap/20.1-RESEARCH.md (§"Pitfall 2: Lockout — operator removes admin role from old account before new account UAT passes")
    - tests/admin-bootstrap-UAT.md (Phase 20-08 run-record format precedent)
  </read_first>
  <action>
**CHECKPOINT — operator browser UAT required.** New `admin@topfarms.co.nz` account verified in DB with role=admin (Task 3). Standalone /admin AdminLoginPage gateway shipped in Plans 03-04. NEW UAT GATE — must pass before any old-role removal in Task 6.

Operator (Harry) performs in browser against the deployed app (or local dev server `pnpm dev` if not deployed):

**Pre-UAT setup:**
1. Open the app URL (production or `http://localhost:5173`).
2. If currently signed in as `harry.symmans.smith@gmail.com`: sign out via the existing user dropdown OR via the new Sidebar Sign Out button shipped in Plan 04 (also confirms the button works for general regression).

**UAT body — new account:**
3. Navigate to `/admin` in the address bar.
4. **Verify CF-AUTH-1 + CF-AUTH-2 (gateway renders form):** Page renders the AdminLoginPage form (NOT a redirect to `/login`). Confirm:
   - Email input visible
   - Password input visible
   - Sign in button visible
   - **NO Google or Facebook OAuth buttons present** (CONTEXT GA-1 lock)
   - "Forgot password?" link visible
   - "Back to main site" link visible
5. Enter email `admin@topfarms.co.nz` + password from operator's password manager.
6. Click **Sign in**. Wait for AuthContext to populate session+role.
7. **Verify CF-AUTH-1 + CF-AUTH-2 (admin branch renders):** Page transitions to AdminLayout (no redirect to /login or other URL — URL stays at `/admin`).
8. Confirm AdminSidebar visible (5 nav items + "Back to app").
9. Confirm DailyBriefing renders.

**Drawer smoke (regression coverage for Phase 20 ProfileDrawer flow):**
10. Click into `/admin/employers` via the sidebar.
11. Click any employer row to open ProfileDrawer.
12. Confirm drawer opens with Active toggle, Notes field, Activity log.
13. Close the drawer (Escape or close button).

**Verify CF-AUTH-3 (no regression on /login admin redirect):**
14. Sign out (use Sidebar Sign Out — also Plan 04 regression).
15. Navigate to `/login`.
16. Sign in with `admin@topfarms.co.nz` again.
17. Confirm post-login URL is `/admin` and AdminLayout renders. (This proves the Login.tsx → dashboardPathFor swap from Plan 02 still routes admin JWTs to `/admin`.)

**Capture for run-record (Task 5):**
- Timestamp of UAT start (ISO 8601 UTC)
- All step outcomes (yes/no per the run-record format in `<interfaces>`)
- Browser used (Chrome/Firefox/Safari + version)
- Any anomalies observed (even if not blocking)

**If any step fails:** Operator types `failed: <which step> — <details>` as the resume-signal. DO NOT proceed to Task 5 (which writes the run-record success path) — first fix the new account (re-check password, re-verify role assignment, etc.). Plan 5 sequence guarantees the old role stays intact while diagnosing.

**If all steps pass:** Operator types `passed` as resume-signal.
  </action>
  <verify>
**Post-checkpoint verification (executor agent runs after operator types `passed`):** All 17 sub-step outcomes must be captured by the operator and verified for completeness when Task 5 writes the run-record. The presence of the `## Run` section with all yes/no fields populated is the gate. If operator types `failed:`, plan execution PAUSES — do not run Task 6.
  </verify>
  <acceptance_criteria>
    - All steps 4–9 pass: AdminLoginPage renders for unauthenticated /admin visit; sign-in transitions to AdminLayout
    - No OAuth provider buttons visible on AdminLoginPage form (regression on CONTEXT GA-1 — visually confirmed by operator)
    - ProfileDrawer smoke (steps 10–13) succeeds
    - CF-AUTH-3 smoke (steps 14–17) succeeds — /login admin redirect to /admin works
    - All 17 outcomes captured for run-record
    - Operator resume-signal is `passed` (NOT `failed:`)
  </acceptance_criteria>
  <done>End-to-end empirical proof that the standalone /admin gateway works for the new dedicated admin account. Old role removal (Task 6) is now safe to perform.</done>
</task>

<task type="auto">
  <name>Task 5: Write/append UAT run-record (Phase 20.1 transfer section)</name>
  <files>tests/admin-bootstrap-UAT.md</files>
  <read_first>
    - tests/admin-bootstrap-UAT.md (Phase 20-08 run-record precedent — read full file; the recommended approach is to APPEND a new `## Run` section here)
    - .planning/phases/20.1-standalone-admin-login-gateway-account-bootstrap/20.1-VALIDATION.md (§"Wave 0 Requirements" — Wave 0 line 4)
    - .planning/phases/20.1-standalone-admin-login-gateway-account-bootstrap/20.1-RESEARCH.md (§"Studio SQL Operator-Action Precedent" — run-record fields)
  </read_first>
  <action>
APPEND a new `## Run` section to existing `tests/admin-bootstrap-UAT.md`. Phase 20-08 already established this file as the canonical run-record file; a new run section keeps audit history co-located.

Append a section with this EXACT structure, filling in real values from Tasks 1-4 (replace every `<...>` placeholder with captured value; replace YYYY-MM-DD with today's actual date in UTC):

```markdown

## Run YYYY-MM-DDTHH:MM:SSZ — Phase 20.1 admin transfer

**Operator:** Harry (harry.symmans.smith@gmail.com)
**Branch:** main (Phase 20.1 plans 01-04 merged)
**Browser:** <Chrome/Firefox/Safari + version>

### Step 1 — Studio Auth user creation
- New user: admin@topfarms.co.nz
- auth.users.id: <UUID from Task 1>
- Auto-confirmed: yes
- Password: stored in operator's password manager (NOT in repo)

### Step 2 — Studio SQL admin role assignment
- SQL block: INSERT INTO public.user_roles ... ON CONFLICT (user_id) DO UPDATE SET role='admin', is_active=true
- Result: <row-count> row(s) affected (expected 1)

### Step 3 — Read-only MCP verification (new account)
- SELECT result: 1 row, email=admin@topfarms.co.nz, role=admin, is_active=true
- created_at: <timestamp>

### Step 4 — UAT browser flow (new account)
- Sign-out from old account: yes
- Navigate to /admin (unauthenticated): AdminLoginPage form rendered: yes
- No Google/Facebook OAuth buttons present: yes
- Forgot password + Back to main site links present: yes
- Sign in with admin@topfarms.co.nz: success
- AdminLayout rendered + AdminSidebar visible + 5 nav items + Back to app: yes
- DailyBriefing rendered: yes
- /admin/employers row click → ProfileDrawer opens: yes
- ProfileDrawer Active toggle + Notes field + Activity log visible: yes
- Sign out via Sidebar Sign Out button (Plan 04 regression): yes
- /login + sign in with admin@topfarms.co.nz → redirected to /admin (CF-AUTH-3): yes

### Step 5 — Studio SQL old-account role removal
- DEFERRED until Task 6 below

### Step 6 — Read-only MCP verification (old account)
- DEFERRED until Task 7 below

### Step 7 — Regression UAT (old account /admin → AccessDenied)
- DEFERRED until Task 8 below

### Verdict (so far)
- New account bootstrap: PASS (CF-ACCOUNT-1 satisfied with empirical evidence)
- New account gateway UAT: PASS (CF-AUTH-1, CF-AUTH-2, CF-AUTH-3 all satisfied with empirical evidence)
- Old account role removal: PENDING (Task 6 below)
```

Save the file. Do NOT yet add the Step 5/6/7 results — those land in Task 8 after the old-role removal completes.

Do NOT commit yet; the final atomic commit is in Task 8 (per CLAUDE §4 — bundle the UAT run-record + VERIFICATION.md + SUMMARY.md + ROADMAP + audit + STATE updates into ONE commit).
  </action>
  <verify>
    <automated>test -f tests/admin-bootstrap-UAT.md && grep -c "Phase 20.1 admin transfer" tests/admin-bootstrap-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - Run-record section exists in `tests/admin-bootstrap-UAT.md`
    - Section header matches `## Run YYYY-MM-DDTHH:MM:SSZ` format with REAL ISO 8601 timestamp (no `YYYY-MM-DD` placeholder remaining): `grep -E "## Run 2026-05-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z" tests/admin-bootstrap-UAT.md` returns at least 1 match
    - Section contains all Step 1-4 outcomes from Tasks 1-4
    - Real auth.users.id UUID recorded (NOT `<UUID from Task 1>`): `grep -c "<UUID from Task 1>" tests/admin-bootstrap-UAT.md` returns `0`
    - All `<...>` placeholders for completed steps replaced with real values: `grep -cE "<row-count>|<timestamp>|<Chrome" tests/admin-bootstrap-UAT.md` returns `0` for the Phase 20.1 section
    - Step 5/6/7 explicitly marked DEFERRED at this point (literal string `DEFERRED` present)
  </acceptance_criteria>
  <done>UAT run-record on disk with empirical evidence for new account bootstrap (Tasks 1-4). Steps 5-7 deferred to Task 8.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 6: Operator — Studio SQL: remove admin role from harry.symmans.smith@gmail.com</name>
  <files>(no repo files modified — Studio SQL Editor action only; modifies public.user_roles)</files>
  <read_first>
    - .planning/phases/20.1-standalone-admin-login-gateway-account-bootstrap/20.1-RESEARCH.md (§"Studio SQL Bootstrap Sequence" — Step 5 SQL + §"Pitfall 2: Lockout")
    - .planning/phases/20.1-standalone-admin-login-gateway-account-bootstrap/20.1-CONTEXT.md (§"Old admin role removal (GA-3)")
    - tests/admin-bootstrap-UAT.md (the just-updated run-record — confirm Step 4 verdict was PASS, NOT failed)
  </read_first>
  <action>
**CHECKPOINT — operator action required.** Tasks 1-5 complete. New admin@topfarms.co.nz account bootstrapped, UAT-confirmed, and run-record on disk. The system has TWO admin-role accounts at this moment (new + legacy). This task removes the legacy.

**GATE: Confirm Task 4 verdict was PASS, NOT failed.** If Task 4 failed (resume-signal was `failed: ...`), DO NOT run this task. Pause, fix the new account, re-run Task 4, only then proceed. (RESEARCH Pitfall 2 — premature old-role removal causes lockout.)

Operator runs this in **Supabase Studio → SQL Editor** (NOT MCP):

1. Open Studio SQL Editor for project ref `inlagtgpynemhipnqvty`.
2. Paste this SQL block VERBATIM:

```sql
UPDATE public.user_roles
SET role = 'seeker', is_active = true
WHERE user_id = (SELECT id FROM auth.users WHERE email = 'harry.symmans.smith@gmail.com');
```

3. Click **Run**. Studio reports `UPDATE 1`.
4. Capture row-count for run-record.

**Why `role='seeker'` (not delete the row):** the user_roles CHECK constraint requires role IN ('employer', 'seeker', 'admin'); demoting to seeker is the documented downgrade path (CONTEXT §"Old admin role removal (GA-3)").

**Audit trail caveat:** This direct SQL bypasses `admin_audit_log` (which only fires via `admin_set_user_active` RPC). Acceptable here as a one-shot bootstrap operation, NOT a runtime admin action (CONTEXT §Deferred Ideas explicit). Document the SQL + timestamp in Task 8 SUMMARY.

**Resume signal:** Operator types `removed: <row-count>` when Studio confirms UPDATE succeeded. If failed, type `failed: <Studio error>`.
  </action>
  <verify>
**Post-checkpoint verification (executor agent runs after operator types `removed:`):** Task 7 below performs the explicit MCP read-only SELECT to confirm role='seeker' landed.
  </verify>
  <acceptance_criteria>
    - Studio reports `UPDATE 1` (exactly 1 row affected)
    - Row count captured for run-record
    - No MCP flag-flip occurred (CLAUDE §2 — Studio only)
    - Operator confirms Step 4 (Task 4) verdict was `passed` BEFORE running this task (gate)
  </acceptance_criteria>
  <done>Old account `harry.symmans.smith@gmail.com` now has `role='seeker'`, `is_active=true`. Admin role removed.</done>
</task>

<task type="auto">
  <name>Task 7: Read-only MCP — verify old account demoted</name>
  <files>(no repo files modified — read-only MCP query against public.user_roles)</files>
  <read_first>
    - .planning/phases/20.1-standalone-admin-login-gateway-account-bootstrap/20.1-RESEARCH.md (§"Studio SQL Bootstrap Sequence" — Step 6 SELECT)
    - CLAUDE.md §1, §2
  </read_first>
  <action>
Via MCP `execute_sql` (read-only OK):

```sql
SELECT u.email, ur.role, ur.is_active
FROM auth.users u
JOIN public.user_roles ur ON ur.user_id = u.id
WHERE u.email = 'harry.symmans.smith@gmail.com';
```

**Expected:** Exactly 1 row, `email='harry.symmans.smith@gmail.com'`, `role='seeker'`, `is_active=true`.

**If `role='admin'` still:** Task 6 SQL didn't land. Diagnose before continuing — re-check Studio SQL output.

**If 0 rows:** unexpected — the `handle_new_user` trigger should have created a row years ago. Investigate via: `SELECT COUNT(*) FROM public.user_roles WHERE user_id = (SELECT id FROM auth.users WHERE email = 'harry.symmans.smith@gmail.com');` — if 0, the row was never created (anomaly), and Task 6's UPDATE updated zero rows. INSERT a new seeker row instead via Studio SQL (not MCP).

**Capture response for Task 8 run-record append.**
  </action>
  <verify>
    <automated>echo "Manual MCP read-only SELECT — operator records JSON response in Task 8 run-record append (cannot automate from CI; gate is the JSON output captured by executor agent)"</automated>
  </verify>
  <acceptance_criteria>
    - MCP SELECT returns exactly 1 row with `role='seeker'`, `is_active=true`
    - Response captured for run-record
  </acceptance_criteria>
  <done>Old account empirically confirmed as `role='seeker'`. Safe to proceed to regression UAT.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 8: Regression UAT + finalize run-record + write VERIFICATION + SUMMARY + ROADMAP flip</name>
  <files>tests/admin-bootstrap-UAT.md, .planning/phases/20.1-standalone-admin-login-gateway-account-bootstrap/20.1-VERIFICATION.md, .planning/phases/20.1-standalone-admin-login-gateway-account-bootstrap/20.1-SUMMARY.md, .planning/ROADMAP.md, .planning/v2.0-MILESTONE-AUDIT.md, .planning/STATE.md, .planning/todos/pending/2026-05-05-add-sign-out-button-to-dashboard-sidebar-footer.md</files>
  <read_first>
    - .planning/phases/20.1-standalone-admin-login-gateway-account-bootstrap/20.1-VALIDATION.md (§"Manual-Only Verifications" — old account UAT row)
    - .planning/phases/20-super-admin-dashboard/20-VERIFICATION.md (Phase 20 VERIFICATION format precedent)
    - .planning/v2.0-MILESTONE-AUDIT.md (current Phase 20.1 carryforward row to update)
    - .planning/STATE.md (current position to update)
    - tests/admin-bootstrap-UAT.md (the in-progress run-record from Task 5 — append Step 5/6/7 results)
    - .planning/todos/pending/2026-05-05-add-sign-out-button-to-dashboard-sidebar-footer.md (Phase 21 todo to close)
    - CLAUDE.md §4 (atomic commit) and §7 (partial-close discipline)
  </read_first>
  <action>
**Hybrid task — operator browser regression UAT THEN executor docs writing + atomic commit.** Plan 5's final task. Tasks 1-7 complete. New admin in place. Old account demoted in DB but not yet smoke-tested in browser.

**Part A — Operator browser regression UAT (old account):**

1. Sign out of admin@topfarms.co.nz (if currently signed in).
2. Sign in with `harry.symmans.smith@gmail.com` (now `role='seeker'`).
3. Navigate to `/admin` in the address bar.
4. **Verify:** AdminLoginPage does NOT appear (user IS authenticated). Inline "Access denied" state appears with:
   - "Access denied" heading visible
   - "Your account does not have admin privileges." body visible
   - "Back to your dashboard" link/button styled as primary CTA
   - URL stays at `/admin` (no redirect to /login or /dashboard)
5. Click "Back to your dashboard" → confirm redirect lands at `/dashboard/seeker` (proves `dashboardPathFor('seeker')` from Plan 02 wired correctly into AccessDeniedView from Plan 03).

Operator types `regression-pass` when steps 1-5 succeed; `regression-fail: <details>` if not.

**Part B — Executor agent writes closure docs (after operator types `regression-pass`):**

**B.1. Append to run-record** (`tests/admin-bootstrap-UAT.md`) — fill in the deferred Step 5/6/7 sections from Task 5:

```markdown
### Step 5 — Studio SQL old-account role removal
- SQL block: UPDATE public.user_roles SET role='seeker', is_active=true WHERE user_id=(SELECT id FROM auth.users WHERE email='harry.symmans.smith@gmail.com')
- Result: 1 row affected
- Audit trail: NOT in admin_audit_log (Studio SQL bypass; documented per CLAUDE §4 atomic-commit + CONTEXT §Deferred)

### Step 6 — Read-only MCP verification (old account)
- SELECT result: 1 row, email=harry.symmans.smith@gmail.com, role=seeker, is_active=true

### Step 7 — Regression UAT (old account /admin → AccessDenied)
- Sign out admin@topfarms.co.nz, sign in harry.symmans.smith@gmail.com: success
- Navigate to /admin: AccessDenied inline state rendered (NOT redirect): yes
- "Access denied" heading visible: yes
- "Back to your dashboard" link visible: yes
- Click Back → redirected to /dashboard/seeker: yes (CF-CODE-1 + CF-AUTH-2 path coverage proven)

### Final Verdict
- CF-AUTH-1, CF-AUTH-2, CF-AUTH-3, CF-ACCOUNT-1, CF-ACCOUNT-2, CF-CODE-1, CF-CODE-2: all PASS with empirical evidence
```

**B.2. Create `.planning/phases/20.1-standalone-admin-login-gateway-account-bootstrap/20.1-VERIFICATION.md`** — mirror 20-VERIFICATION.md format:

```markdown
---
phase: 20.1-standalone-admin-login-gateway-account-bootstrap
verified: 2026-05-XXTHH:MM:SSZ
verifier: Harry (operator) + Claude (executor)
status: PASS
plans_executed: 5/5
cf_ids_pass: 7/7
---

# Phase 20.1 — Verification Report

## Carryforward IDs (7/7 PASS)

| CF-ID | Description | Verdict | Evidence |
|-------|-------------|---------|----------|
| CF-AUTH-1 | Standalone admin login gateway at /admin | PASS | tests/admin-login.test.tsx 4/4 green; UAT run-record Step 4 — AdminLoginPage rendered for unauthenticated /admin |
| CF-AUTH-2 | /admin renders AdminLoginPage for unauth/non-admin | PASS | tests/admin-login.test.tsx 4 branches green; UAT Step 4 (admin sign-in → AdminLayout) + Step 7 (old account → AccessDenied) |
| CF-AUTH-3 | /login redirects admin JWT to /admin | PASS | UAT run-record Step 4 (steps 14-17): /login → admin sign-in → /admin URL final |
| CF-ACCOUNT-1 | admin@topfarms.co.nz dedicated account | PASS | UAT run-record Step 1 (Studio Auth create) + Step 2 (SQL role assign) + Step 3 (MCP SELECT confirms role=admin) |
| CF-ACCOUNT-2 | Admin role removed from old account after UAT | PASS | UAT run-record Step 5 (Studio SQL UPDATE role=seeker) + Step 6 (MCP SELECT confirms role=seeker) + Step 7 (browser /admin → AccessDenied) |
| CF-CODE-1 | dashboardPathFor helper unifies 5 callsites | PASS | tests/dashboard-routing.test.ts 3/3 green; grep -rn "role === 'admin' ? '/admin'" src/ returns zero |
| CF-CODE-2 | Sign Out button in dashboard sidebar footer | PASS | tests/sidebar-signout.test.tsx 1/1 green; UAT Step 4 step 14 (sign out via Sidebar regression) |

## Plans (5/5 PASS)

| Plan | Subsystem | Status | Artefacts |
|------|-----------|--------|-----------|
| 20.1-01 | dashboardPathFor helper + 3 unit tests | PASS | src/lib/routing.ts + tests/dashboard-routing.test.ts |
| 20.1-02 | Swap 5 callsites | PASS | 5 src files updated |
| 20.1-03 | AdminLoginPage + AdminGate hybrid + 4 tests | PASS | src/pages/admin/AdminLoginPage.tsx + main.tsx + tests/admin-login.test.tsx |
| 20.1-04 | Sign Out button in Sidebar | PASS | src/components/layout/Sidebar.tsx + tests/sidebar-signout.test.tsx |
| 20.1-05 | Bootstrap UAT + role removal + ship | PASS | UAT run-record + this VERIFICATION.md + SUMMARY.md + ROADMAP flip |

## Audit-trail caveat (per CONTEXT §Deferred + CLAUDE §4)

Both Studio SQL operations (Task 2 admin-role assign + Task 6 old-role removal) bypass `admin_audit_log` — direct table writes, not via `admin_set_user_active` RPC. Acceptable for one-shot bootstrap operations. Documented here for audit completeness.

## Sign-off

- [x] All 7 CF-IDs PASS with empirical evidence
- [x] Full vitest suite green (post-Plan-04: 175+ passed | 113 todo | 0 failed)
- [x] pnpm tsc --noEmit clean
- [x] CLAUDE.md §3 honoured
- [x] CLAUDE.md §7 honoured
- [x] CLAUDE.md §4 honoured
```

**B.3. Create `.planning/phases/20.1-standalone-admin-login-gateway-account-bootstrap/20.1-SUMMARY.md`** — short closure summary linking to VERIFICATION.md and listing per-plan SUMMARYs (01-04 each have their own SUMMARY.md).

**B.4. Update `.planning/ROADMAP.md`** — Phase 20.1 entry. **ONLY flip `[ ]` → `[x]` if every CF is PASS in VERIFICATION.md (CLAUDE §7 partial-close discipline).** If any CF is PARTIAL, leave `[ ]` and add a partial-close note inline.

Specifically, in the "Phase 20.1: Standalone Admin Login Gateway + Account Bootstrap (INSERTED)" section:
- Update `**Plans:**` from `0 plans` to `5/5 plans complete`
- Update Plans list — change `- [ ] TBD (run /gsd:plan-phase 20.1 to break down)` to:
  ```
  - [x] 20.1-01-PLAN.md — dashboardPathFor helper + 3 unit tests
  - [x] 20.1-02-PLAN.md — Swap 5 inline ternaries to dashboardPathFor
  - [x] 20.1-03-PLAN.md — AdminLoginPage + AdminGate hybrid /admin route + 4 tests
  - [x] 20.1-04-PLAN.md — Sign Out button in dashboard Sidebar footer + 1 test
  - [x] 20.1-05-PLAN.md — Bootstrap UAT + role removal + 20.1-VERIFICATION + ROADMAP ship
  ```
- Add to milestone-summary section near top (in v2.0 section after Phase 20 entry):
  `- [x] **Phase 20.1: Standalone Admin Login Gateway + Account Bootstrap** — completed 2026-05-XX (see 20.1-VERIFICATION.md PASS verdict — 7/7 CF-IDs PASS)`

**B.5. Update `.planning/v2.0-MILESTONE-AUDIT.md`** — Phase 20.1 carryforward row. If all CFs PASS, mark CLOSED with reference to 20.1-VERIFICATION.md.

**B.6. Update `.planning/STATE.md`** — `progress.completed_phases` increment, `progress.completed_plans` += 5, `stopped_at` updated to "Phase 20.1 COMPLETE — Standalone Admin Login Gateway + Account Bootstrap shipped (7/7 CF-IDs PASS, 5/5 plans executed, ROADMAP flipped [x] 2026-05-XX)" with current ISO 8601 timestamp.

**B.7. Move (or annotate closed) `.planning/todos/pending/2026-05-05-add-sign-out-button-to-dashboard-sidebar-footer.md`** — if `.planning/todos/done/` directory exists, `git mv` the file there. Otherwise add `closed_by: 20.1-04` frontmatter field to the file in place.

**B.8. Final atomic commit** (per CLAUDE §4 — one commit for the closure doc trio + ROADMAP + audit + STATE + todo close):

```bash
node "/Users/harrysmith/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(20.1-05): close Phase 20.1 (UAT PASS + VERIFICATION + ROADMAP flip)" --files tests/admin-bootstrap-UAT.md .planning/phases/20.1-standalone-admin-login-gateway-account-bootstrap/20.1-VERIFICATION.md .planning/phases/20.1-standalone-admin-login-gateway-account-bootstrap/20.1-SUMMARY.md .planning/ROADMAP.md .planning/v2.0-MILESTONE-AUDIT.md .planning/STATE.md .planning/todos/pending/2026-05-05-add-sign-out-button-to-dashboard-sidebar-footer.md
```

(If todos/done was used, also add the new path to the commit `--files` list.)

**Resume signal:** Operator types `closed` when all docs are written, ROADMAP flipped (or partial-close noted), and final commit lands. Type `partial: <which CF lacks evidence>` if any CF can't be empirically confirmed — Claude will then write VERIFICATION.md as PARTIAL and update v2.0-MILESTONE-AUDIT.md with the carryforward row instead of flipping ROADMAP [x].
  </action>
  <verify>
    <automated>test -f .planning/phases/20.1-standalone-admin-login-gateway-account-bootstrap/20.1-VERIFICATION.md && grep -c "^| CF-" .planning/phases/20.1-standalone-admin-login-gateway-account-bootstrap/20.1-VERIFICATION.md && grep -c "Phase 20.1 admin transfer" tests/admin-bootstrap-UAT.md && pnpm test 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - tests/admin-bootstrap-UAT.md contains the full Phase 20.1 run-record with Steps 1-7 all populated (no `<...>` or `TBD` placeholders): `grep -cE "<UUID|<row-count>|<timestamp>|TBD" tests/admin-bootstrap-UAT.md` returns `0` for the Phase 20.1 section
    - File `.planning/phases/20.1-standalone-admin-login-gateway-account-bootstrap/20.1-VERIFICATION.md` exists with at least 7 CF-ID rows: `grep -c "^| CF-" .planning/phases/20.1-standalone-admin-login-gateway-account-bootstrap/20.1-VERIFICATION.md` returns at least `7`
    - File `.planning/phases/20.1-standalone-admin-login-gateway-account-bootstrap/20.1-SUMMARY.md` exists
    - `.planning/ROADMAP.md` Phase 20.1 row reflects empirical state — IF all 7 CFs PASS: `grep -c "Phase 20.1.*completed 2026-05" .planning/ROADMAP.md` returns at least `1`. If partial: grep should NOT match and v2.0-MILESTONE-AUDIT.md must show carryforward.
    - `.planning/STATE.md` `stopped_at` mentions "Phase 20.1 COMPLETE" or "Phase 20.1 PARTIAL" matching VERIFICATION verdict
    - `.planning/v2.0-MILESTONE-AUDIT.md` Phase 20.1 carryforward row updated (CLOSED or PARTIAL)
    - Phase 21 todo file moved to `.planning/todos/done/` OR annotated `closed_by: 20.1-04`
    - Single atomic git commit ships all 6-7 doc files (per CLAUDE §4)
    - Full vitest suite still green: `pnpm test` exits 0 (no doc edit broke source)
  </acceptance_criteria>
  <done>Phase 20.1 closed. All 7 CF-IDs empirically verified (or partial-close per CLAUDE §7 if not). All closure docs land in one atomic commit.</done>
</task>

</tasks>

<verification>
Final phase-level gate:

```bash
# 1. All test suites still green
pnpm test                          # expect 175+ passed | 113 todo | 0 failed
pnpm tsc --noEmit                  # expect exit 0
pnpm build 2>&1 | tail -5          # expect exit 0

# 2. Verification doc exists with all 7 CF-IDs
test -f .planning/phases/20.1-standalone-admin-login-gateway-account-bootstrap/20.1-VERIFICATION.md
grep -c "^| CF-" .planning/phases/20.1-standalone-admin-login-gateway-account-bootstrap/20.1-VERIFICATION.md  # expected: at least 7

# 3. ROADMAP reflects truth
grep -A 1 "Phase 20.1" .planning/ROADMAP.md | head -10

# 4. UAT run-record has the Phase 20.1 section
grep -c "Phase 20.1 admin transfer" tests/admin-bootstrap-UAT.md  # expected: at least 1
```

If any check fails, do NOT mark plan done. Diagnose per CLAUDE §3.
</verification>

<success_criteria>
1. New `admin@topfarms.co.nz` Auth user exists with `role='admin'`, `is_active=true` (CF-ACCOUNT-1)
2. New account UAT PASS — sign-in via standalone /admin AdminLoginPage → AdminLayout (CF-AUTH-1, CF-AUTH-2)
3. Login.tsx admin redirect to /admin still works (CF-AUTH-3 regression)
4. Old account `harry.symmans.smith@gmail.com` has `role='seeker'`, `is_active=true` (CF-ACCOUNT-2)
5. Old account regression UAT — /admin shows inline AccessDenied; "Back to your dashboard" → /dashboard/seeker
6. UAT run-record (`tests/admin-bootstrap-UAT.md`) appended with full Phase 20.1 transfer log
7. `20.1-VERIFICATION.md` written with all 7 CF-IDs marked PASS (or PARTIAL with carryforward per CLAUDE §7)
8. `20.1-SUMMARY.md` written
9. ROADMAP.md Phase 20.1 row reflects empirical verdict (NO premature [x] flip if any CF lacks proof)
10. v2.0-MILESTONE-AUDIT.md Phase 20.1 carryforward row updated (CLOSED or PARTIAL)
11. STATE.md updated to mark phase complete
12. Phase 21 Sign Out todo closed
13. Single atomic doc-trio commit per CLAUDE §4
14. Audit-trail caveat (Studio SQL bypasses admin_audit_log) documented in SUMMARY
</success_criteria>

<output>
After completion, ensure these artifacts are on disk and committed:
- `tests/admin-bootstrap-UAT.md` — UAT run-record with new Phase 20.1 section
- `.planning/phases/20.1-standalone-admin-login-gateway-account-bootstrap/20.1-VERIFICATION.md` — phase verification
- `.planning/phases/20.1-standalone-admin-login-gateway-account-bootstrap/20.1-SUMMARY.md` — phase summary
- `.planning/ROADMAP.md` — Phase 20.1 row updated
- `.planning/v2.0-MILESTONE-AUDIT.md` — Phase 20.1 carryforward row updated
- `.planning/STATE.md` — current position updated
- `.planning/todos/{done,pending}/2026-05-05-add-sign-out-button-to-dashboard-sidebar-footer.md` — moved or annotated

Phase 20.1 ships when commit lands and `git status` is clean.
</output>
