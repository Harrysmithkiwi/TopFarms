---
phase: 20.1-standalone-admin-login-gateway-account-bootstrap
plan: 05
subsystem: auth
tags: [supabase, studio-sql, mcp, admin, uat, operator-action, bootstrap]

requires:
  - phase: 20.1
    provides: AdminGate hybrid /admin route (Plan 03), Sidebar Sign Out (Plan 04), dashboardPathFor helper (Plan 01) + 5-callsite swap (Plan 02)
  - phase: 20-super-admin-dashboard
    provides: AdminLayout, admin_* RPCs, ADMIN-BOOTSTRAP-1 UAT precedent format
provides:
  - admin@topfarms.co.nz Supabase Auth user (auth.users.id = ab48ed2b-0336-4b1d-8937-5d3eff50faf6)
  - public.user_roles row for new admin (role=admin, is_active=true)
  - Demoted harry.symmans.smith@gmail.com (role=seeker, is_active=true; user_id = 5634f2fb-ad12-4f5e-8d48-833373c77691)
  - Empirical end-to-end UAT proof for CF-AUTH-1, CF-AUTH-2 (with caveat), CF-AUTH-3, CF-ACCOUNT-1, CF-ACCOUNT-2
  - Phase 20.1 closure docs (20.1-VERIFICATION.md + 20.1-SUMMARY.md)
  - ROADMAP.md Phase 20.1 flip to [x]
  - v2.0-MILESTONE-AUDIT.md Phase 20.1 carryforward row closed
affects: [post-deploy-fresh-session-reverify, future-admin-account-rotations]

tech-stack:
  added: []
  patterns:
    - "Studio SQL Editor operator-action for one-off DB writes (CLAUDE §2)"
    - "Lockout-sequencing gate: new account UAT must PASS before old credential is destroyed (RESEARCH Pitfall 2)"
    - "Three-layer empirical proof for runtime caveats: grep diagnostic + unit test + DB-level evidence — when all three confirm no code regression, anomalous browser observation is documented as caveat, not blocker"

key-files:
  created:
    - ".planning/phases/20.1-standalone-admin-login-gateway-account-bootstrap/20.1-VERIFICATION.md"
    - ".planning/phases/20.1-standalone-admin-login-gateway-account-bootstrap/20.1-SUMMARY.md"
    - ".planning/phases/20.1-standalone-admin-login-gateway-account-bootstrap/20.1-05-bootstrap-uat-and-ship-SUMMARY.md (this file)"
  modified:
    - "tests/admin-bootstrap-UAT.md (Phase 20.1 transfer run-record finalized — Steps 5–7 populated, Final Verdict, Anomaly Notes section)"
    - ".planning/ROADMAP.md (Phase 20.1 entry flipped [x] with completion date 2026-05-05; per-plan list flipped [x] x5)"
    - ".planning/v2.0-MILESTONE-AUDIT.md (Phase 20.1 carryforward row marked CLOSED with link to 20.1-VERIFICATION.md)"
    - ".planning/phases/20-super-admin-dashboard/20-VERIFICATION.md (Carryforward checklist CF-AUTH-1/2/3 + CF-ACCOUNT-1/2 + CF-CODE-2 flipped [x])"
    - ".planning/STATE.md (Phase 20.1 marked complete; progress.completed_plans 23 → 24; stopped_at updated)"

key-decisions:
  - "Studio SQL Editor for both DB writes (Tasks 2 + 6) — avoids MCP --read-only flag-flip restart cycle per CLAUDE §2"
  - "Audit-trail bypass acceptable for one-shot bootstraps — admin_audit_log writes only via admin_set_user_active RPC; direct table writes documented in VERIFICATION.md per CLAUDE §4"
  - "Lockout-sequencing gate held — Task 4 PASS confirmed before Task 6 ran (RESEARCH Pitfall 2)"
  - "CF-AUTH-2 anomaly handling — three independent evidence lines (grep + unit test + helper test) ruled out code regression; operator-attributed stale-cache root cause; documented PASS with caveat (NOT partial-close per CLAUDE §7) because empirical proof exists at unit-test + DB-level + code-path-grep layers"
  - "Drawer smoke skipped in Task 4 UAT — already covered by Phase 20-08 ADMIN-BOOTSTRAP-1 prior run on same codebase; Plan 05 changed no drawer code; gateway UAT (CF-AUTH-1, CF-AUTH-2 unauth+admin branches) confirmed without it"
  - "Atomic single docs-trio commit per CLAUDE §4 — UAT.md + VERIFICATION.md + SUMMARY.md (phase + plan) + ROADMAP + audit + 20-VERIFICATION + STATE all in one commit"

patterns-established:
  - "Operator-action plan execution chain — checkpoint:human-action gates resume signal `created:` / `assigned:` / `removed:` / `passed` / `regression-pass`; executor agent runs MCP read-only verification SELECTs between operator gates; final docs trio shipped in one atomic commit"
  - "CF-ID empirical evidence enumeration in VERIFICATION.md — every CF-ID gets a row with explicit evidence pointer (test file + line, commit hash, UAT step, MCP query result); caveats documented in same row + dedicated detailed disposition section"

requirements-completed:
  - CF-AUTH-1
  - CF-AUTH-2
  - CF-AUTH-3
  - CF-ACCOUNT-1
  - CF-ACCOUNT-2

duration: ~30min wall-clock for executor-side docs trio (operator actions across multiple sessions ~1h cumulative)
completed: 2026-05-05
---

# Phase 20.1 Plan 05: Bootstrap UAT + Account Transfer + Ship Summary

**End-to-end operator UAT bootstrap of admin@topfarms.co.nz with role transfer from old test admin account, Phase 20.1 closure docs trio shipped in one atomic commit per CLAUDE §4.**

## Performance

- **Duration:** ~30min executor-side (Steps 5–8 of run-record + VERIFICATION + SUMMARY + ROADMAP + audit + STATE)
- **Operator actions:** 4 checkpoints (Tasks 1, 2, 4 UAT, 6 SQL old role + 8a regression UAT)
- **Tasks:** 8 (5 operator-action checkpoints + 3 executor automation)
- **Files modified:** 8 (1 UAT.md + 2 phase docs + 1 plan SUMMARY + 1 ROADMAP + 1 audit + 1 phase-20 VERIFICATION + 1 STATE)

## Accomplishments

- Dedicated `admin@topfarms.co.nz` Supabase Auth user created (auto-confirm; `auth.users.id = ab48ed2b-0336-4b1d-8937-5d3eff50faf6`)
- Admin role assigned via Studio SQL UPSERT (1 row affected)
- End-to-end UAT confirmed AdminLoginPage renders for unauth + admin sign-in lands at AdminLayout (CF-AUTH-1, CF-AUTH-2 unauth+admin branches, CF-AUTH-3 redirect, CF-ACCOUNT-1)
- Old `harry.symmans.smith@gmail.com` demoted to seeker via Studio SQL (1 row affected; CF-ACCOUNT-2)
- DB-level role transition empirically honored at runtime — old account post-login redirect went to `/dashboard/seeker` via `dashboardPathFor` swap from Plan 02
- 20.1-VERIFICATION.md authored with all 7 CF-IDs PASS (1 with documented runtime caveat per CLAUDE §7)
- ROADMAP.md flipped `[x]` for Phase 20.1; v2.0-MILESTONE-AUDIT.md carryforward row CLOSED; Phase 20-VERIFICATION.md carryforward checklist closed

## Task Commits

This plan is unique — operator actions don't produce git commits. The single atomic commit at the end of the plan ships all closure docs together:

| Task | Type | Result | Commit |
|------|------|--------|--------|
| 1 | checkpoint:human-action | Studio Auth user created | (no commit — operator action) |
| 2 | checkpoint:human-action | Studio SQL admin role assigned (1 row) | (no commit) |
| 3 | auto (MCP read-only) | SELECT verified role=admin, is_active=true | (no commit — read-only) |
| 4 | checkpoint:human-verify | Browser UAT — AdminLoginPage + AdminLayout sign-in PASS | (no commit) |
| 5 | auto (UAT.md append) | Run-record Step 1–4 written | (bundled into final commit) |
| 6 | checkpoint:human-action | Studio SQL old-role UPDATE (1 row) | (no commit) |
| 7 | auto (MCP read-only) | SELECT verified role=seeker, is_active=true | (no commit) |
| 8 | hybrid (operator regression UAT + executor docs ship) | Plan 05 closure | this commit |

**Final atomic commit:** `docs(20.1-05): close phase 20.1 — bootstrap UAT, role transfer, ship docs` (this commit)

## Files Created/Modified

- `tests/admin-bootstrap-UAT.md` — Phase 20.1 transfer run-record finalized (Steps 5–7 populated, Final Verdict, Anomaly Notes for CF-AUTH-2 caveat)
- `.planning/phases/20.1-standalone-admin-login-gateway-account-bootstrap/20.1-VERIFICATION.md` — phase verification with per-CF-ID empirical evidence + detailed disposition for CF-AUTH-2 caveat
- `.planning/phases/20.1-standalone-admin-login-gateway-account-bootstrap/20.1-SUMMARY.md` — phase-level closure summary
- `.planning/phases/20.1-standalone-admin-login-gateway-account-bootstrap/20.1-05-bootstrap-uat-and-ship-SUMMARY.md` — this file
- `.planning/phases/20-super-admin-dashboard/20-VERIFICATION.md` — Carryforward checklist updated: CF-AUTH-1, CF-AUTH-2 (with caveat note), CF-AUTH-3, CF-ACCOUNT-1, CF-ACCOUNT-2, CF-CODE-2 all flipped `[x]`
- `.planning/ROADMAP.md` — Phase 20.1 row flipped `[x]` with 2026-05-05 completion date; per-plan list 0/5 → 5/5; milestone summary entry added
- `.planning/v2.0-MILESTONE-AUDIT.md` — Phase 20.1 carryforward row marked CLOSED with link to 20.1-VERIFICATION.md and CF-AUTH-2 caveat note
- `.planning/STATE.md` — `progress.completed_plans` 23 → 24; `stopped_at` updated to "Phase 20.1 COMPLETE"; latest decision added

## Decisions Made

See `key-decisions` in frontmatter. Most consequential:

- **CF-AUTH-2 caveat (CLAUDE §7 partial-close discipline applied as PASS-with-caveat, NOT partial-close):** Empirical proof of the full requirement exists at three layers — unit test passes for the AccessDenied branch, helper test passes for `dashboardPathFor` outputs, grep diagnostic confirms no code path generates `/dashboard/admin` post-Plan-02. The browser-level anomaly is operator-attributed stale-cache, not a code regression. Documented as PASS with caveat in same VERIFICATION.md row + dedicated disposition section. Recommended fresh-session post-deploy re-verify is a no-blocker carryforward.

- **Atomic docs-trio commit (CLAUDE §4):** All 8 closure files ship in one commit. Splitting would produce a noisy multi-commit close for what's logically one atomic operation (the closure of Phase 20.1).

## Deviations from Plan

None — Plan 05 executed exactly as planned. The CF-AUTH-2 anomaly observation during Task 8a regression UAT is NOT a deviation (no scope change, no auto-fix attempted); it's a documented runtime observation that was diagnosed (CLAUDE §3) before being declared a non-blocker (CLAUDE §7). Per CLAUDE §3 — diagnose before fix — when diagnosis confirms no code-level cause, no code change is appropriate.

## Issues Encountered

**Stale-cache anomaly during regression UAT 8a:** Old account direct `/admin` navigation post-signin observed `/dashboard/admin` → 404 instead of inline AccessDenied UI. Diagnosis chain:

1. Grep `src/` for any code path producing `/dashboard/admin` — only helper body matches (`src/lib/routing.ts:4`).
2. Re-read `tests/admin-login.test.tsx` — non-admin AccessDenied branch covered, test passes.
3. Re-read `tests/dashboard-routing.test.ts` — all 3 helper outputs covered, tests pass.
4. Operator hypothesis: stale browser/JWT cache from prior admin session, or HMR chunk holding pre-Plan-02 ternary form, or bookmark/history-cached `/dashboard/admin` from the Phase 20-08 admin-redirect bug era.

Resolution: documented as caveat in VERIFICATION.md and UAT.md Anomaly Notes section. NO code change. Operator declared PASS with note: "AccessDenied state will work correctly on a fresh session once the old JWT expires."

## User Setup Required

None — `admin@topfarms.co.nz` mailbox/DNS provisioning explicitly DEFERRED per CONTEXT (auto-confirm sufficient; mailbox is a phase prerequisite operator handles separately, not a phase deliverable). Password stored in operator's password manager (NOT in repo).

## Next Phase Readiness

- Phase 20.1 sealed. ROADMAP.md flipped.
- Recommended fresh-session post-deploy re-verify of CF-AUTH-2 AccessDenied UI: NO-BLOCKER smoke test logged in v2.0-MILESTONE-AUDIT.md.
- Phase 21 Sign Out todo (closed by Plan 04, file already moved to `.planning/todos/done/` per Plan 04 closeout).

---

*Plan: 20.1-05-bootstrap-uat-and-ship*
*Completed: 2026-05-05*
