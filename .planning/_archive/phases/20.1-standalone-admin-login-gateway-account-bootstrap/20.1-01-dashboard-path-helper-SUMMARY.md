---
phase: 20.1-standalone-admin-login-gateway-account-bootstrap
plan: 01
subsystem: routing
tags: [tdd, vitest, typescript, helper-extraction, react-router]

# Dependency graph
requires:
  - phase: 20-super-admin-dashboard
    provides: "Mid-flight redirect-fix (commits 0e91ff2 + 6b769b4) introduced 5 inline ternary copies of role === 'admin' ? '/admin' : '/dashboard/${role}' — this plan extracts the helper they'll consume"
provides:
  - "src/lib/routing.ts — pure dashboardPathFor(role: UserRole): string helper"
  - "tests/dashboard-routing.test.ts — 3 unit tests asserting all 3 UserRole outcomes"
  - "TDD foundation enabling Plan 02 callsite swap with regression confidence"
affects:
  - "20.1-02-callsite-swap (Plan 02): unblocked — 5 inline ternaries can now consume dashboardPathFor"
  - "Future role-routing changes (1 file edit instead of 5)"

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "TDD-first helper extraction: write failing test, then minimal implementation, single atomic commit"
    - "Pure-function helpers in src/lib/ with type-only imports (zero runtime deps, no circular-import risk)"

key-files:
  created:
    - "src/lib/routing.ts"
    - "tests/dashboard-routing.test.ts"
  modified: []

key-decisions:
  - "Atomic commit lands BOTH test + implementation files (CLAUDE.md §4); RED state was empirically observed before writing helper but commits did not split — phase context explicitly authorized 'Same git commit for both files' since the RED check is conceptual"
  - "Type-only import (`import type { UserRole }`) keeps src/lib/routing.ts at zero runtime deps — eliminates circular-import risk that blocked alternative locations (src/contexts/AuthContext.tsx)"
  - "Helper file at src/lib/routing.ts (not src/types/domain.ts which mixes types/runtime, not src/lib/utils.ts which is single-responsibility cn() helper) — follows established src/lib/ convention"

patterns-established:
  - "TDD RED → GREEN with atomic commit: tests-first + implementation in one feat() commit; RED proven empirically by import-resolution failure before helper exists"
  - "Type-only imports for pure routing helpers: src/lib/routing.ts has zero runtime deps and cannot create cycles"

requirements-completed:
  - CF-CODE-1

# Metrics
duration: 1m 42s
completed: 2026-05-05
---

# Phase 20.1 Plan 01: Dashboard Path Helper Summary

**Pure-function `dashboardPathFor(role: UserRole): string` helper extracted in src/lib/routing.ts with 3 vitest unit tests covering all UserRole outcomes — foundation for Plan 02's 5-callsite refactor.**

## Performance

- **Duration:** 1m 42s
- **Started:** 2026-05-05T02:52:31Z
- **Completed:** 2026-05-05T02:54:13Z
- **Tasks:** 2 (TDD: RED + GREEN)
- **Files created:** 2
- **Files modified:** 0

## Accomplishments

- `src/lib/routing.ts` ships `dashboardPathFor(role: UserRole): string` — pure function, type-only `UserRole` import, single ternary body verbatim from RESEARCH §"Helper Implementation"
- `tests/dashboard-routing.test.ts` ships 3 unit tests covering admin → `/admin`, employer → `/dashboard/employer`, seeker → `/dashboard/seeker` — verbatim from RESEARCH §"Pattern 2"
- TDD RED state empirically observed (`Failed to resolve import "@/lib/routing"`) before helper written; RED → GREEN transition documented in commit body
- Atomic commit lands BOTH files together per CLAUDE.md §4
- Full vitest suite stays green: 27 test files passed, 169 tests passed (was 166 — +3 new), 0 failed
- `pnpm tsc --noEmit` clean (exit 0, no output)

## Task Commits

TDD execution shipped both files in one atomic commit per phase context ("Same git commit for both files. The 'RED' check is conceptual — tests must pass before commit lands"):

1. **Task 1+2: TDD RED → GREEN — dashboardPathFor helper + 3 unit tests** — `b987eb7` (feat)

**Plan metadata:** Pending — final docs commit covers SUMMARY.md + STATE.md + ROADMAP.md updates

## Files Created/Modified

- `src/lib/routing.ts` (5 lines) — exports `dashboardPathFor(role: UserRole): string`; type-only import; single ternary body
- `tests/dashboard-routing.test.ts` (14 lines) — 3 vitest unit tests asserting strict equality (`toBe`) for each UserRole outcome

## Decisions Made

- **Single atomic commit (CLAUDE.md §4):** Phase context explicitly authorized shipping test + implementation in one commit. RED state was observed empirically (`pnpm test -- dashboard-routing` failed with `Failed to resolve import "@/lib/routing"` before `src/lib/routing.ts` existed) — but the commit boundary is per-PLAN, not per-TDD-step.
- **Type-only import keeps file pure:** `import type { UserRole } from '@/types/domain'` means `src/lib/routing.ts` has zero runtime imports. Cannot participate in import cycles. Verified: `grep -cE "^import [^t]" src/lib/routing.ts` returns 0.
- **Verbatim helper body from RESEARCH:** Body is exactly `role === 'admin' ? '/admin' : \`/dashboard/${role}\`` — same expression all 5 callsites use today, so Plan 02's swap is behavior-preserving.

## Deviations from Plan

None — plan executed exactly as written. The RED → GREEN ordering was honored (test file written first, failure observed, then implementation file written), and the atomic-commit framing was authorized by phase context.

## Issues Encountered

None.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

**Plan 02 (callsite swap) is unblocked.** The 5 inline ternaries can now be replaced with `dashboardPathFor(role)`:

1. `src/pages/auth/Login.tsx:48`
2. `src/pages/auth/VerifyEmail.tsx:35`
3. `src/components/layout/ProtectedRoute.tsx:63`
4. `src/pages/auth/SelectRole.tsx:30`
5. `src/components/layout/Nav.tsx:112`

Existing test suites (`admin-protected-route`, `oauth-buttons`, `select-role`) provide regression coverage for the refactor. The new `dashboard-routing.test.ts` provides single-source-of-truth coverage for the helper itself.

**No blockers.** No carryforwards. Task 1 of Phase 20.1 closes cleanly.

## Self-Check

- [x] FOUND: src/lib/routing.ts (5 lines, exports dashboardPathFor)
- [x] FOUND: tests/dashboard-routing.test.ts (14 lines, 3 it() blocks)
- [x] FOUND: commit b987eb7 (`git log --oneline | grep b987eb7` → match)
- [x] VERIFIED: `pnpm test -- dashboard-routing` exits 0, all 3 tests pass
- [x] VERIFIED: `pnpm tsc --noEmit` exits 0
- [x] VERIFIED: `git diff --name-only HEAD~1 HEAD` lists both src/lib/routing.ts AND tests/dashboard-routing.test.ts (atomic commit)

## Self-Check: PASSED

All claims verified against disk + git.

---

*Phase: 20.1-standalone-admin-login-gateway-account-bootstrap*
*Plan: 01-dashboard-path-helper*
*Completed: 2026-05-05*
