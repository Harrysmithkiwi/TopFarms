---
phase: 20.1-standalone-admin-login-gateway-account-bootstrap
plan: 02
type: execute
wave: 2
depends_on:
  - "20.1-01"
files_modified:
  - src/pages/auth/Login.tsx
  - src/pages/auth/VerifyEmail.tsx
  - src/components/layout/ProtectedRoute.tsx
  - src/pages/auth/SelectRole.tsx
  - src/components/layout/Nav.tsx
autonomous: true
requirements:
  - CF-CODE-1
must_haves:
  truths:
    - "Zero inline `role === 'admin' ? '/admin' :` ternaries remain in src/"
    - "All 5 callsites import dashboardPathFor from '@/lib/routing'"
    - "Existing test suites (admin-protected-route, oauth-buttons, select-role, etc.) remain green — refactor is behavior-preserving"
    - "TypeScript compiles clean (`pnpm tsc --noEmit` exit 0)"
  artifacts:
    - path: "src/pages/auth/Login.tsx"
      provides: "Login post-signin redirect via dashboardPathFor"
      contains: "dashboardPathFor(role"
    - path: "src/pages/auth/VerifyEmail.tsx"
      provides: "Verify-email post-confirm redirect via dashboardPathFor"
      contains: "dashboardPathFor(role"
    - path: "src/components/layout/ProtectedRoute.tsx"
      provides: "Wrong-role redirect via dashboardPathFor"
      contains: "dashboardPathFor(role"
    - path: "src/pages/auth/SelectRole.tsx"
      provides: "Already-roled user redirect via dashboardPathFor"
      contains: "dashboardPathFor(role"
    - path: "src/components/layout/Nav.tsx"
      provides: "Dashboard NavLink target via dashboardPathFor"
      contains: "dashboardPathFor(role"
  key_links:
    - from: "src/pages/auth/Login.tsx"
      to: "src/lib/routing.ts"
      via: "import { dashboardPathFor }"
      pattern: "from '@/lib/routing'"
    - from: "src/components/layout/ProtectedRoute.tsx"
      to: "src/lib/routing.ts"
      via: "import { dashboardPathFor }"
      pattern: "from '@/lib/routing'"
    - from: "src/components/layout/Nav.tsx"
      to: "src/lib/routing.ts"
      via: "import { dashboardPathFor }"
      pattern: "from '@/lib/routing'"
---

<objective>
Replace 5 inline `role === 'admin' ? '/admin' : `/dashboard/${role}`` ternaries with calls to `dashboardPathFor(role)` from `src/lib/routing.ts` (created in Plan 01).

Purpose: Single source of truth for role-based dashboard routing. Eliminates the drift risk of 5 duplicate ternaries (Phase 20-08 mid-flight pattern). Closes CF-CODE-1 carryforward from Phase 20.

Output:
- 5 files updated with `dashboardPathFor` import + call
- Existing test suites continue to PASS (regression coverage already in place)
- One atomic commit (per CLAUDE §4)
</objective>

<execution_context>
@/Users/harrysmith/.claude/get-shit-done/workflows/execute-plan.md
@/Users/harrysmith/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/20.1-standalone-admin-login-gateway-account-bootstrap/20.1-CONTEXT.md
@.planning/phases/20.1-standalone-admin-login-gateway-account-bootstrap/20.1-RESEARCH.md
@.planning/phases/20.1-standalone-admin-login-gateway-account-bootstrap/20.1-01-SUMMARY.md

<interfaces>
<!-- Helper API contract from Plan 01: -->
```typescript
// src/lib/routing.ts
import type { UserRole } from '@/types/domain'
export function dashboardPathFor(role: UserRole): string
```

<!-- The 5-callsite verification table (from RESEARCH §"Pitfall 4"): -->

| # | File | Approx Line | Existing | Replace with |
|---|------|------|---------------|-------------|
| 1 | src/pages/auth/Login.tsx | 48 | `const dest = role === 'admin' ? '/admin' : \`/dashboard/${role}\`` | `const dest = dashboardPathFor(role)` |
| 2 | src/pages/auth/VerifyEmail.tsx | 35 | `const dest = role === 'admin' ? '/admin' : \`/dashboard/${role}\`` (role is `(roleData?.role as UserRole) ?? 'seeker'`) | `const dest = dashboardPathFor(role)` |
| 3 | src/components/layout/ProtectedRoute.tsx | 63 | `const dest = role === 'admin' ? '/admin' : \`/dashboard/${role}\`` | `const dest = dashboardPathFor(role)` |
| 4 | src/pages/auth/SelectRole.tsx | 30 | `const dest = role === 'admin' ? '/admin' : \`/dashboard/${role}\`` (inside `if (role)` guard) | `const dest = dashboardPathFor(role)` |
| 5 | src/components/layout/Nav.tsx | 112 | `to={role === 'admin' ? '/admin' : \`/dashboard/${role}\`}` (inline JSX, role null-checked at line 110) | `to={dashboardPathFor(role)}` |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor all 5 callsites to use dashboardPathFor</name>
  <files>src/pages/auth/Login.tsx, src/pages/auth/VerifyEmail.tsx, src/components/layout/ProtectedRoute.tsx, src/pages/auth/SelectRole.tsx, src/components/layout/Nav.tsx</files>
  <read_first>
    - src/lib/routing.ts (helper from Plan 01 — confirm exact export signature)
    - src/pages/auth/Login.tsx (the file being modified — read full file before editing; line 48 ternary lives in the post-signIn redirect block)
    - src/pages/auth/VerifyEmail.tsx (line 35 ternary; note `role` here is `(roleData?.role as UserRole) ?? 'seeker'` — locally cast, NOT from useAuth())
    - src/components/layout/ProtectedRoute.tsx (line 63 ternary; inside `if (requiredRole && role !== requiredRole)` block)
    - src/pages/auth/SelectRole.tsx (line 30 ternary; inside `if (role)` guard, so role is non-null at the call site)
    - src/components/layout/Nav.tsx (line 112 ternary; inline JSX `to={...}`; role is null-checked at line 110 via `{role && ...}`)
    - tests/admin-protected-route.test.tsx (existing regression coverage — must remain green)
    - .planning/phases/20.1-standalone-admin-login-gateway-account-bootstrap/20.1-RESEARCH.md (§"Pitfall 4: 5 callsite refactor — exact line numbers and ternary forms")
  </read_first>
  <action>
For EACH of the 5 files, perform exactly these two edits:

1. **Add import** at the top of the file (alongside other `@/`-prefixed imports — match existing import grouping style):

```ts
import { dashboardPathFor } from '@/lib/routing'
```

2. **Replace the inline ternary** with a call to `dashboardPathFor(role)`. Specific replacements per file:

**File 1 — `src/pages/auth/Login.tsx` (~line 48):**
- BEFORE: `const dest = role === 'admin' ? '/admin' : ` `` `/dashboard/${role}` `` ``
- AFTER: `const dest = dashboardPathFor(role)`

**File 2 — `src/pages/auth/VerifyEmail.tsx` (~line 35):**
- BEFORE: `const dest = role === 'admin' ? '/admin' : ` `` `/dashboard/${role}` `` ``
- AFTER: `const dest = dashboardPathFor(role)`
- Note: the local `role` variable here is `(roleData?.role as UserRole) ?? 'seeker'` from the surrounding code — already typed as `UserRole`, so the helper accepts it directly. Do NOT change the surrounding role-resolution logic.

**File 3 — `src/components/layout/ProtectedRoute.tsx` (~line 63):**
- BEFORE: `const dest = role === 'admin' ? '/admin' : ` `` `/dashboard/${role}` `` ``
- AFTER: `const dest = dashboardPathFor(role)`
- Note: at this point in the file, the code has already passed the `if (!role)` guard at line 58, so `role` is `UserRole` (non-null) — type-narrowed by TypeScript.

**File 4 — `src/pages/auth/SelectRole.tsx` (~line 30):**
- BEFORE: `const dest = role === 'admin' ? '/admin' : ` `` `/dashboard/${role}` `` ``
- AFTER: `const dest = dashboardPathFor(role)`
- Note: inside `if (role) { ... }` guard so role is non-null.

**File 5 — `src/components/layout/Nav.tsx` (~line 112):**
- BEFORE (inline JSX): `to={role === 'admin' ? '/admin' : ` `` `/dashboard/${role}` `` `}`
- AFTER: `to={dashboardPathFor(role)}`
- Note: at line 110, `role` is null-checked via `{role && (...)}`, so non-null at the JSX site.

**DO NOT** modify any other line in these files. **DO NOT** restructure imports (just add the new one in the existing `@/`-prefix block). **DO NOT** rename the local `dest` variable.

**Before committing, run regression suite:**
```bash
pnpm test          # expect: 166+ passed | 0 failed
pnpm tsc --noEmit  # expect: exit 0
```

If ANY existing test fails, the refactor introduced a regression. Diagnose before fixing (per CLAUDE §3) — likely cause is a missed import, typo in helper call, or wrong file edit. Do NOT amend the helper to compensate.

Commit atomically per CLAUDE §4:
```bash
node "/Users/harrysmith/.claude/get-shit-done/bin/gsd-tools.cjs" commit "refactor(20.1-02): swap 5 inline role-redirect ternaries for dashboardPathFor" --files src/pages/auth/Login.tsx src/pages/auth/VerifyEmail.tsx src/components/layout/ProtectedRoute.tsx src/pages/auth/SelectRole.tsx src/components/layout/Nav.tsx
```
  </action>
  <verify>
    <automated>pnpm test 2>&1 | tail -5 && pnpm tsc --noEmit 2>&1 | tail -3 && grep -rn "role === 'admin' ? '/admin'" src/ | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - All 5 files import dashboardPathFor: `grep -l "from '@/lib/routing'" src/pages/auth/Login.tsx src/pages/auth/VerifyEmail.tsx src/components/layout/ProtectedRoute.tsx src/pages/auth/SelectRole.tsx src/components/layout/Nav.tsx | wc -l` returns `5`
    - Zero inline ternaries remain: `grep -rn "role === 'admin' ? '/admin'" src/ | wc -l` returns `0`
    - dashboardPathFor is called in all 5 files: `grep -l "dashboardPathFor(role)" src/pages/auth/Login.tsx src/pages/auth/VerifyEmail.tsx src/components/layout/ProtectedRoute.tsx src/pages/auth/SelectRole.tsx src/components/layout/Nav.tsx | wc -l` returns `5`
    - Full vitest suite passes: `pnpm test` exits 0 (and reports at least the Phase 20 baseline of 166 passed | 113 todo | 0 failed; new dashboard-routing tests add 3 → 169 passed)
    - TypeScript clean: `pnpm tsc --noEmit` exits 0
    - admin-protected-route regression test still green: `pnpm test -- admin-protected-route` exits 0
    - Single atomic commit lists exactly these 5 files: `git diff --name-only HEAD~1 HEAD | sort` matches the 5-file list above
  </acceptance_criteria>
  <done>All 5 callsites refactored, full vitest suite green, TypeScript clean, single atomic commit shipped, CF-CODE-1 satisfied.</done>
</task>

</tasks>

<verification>
Final pre-merge gate:

```bash
# 1. Zero inline ternaries remain
grep -rn "role === 'admin' ? '/admin'" src/
# Expected: zero output

# 2. Full suite passes
pnpm test
# Expected: 169+ passed | 0 failed (166 from Phase 20 + 3 new dashboard-routing)

# 3. TypeScript clean
pnpm tsc --noEmit
# Expected: exit 0
```
</verification>

<success_criteria>
1. All 5 files import `dashboardPathFor` from `@/lib/routing`
2. All 5 callsites replaced with `dashboardPathFor(role)`
3. Zero `role === 'admin' ? '/admin' :` ternaries remain in `src/`
4. Full vitest suite green (no regressions in admin-protected-route, oauth-buttons, select-role, etc.)
5. TypeScript clean
6. Single atomic commit per CLAUDE §4
7. CF-CODE-1 carryforward from Phase 20 closed
</success_criteria>

<output>
After completion, create `.planning/phases/20.1-standalone-admin-login-gateway-account-bootstrap/20.1-02-SUMMARY.md` with:
- 5 files refactored (paths + diff line counts)
- Regression check output (full vitest suite green)
- TypeScript check output (clean)
- Commit SHA
- CF-CODE-1 closure note (carryforward from Phase 20-VERIFICATION.md "Code quality" section)
</output>
