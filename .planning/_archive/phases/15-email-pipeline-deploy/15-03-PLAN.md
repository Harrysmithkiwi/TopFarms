---
phase: 15-email-pipeline-deploy
plan: "03"
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - .github/workflows/supabase-deploy.yml
  - supabase/config.toml
  - .planning/phases/15-email-pipeline-deploy/15-03-SUMMARY.md
autonomous: false
requirements:
  - DEPLOY-01
must_haves:
  truths:
    - "User has explicitly approved the four CI gated sub-decisions (trigger, deploy scope, permissions, failure mode) BEFORE any workflow YAML is committed to git"
    - "`.github/workflows/supabase-deploy.yml` exists and triggers on `push` to `main` with path filter on `supabase/migrations/**` and `supabase/functions/**`"
    - "Workflow has a migration-apply step (block on failure) and a function-deploy step (notify-only on failure) — independent failure modes"
    - "GitHub Actions repo secrets `SUPABASE_ACCESS_TOKEN` and `SUPABASE_DB_PASSWORD` are seeded BEFORE the workflow first runs"
    - "Optional: `supabase/config.toml` exists with explicit `[functions.<name>]` `verify_jwt = true` blocks for the 4 disk-only functions (per Claude's discretion in CONTEXT.md)"
  artifacts:
    - path: ".github/workflows/supabase-deploy.yml"
      provides: "GitHub Actions deploy workflow — DEPLOY-01 closure"
      contains: "supabase/setup-cli, supabase functions deploy --use-api, supabase db push"
      min_lines: 40
    - path: "supabase/config.toml"
      provides: "Per-function deploy-time settings; pins verify_jwt against future CLI default drift"
      contains: "[functions.notify-job-filled]"
    - path: ".planning/phases/15-email-pipeline-deploy/15-03-SUMMARY.md"
      provides: "Approved-decision record (4 gated sub-decisions); first-run outcome (workflow_dispatch trigger); evidence of secrets seeded"
  key_links:
    - from: "push to main touching supabase/migrations/** OR supabase/functions/**"
      to: "GitHub Actions runs supabase-deploy.yml"
      via: "GH Actions path filter + concurrency group"
      pattern: "name: Supabase Deploy"
    - from: "supabase-deploy.yml migration step"
      to: "supabase db push --linked --dry-run + apply"
      via: "SUPABASE_ACCESS_TOKEN + SUPABASE_DB_PASSWORD"
      pattern: "supabase db push --linked"
    - from: "supabase-deploy.yml functions step"
      to: "supabase functions deploy --use-api"
      via: "SUPABASE_ACCESS_TOKEN, continue-on-error: true (notify-only failure mode)"
      pattern: "supabase functions deploy --use-api --project-ref"
---

<objective>
Add a GitHub Actions workflow (`.github/workflows/supabase-deploy.yml`) that automates `supabase db push` (migrations) + `supabase functions deploy` (Edge Functions) on every push to `main` touching `supabase/migrations/**` or `supabase/functions/**`. This closes DEPLOY-01 — the cross-cutting CI gap that allowed 4 functions on disk + 0 deployed for the duration of v1.0 + most of v2.0.

Purpose: Per CONTEXT.md "Manual deploy first, CI second" — Plan 15-01 ships the manual deploy that stops the prod silent-failure now; Plan 15-03 prevents the drift recurring. Per the orchestrator directive, this plan MUST pause for explicit user confirmation of the 4 gated sub-decisions BEFORE the YAML is committed.

Output: workflow YAML + optional `supabase/config.toml` + summary recording the user-approved decision matrix and first-run trigger outcome.
</objective>

<execution_context>
@/Users/harrysmith/.claude/get-shit-done/workflows/execute-plan.md
@/Users/harrysmith/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-email-pipeline-deploy/15-CONTEXT.md
@.planning/phases/15-email-pipeline-deploy/15-RESEARCH.md
@.planning/phases/15-email-pipeline-deploy/15-VALIDATION.md
@.planning/phases/15-email-pipeline-deploy/15-01-SUMMARY.md
@CLAUDE.md
@supabase/migrations/NAMING.md

<interfaces>
<!-- Canonical workflow YAML pattern from RESEARCH.md §Pattern 1, parameterised on the 4 user-confirmed decisions. -->

CLI flags (verified RESEARCH.md §Standard Stack):
- `supabase functions deploy --use-api --project-ref <ref>` — REQUIRED for CI (no Docker, fixes parallel race)
- `supabase db push --linked --dry-run` — preflight; non-destructive
- `supabase db push --linked` — applies migrations using SUPABASE_DB_PASSWORD env

Required GH Actions secrets (seeded as part of Task 4):
- `SUPABASE_ACCESS_TOKEN` — value = the `topfarms-ci-deploy` PAT created in plan 15-01 Task 2
- `SUPABASE_DB_PASSWORD` — value = TopFarms Supabase project DB password (find in Supabase dashboard → Project Settings → Database)

Service role key explicitly NOT in CI (per CONTEXT.md):
- `SUPABASE_SERVICE_ROLE_KEY` belongs in Edge Function runtime env only; do not put it in GitHub Actions secrets

Project ref: hardcoded `inlagtgpynemhipnqvty` (per CONTEXT.md — already public via `.mcp.json`).

Recommended action versions (per RESEARCH.md):
- `actions/checkout@v4`
- `supabase/setup-cli@v1` with explicit `version: 2.95.4` pin (or v2 with explicit pin if preferred — Claude's discretion)
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm the 4 CI gated sub-decisions BEFORE workflow YAML is committed (MANDATORY pause)</name>
  <decision>
    Per CONTEXT.md ("This task is a gated sub-decision — pause within Phase 15 to confirm before implementation") and the orchestrator's planning directive, Plan 15-03 MUST pause for user approval of the four CI design decisions BEFORE writing `.github/workflows/supabase-deploy.yml` to disk. RESEARCH.md proposed defaults; the user must explicitly approve (or override) each one.
  </decision>
  <context>
    These four decisions shape the workflow file the executor will write in Task 2. Locking them in before the YAML exists prevents wasted churn and ensures the workflow matches the user's intended deploy semantics. Defaults below come from RESEARCH.md §User Constraints (Locked Decisions) — they are not arbitrary guesses, they are the recommendations after the discuss-phase and research-phase passes. The user can accept all four with a one-word reply or override any of them.

    The approved decision tuple becomes the source of truth for Task 2 and is recorded verbatim in 15-03-SUMMARY.md frontmatter.
  </context>
  <options>
    <option id="default">
      <name>Accept all four RESEARCH.md / CONTEXT.md defaults</name>
      <pros>
        - Decisions already validated by /gsd:discuss-phase (CONTEXT.md) and /gsd:research-phase (RESEARCH.md §Standard Stack + Pattern 1)
        - Matches Supabase official GitHub Actions example
        - Migration apply blocks (loud failure, which is the whole point — drift is the original problem this phase fixes)
        - Function deploy is notify-only (Supabase CLI rollback support is thin; auto-rollback is over-engineered)
      </pros>
      <cons>
        - Single environment (no staging); push-to-main is a prod release. Acceptable given solo-dev / single-environment project per CONTEXT.md
      </cons>
      <details>
        Default tuple:
          (a) Trigger: push to `main` with path filter on `supabase/migrations/**` OR `supabase/functions/**`. Plus `workflow_dispatch` for manual re-runs.
          (b) Deploy scope: BOTH migrations AND functions, in two independent jobs (migrations first; functions deploy after migrations succeeds).
          (c) Permissions: GitHub Actions repo secrets `SUPABASE_ACCESS_TOKEN` (= the `topfarms-ci-deploy` PAT from plan 15-01) + `SUPABASE_DB_PASSWORD`. Service role key NOT in CI.
          (d) Failure mode: BLOCK (workflow exits non-zero) on migration apply failure; NOTIFY-ONLY (`continue-on-error: true` on the functions job, GH summary annotation) on function-deploy failure. NO auto-rollback for either.
      </details>
    </option>
    <option id="override">
      <name>Override one or more defaults</name>
      <pros>
        - User retains full control over CI semantics
        - Can scope down (e.g. functions-only CI; manual db push) if migration risk feels too high to automate
      </pros>
      <cons>
        - Re-opens decisions already validated in /gsd:discuss-phase
        - Requires the user to specify alternatives explicitly
      </cons>
      <details>
        State which of (a) / (b) / (c) / (d) to change and the alternative. Examples:
          (a) "trigger: tag-driven only, not push-to-main"
          (b) "deploy scope: functions only, leave migrations manual"
          (c) "permissions: add OIDC token instead of PAT"
          (d) "failure mode: BLOCK on function-deploy failure too"
      </details>
    </option>
    <option id="defer">
      <name>Defer the workflow entirely — skip 15-03 + 15-04 stays unchanged</name>
      <pros>
        - Phase still ships the manual deploy fix (15-01) + empirical verification (15-02)
        - DEPLOY-01 stays open as a tracked deferral; can be addressed in a future phase
      </pros>
      <cons>
        - DEPLOY-01 stays unfixed — drift can recur the next time someone adds a function or migration without manual deploy discipline
        - Phase 15 success criteria #3 (CI step exists) marked DEFERRED rather than CLOSED
      </cons>
      <details>
        If chosen: skip Tasks 2-5 of this plan; mark plan 15-03 as DEFERRED in 15-03-SUMMARY.md with rationale; carry DEPLOY-01 forward as an explicit deferral. Plan 15-04 (13-VERIFICATION.md backfill) still runs.
      </details>
    </option>
  </options>
  <files>.planning/phases/15-email-pipeline-deploy/15-03-EVIDENCE/decision_record.txt</files>
  <read_first>
    - .planning/phases/15-email-pipeline-deploy/15-CONTEXT.md (§CI permissions, §CI trigger + scope, §CI failure mode — locked decisions)
    - .planning/phases/15-email-pipeline-deploy/15-RESEARCH.md (§User Constraints, §Pattern 1 — proposed defaults)
  </read_first>
  <action>
    PRESENT the <decision> + <context> + <options> blocks above to the user verbatim. The four CI sub-decisions cannot be auto-resolved — CONTEXT.md explicitly mandates a pause-for-confirmation before workflow YAML is committed.

    Wait for the resume-signal. Acceptable replies:
      - "default" — accept all four RESEARCH.md tuples
      - "override: (a) <X>; (b) <Y>; (c) <Z>; (d) <W>" — partial or full override of the default tuple
      - "defer" — skip Tasks 2-5 entirely; DEPLOY-01 remains open

    Once the user replies, write  (mkdir -p as needed) with the verbatim user reply + ISO timestamp. Example shape:
      

    Do NOT proceed to Task 2 until decision_record.txt exists on disk. Do NOT write  before this file exists — that is the load-bearing pause.

    If user replies "defer", STILL write decision_record.txt with reply=defer, then skip Tasks 2-4 and proceed directly to Task 5 (which records the deferral in 15-03-SUMMARY.md).
  </action>
  <verify>
    <automated>test -f .planning/phases/15-email-pipeline-deploy/15-03-EVIDENCE/decision_record.txt && grep -qE "User reply: (default|override|defer)" .planning/phases/15-email-pipeline-deploy/15-03-EVIDENCE/decision_record.txt</automated>
  </verify>
  <acceptance_criteria>
    - File decision_record.txt exists at .planning/phases/15-email-pipeline-deploy/15-03-EVIDENCE/
    - File contains literal string "User reply: " followed by one of: default, override, defer
    - File contains ISO8601 timestamp under "Decided at:"
    - If reply was "default" or "override", file lists the resolved tuple for (a) (b) (c) (d) explicitly
  </acceptance_criteria>
  <done>User has explicitly approved (or deferred) the four CI sub-decisions; decision recorded on disk; Task 2 can proceed using the resolved tuple.</done>
  <resume-signal>
    Reply with one of:
      - "default" (accept all four — proceed to Task 2 with the RESEARCH.md tuple)
      - "override: (a) <new value>; (b) <new value>; (c) <new value>; (d) <new value>" (change any subset)
      - "defer" (skip Tasks 2-5 and mark DEPLOY-01 deferred)

    The chosen tuple — including any overrides — must be quoted verbatim in 15-03-SUMMARY.md `decisions:` frontmatter.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Write `.github/workflows/supabase-deploy.yml` per the approved decision tuple</name>
  <files>.github/workflows/supabase-deploy.yml</files>
  <read_first>
    - .planning/phases/15-email-pipeline-deploy/15-RESEARCH.md (§Pattern 1 GH Actions YAML; §Pitfall 1 Studio-applied migration handling; §Pitfall 4 --use-api flag)
    - .planning/phases/15-email-pipeline-deploy/15-01-SUMMARY.md (§Migration Registry Quirk Decision — read which Option (A or B) was chosen; the workflow YAML must match)
    - supabase/migrations/NAMING.md (Studio-applied migration registry quirk)
    - CLAUDE.md (§4 atomic commits — this task contributes to a single atomic commit for plan 15-03)
  </read_first>
  <action>
    PRECONDITION: Task 1 must have produced an approved decision tuple. If user replied "defer", skip this task entirely and jump to Task 5 (write SUMMARY recording the deferral). Otherwise:

    Create directory `.github/workflows/` and write `supabase-deploy.yml` with the canonical structure from RESEARCH.md §Pattern 1, parameterised on the user's tuple.

    Default-tuple YAML (use verbatim if user replied "default"):

    ```yaml
    # Source: adapted from https://supabase.com/docs/guides/functions/examples/github-actions
    # TopFarms Phase 15 plan 15-03 — DEPLOY-01 closure.
    # Migration registry quirk handling: see plan 15-01 SUMMARY §Migration Registry Quirk Decision.

    name: Supabase Deploy

    on:
      push:
        branches: [main]
        paths:
          - 'supabase/migrations/**'
          - 'supabase/functions/**'
      workflow_dispatch:

    concurrency:
      group: supabase-deploy
      cancel-in-progress: false

    env:
      SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
      SUPABASE_DB_PASSWORD: ${{ secrets.SUPABASE_DB_PASSWORD }}
      PROJECT_ID: inlagtgpynemhipnqvty

    jobs:
      migrations:
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v4
          - uses: supabase/setup-cli@v1
            with:
              version: 2.95.4

          - name: Link to Supabase project
            run: supabase link --project-ref $PROJECT_ID

          - name: Dry-run migration preview
            # Studio-applied migrations 016 + 017 will appear as "pending" here — see supabase/migrations/NAMING.md.
            # Plan 15-01 §Migration Registry Quirk Decision documents whether registry was repaired (Option A) or
            # idempotency is being trusted (Option B). Either way, dry-run is non-destructive.
            run: supabase db push --linked --dry-run

          - name: Apply migrations
            run: supabase db push --linked
            # SUPABASE_DB_PASSWORD env satisfies the password prompt non-interactively

      functions:
        runs-on: ubuntu-latest
        needs: migrations
        continue-on-error: true   # Per CONTEXT.md: notify-only on function-deploy failure (no auto-rollback)
        steps:
          - uses: actions/checkout@v4
          - uses: supabase/setup-cli@v1
            with:
              version: 2.95.4

          - name: Deploy all functions
            # `supabase functions deploy` (no name) deploys all functions in supabase/functions/ since CLI v1.62.0.
            # `--use-api` is REQUIRED in CI: server-side bundling, no Docker, fixes parallel-deploy race.
            run: supabase functions deploy --use-api --project-ref $PROJECT_ID

          - name: Surface deploy outcome
            if: always()
            run: |
              echo "::notice::Function deploy finished with status: ${{ job.status }}"
    ```

    If the user overrode any of (a) / (b) / (c) / (d) in Task 1: adapt the YAML accordingly. Examples:
      - Override (a) "tag-driven only" → replace the `on:` block with `on: { push: { tags: ['v*'] }, workflow_dispatch: {} }`
      - Override (b) "functions only" → delete the `migrations` job and remove `needs: migrations` from the functions job
      - Override (d) "BLOCK on function-deploy failure" → remove `continue-on-error: true` from the `functions` job

    Do NOT add a `--no-verify-jwt` flag (anti-pattern per RESEARCH.md §Anti-Patterns). Per-function JWT settings live in `supabase/config.toml` (Task 3).

    Validate the YAML parses by running:
      python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/supabase-deploy.yml'))" && echo "YAML OK"
  </action>
  <verify>
    <automated>test -f .github/workflows/supabase-deploy.yml && grep -q "supabase functions deploy --use-api" .github/workflows/supabase-deploy.yml && grep -q "supabase db push --linked" .github/workflows/supabase-deploy.yml && grep -q "supabase/setup-cli" .github/workflows/supabase-deploy.yml && grep -q "PROJECT_ID: inlagtgpynemhipnqvty" .github/workflows/supabase-deploy.yml</automated>
  </verify>
  <acceptance_criteria>
    - File `.github/workflows/supabase-deploy.yml` exists
    - Contains literal string `supabase functions deploy --use-api`
    - Contains literal string `supabase db push --linked` (the apply step)
    - Contains literal string `supabase/setup-cli` (the canonical action)
    - Contains literal string `PROJECT_ID: inlagtgpynemhipnqvty` (project ref hardcoded per CONTEXT.md)
    - YAML parses without error (`python3 -c "import yaml; yaml.safe_load(open('.github/workflows/supabase-deploy.yml'))"`)
    - File does NOT contain `--no-verify-jwt` (CLI flag anti-pattern per RESEARCH.md §Anti-Patterns)
    - File does NOT contain `SUPABASE_SERVICE_ROLE_KEY` (per CONTEXT.md exclusion)
  </acceptance_criteria>
  <done>Workflow YAML on disk, matches the user-approved decision tuple, parses cleanly.</done>
</task>

<task type="auto">
  <name>Task 3: Write minimal `supabase/config.toml` pinning verify_jwt for the 4 disk-only functions (recommended)</name>
  <files>supabase/config.toml</files>
  <read_first>
    - .planning/phases/15-email-pipeline-deploy/15-RESEARCH.md (§Pattern 2 supabase/config.toml; §Open Questions #4)
    - supabase/functions/notify-job-filled/index.ts
    - supabase/functions/send-followup-emails/index.ts
    - supabase/functions/acknowledge-placement-fee/index.ts
    - supabase/functions/create-placement-invoice/index.ts
  </read_first>
  <action>
    Per RESEARCH.md §Open Questions #4 + CONTEXT.md (Claude's discretion): "add a minimal config.toml with one block per disk-only function explicitly setting `verify_jwt = true`. It's 12 lines and prevents future drift if a Supabase CLI default ever flips."

    Confirm `supabase/config.toml` does NOT already exist. If it exists, read its current contents and APPEND new `[functions.<name>]` blocks (do not overwrite — file may have legitimate other settings).

    If the file does not exist, create it with the following content verbatim:

    ```toml
    # TopFarms Supabase project config
    # Created in Phase 15 plan 15-03 to pin per-function deploy-time settings explicitly.
    # See https://supabase.com/docs/guides/functions/function-configuration

    [functions.notify-job-filled]
    # Called by pg_net trigger with service_role JWT — gateway-trust + local atob decode pattern (CLAUDE.md §5).
    # In Phase 15-01 BFIX-05 audit, this function was confirmed NOT to use the auth.getUser(token) antipattern.
    verify_jwt = true

    [functions.send-followup-emails]
    # Cron-driven; called with service_role JWT.
    verify_jwt = true

    [functions.acknowledge-placement-fee]
    # Called from authenticated employer client (src/pages/dashboard/employer/ApplicantDashboard.tsx:361).
    verify_jwt = true

    [functions.create-placement-invoice]
    # Called from authenticated employer client (src/pages/dashboard/employer/ApplicantDashboard.tsx:423).
    verify_jwt = true
    ```

    If the user explicitly opted out of `supabase/config.toml` during Task 1 (e.g. override included "skip config.toml"), record that decision in 15-03-SUMMARY.md and skip this task. Otherwise proceed.

    Note: this file is COMMITTED to the repo (not gitignored). It's source-of-truth for per-function settings. The CLI reads it during `functions deploy` to apply the settings.
  </action>
  <verify>
    <automated>test -f supabase/config.toml && grep -q "\[functions.notify-job-filled\]" supabase/config.toml && grep -q "\[functions.send-followup-emails\]" supabase/config.toml && grep -q "\[functions.acknowledge-placement-fee\]" supabase/config.toml && grep -q "\[functions.create-placement-invoice\]" supabase/config.toml</automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/config.toml` exists
    - Contains `[functions.notify-job-filled]` block with `verify_jwt = true`
    - Contains `[functions.send-followup-emails]` block with `verify_jwt = true`
    - Contains `[functions.acknowledge-placement-fee]` block with `verify_jwt = true`
    - Contains `[functions.create-placement-invoice]` block with `verify_jwt = true`
    - TOML parses without error (e.g. `python3 -c "import tomllib; tomllib.load(open('supabase/config.toml','rb'))"` on Python 3.11+; or visual inspection if tomllib unavailable)
  </acceptance_criteria>
  <done>config.toml committed with explicit per-function verify_jwt blocks; defends against future CLI default drift.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Seed GitHub Actions repo secrets BEFORE first workflow run (human-required)</name>
  <files>.planning/phases/15-email-pipeline-deploy/15-03-EVIDENCE/secrets_seeded.txt</files>
  <read_first>
    - .planning/phases/15-email-pipeline-deploy/15-01-SUMMARY.md (§PAT Rotation Tracking — the PAT name + creation date is the value to store)
    - .planning/phases/15-email-pipeline-deploy/15-CONTEXT.md (§CI permissions — service role key explicitly excluded)
  </read_first>
  <what-built>
    Tasks 2 + 3 wrote `.github/workflows/supabase-deploy.yml` and `supabase/config.toml`. The workflow references `${{ secrets.SUPABASE_ACCESS_TOKEN }}` and `${{ secrets.SUPABASE_DB_PASSWORD }}`. Without those repo secrets, the first workflow run will fail with "missing secret" errors.

    Seeding GitHub Actions repo secrets requires either (a) the GitHub UI (`Settings → Secrets and variables → Actions → New repository secret`) or (b) the `gh` CLI (`gh secret set NAME --body VALUE`). Both require interactive auth — Claude can attempt the `gh secret set` path autonomously if `gh auth status` shows authenticated, but verification of the actual stored value requires human confirmation (GH masks `secret value` and Claude cannot retrieve it back).
  </what-built>
  <how-to-verify>
    Step 1 — seed `SUPABASE_ACCESS_TOKEN`:
      Take the PAT value from your local `supabase login` keychain (or re-paste the value you originally copied when creating `topfarms-ci-deploy` in plan 15-01 Task 2). Run from repo root:
        ```
        gh secret set SUPABASE_ACCESS_TOKEN --body '<paste-PAT-here>'
        ```
      Or use the GitHub UI: repo → Settings → Secrets and variables → Actions → New repository secret → name `SUPABASE_ACCESS_TOKEN`, value = the PAT.

    Step 2 — seed `SUPABASE_DB_PASSWORD`:
      The DB password is in Supabase dashboard → Project `inlagtgpynemhipnqvty` → Project Settings → Database → "Database password" (you may have to reset it if you don't have it; doing so does not invalidate the existing service-role JWTs because they're separate). Run:
        ```
        gh secret set SUPABASE_DB_PASSWORD --body '<paste-db-password-here>'
        ```

    Step 3 — verify both secrets exist (cannot inspect values, only existence):
      ```
      gh secret list
      ```
      Confirm both `SUPABASE_ACCESS_TOKEN` and `SUPABASE_DB_PASSWORD` appear.

    Step 4 — write evidence file. Create `.planning/phases/15-email-pipeline-deploy/15-03-EVIDENCE/secrets_seeded.txt` with content:
      ```
      Secrets seeded: <ISO timestamp>
      Verified via: gh secret list
      Names present: SUPABASE_ACCESS_TOKEN, SUPABASE_DB_PASSWORD
      Service-role key in CI: NO (per CONTEXT.md exclusion)
      ```

    Step 5 — confirm to Claude that secrets are seeded. Reply "secrets seeded" with the contents of `gh secret list` (or paste verbatim).
  </how-to-verify>
  <action>
    PRECONDITION: Tasks 2 + 3 wrote .github/workflows/supabase-deploy.yml and supabase/config.toml. Tasks 4 + 5 require these files to already be on disk so the user can verify the workflow exists before seeding secrets.

    PRESENT the <how-to-verify> instructions above to the user. The executor cannot seed GitHub Actions repo secrets autonomously because:
      (1) The PAT value lives in the developer's local supabase login keychain (or the original copy buffer from plan 15-01 Task 2) — Claude does not have access to the secret value
      (2) The DB password lives in the Supabase dashboard — Claude does not have access
      (3) gh secret set requires interactive auth + the actual secret value

    The executor MAY autonomously attempt the verification step (`gh secret list`) once the user reports back. The executor MUST autonomously write the evidence file once the user confirms.

    Concrete executor steps:
      (1) Pause and present Steps 1-5 from <how-to-verify>. Wait for user resume-signal.
      (2) On user reply ("secrets seeded" + gh secret list output), parse the user's pasted output to confirm both SUPABASE_ACCESS_TOKEN and SUPABASE_DB_PASSWORD names appear.
      (3) Optionally re-run `gh secret list` autonomously (if gh CLI is authenticated in the executor's shell) to cross-check.
      (4) Write `.planning/phases/15-email-pipeline-deploy/15-03-EVIDENCE/secrets_seeded.txt` with the shape from Step 4 above.

    If user reports a blocker (e.g. "gh CLI not authenticated"), surface a remediation ("run `gh auth login`") and re-pause.
  </action>
  <verify>
    <automated>test -f .planning/phases/15-email-pipeline-deploy/15-03-EVIDENCE/secrets_seeded.txt && grep -q "SUPABASE_ACCESS_TOKEN" .planning/phases/15-email-pipeline-deploy/15-03-EVIDENCE/secrets_seeded.txt && grep -q "SUPABASE_DB_PASSWORD" .planning/phases/15-email-pipeline-deploy/15-03-EVIDENCE/secrets_seeded.txt</automated>
  </verify>
  <acceptance_criteria>
    - File secrets_seeded.txt exists at .planning/phases/15-email-pipeline-deploy/15-03-EVIDENCE/
    - File contains literal string "SUPABASE_ACCESS_TOKEN"
    - File contains literal string "SUPABASE_DB_PASSWORD"
    - File contains literal string "Service-role key in CI: NO" (CONTEXT.md exclusion confirmed in writing)
  </acceptance_criteria>
  <done>GitHub Actions repo secrets seeded; user-confirmed via resume-signal; evidence captured on disk for plan 15-03 Task 5 to consume.</done>
  <resume-signal>Type "secrets seeded" with the `gh secret list` output, OR describe blockers (e.g. "gh CLI not authenticated; user must authenticate first")</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Trigger first workflow run via workflow_dispatch and capture outcome</name>
  <files>.planning/phases/15-email-pipeline-deploy/15-03-EVIDENCE/first_run.json, .planning/phases/15-email-pipeline-deploy/15-03-SUMMARY.md</files>
  <read_first>
    - .planning/phases/15-email-pipeline-deploy/15-03-EVIDENCE/secrets_seeded.txt
    - .github/workflows/supabase-deploy.yml
    - .planning/phases/13-email-notifications/13-01-SUMMARY.md (SUMMARY shape)
    - .planning/phases/15-email-pipeline-deploy/15-01-SUMMARY.md (atomic commit + frontmatter conventions)
  </read_first>
  <action>
    PRECONDITION: Task 4 produced `secrets_seeded.txt` confirming both `SUPABASE_ACCESS_TOKEN` and `SUPABASE_DB_PASSWORD` exist as repo secrets.

    Step 1 — commit + push the workflow file + config.toml. Per CLAUDE.md §4 (atomic commits, one phase/sub-phase per commit), single commit body:
      ```
      git add .github/workflows/supabase-deploy.yml supabase/config.toml
      git commit -m "feat(15-03): supabase-deploy workflow + per-function config.toml

      DEPLOY-01 closure. Workflow triggers on push to main with path filters on
      supabase/migrations/** and supabase/functions/**. Migration apply blocks on
      failure (loud); function deploy is notify-only (no auto-rollback per
      CONTEXT.md). Per-function verify_jwt pinned in supabase/config.toml.

      Decision tuple approved by user in plan 15-03 Task 1: <quote tuple here>"
      git push
      ```

    Step 2 — kick off the first run via `workflow_dispatch` (so we test BEFORE any real deploy-touching commit lands):
      ```
      gh workflow run supabase-deploy.yml --ref main
      ```

    Step 3 — wait for the run to finish. Poll via:
      ```
      gh run list --workflow=supabase-deploy.yml --limit 1 --json databaseId,status,conclusion,headBranch,createdAt > .planning/phases/15-email-pipeline-deploy/15-03-EVIDENCE/first_run.json
      ```
      Or follow live with `gh run watch <run_id>`. Final status should be `completed` with conclusion `success` (or `failure` on the functions job IF migrations passed and `continue-on-error: true` allowed it through — that's still acceptable per the notify-only failure mode).

    Step 4 — interpret the result:
      - `migrations` job conclusion = `success` → CORE PASS (DEPLOY-01 satisfied for migration side)
      - `migrations` job conclusion = `failure` → FAIL. Read the run log via `gh run view <id> --log-failed`. Most likely cause: Studio-applied migration 016/017 idempotency edge (RESEARCH.md §Pattern 3). Plan 15-01 §Migration Registry Quirk Decision documents the chosen handling (Option A vs B) — re-read it; if Option B was chosen and re-applying 016/017 actually fails (not just appears as pending), revisit and switch to Option A (Studio INSERT into supabase_migrations.schema_migrations).
      - `functions` job conclusion = `success` → CORE PASS (DEPLOY-01 satisfied for functions side)
      - `functions` job conclusion = `failure` BUT migrations passed → notify-only mode caught it as designed. Read the failure log; surface it to the user but DO NOT re-trigger automatically. Document in `15-03-SUMMARY.md` `## First Run Notes` for follow-up.

    Step 5 — write `15-03-SUMMARY.md`. Required frontmatter:
      phase: 15-email-pipeline-deploy
      plan: "03"
      subsystem: ci-cd
      tags: [github-actions, deploy-01, supabase-cli, ci]
      dependency_graph:
        requires: [supabase-cli-installed, topfarms-ci-deploy-pat-exists, db-password-known]
        provides: [supabase-deploy-workflow, per-function-config-toml]
        affects: [.github/workflows, supabase/config.toml]
      tech_stack:
        added: [github-actions, supabase-setup-cli-v1, supabase-config-toml]
        patterns: [push-to-main-path-filtered, sequential-migrations-then-functions, notify-only-on-function-failure]
      key_files:
        created:
          - .github/workflows/supabase-deploy.yml
          - supabase/config.toml
          - .planning/phases/15-email-pipeline-deploy/15-03-EVIDENCE/secrets_seeded.txt
          - .planning/phases/15-email-pipeline-deploy/15-03-EVIDENCE/first_run.json
        modified: []
      decisions:
        - "<quote the user-approved tuple from Task 1 verbatim — (a) trigger, (b) deploy scope, (c) permissions, (d) failure mode>"
        - "<quote any overrides applied to the default tuple>"
      requirements_completed: [DEPLOY-01]

      Body sections:
        1. "## Approved Decision Tuple" — verbatim user reply from Task 1, plus rationale
        2. "## First Run Outcome" — table: job_name | conclusion | log_summary
        3. "## Secrets Seeded" — paste contents of secrets_seeded.txt (no values, just names)
        4. "## Recovery Path" — `workflow_dispatch` re-run via `gh workflow run supabase-deploy.yml --ref main`
        5. "## Atomic Commit Note" — single commit per CLAUDE.md §4

    Run `gsd-tools frontmatter validate` against the new SUMMARY and ensure `valid: true`.
  </action>
  <verify>
    <automated>test -f .planning/phases/15-email-pipeline-deploy/15-03-EVIDENCE/first_run.json && test -f .planning/phases/15-email-pipeline-deploy/15-03-SUMMARY.md && grep -q "Approved Decision Tuple" .planning/phases/15-email-pipeline-deploy/15-03-SUMMARY.md && grep -q "First Run Outcome" .planning/phases/15-email-pipeline-deploy/15-03-SUMMARY.md && node /Users/harrysmith/.claude/get-shit-done/bin/gsd-tools.cjs frontmatter validate .planning/phases/15-email-pipeline-deploy/15-03-SUMMARY.md --schema summary | grep -q '"valid": true'</automated>
  </verify>
  <acceptance_criteria>
    - File `first_run.json` exists at the EVIDENCE path; contains a `status` field equal to `"completed"` (run actually finished, not still queued)
    - File `15-03-SUMMARY.md` exists; frontmatter validates against `summary` schema
    - Body contains literal heading `## Approved Decision Tuple`
    - Body contains literal heading `## First Run Outcome`
    - Body has the user-approved tuple (default OR specific overrides) quoted verbatim from Task 1
    - `requirements_completed` frontmatter list contains `DEPLOY-01`
  </acceptance_criteria>
  <done>Workflow committed + pushed + first-run-dispatched + outcome captured; DEPLOY-01 satisfied (or escalation documented if first-run failed).</done>
</task>

</tasks>

<verification>
- User explicitly approved (or overrode) the 4 gated CI sub-decisions in Task 1 BEFORE any YAML was written
- `.github/workflows/supabase-deploy.yml` exists with all expected literal strings; YAML parses cleanly
- `supabase/config.toml` exists with `[functions.<name>]` blocks for the 4 disk-only functions
- GitHub Actions repo secrets `SUPABASE_ACCESS_TOKEN` + `SUPABASE_DB_PASSWORD` are seeded (verified via `gh secret list`)
- First workflow run dispatched and outcome captured in `first_run.json` and 15-03-SUMMARY.md
- 15-03-SUMMARY.md frontmatter validates against the summary schema
</verification>

<success_criteria>
1. `.github/workflows/supabase-deploy.yml` triggers on push to `main` with path filter on `supabase/migrations/**` and `supabase/functions/**`
2. Workflow uses `supabase functions deploy --use-api --project-ref inlagtgpynemhipnqvty` (per RESEARCH.md §Pitfall 4)
3. Workflow includes a dry-run preflight (`supabase db push --linked --dry-run`) before the apply step
4. Migration job blocks on failure; functions job uses `continue-on-error: true` (per CONTEXT.md notify-only failure mode) — UNLESS user overrode in Task 1
5. `supabase/config.toml` exists with `[functions.<name>]` `verify_jwt = true` blocks for the 4 disk-only functions (UNLESS user explicitly opted out)
6. GitHub Actions repo secrets `SUPABASE_ACCESS_TOKEN` and `SUPABASE_DB_PASSWORD` exist (verified via `gh secret list`)
7. First workflow run via `workflow_dispatch` completed; outcome (success or notify-only function-failure) captured in 15-03-SUMMARY.md `## First Run Outcome`
8. 15-03-SUMMARY.md `decisions:` frontmatter quotes the user-approved tuple verbatim
</success_criteria>

<output>
After completion, create `.planning/phases/15-email-pipeline-deploy/15-03-SUMMARY.md` (Task 5).

If user replied "defer" in Task 1: the SUMMARY records the deferral with rationale; DEPLOY-01 stays in REQUIREMENTS.md as `[ ]` and is carried forward to a future phase. Plan 15-04 still runs (13-VERIFICATION.md backfill is independent of CI).

Plan 15-04 will reference this plan's `first_run.json` + `secrets_seeded.txt` as DEPLOY-01 evidence in 13-VERIFICATION.md.
</output>
