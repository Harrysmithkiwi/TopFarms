---
phase: 18.1-pre-launch-hardening
plan: "05"
subsystem: testing
tags: [vitest, static-source, regex, document-types, rls, privacy]

# Dependency graph
requires:
  - phase: 18.1-00
    provides: Wave 0 stub (it.todo() scaffold) for employer-visible-document-types-drift.test.ts
provides:
  - "Static-source drift guard asserting EMPLOYER_VISIBLE_DOCUMENT_TYPES is identical across TS canonical (domain.ts:200), Edge fn whitelist (get-applicant-document-url:43), and SQL RLS policy (020_seeker_documents_employer_policy.sql:35)"
  - "Privacy regression guard: 'identity' must not appear in canonical list (BFIX-02 / PRIV-02 defence)"
affects:
  - src/types/domain.ts
  - supabase/functions/get-applicant-document-url/index.ts
  - supabase/migrations/020_seeker_documents_employer_policy.sql

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Static-source regression guard via readFileSync + targeted regex (Phase 17-04 precedent extended to SQL layer)"

key-files:
  created: []
  modified:
    - tests/employer-visible-document-types-drift.test.ts

key-decisions:
  - "Single readFileSync + regex per source file: no AST parsing, no import() eval — pure-Node, no jsdom, runs in 8ms (<<50ms target)"
  - "Sorted-set equality (deduped + sorted) for order-independent comparison across all 3 layers"
  - "extractTsList regex /EMPLOYER_VISIBLE_DOCUMENT_TYPES[^=]*=\\s*\\[([^\\]]+)\\]/ covers BOTH TS layers (domain.ts export + Edge fn const) without per-file variants"
  - "if (!m) throw new Error(...) guard ensures LOUD failure when literal layout shifts (e.g., maintainer wraps across multiple lines)"
  - "Stretch cross-reference comments in 3 source files explicitly out of scope per PLAN.md — drift guard alone is sufficient for SC-1"

patterns-established:
  - "Static-source assertion extended to SQL layer: extractSqlList() regex /document_type\\s+IN\\s*\\(([^)]+)\\)/ anchored on IN keyword to avoid false-match on unrelated string literals"

requirements-completed: []

# Metrics
duration: 3min
completed: "2026-05-10"
---

# Phase 18.1 Plan 05: Doc-Types Drift Guard Summary

**Pure-Node vitest drift guard asserting EMPLOYER_VISIBLE_DOCUMENT_TYPES is byte-identical across TS canonical, Edge fn whitelist, and SQL RLS policy — 2 tests GREEN in 8ms, closes SC-1 at code side**

## Performance

- **Duration:** ~3 min
- **Started:** 2026-05-10T16:18:52Z
- **Completed:** 2026-05-10T16:21:34Z
- **Tasks:** 1 of 1
- **Files modified:** 1

## Accomplishments

- Replaced 2 `it.todo()` stubs with full assertion bodies in `tests/employer-visible-document-types-drift.test.ts`
- `extractTsList` + `extractSqlList` functions extract list members from each source file via regex; both functions throw `Error` loudly if literal layout shifts
- Test 1: sorted-set equality across TS canonical (`domain.ts:200`) + Edge fn whitelist (`get-applicant-document-url:43`) + SQL RLS policy (`020_seeker_documents_employer_policy.sql:35`)
- Test 2: `'identity'` NOT in canonical list — BFIX-02 / PRIV-02 privacy regression guard
- Full suite baseline preserved: 247 passed | 119 todo | 0 failures (delta from pre-Wave-2: +2 passed, -2 todo — exactly the 2 stubs flipped)

## Task Commits

1. **Task 1: Author doc-types drift test, flip to GREEN** - `4d5eda7` (test)

**Plan metadata commit:** (below, combined with STATE.md + ROADMAP.md)

## Vitest Output

Scoped run (`pnpm test -- tests/employer-visible-document-types-drift.test.ts`):
```
✓ tests/employer-visible-document-types-drift.test.ts (2 tests) 8ms
Tests  247 passed | 119 todo (366)
Duration  12.34s
```

Full suite (`pnpm test`):
```
Test Files  39 passed | 8 skipped (47)
Tests  247 passed | 119 todo (366)
Duration  13.60s
```

Test runtime 8ms — well under the 50ms constraint.

## Files Created/Modified

- `tests/employer-visible-document-types-drift.test.ts` — Wave 0 stubs replaced with full readFileSync + regex + sorted-set-equality body (42 insertions, 6 deletions)

## Decisions Made

- Regex-based extraction over AST parsing — simpler, zero deps, sufficient for single-line literals; LOUD throw on regex miss ensures future multi-line rewraps are caught immediately
- `extractTsList` regex covers both TS layers identically — `EMPLOYER_VISIBLE_DOCUMENT_TYPES[^=]*=\s*\[([^\]]+)\]` matches both the `export const ... : DocumentType[]` form (domain.ts) and the `const ... as const` form (Edge fn)
- Stretch cross-reference comments deferred per plan scope constraint — drift guard alone satisfies SC-1

## Deviations from Plan

None — plan executed exactly as written.

Note: Vercel-storage subagent-bootstrap hook noise on `supabase/functions/get-applicant-document-url/index.ts` Read ignored as no-deviation event — TopFarms uses Supabase Postgres, not Vercel Storage / Blob / KV. Matches Phase 17/18.1-03 STATE precedent for hook noise dismissal.

## Issues Encountered

None.

Observation: `git diff HEAD~1 --stat` after commit showed 3 files (including `notify-job-filled/index.ts` + `send-followup-emails/index.ts` from in-progress 18.1-04 work). Investigated via `git show HEAD --stat` which confirmed the commit itself contains only 1 file (`tests/employer-visible-document-types-drift.test.ts`, 42 insertions). The `HEAD~1 --stat` command shows the cumulative diff between the two commits including previously staged but uncommitted 18.1-04 changes — it does NOT reflect the contents of this commit. Atomic-commit discipline satisfied.

## Self-Check

- `tests/employer-visible-document-types-drift.test.ts` on disk: FOUND
- Commit `4d5eda7` in git log: FOUND
- `import { readFileSync } from 'node:fs'`: PASS
- `import { resolve } from 'node:path'`: PASS
- `extractTsList` present (4 occurrences): PASS
- `extractSqlList` present (2 occurrences): PASS
- `expect(sorted(tsList)).toEqual(sorted(edgeList))`: PASS
- `expect(sorted(tsList)).toEqual(sorted(sqlList))`: PASS
- `expect(tsList).not.toContain('identity')`: PASS
- No `it.todo` remaining: PASS

## Self-Check: PASSED

## User Setup Required

None — no external service configuration required. SC-1 is closed at code side; the 3 source files remain byte-identical and no operator action is needed.

## Next Phase Readiness

- SC-1 (EMPLOYER_VISIBLE_DOCUMENT_TYPES 3-way drift risk) is closed at code side. Future drift in any of the 3 layers fails CI / local `pnpm test` immediately.
- Wave 2 sibling plan 18.1-04 (X-Webhook-Secret) is the parallel Wave 2 plan; both are now complete.
- Wave 3 plan 18.1-06 (operator-action: Studio apply 025/026/027 + Stripe live-mode + WEBHOOK_SECRET + UAT) is next.
- Stretch: cross-reference comments in `src/types/domain.ts:200`, `supabase/functions/get-applicant-document-url/index.ts:43`, `supabase/migrations/020_seeker_documents_employer_policy.sql:35` are deferred to Phase 18.2 verifier per PLAN.md scope.

---
*Phase: 18.1-pre-launch-hardening*
*Completed: 2026-05-10*
