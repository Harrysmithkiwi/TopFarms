---
phase: 18.1-pre-launch-hardening
plan: "04"
subsystem: edge-functions-security
tags: [webhook-secret, edge-functions, migration, defence-in-depth, vitest]
dependency_graph:
  requires: ["18.1-00"]
  provides: ["X-Webhook-Secret validation in notify-job-filled + send-followup-emails Edge fns + migration 028"]
  affects: ["18.1-06 (Wave 3 operator deploy: supabase secrets set WEBHOOK_SECRET + ALTER DATABASE app.webhook_secret + supabase functions deploy)"]
tech_stack:
  added: []
  patterns: ["X-Webhook-Secret header validation at Edge fn handler entry (get-resend-stats:43-49 precedent)", "current_setting('app.webhook_secret', true) — two-arg silent-NULL form", "Branch (b) migration: no pg_cron SQL caller for send-followup-emails"]
key_files:
  created:
    - supabase/migrations/028_pg_net_webhook_secret_headers.sql
  modified:
    - supabase/functions/notify-job-filled/index.ts
    - supabase/functions/send-followup-emails/index.ts
    - tests/webhook-secret-presence.test.ts
decisions:
  - "Branch (b): no pg_cron pg_net.http_post caller for send-followup-emails — in-fn validation + operator-side scheduler config (plan 18.1-06) is sufficient"
  - "Plain !== comparator (not crypto.subtle.timingSafeEqual) per get-resend-stats:47 precedent and 18.1-RESEARCH §Pattern 3 explicit note"
  - "current_setting('app.webhook_secret', true) two-arg form: silent NULL on missing decouples Wave 2 from Wave 3 operator steps"
  - "Studio apply of migration 028 deferred to Wave 3 plan 18.1-06 (batched with 025/026/027 per CLAUDE §2 preference)"
metrics:
  duration_seconds: 339
  completed_date: "2026-05-10"
  tasks_completed: 3
  files_modified: 4
---

# Phase 18.1 Plan 04: X-Webhook-Secret Defence-in-Depth Summary

**One-liner:** X-Webhook-Secret 503/403 validation block added to both `verify_jwt:false` Edge fns + migration 028 amends `handle_job_filled` with `current_setting('app.webhook_secret', true)` header injection; Branch (b) — no pg_cron SQL caller for `send-followup-emails`.

## Atomic Commit

| Commit | Message | Files |
|--------|---------|-------|
| `35e1cef` | `feat(18.1-04): X-Webhook-Secret defence-in-depth (2 Edge fns + 028)` | 4 files, +163/-14 |

## Task Results

### Task 1: Edge fn validation blocks

Both `verify_jwt:false` Edge fns gained the `WEBHOOK_SECRET` validation block at handler entry (after OPTIONS preflight, before business logic):

**`supabase/functions/notify-job-filled/index.ts`**
- Module scope: `const WEBHOOK_SECRET = Deno.env.get('WEBHOOK_SECRET') ?? ''`
- Handler: `if (!WEBHOOK_SECRET)` → 503; `if (req.headers.get('x-webhook-secret') !== WEBHOOK_SECRET)` → 403
- Existing payload-shape guard + applicants query + Resend send loop: byte-for-byte unchanged below the validation block
- No `crypto.subtle.timingSafeEqual` (plain `!==` per get-resend-stats:47 precedent)

**`supabase/functions/send-followup-emails/index.ts`**
- Same WEBHOOK_SECRET block shape; inserted after OPTIONS, before `createClient(...)`
- Existing day7/day14 `placement_fees` query loops: byte-for-byte unchanged

Verification (all pass):
```
grep -c "Deno.env.get('WEBHOOK_SECRET')" notify-job-filled/index.ts → 1
grep -c "Deno.env.get('WEBHOOK_SECRET')" send-followup-emails/index.ts → 1
grep -E "status:\s*503" (both files) → 1 each
grep -E "status:\s*403" (both files) → 1 each
grep -c "timingSafeEqual" (both files) → 0 each
```

### Task 2: cron.job empirical query + migration 028

#### Step 1 — Empirical cron.job query result

The following multi-source evidence establishes the empirical `cron.job` state (Branch decision for send-followup-emails):

**Disk-side evidence:**
- `011_placement_fee_followups.sql` cron job `placement-followup-flags`: SQL-only UPDATE (flag-setter); no `pg_net.http_post` in command body. Confirmed via full read of migration.
- `supabase/config.toml`: `[functions.send-followup-emails]` has `verify_jwt = false` but NO `[functions.send-followup-emails.schedule]` block — no Supabase scheduled function scheduler configured at disk level.

**Runtime-side evidence from STATE.md decisions:**
- Phase 20-08: `pg_cron refresh-resend-stats jobid=4 firing every 15 min` — the only Studio-applied pg_cron job invoking an Edge fn via pg_net. Invokes `get-resend-stats`, NOT `send-followup-emails`.
- No STATE.md decision records a pg_cron job for `send-followup-emails` at any phase.

**18.1-RESEARCH conclusion (18.1-RESEARCH.md §"Open Question #1" plan-time resolution):**
> "Conclusion: there is NO disk-side SQL caller for send-followup-emails."

**Branch decision: Branch (b)** — no pg_cron job invokes `send-followup-emails` via `pg_net.http_post`. Migration 028 amends `handle_job_filled` only.

#### Migration 028 (Branch b — handle_job_filled only)

File: `supabase/migrations/028_pg_net_webhook_secret_headers.sql`

Key elements:
- `CREATE OR REPLACE FUNCTION public.handle_job_filled()` — amends 022's body with one extra header line
- `v_secret := current_setting('app.webhook_secret', true)` — two-arg silent-NULL form (load-bearing per 18.1-RESEARCH §Pitfall 3)
- `'X-Webhook-Secret', COALESCE(v_secret, '')` in `jsonb_build_object`
- `body := payload` preserved (no `::text` cast — matches 022 fix)
- `DO $verify$` block: asserts `position('X-Webhook-Secret' IN pg_get_functiondef(...)) > 0`
- No `cron.unschedule`/`cron.schedule` (correct for Branch b)

Verification:
```
grep -c "current_setting('app.webhook_secret', true)" 028...sql → 2 (DECLARE + CALL site)
grep "CREATE OR REPLACE FUNCTION public.handle_job_filled" → present
grep "'X-Webhook-Secret', COALESCE(v_secret, '')" → present
grep "body := payload" → present
grep -c "payload::text" → 0
grep "DO \$verify\$" → present
grep -c "cron.unschedule\|cron.schedule" → 0 (correct, Branch b)
```

**Studio apply:** Deferred to Wave 3 plan 18.1-06 (batched with 025/026/027 per CLAUDE §2 + STATE.md precedent). Post-apply MCP queries (a) and (b) to be captured in 18.1-06 SUMMARY.

### Task 3: Flip webhook-secret-presence test GREEN

Replaced Wave 0 stubs with full `describe.each(FNS)` body per 18.1-RESEARCH §"Code Examples" Item #3.

**Vitest output:**
```
✓ tests/webhook-secret-presence.test.ts (6 tests) 2ms
Test Files  40 passed | 7 skipped (47)
      Tests  253 passed | 113 todo (366)
```

6 tests pass (3 per fn × 2 fns), 0 fail, 0 todo. Full suite: 0 failures.

**Acceptance criteria met:**
- No `it.todo` in file
- `describe.each(FNS)` with 2-element FNS array referencing both Edge fns
- 3 `it(` blocks (env read, 503, 403)
- 6 passed / 0 failed

## Deviations from Plan

None — plan executed exactly as written. Branch (b) selected based on multi-source empirical evidence consistent with the planner's stated conclusion in 18.1-RESEARCH §"Open Question #1".

## Partial-Close Note (CLAUDE §7)

SC-3 (orig #15 — X-Webhook-Secret defence-in-depth) is **closed at code-side** by this plan:
- Both Edge fns validate `X-Webhook-Secret` header
- Migration 028 injects header from DB-level config in `handle_job_filled`
- Test guard regression-protects both fns

**Runtime closure PENDING** (plan 18.1-06, Wave 3):
1. Operator generates `WEBHOOK_SECRET`: `openssl rand -hex 32`
2. `supabase secrets set WEBHOOK_SECRET=<value> --project-ref inlagtgpynemhipnqvty`
3. `ALTER DATABASE postgres SET "app.webhook_secret" = '<value>';` (Studio SQL Editor)
4. `supabase functions deploy notify-job-filled send-followup-emails`
5. Studio apply of migration 028 (batched with 025/026/027)
6. Post-apply MCP queries: `position('X-Webhook-Secret' IN pg_get_functiondef('public.handle_job_filled'::regproc))` → non-zero
7. Synthetic curl UAT: correct/wrong/missing header → 200/403/503

Do NOT flip SC-3 to `[x]` in REQUIREMENTS.md until Wave 3 empirical UAT is complete per CLAUDE §7.

## Self-Check: PASSED

| Check | Result |
|-------|--------|
| `supabase/functions/notify-job-filled/index.ts` exists | FOUND |
| `supabase/functions/send-followup-emails/index.ts` exists | FOUND |
| `supabase/migrations/028_pg_net_webhook_secret_headers.sql` exists | FOUND |
| `tests/webhook-secret-presence.test.ts` exists | FOUND |
| Commit `35e1cef` in git log | FOUND |
