---
phase: 18.2-code-quality-ux-polish
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/get-applicant-document-url/index.ts
  - src/components/layout/Nav.tsx
  - src/components/layout/Sidebar.tsx
  - src/pages/dashboard/employer/ApplicantDashboard.tsx
  - supabase/migrations/NAMING.md
  - .planning/phases/12-oauth-authentication/12-VALIDATION.md
  - .planning/phases/13-email-notifications/13-VALIDATION.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "The Edge Function comment at index.ts:8 accurately describes the gateway-trust pattern, not the deprecated getUser call"
    - "Seeker nav shows My Documents between My Applications and Saved searches in both Nav.tsx and Sidebar.tsx"
    - "ApplicantDashboard.tsx has no dead first_name field in the SeekerProfile interface or seeker_name assignment"
    - "NAMING.md documents the jobs.benefits jsonb vs text[] discrepancy and the couples_welcome semantic distinction"
    - "Phase 14 SUMMARY frontmatter convention is codified; Phase 12 and 13 VALIDATION.md have definitive nyquist_compliant values, not draft"
    - "REQUIREMENTS.md has entries for SelectRole OAuth bypass known behaviour"
  artifacts:
    - path: "supabase/functions/get-applicant-document-url/index.ts"
      provides: "Updated comment line 8 — no getUser reference"
      contains: "gateway-trust"
    - path: "src/components/layout/Nav.tsx"
      provides: "seekerLinks with My Documents entry"
      contains: "My Documents"
    - path: "src/components/layout/Sidebar.tsx"
      provides: "seekerItems with My Documents entry using FolderOpen icon"
      contains: "My Documents"
    - path: "src/pages/dashboard/employer/ApplicantDashboard.tsx"
      provides: "SeekerProfile interface without first_name; seeker_name uses null literal"
    - path: "supabase/migrations/NAMING.md"
      provides: "jobs.benefits and couples_welcome semantic docs"
      contains: "jsonb"
    - path: ".planning/REQUIREMENTS.md"
      provides: "SelectRole known-behaviour entry"
      contains: "SelectRole"
  key_links:
    - from: "src/components/layout/Sidebar.tsx"
      to: "/dashboard/seeker/documents"
      via: "seekerItems array"
      pattern: "dashboard/seeker/documents"
    - from: "src/components/layout/Nav.tsx"
      to: "/dashboard/seeker/documents"
      via: "seekerLinks array"
      pattern: "dashboard/seeker/documents"
---

<objective>
Fix 9 mechanical audit-residue items: stale Edge Function comment, seeker nav My Documents link, dead first_name semantics, migration 020 doc note (already in NAMING.md — confirm), frontmatter convention codified, Phase 12/13 VALIDATION.md finalised, jobs.benefits and couples_welcome semantic docs, SelectRole OAuth bypass known-behaviour entry.

Purpose: Clear the audit residue accumulated since Phase 14 and Phase 12/13. All items are low-risk edits to comments, docs, config arrays, and type declarations.
Output: 10 files updated with no behaviour change except the new My Documents nav link.
</objective>

<execution_context>
@/Users/harrysmith/.claude/get-shit-done/workflows/execute-plan.md
@/Users/harrysmith/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18.2-code-quality-ux-polish/18.2-CONTEXT.md
@.planning/phases/18.2-code-quality-ux-polish/18.2-RESEARCH.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SC-1 + SC-3 — Edge Function comment + ApplicantDashboard dead first_name</name>
  <files>
    supabase/functions/get-applicant-document-url/index.ts
    src/pages/dashboard/employer/ApplicantDashboard.tsx
  </files>
  <read_first>
    - supabase/functions/get-applicant-document-url/index.ts (lines 1-20 — see exact comment at line 8)
    - src/pages/dashboard/employer/ApplicantDashboard.tsx (lines 18-30 for SeekerProfile interface; lines 425-445 for seeker_name assignment)
  </read_first>
  <action>
**SC-1 — Edge Function comment (index.ts line 8):**

Replace line 8 exactly:
```
//   2. Bearer-token auth    — supabase.auth.getUser(token) → 401 if invalid.
```
With:
```
//   2. Bearer-token auth    — gateway verify_jwt:true validates signature upstream;
//                              payload decoded locally (BFIX-05 gateway-trust pattern).
```

This is a single-line comment change. Do NOT touch any other line in the comment block or any function logic.

**SC-3 — ApplicantDashboard dead first_name semantics:**

Three changes in `src/pages/dashboard/employer/ApplicantDashboard.tsx`:

1. Line 24 — Remove `first_name?: string` from the `SeekerProfile` interface. The interface block currently includes this field; it does not exist on `seeker_profiles` in the DB (confirmed via BFIX-04 in REQUIREMENTS.md). Remove only that line from the interface.

2. Line 178 — The comment "Note: 'first_name' was previously selected here but the column doesn't exist on seeker_profiles — query rejected silently, applicants list was empty." — KEEP this comment unchanged. It is a load-bearing historical note explaining the prior bug.

3. Line 435 — Replace:
```typescript
seeker_name: pendingHireApp.seeker_profiles?.first_name ?? null,
```
With:
```typescript
seeker_name: null,
```
The `create-placement-invoice` Edge Function accepts `seeker_name: null` — the parameter defaults and is already handled. The API body shape is unchanged; only the null-producing dead-access is removed.
  </action>
  <verify>
    <automated>grep -n "gateway-trust\|BFIX-05" supabase/functions/get-applicant-document-url/index.ts | head -5</automated>
    <automated>grep -n "first_name" src/pages/dashboard/employer/ApplicantDashboard.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "getUser" supabase/functions/get-applicant-document-url/index.ts` returns line 8 does NOT contain `getUser` (the old comment is gone)
    - `grep -n "gateway-trust\|BFIX-05" supabase/functions/get-applicant-document-url/index.ts` returns at least 1 match in the comment block around line 8
    - `grep -c "first_name" src/pages/dashboard/employer/ApplicantDashboard.tsx` returns 1 (only the historical comment at line 178 remains, not the interface field or the assignment)
    - `grep -n "seeker_name" src/pages/dashboard/employer/ApplicantDashboard.tsx` shows `seeker_name: null,` (no `?.first_name`)
    - `npx vitest run --reporter=verbose 2>&1 | tail -5` exits 0 (no regressions)
  </acceptance_criteria>
  <done>Edge Function comment updated; SeekerProfile.first_name removed; seeker_name: null literal in place; no test regressions.</done>
</task>

<task type="auto">
  <name>Task 2: SC-2 — My Documents link in Nav.tsx and Sidebar.tsx</name>
  <files>
    src/components/layout/Nav.tsx
    src/components/layout/Sidebar.tsx
  </files>
  <read_first>
    - src/components/layout/Nav.tsx (lines 1-25 — seekerLinks array, current entries)
    - src/components/layout/Sidebar.tsx (lines 1-40 — imports, seekerItems array, NavItem shape)
  </read_first>
  <action>
**Nav.tsx — seekerLinks array:**

The current `seekerLinks` array at line 14-20 is:
```typescript
const seekerLinks = [
  { to: '/jobs', label: 'Find Work' },
  { to: '/dashboard/seeker/applications', label: 'My Applications' },
  // 'My Profile' (/profile) and 'Settings' (/settings) removed 2026-05-04 ...
]
```

Insert one entry after `My Applications`, before the comment:
```typescript
{ to: '/dashboard/seeker/documents', label: 'My Documents' },
```

The route `/dashboard/seeker/documents` is already registered in `main.tsx:181` — NO route change needed.

**Sidebar.tsx — seekerItems array + import:**

1. Add `FolderOpen` to the existing lucide-react import line. Current import (from grep): `LayoutDashboard, Search, FileText, LogOut, Bookmark`. Update to include `FolderOpen`:
```typescript
import { LayoutDashboard, Search, FileText, FolderOpen, LogOut, Bookmark } from 'lucide-react'
```
(Do not remove any existing import. Add `FolderOpen` in alphabetical order within the import.)

2. The `seekerItems` array at line 26:
```typescript
const seekerItems: NavItem[] = [
  { to: '/dashboard/seeker', label: 'Dashboard', icon: LayoutDashboard },
  { to: '/jobs', label: 'Find Jobs', icon: Search },
  { to: '/dashboard/seeker/applications', label: 'My Applications', icon: FileText },
  { to: '/dashboard/seeker/saved-searches', label: 'Saved searches', icon: Bookmark },
  { to: '/onboarding/seeker', label: 'Edit Profile', icon: User },
]
```
Insert after the `My Applications` entry:
```typescript
{ to: '/dashboard/seeker/documents', label: 'My Documents', icon: FolderOpen },
```

Final order: Dashboard → Find Jobs → My Applications → **My Documents** → Saved searches → Edit Profile

IMPORTANT: The `end` prop logic at Sidebar.tsx line 52 evaluates `end={item.to.includes('/dashboard')}`. The route `/dashboard/seeker/documents` includes `/dashboard`, so `end=true` — this is correct behaviour (exact-match active state for a leaf route). No changes to the end prop logic needed.
  </action>
  <verify>
    <automated>grep -n "My Documents" src/components/layout/Nav.tsx src/components/layout/Sidebar.tsx</automated>
    <automated>npx vitest run tests/sidebar-signout.test.tsx --reporter=verbose 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "dashboard/seeker/documents" src/components/layout/Nav.tsx` returns exactly 1 match with label "My Documents"
    - `grep -n "dashboard/seeker/documents" src/components/layout/Sidebar.tsx` returns exactly 1 match with label "My Documents" and `icon: FolderOpen`
    - `grep -n "FolderOpen" src/components/layout/Sidebar.tsx` returns a match in the import line AND the seekerItems entry
    - `grep -n "My Applications\|My Documents\|Saved searches" src/components/layout/Sidebar.tsx` shows My Applications before My Documents before Saved searches (order check)
    - `npx vitest run tests/sidebar-signout.test.tsx --reporter=verbose 2>&1 | tail -5` exits 0
  </acceptance_criteria>
  <done>Nav.tsx seekerLinks and Sidebar.tsx seekerItems both contain My Documents at /dashboard/seeker/documents in the correct position; FolderOpen imported; existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: SC-4 + SC-5 + SC-6 + SC-8 + SC-9 + SC-12 — Doc hygiene sweep</name>
  <files>
    supabase/migrations/NAMING.md
    .planning/phases/12-oauth-authentication/12-VALIDATION.md
    .planning/phases/13-email-notifications/13-VALIDATION.md
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    - supabase/migrations/NAMING.md (full file — understand current lookup table and sections to append to)
    - .planning/phases/14-bug-fixes/14-01-SUMMARY.md (lines 1-15 — READ-ONLY REFERENCE, do NOT modify; inspect tags frontmatter only)
    - .planning/phases/14-bug-fixes/14-03-SUMMARY.md (lines 1-15 — READ-ONLY REFERENCE, do NOT modify; inspect tags frontmatter only)
    - .planning/phases/12-oauth-authentication/12-VALIDATION.md (lines 1-10 — frontmatter)
    - .planning/phases/13-email-notifications/13-VALIDATION.md (lines 1-10 — frontmatter)
    - .planning/REQUIREMENTS.md (lines 68-80 — Deferred Investigations section)
  </read_first>
  <action>
**SC-4 — Migration 020 disk/registry mismatch (NAMING.md):**

The lookup table row for `020_seeker_documents_employer_policy.sql` already exists at line 38:
```
| `020_seeker_documents_employer_policy.sql` | `20260429031148` | `seeker_documents_employer_policy` | 2026-04-29 (e8f0882) | BFIX-02 employer SELECT RLS policy. `name` was passed without `020_` prefix. |
```

This is already documented. Confirm the note text accurately describes the discrepancy. If it does, SC-4 is satisfied by the existing entry — add a verification note at the end of the lookup table footnote:
> _SC-4 verified 2026-05-10: 020 disk/registry mismatch was already documented in this table. No additional entries required._

Also add the missing migrations 024 through 029 to the lookup table (they are all Studio-applied and not in the registry). Append these rows to the table:

| Disk filename | Live version | Live name | Applied | Notes |
|---|---|---|---|---|
| `024_saved_searches.sql` | NOT IN REGISTRY | — | 2026-05-05 (Phase 17-01) | Studio-applied. Runtime artefacts: pg_class.relrowsecurity=true, 4 pg_policy rows, 3 pg_indexes including pkey (see 17-01-SUMMARY.md). |
| `025_fk_indexes.sql` | NOT IN REGISTRY | — | 2026-05-10 (Phase 18.1-06) | Studio-applied. 15× CREATE INDEX IF NOT EXISTS for unindexed FKs. Verified via pg_indexes (see 18.1-06 operator UAT). |
| `026_mark_job_filled_rpc.sql` | NOT IN REGISTRY | — | 2026-05-10 (Phase 18.1-06) | Studio-applied. SECURITY DEFINER RPC mark_job_filled. Verified via pg_proc (see 18.1-06 operator UAT). |
| `027_match_scores_cleanup_trigger.sql` | NOT IN REGISTRY | — | 2026-05-10 (Phase 18.1-06) | Studio-applied. AFTER UPDATE trigger on jobs.status for match_scores cleanup. Verified via pg_trigger (see 18.1-06 operator UAT). |
| `028_pg_net_webhook_secret_headers.sql` | NOT IN REGISTRY | — | 2026-05-10 (Phase 18.1-06) | Studio-applied. pg_net call updated to include X-Webhook-Secret header in handle_job_filled. |
| `029_pg_net_webhook_secret_vault.sql` | NOT IN REGISTRY | — | 2026-05-10 (Phase 18.1-06) | Studio-applied. vault.create_secret() for WEBHOOK_SECRET. Verified via vault.decrypted_secrets secret_len=64 (see 18.1-06 operator UAT). |

**SC-5 — BFIX-01/03 SUMMARY frontmatter (convention codified):**

Add a `## SUMMARY frontmatter convention` section to `NAMING.md` BEFORE the `## When to revisit this decision` section:

```markdown
## SUMMARY frontmatter convention

Phase SUMMARY.md files use `tags: [req-id, ...]` as the canonical signal for which requirements a plan closes. This is the established convention (confirmed by 14-01, 14-03, and all subsequent SUMMARY files).

The `requirements_completed` field is an optional supplement used when a plan closes multiple REQ-IDs that need explicit cross-referencing. It is NOT required if `tags` already captures the requirements closed.

Example — `14-01-SUMMARY.md` closes BFIX-01 via `tags: [bfix-01, ...]`.
Example — `14-03-SUMMARY.md` closes BFIX-02 + BFIX-03 via `tags: [bfix-02, edge-function, ...]`.

No retroactive backfill of `requirements_completed` is needed for Phase 14 files — `tags` is sufficient.
```

**SC-8 — jobs.benefits mismatch (NAMING.md):**

Add a `## Schema/types intent mismatches` section immediately after the `## SUMMARY frontmatter convention` section:

```markdown
## Schema/types intent mismatches

### jobs.benefits — text[] vs jsonb

Migration 013 declared `benefits text[]` in the original schema intent comments. The live schema has `benefits jsonb` (applied during phantom-apply reconciliation 2026-05-03). At the JavaScript layer they are functionally equivalent for the array-of-strings usage pattern in TopFarms — `JSON.parse` on a jsonb string and direct array indexing on text[] both produce `string[]`. No frontend code change is needed.

Source of truth: `information_schema.columns WHERE table_name='jobs' AND column_name='benefits'` returns `data_type='jsonb'`. The frontend types and any PostgREST `.eq()` / `.overlaps()` queries treat it as a string array.

No fix required — intent captured here for future reference.

### couples_welcome — boolean vs accommodation_extras text[] member

`seeker_profiles.couples_welcome` is a **boolean** representing a seeker's preference (the seeker is a couple, looking for accommodation together).

`jobs.accommodation_extras` is a **text[] array** that may contain the string `'Couples welcome'` as one of several accommodation feature flags (the employer's accommodation supports couples).

These are semantically distinct:
- `seeker_profiles.couples_welcome = true` means "this seeker needs couples-friendly accommodation"
- `'Couples welcome' ∈ jobs.accommodation_extras` means "this job's accommodation is suitable for couples"

The matching engine joins them: a seeker with `couples_welcome = true` gets a higher match score on jobs where `accommodation_extras` contains `'Couples welcome'`.

Source of truth for the seeker side: `src/pages/onboarding/steps/SeekerStep5LifeSituation.tsx` (couples_welcome field).
Source of truth for the job side: `src/pages/dashboard/employer/steps/EmployerStep5Accommodation.tsx` (accommodation_extras chips including 'Couples welcome').

No fix required — semantic distinction captured here for future reference.
```

**SC-6 — Phase 12/13 VALIDATION.md finalisation:**

In `.planning/phases/12-oauth-authentication/12-VALIDATION.md`, update the YAML frontmatter:
- Change `status: draft` → `status: complete`
- Change `nyquist_compliant: false` → `nyquist_compliant: true`
- Change `wave_0_complete: false` → `wave_0_complete: true`

Add a rationale comment directly after the frontmatter block (after the closing `---`):
```markdown
<!-- SC-6 finalised 2026-05-10: Phase 12 predates the Nyquist Wave 0 scaffolding workflow adoption.
Retroactively assessed: 3 test files exist covering the primary deliverables —
`tests/oauth-buttons.test.tsx`, `tests/select-role.test.tsx`, `tests/protected-route-oauth.test.tsx`.
Coverage is adequate for the shipped features. nyquist_compliant: true with this rationale.
wave_0_complete: true (tests exist, though written alongside implementation rather than before). -->
```

In `.planning/phases/13-email-notifications/13-VALIDATION.md`, update the YAML frontmatter:
- Change `status: draft` → `status: complete`
- Change `nyquist_compliant: false` → `nyquist_compliant: false` (KEEP false — see rationale)
- Change `wave_0_complete: false` → `wave_0_complete: false` (KEEP false)

Add a rationale comment directly after the frontmatter block:
```markdown
<!-- SC-6 finalised 2026-05-10: Phase 13 predates the Nyquist Wave 0 scaffolding workflow adoption.
Retroactively assessed as nyquist_compliant: false (definitive, not draft).
The primary deliverable — notify-job-filled Edge Function — has no automated Vitest/unit tests.
Validation was performed via UAT in Phase 15-02 (empirical trigger fire, email delivery observed).
UAT is the appropriate mechanism for a Deno Edge Function that exercises pg_net and Resend.
status: complete (validation was performed; just not via automated unit tests). -->
```

**SC-12 — SelectRole OAuth bypass known behaviour (REQUIREMENTS.md):**

In `.planning/REQUIREMENTS.md`, locate the `### Deferred Investigations` section. After the `AUTH-FIX-02` entry (around line 71), add:

```markdown
- **OAUTH-SEEKER-DEFAULT** *(known behaviour — no code change required)*: New OAuth signups (Google, Facebook) auto-default to role `'seeker'` via the `handle_new_user` trigger: `COALESCE(raw_user_meta_data->>'role', 'seeker')`. OAuth providers don't pass `role` in user metadata, so every new OAuth signup lands as seeker. `SelectRole.tsx:29` has `if (role) return <Navigate>` which redirects past the role-picker before it can render for the returning-seeker path. Current behaviour is intentional — seeker is the correct default for the marketing funnel (most signups are seekers). If employer OAuth signup volume becomes meaningful, revisit by either nulling out the default role for OAuth signups or surfacing a "Were you signing up as an employer?" prompt in onboarding. Logged 2026-05-04 during UAT-04 new-user round-trip; confirmed known-behaviour 2026-05-10 per Phase 18.2 SC-12.
```
  </action>
  <verify>
    <automated>grep -n "benefits\|jsonb\|couples_welcome" supabase/migrations/NAMING.md | head -10</automated>
    <automated>grep -n "nyquist_compliant" .planning/phases/12-oauth-authentication/12-VALIDATION.md .planning/phases/13-email-notifications/13-VALIDATION.md</automated>
    <automated>grep -n "OAUTH-SEEKER-DEFAULT\|SelectRole" .planning/REQUIREMENTS.md | head -5</automated>
    <automated>grep -n "024_saved_searches\|025_fk_indexes\|026_mark_job" supabase/migrations/NAMING.md | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "jsonb\|benefits" supabase/migrations/NAMING.md` returns at least 2 matches (the section heading and the body)
    - `grep -n "couples_welcome" supabase/migrations/NAMING.md` returns at least 1 match in the schema mismatches section
    - `grep "nyquist_compliant" .planning/phases/12-oauth-authentication/12-VALIDATION.md` returns `nyquist_compliant: true`
    - `grep "status:" .planning/phases/12-oauth-authentication/12-VALIDATION.md` returns `status: complete`
    - `grep "nyquist_compliant" .planning/phases/13-email-notifications/13-VALIDATION.md` returns `nyquist_compliant: false`
    - `grep "status:" .planning/phases/13-email-notifications/13-VALIDATION.md` returns `status: complete`
    - `grep -n "OAUTH-SEEKER-DEFAULT" .planning/REQUIREMENTS.md` returns 1 match
    - `grep -n "SUMMARY frontmatter convention" supabase/migrations/NAMING.md` returns 1 match (new section heading)
    - `grep -n "024_saved_searches" supabase/migrations/NAMING.md` returns 1 match (new lookup row)
  </acceptance_criteria>
  <done>NAMING.md updated with 024-029 lookup rows + benefits/couples_welcome semantic docs + SUMMARY convention section; 12-VALIDATION.md status:complete nyquist_compliant:true; 13-VALIDATION.md status:complete nyquist_compliant:false (definitive); REQUIREMENTS.md has OAUTH-SEEKER-DEFAULT entry.</done>
</task>

</tasks>

<verification>
Run after all tasks:
1. `grep -n "gateway-trust" supabase/functions/get-applicant-document-url/index.ts` — must match line ~8
2. `grep -c "first_name" src/pages/dashboard/employer/ApplicantDashboard.tsx` — must be 1 (historical comment only)
3. `grep -n "My Documents" src/components/layout/Nav.tsx src/components/layout/Sidebar.tsx` — must match both files
4. `grep "nyquist_compliant" .planning/phases/12-oauth-authentication/12-VALIDATION.md` — must be `true`
5. `grep "nyquist_compliant" .planning/phases/13-email-notifications/13-VALIDATION.md` — must be `false`
6. `grep "status:" .planning/phases/12-oauth-authentication/12-VALIDATION.md .planning/phases/13-email-notifications/13-VALIDATION.md` — both must be `complete`
7. `grep -n "OAUTH-SEEKER-DEFAULT" .planning/REQUIREMENTS.md` — must match
8. `npx vitest run 2>&1 | tail -5` — must exit 0
</verification>

<success_criteria>
- SC-1: `grep "getUser" supabase/functions/get-applicant-document-url/index.ts` — line 8 no longer contains `getUser`; `grep "gateway-trust"` returns match
- SC-2: Both Nav.tsx seekerLinks and Sidebar.tsx seekerItems contain `{ to: '/dashboard/seeker/documents', label: 'My Documents' ... }` in correct position
- SC-3: `grep -c "first_name" src/pages/dashboard/employer/ApplicantDashboard.tsx` returns 1 (historical comment only); `grep "seeker_name"` shows `null` not `?.first_name`
- SC-4: NAMING.md lookup table has rows for 024–029 with Studio-applied notes; 020 mismatch already documented (confirmed)
- SC-5: NAMING.md has `## SUMMARY frontmatter convention` section codifying tags as canonical convention
- SC-6: 12-VALIDATION.md is `nyquist_compliant: true, status: complete`; 13-VALIDATION.md is `nyquist_compliant: false, status: complete`
- SC-8: NAMING.md has `## Schema/types intent mismatches` with `jobs.benefits — text[] vs jsonb` subsection
- SC-9: NAMING.md has `couples_welcome` semantic distinction documented
- SC-12: REQUIREMENTS.md has `OAUTH-SEEKER-DEFAULT` known-behaviour entry
- Full vitest suite passes (0 regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/18.2-code-quality-ux-polish/18.2-01-SUMMARY.md`
</output>
