---
phase: leads (commercial Firecrawl lane)
milestone: L2 commercial feed — built, staged, NOT applied
status: in_progress
last_updated: 2026-06-11T10:53:19Z
---

<current_state>
Leads L0–L4 are LIVE (pushed, green). The commercial Firecrawl lane
(`lead-harvest`) is BUILT + verified-by-prototype but **NOT applied, NOT
pushed** — staged in commit `ba59deb`, awaiting operator diff review then a
joint apply+deploy. Full spec in `.planning/PHASE-LEADS-FEEDS-ARCHITECTURE.md`
(rewrite spec at the bottom). Nothing on prod changed this session beyond what
was already live. (This file replaced a stale 2026-03 new-project-init handoff.)
</current_state>

<completed_work>
- Leads L0–L4: complete, deployed, suite 414 green (prior sessions).
- Firecrawl prototype PROVED via CLI on nzfarmingjobs `/job/` ads:
  - Clonkeen → all fields clean (farm name, region mapped, salary,
    email/name/phone/notes, summary). 5 credits/extract.
  - Otamatea → never-infer HOLDS (absent email/name/salary = null; printed
    phone captured). Surfaced recruiter-vs-employer split.
- Recruiter decision RESOLVED: business_name=hiring farm; advertiser_name=
  agency; is_recruiter flag (fixes lead quality AND fuzzy-dedupe collision).
- Firecrawl `/v2/map` + `/v2/scrape` (json+schema, result at `data.json`)
  shapes verified vs docs.
- `lead-harvest` REWRITTEN (ba59deb): per-board config; map(/jobs/) → filter
  (/job/ in, /company/+/resume/ out) → dedupe vs existing source_refs BEFORE
  scraping → cap 25/run (over-cap reported) → /v2/scrape → normalise →
  _lead_intake. `deno check` green.
- Migration 044 REWRITTEN (staged): leads += salary_text, company_profile_url,
  summary, advertiser_name, is_recruiter; source CHECK widened (+nzfarmingjobs)
  on leads + lead_staging; admin_lead_approve carries all — purely additive
  (gate-first intact; suppression/dedupe in _lead_intake untouched).
- Rich schema at /tmp/topfarms-ad-schema.json (regenerate from migration 044 /
  the architecture doc field list if /tmp cleared).
</completed_work>

<remaining_work>
Next-session order (operator-agreed):
1. Review the 044 + lead-harvest diff (commit ba59deb).
2. Add 'nzfarmingjobs' to frontend SOURCE_LABELS (AdminLeadsStaging.tsx +
   AdminLeads.tsx) so the UI doesn't show the raw source value.
3. Apply migration 044 (Management API) AND deploy lead-harvest (push) TOGETHER.
4. Trigger ONE real /job/ harvest (operator sets FIRECRAWL_API_KEY +
   LEAD_INTAKE_SECRET as Edge secrets; invoke with the X-Webhook-Secret header).
5. Confirm a real lead (farm name, Isaac-style contact, summary, recruiter
   fields) lands in /admin/leads/staging. THAT closes the commercial lane.
Later: Apify FB own-group lane (first live Haiku run); Seek/TradeMe (JS-heavy,
deferred); apply 043 cron only after manual proof; registry repair (036–044);
pre-launch queue (live Stripe PEND-01, F9 leaked-password toggle).
</remaining_work>

<decisions_made>
- Seek/TradeMe → Firecrawl; FB own-group → Apify actor (operator owns
  ToS/compliance — privacy lawyer; deliberate, not drift).
- Scrape /job/ ads, NEVER /company/ (spam-polluted). Live /job/ = real employer.
- map→filter→dedupe→per-ad-extract (lead-harvest self-contained);
  dedupe-before-extract bounds 5-credit scrapes to NEW ads only.
- Recruiter split (business_name=farm / advertiser_name=agency / is_recruiter).
- Nothing applied until diff-reviewed; registry backlog stays deferred.
</decisions_made>

<blockers>
- None blocking. Going-live needs operator Edge secrets via
  `supabase secrets set --env-file supabase/functions/.env` (gitignored —
  Claude must NOT read that file).
- Firecrawl scrape `data.json` path verified-by-docs, not yet live-confirmed;
  function logs the shape on mismatch so the first real run surfaces drift.
</blockers>

<context>
Discipline held all session: prove-before-automate, show-diff-before-apply,
honest-degrade, nothing pushed/applied without operator nod. Commercial lane is
genuinely valuable — contactable leads (real farm + contact), not just names.
The function rewrite was deferred from a prior low-context session and built
cleanly here with verified API shapes.
</context>

<next_action>
Start with: read commit ba59deb (migration 044 + lead-harvest), show the
operator the diff, then add 'nzfarmingjobs' to the two SOURCE_LABELS maps. Do
NOT apply 044 or push until the operator approves. Then apply 044 + deploy
together and prove one real /job/ ad into /admin/leads/staging.
</next_action>
