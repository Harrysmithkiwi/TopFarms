---
phase: leads (commercial Firecrawl lane)
milestone: L2 commercial feed — LIVE + proven end-to-end
status: paused
last_updated: 2026-06-15T02:10:00Z
---

<current_state>
The commercial Firecrawl lane (`lead-harvest`, nzfarmingjobs) is LIVE and proven
END-TO-END: harvest → extract → stage → clean → approve → leads, audit-logged.
Migration 044 is APPLIED, the function is DEPLOYED (sitemap discovery + board-guard
+ region canonicalisation + blank raw_excerpt), the staging UI surfaces + promotes
the rich fields, 39 real leads were harvested and cleaned, and 1 (Ferniehurst) was
approved through the gate with full 044 carry-through verified. Dev server torn
down, screenshots removed, working tree clean (only untracked .claude/skills/
playwright-skill, pre-existing). Nothing left mid-flight.
</current_state>

<completed_work>
- Migration 044 APPLIED via Management API (columns + source CHECK widening +
  additive admin_lead_approve). Verified: 5 cols present, both CHECKs include
  nzfarmingjobs, gate-first intact.
- `lead-harvest` DEPLOYED (functions CI; migrations job stays gated/skipped):
  - Discovery solved: map site ROOT with sitemap='only' (not /jobs/, not search
    keyword which silently dropped 25/39 ads). Returns 39 /job/ ads.
  - Cap MAX_EXTRACTS_PER_BOARD=10 (60s gateway wall-clock safety — a cap, not the
    fix; see remaining #1).
  - Deterministic normalise() guards (commit f857013): isBoardName() nulls
    board-as-advertiser + clears is_recruiter; canonicalRegion() maps to the 16
    NZ regions (exact→alias→null); raw_excerpt blanked for harvested rows.
- Staging review UI (commits 6e17cae + 4264687): renders all rich fields; contact
  promoted to hero block; recruiter badge colour-coded amber; summary demoted.
- 39 leads harvested → one-off SQL cleanup (Studio, mirrors normalise() exactly):
  22 board-misflags nulled, 6 regions canonicalised, 39 raw_excerpts blanked.
  Dry-run verified 6 fixed / 32 unchanged / 1 correct-null (Saskatchewan=non-NZ).
- Gate proven: Ferniehurst approved → leads row with contact jsonb, salary,
  summary, region=Canterbury, fingerprint, AND cleaned advertiser_name=null /
  is_recruiter=false carried through. Staging flipped approved, audit logged.
- All green throughout: deno check, tsc, eslint, 414 vitest, prod build.
</completed_work>

<remaining_work>
Next-session priority order (operator-confirmed):
1. waitUntil SCALE REFACTOR — the one genuinely open problem. 60s gateway caps
   the synchronous response; sequential LLM scrapes (~5-10s each) can't fit >~8
   per invocation. FIRST verify the exact Supabase Edge background wall-clock
   limit (don't guess), THEN refactor: return 202 fast + run the scrape loop via
   EdgeRuntime.waitUntil + log the final tally. Removes the cap-10 throughput limit.
2. Non-NZ triage — Reject+suppress the overseas leads at the gate now (Saskiwi/
   Saskatchewan, Waverley/King Island AU); drop-at-extraction (region-not-in-16 →
   skip) is the later harvest enhancement.
3. Scoped "faster to work" UI pass (plan-first, operator reviews BOTH staging +
   leads pages before push): row-level contact-at-a-glance, in-row recruiter flag,
   Tautara long-name wrap fix, bulk-select→approve. NOT beautification.
Older backlog (unchanged): migration 043 lead-harvest cron (apply ONLY AFTER the
scale fix), registry repair 036-044, live Stripe PEND-01, F9 leaked-password toggle.
</remaining_work>

<decisions_made>
- Discovery = sitemap='only' on site root (authoritative complete /job/ list);
  search keyword pre-filters and silently drops ads — never use it for discovery.
- Recruiter/region fixes are DETERMINISTIC guards in normalise() (not prompt) so
  they don't drift; the cleanup SQL mirrors the SAME logic — if either changes,
  both must change in lockstep.
- 39-row cleanup = one-off SQL, NOT re-harvest (dedupe would block re-harvest;
  fixes are transforms, not re-extraction; zero credits).
- waitUntil (not a smaller cap) is the agreed steady-state scale fix.
- Non-NZ leads: suppress-at-gate now, drop-at-extraction later.
</decisions_made>

<blockers>
- None blocking. waitUntil work needs the verified background wall-clock limit
  first (research step, not a blocker).
- Admin password was rotated mid-session (shared in chat); the dev-server admin
  login is no longer valid — next browser-based check needs the new password.
</blockers>

<context>
Discipline held: diagnose-before-fix (logs showed pure 60s timeout, not a hung
extract), show-diff-before-apply (044 + normalise() + cleanup SQL all reviewed
before write), honest-degrade, prove-before-automate (eyeballed the UI → caught
22/39 recruiter false-positives + off-set regions that "it's in Postgres" would
have hidden), nothing pushed/applied without operator nod. Deferred items also in
auto-memory: project_lead_harvest_deferred.md.
</context>

<next_action>
Start with priority #1: research the exact Supabase Edge Function background
(EdgeRuntime.waitUntil) wall-clock limit, THEN refactor lead-harvest to return
202 immediately and run the scrape loop in the background with a logged final
tally — removing the cap-10 / 60s-gateway throughput ceiling. Do not apply
migration 043 (cron) until the scale fix lands.
</next_action>
