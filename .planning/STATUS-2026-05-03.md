# TopFarms — Session Status (2026-05-03)

**Type:** session accomplishments record
**Scope:** drift audit (Stages 1–3.5), reconciliation (BLOCK 1/2/3), frontend simplification (Stage 4 frontend), `rls_auto_enable` capture (Stage 4 finish), planning capture (Stage 5)

**Read with:** `DRIFT-AUDIT-2026-05-03.md` (canonical drift reference), `SUPABASE-TICKET-UPDATE-2026-05-03.md` (support amendment).

---

## Headline finding

Four consecutive migrations (011/012/013/014) discovered in phantom-applied state — registry rows present with full SQL bodies, runtime schema effects absent. Pattern strongly suggests a Supabase platform event (likely DB restore from snapshot) on the `inlagtgpynemhipnqvty` project. All four reconciled this session via Studio SQL with pre-flight guards.

---

## Discovery sequence

1. UXBUG-01 root-cause investigation triggered re-examination of 013 (Step7Preview reads "dropped" columns) → schema check revealed columns weren't actually dropped → 013 phantom-applied confirmed.
2. 014 cascading discovery (`ownership_type` still scalar text).
3. Cron-schedule drift check (Drift Item C) surfaced 011 phantom-applied (`placement_fees` columns + cron absent).
4. HOMEBUG-01 root-cause investigation surfaced 012 phantom-applied (`platform_stats()` RPC absent).
5. Comprehensive drift audit across 9 layers confirmed the phantom range was contiguous (011–014), nothing outside that range.
6. `rls_auto_enable` + `ensure_rls` discovered as undeclared production objects (separate drift class — never in any migration).

---

## What landed

| Action | Mechanism | Verified |
|---|---|---|
| BLOCK 1 — re-apply 013 | Studio SQL + pre-flight + post-verify | wizard columns present across `employer_profiles` / `jobs` / `seeker_profiles` |
| BLOCK 2 — re-apply 014 | Studio SQL + pre-flight + post-verify | `ownership_type` now `text[]` |
| BLOCK 3 — re-apply 011 §1 + §3 + 012 | Studio SQL + pre-flight + post-verify | `placement_fees` columns present, cron registered, `platform_stats()` RPC present |
| Frontend simplification (`d5e8dfc`) | Direct edit | Step7Preview defensive reads removed |
| Migration 021 capture (`daedb41`) | Studio SQL + manual registry insert | `rls_auto_enable` + `ensure_rls` declared, registry row present |
| Drift audit | Read-only MCP across 9 layers | all clean post-remediation |

---

## What's now unblocked

- **Phase 15-02 E2E email test** — `placement_fees` columns + cron + Resend infrastructure all confirmed in place. Still requires `RESEND_API_KEY` set in prod secrets per existing carryforward.
- **Placement fee revenue flow** — was schema-broken since v1.0 ship (011 phantom-applied), now unblocked at schema level.
- **HOMEBUG-01** — root cause identified (012 phantom-applied), remediated this session.
- **UXBUG-01** — original framing was wrong twice; final framing is schema-vs-types drift across the phantom-applied range, now resolved.

---

## What's deferred

- **Smoke tests** — no Playwright runs this session.
- **Stripe production-mode verification** — flagged for pre-launch; carried to Phase 18.
- **Phase 18 perf cleanup** — ~115 advisor lints, deferred.
- **Supabase support response** — ticket amended this session, awaiting platform-event confirmation.
- **Push to origin/main** — held until Stage 5 chain complete; will push after commit 2 lands.

---

## Commits this session

| Commit | Scope |
|---|---|
| `daedb41` | `fix(db): capture rls_auto_enable function and ensure_rls event trigger in migration 021` |
| Stage 5 commit 1 (pending) | `docs(planning): Stage 5 drift capture + status + Phase 18 carryforwards` |
| Stage 5 commit 2 (pending) | `docs(audit): correction note + UXBUG-01 reframe` |

Carried forward from prior session (local at start of this session): `d5e8dfc` (frontend simplification).

Already pushed today: `fe68bc2` (CLAUDE.md §6/§7), `53d44c8` (FROM_EMAIL standardisation), `ae27981` (Stage 1 defensive accommodation reads).

---

## Open items at session end

- Stage 5 commits 1 + 2 to land, then `git push origin main`.
- Supabase support response (when it arrives).
- Phase 15-02 E2E email test (independently scheduled).
- Phase 18 entries planned (see ROADMAP.md §"Phase 18: Tech Debt Cleanup").

---

## Correction — evening 2026-05-03

The morning handoff claimed "DNS records active in Cloudflare with topfarms.co.nz domain verified at Resend." This was wrong. Verified directly via resend.com/domains: domain `topfarms.co.nz` shows **"Not Started"** for all three required DNS records (DKIM, MX/SPF, SPF TXT). The earlier pre-conditions audit in this session inherited the morning handoff's claim and didn't catch the gap; only direct Resend dashboard inspection surfaced it.

Supporting evidence:
- `dig NS topfarms.co.nz` → `gwen.ns.cloudflare.com` + `chuck.ns.cloudflare.com` (Cloudflare is the actual DNS authority)
- CrazyDomains is registrar only; Resend dashboard's "CrazyDomains" provider identification is incorrect
- Records must be added in Cloudflare, not CrazyDomains

**Implication:** Phase 15-02 E2E test was claimed unblocked but is actually still blocked on DNS verification. DKIM=pass headers are §7 evidence requirement for MAIL-01 closure; without them Task 3 cannot satisfy the success criteria.

**Action queued (manual, no Claude involvement):**
1. Add three DNS records to Cloudflare via dashboard (~15 min)
2. Wait for propagation (~minutes to hours)
3. Verify on Resend
4. Run Phase 15-02 in a fresh session

**Documents updated as part of this correction:**
- `.planning/phases/15-email-pipeline-deploy/15-02-PLAN.md` — added top-level `<preconditions>` block surfacing the DNS gap as item 5 (BLOCKING)
- `.planning/ROADMAP.md` Phase 18 — added Cloudflare MCP install entry (item 14) for future DNS / SSL / firewall ops from Claude sessions

**Lesson for handoff hygiene:** future session handoffs should distinguish between "infrastructure deployed" (in-repo / CI / Supabase Functions) and "third-party domain verifications" (Resend, Stripe, etc.). The latter live outside the repo and outside Supabase MCP visibility — they need direct dashboard checks, not inferred status. This is the same methodology gap captured in `SENSE_CHECK_AUDIT_2026-05-01.md` §"Correction Note 2026-05-03" §"Methodology gap" — different surface, same root pattern.
