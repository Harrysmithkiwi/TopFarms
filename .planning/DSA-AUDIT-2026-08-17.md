# DSA + organizing-model audit — 2026-08-17

**Baseline:** `c0e564f`, tree clean. **Read-only:** no file modified, no test run, no migration applied.
**Method:** 19 bounded single-subsystem reviews (fresh read-only agents, two-finding cap, "skip is a
good answer"), each finding then **re-verified by the coordinator against the live database** —
`pg_policy`, `pg_attribute.attacl`, `pg_proc`, `pg_constraint` — not accepted from the agent.

Report artifact: https://claude.ai/code/artifact/e37afbba-7321-4f37-8098-bf863f38a64f

**Corrections made during verification** (recorded so they are not re-litigated):
- **REJECTED** — an agent claimed employer application writes are unconstrained because the policy
  has no `WITH CHECK`. Postgres uses `USING` as the check when omitted; employer ownership IS
  enforced. That finding stands on the status/actor axis only.
- **NARROWED** — `VerifyEmail` was reported as a "silent wrong-role redirect". `ProtectedRoute:63`
  renders `AccessDenied` on a mismatch, so it is NOT a data leak. Impact is a wall on the
  confirm-email screen, not exposure.
- **COVERAGE GAP FOUND** — the initial inventory missed the shared UI-primitive layer (47
  components). Added as subsystem 19 rather than widening a finished boundary; it immediately
  produced a live defect (F-34).

---

## Tier 0 — blocks onboarding / outreach

| ID | Finding | Fix |
|----|---------|-----|
| **F-21** | **Opt-out cannot be recorded for anyone you email.** `lead_suppression` is read only in `_lead_intake` (041:117, 081:60) and written only in `admin_lead_reject` (041:347), which needs a *staging* row. No `admin_lead_suppress` exists; `AdminLeads.tsx:55` offers only new/contacted/onboarded/dead. `docs/OUTREACH-EMAIL.md:52` documents a control that does not exist for promoted leads. Key is also `name\|region\|type` while 081:13 records region null ~1 row in 11. | One RPC writing `lead_suppression` from a `leads` row + a button. Key on name+type. |
| **F-11** | **No employer can complete verification.** 073 revoked `status`/`verified_at` from `authenticated` (live ACL: INSERT `employer_id, method, nzbn_number, document_url`; UPDATE `document_url, nzbn_number`). Four of five writers still send them → 42501. `PhoneVerification.tsx:93` swallows the denial then fires `toast.success('Phone number verified!')`. `EmployerVerification` retries every mount. | Drop `status`/`verified_at` from 4 payloads (`DocumentUpload.tsx:64` is the reference shape). Email/phone need a definer RPC syncing from `auth.users`. |
| **F-22** | **Every role filter on `/jobs` returns zero results.** `FilterSidebar.tsx:21` holds a private snake_case list (`farm_manager`, `head_stockman`…); jobs store display strings from `constants.ts` via `JobStep1Basics.tsx:36`; `JobSearch.tsx:356` does `.eq()` with no mapping. No translation exists anywhere. | Delete the fork; import `ROLE_TYPES`. Net −8 lines, one file. |
| **F-12** | **New employer can land on "Access Denied".** `VerifyEmail.tsx:31` discards `error`; `:39` `?? 'seeker'`. `ProtectedRoute:63` then correctly refuses. Not a leak — a wall on the highest-traffic step. | Read the error; route on a real role or `/auth/select-role`. |

## Tier 1 — security / revenue, before real data

| ID | Finding | Fix |
|----|---------|-----|
| **F-01** | **Suspension gates nothing.** Live: `get_user_role` = `SELECT role FROM user_roles WHERE user_id = p_user_id` — no `is_active`. `_admin_gate` delegates to it → all 51 admin RPCs. Zero edge functions read it. Only consumer is `AuthContext.tsx:42`. Suspended admin can un-suspend themselves. | `AND is_active` in one function body. **Best single first slice** — no policy DDL, inherited by ~30 policies + 51 RPCs. |
| **F-02** | **38 employer columns readable by any authenticated user.** Live policy `employer_profiles: public marketplace read` is `TO anon, authenticated`. Live grants: anon 10 cols, authenticated 38 — incl. `salary_min/max`, `subscription_tier`, `verification_tier`, `rating`, `billing_period`, `user_id`. Re-opens what 040 closed. `stripe_customer_id` correctly excluded. | View to owner-privilege, drop base-table policy, revoke anon. All 11 seeker/anon call sites already use the view. |
| **F-05** | **Placement invoice can double-bill.** `create-placement-invoice`: guard reads `confirmed_at` (:108) which is written at :316 — *after* `finalizeInvoice` (:312) has emailed a payable invoice. Error swallowed at :335, returns success. **Zero `idempotencyKey` in all 17 edge functions.** | Stripe idempotency keys from `application_id` (already UNIQUE). 3 args, no migration. |
| **F-03** | **Seeker can self-set `hired`.** Live policy constrains `seeker_id` only. `VALID_TRANSITIONS` used only at `ApplicantPanel.tsx:162` (render). `pipeline-transitions.test.ts` = 11 `it.todo`, 0 assertions. | `BEFORE UPDATE OF status` trigger with a `(from,to,actor)` table. |
| **F-23** | **Step 5 overwrites the seeker's region.** `SeekerStep5LifeSituation.tsx:79` writes `region: preferred_regions?.[0]`; that list has 8 of 16 regions and is captured in tap order. Drives the 25-pt location dimension. On the profile editor, saving "Life situation" rewrites the Region shown above it. | Delete the `region:` line; derive `PREFERRED_REGION_OPTIONS` from `NZ_REGIONS`. |

## Tier 2 — live correctness defects

| ID | Finding | Fix |
|----|---------|-----|
| **F-14** | **Contact guard covers failed read, not pending.** `useSeekerContact.ts:26` `prefillFailed=false` default IS the full-overwrite branch (:70) → save before fetch settles nulls name+phone. **Pre-existing shape**, preserved by my 2026-08-16 extraction. | Invert to `loaded`, true only on settled non-error. |
| **F-34** | **Salary band cannot be cleared.** `ChipSelector.tsx:57` single mode is `onChange([optionValue])` — no deselect. Multi mode toggles fine. Only caller is optional `min_salary`. | 1 line now; discriminated union where mode *is* the value shape is the real fix. |
| **F-24** | **`storage_path` unconstrained.** Live: `seeker_documents` has 2 CHECKs, **0 triggers**. Seeker-supplied free text, then used by service-role `.remove()` and `.createSignedUrl()`. | `BEFORE INSERT OR UPDATE OF storage_path` trigger asserting first segment = owner `user_id`. Audit legacy rows first (019 backfill). |
| **F-25** | **Retry button that refetches nothing.** `FarmPhotoUpload.tsx:200` clears the flag; the photos effect (`:92`) doesn't depend on `reloadNonce` (only the profile effect at `:61` does). Render guard then hides everything → blank panel. Cause: loading/error/refetch hand-wired 14× in 4 dialects. | Fix the file; then a ~20-line `useLoadState` hook, migrated opportunistically. |
| **F-18** | **Herd-size filter doesn't filter.** `JobSearch.tsx:342` emits `min.lte.500,max.gte.200` into `.or()`; the comma is PostgREST's OR separator → both range buckets near-unfiltered. | Bounds as data, grouping derived from arity. |
| **F-17** | **`q` in no filter registry.** Read at `JobSearch.tsx:289`, sent by `OpenRolesSection.tsx:97`, absent from savedSearch/pills/sidebar → no pill, unsaveable, wrong empty state. 4 more filters (`mentorship`, `vehicle`, `dairynz_pathway`, `dairynz_level`) render+persist and filter nothing. | One filter-descriptor table; delete the 4 dead filters. |
| **F-15** | **Rescore guard omits `pets`/`family`** — the two columns `compute_match_score` reads for accommodation. Whole-row upsert per section save → no rescore for 24h. Also: narrowing `sector_pref` orphans `match_scores` rows read by search AND the operator alert. | `OLD IS NOT DISTINCT FROM NEW` + reconciling DELETE in the cron. **Do not** apply job-side. |
| **F-19** | **No delivery record anywhere.** No notification/email/outbox table exists. `084:31` re-fires on any transition into `active`, and `paused` is employer-reachable → pause/resume re-sends. Resend failure returns 200. | `notification_sends` with `UNIQUE (kind, subject_id, recipient)`; claim-by-insert. |
| **F-04** | **`hired` has 3 writers, `placements` has 1.** `026:74` sets hired with no `placements` reference. | Placement becomes the writer via trigger. **Depends on F-03.** |
| **F-06** | **Fee countable as paid AND written off.** Failure webhook branch has no `paid_at` guard. | `CHECK (paid_at IS NULL OR status='paid')` + `.is('paid_at', null)`. **After F-05.** |

## Tier 3 — model debt / false assurance

`F-27` 108 `it.todo` counted green; 80 in 5 files with **zero** `expect()`; `match-scoring.test.ts`
specifies a recency multiplier 072 removed and another test forbids · `F-13` "complete" is 5 signals,
`onboarding_complete ∧ sector_pref=∅` representable → seeker invisible forever · `F-07` publication =
3 writes/2 tables, guard on wrong table, stuck draft reports itself live · `F-16` not-applicable
asserted to the LLM as `0` → summary claims a shed gap on shed-less jobs · `F-20` explanations never
invalidated · `F-28` bundle gate measures max chunk (105kB/500kB) while total is ~593kB · `F-26`
`analytics_events` has no writer anywhere · `F-29` CV gate has 3 expressions, 2 meanings · `F-30`
rejected verification unrepresentable + terminal · `F-31` 4 routes declared twice · `F-32`
`placement_fees` has 4 tenant keys, no consistency constraint · `F-33` nested unavailable marker →
false red 0% on the briefing.

## Cross-cutting patterns

1. **Two writers, no transaction, guard reads the wrong row** — publication, hire, invoice, contact.
2. **The rule is a comment, not a constraint** — 040 vs 059, `VALID_TRANSITIONS`, 069's "mirrored",
   010's "managed separately", the documented unsubscribe.
3. **Flag defaults to the unsafe branch** — `prefillFailed`, `?? 'seeker'`, nested unavailable.
4. **Column grants cannot separate two tenants sharing `authenticated`** — root cause of F-02.

## Justified skips (completed coverage, not gaps)

`_admin_gate` 51/51 · audit log 20/21 mutations · `normalise()` TS/SQL mirror currently AGREES ·
email template triplication · match weights table · dual seeker tenant key (blast radius) ·
saved-search versioning.
