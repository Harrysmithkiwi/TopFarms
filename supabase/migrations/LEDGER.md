# Migration ledger manifest

Machine-checked by `tests/migration-ledger-drift.test.ts`. **Every `.sql` file in this directory must
have a row here.** CI fails otherwise.

Why this file exists: production migrations are applied through Supabase Studio / the claude.ai
connector (pooler auth is blocked — see `README.md`), and neither writes
`supabase_migrations.schema_migrations`. Between 2026-06 and 2026-07 that silently produced a ledger
holding 45 rows against 64 files. This manifest is the repo-side record that CI can enforce; the
database ledger is the runtime record.

**Reconciled 2026-07-30 (post-Phase-3):** 75 files · 78 ledger rows · 3 documented duplicates. Balanced.

| File | Ledger version | Notes |
|---|---|---|
| 001_initial_schema | `001` | |
| 002_rls_policies | `002` | |
| 003_skills_seed | `003` | |
| 004_employer_profile_columns | `004` | |
| 005_employer_verifications | `005` | |
| 006_jobs_status_and_benefits | `006` | |
| 007_storage_buckets | `007` | |
| 008_job_expiry_cron | `008` | |
| 009_seeker_onboarding | `009` | |
| 010_match_scores_precompute | `010` | |
| 011_placement_fee_followups | `011` | |
| 012_platform_stats_rpc | `012` | |
| 013_phase8_wizard_fields | `013` | |
| 014_ownership_type_array | `014` | |
| 015_phase9_schema | `015` | |
| 016_phase11_backend_features | `016` | |
| 017_notify_job_filled_webhook | `017` | |
| 018_set_user_role_rpc | `20260428043338` | CLI-applied, timestamp version |
| 019_seeker_documents | `20260428053314` | CLI-applied, timestamp version |
| 020_seeker_documents_employer_policy | `20260429031148` | CLI-applied, timestamp version |
| 021_rls_auto_enable_capture | `20260503000000` | timestamp version, name carries `021_` prefix |
| 022_fix_pg_net_http_post_signature | `20260503210000` | timestamp version, name carries `022_` prefix |
| 023_admin_rpcs | `023` | |
| 024_saved_searches | `024` | |
| 025_phase_18_1_fk_indexes | `025` | |
| 026_mark_job_filled_rpc | `026` | |
| 027_match_scores_cleanup_trigger | `027` | |
| 028_pg_net_webhook_secret_headers | `028` | |
| 029_pg_net_webhook_secret_vault | `029` | |
| 030_rls_get_applicants_for_job_rpc | `030` | superseded at runtime by 058 |
| 031_rls_initplan_performance | `031` | |
| 032_doc_verification_queue | `032` | |
| 033_admin_doc_rpcs | `033` | |
| 034_skills_taxonomy_v2 | `034` | |
| 035_admin_rpc_jsonb_fix | `035` | |
| 036_employer_photos_listing_policy | `036` | backfilled 2026-07-30 |
| 037_definer_function_hardening | `037` | backfilled 2026-07-30 |
| 038_marketplace_employer_profiles_view | `038` | backfilled 2026-07-30 |
| 039_admin_analytics_rpcs | `039` | backfilled 2026-07-30 |
| 040_drop_seeker_employer_profiles_policy | `040` | backfilled 2026-07-30 |
| 041_leads_pipeline | `041` | backfilled 2026-07-30 |
| 042_leads_funnel_wiring | `042` | backfilled 2026-07-30 |
| 043_lead_harvest_cron | `043` | backfilled 2026-07-30 |
| 044_leads_rich_fields | `044` | backfilled 2026-07-30 |
| 045_lead_harvest_runs | `045` | backfilled 2026-07-30 |
| 046_leads_keep_categorise | `046` | backfilled 2026-07-30 |
| 047_lead_triage_outreach | `047` | backfilled 2026-07-30 |
| 048_staging_search_broaden | `048` | backfilled 2026-07-30 |
| 049_seed_outreach_config | `049` | backfilled 2026-07-30 |
| 050_briefing_prior_day_counts | `050` | backfilled 2026-07-30 |
| 051_signups_trend_rpc | `051` | backfilled 2026-07-30 |
| 052_placements_summary_rpc | `052` | backfilled 2026-07-30 |
| 053_staging_sortable | `053` | backfilled 2026-07-30 |
| 054_staging_source_filter | `054` | backfilled 2026-07-30 |
| 055_outreach_sortable | `055` | backfilled 2026-07-30 |
| 056_lead_harvest_notify | `056` | backfilled 2026-07-30 |
| 057_fix_admin_list_seekers_contact_join | `20260722232550` | |
| 058_fix_admin_profile_doc_queue_applicants | `20260722232803` | ledger name differs: `fix_admin_profile_and_doc_queue_joins` |
| 059_security_hardening_o4 | `20260723032451` | |
| 060_fix_marketplace_policy_recursion | `20260723032721` | |
| 061_leads_segmentation | `20260723062011` | |
| 062_manual_paste_source | `20260723062752` | |
| 063_leads_bulk_actions | `20260729091412` | |
| 064_lane_a_outreach_worklist | `20260729094845` | |
| 065_revoke_staging_list_public_grant | `20260730010822` | connector-applied, timestamp version |
| 066_phase1_rls_gaps | `20260730024306` | connector-applied, timestamp version |
| 067_employer_verifications_column_grants | `20260730031536` | connector-applied; found by probe P10, not by review |
| 068_phase2_revenue_schema | `20260730051318` | Phase 2 — entitlement ledger, placements, collectibility columns; connector-applied |
| 069_phase2_cv_gate | `20260730051345` | Phase 2 — Option C contact gate + get_applicants_for_job P0-5 fix; connector-applied |
| 070_phase2_revenue_reconciliation | `20260730051408` | Phase 2 — admin_revenue_reconciliation RPC; connector-applied |
| 071_phase2_followup_cron | `20260730051412` | Phase 2 — wires send-followup-emails via pg_cron+pg_net; connector-applied |
| 072_match_scoring_v2 | `20260730055440` | Phase 3 — sector-aware normalisation, recency removed, CHECK + algorithm_version; connector-applied |
| 073_verification_trust | `20260730060339` | Phase 3 — employer self-verification closed + admin queue RPCs + document-view audit; connector-applied |
| 074_fix_verification_queue_jsonb | `20260730060952` | Phase 3 — row_to_jsonb→to_jsonb in 073's queue RPC (same defect 058 fixed in 033); connector-applied |
| 075_privacy_retention | `20260730063159` | Phase 3 — lead retention windows, identity-doc purge, admin_delete_account; connector-applied |
| 076_storage_purge_via_api | `20260730063416` | Phase 3 — 075's SQL storage deletes were inert (storage.protect_delete); split to Edge Fn + ordering guard. Also carries `list_user_storage_objects` (`20260730063632`) |
| 077_seeker_contacts_truth | `20260730064300` | Phase 3 — the paywalled table was empty for 3 of 4 seekers; trigger + backfill + first/last name; connector-applied |
| 078_audit_log_outlives_the_actor | `20260730065000` | Phase 3 — admin_id was NOT NULL + ON DELETE SET NULL, so no admin could ever be deleted; FK dropped |
| 079_training_demand | `20260807032652` | Go-live S1 — training demand capture keyed to the skills taxonomy; own-row RLS + `admin_training_demand_summary` behind `_admin_gate()`; connector-applied, verified via pg_catalog |
| 080_revoke_compute_match_score_from_authenticated | `20260810220505` | go-live M4 — the two SECURITY DEFINER match functions took an arbitrary seeker_id with EXECUTE granted to `authenticated` and no auth.uid() check, exposing the §1.4 breakdown to any signed-in user. Grant was vestigial (blanket re-grant in 037). No caller in src/ or supabase/functions/; all 5 internal callers are definer-owned by postgres. service_role retained; connector-applied |
| 081_dedupe_sees_staging | `20260811013748` | Leads — the "Possible duplicate" badge had never fired: the fuzzy pass searched `leads` (2 rows) not `lead_staging` (93), and the exact fingerprint keyed on `region`, which the harvester nulls ~1 row in 11. Two-tier match (>=0.9 region-blind, 0.6–0.9 region-tolerant) + backfill; 13 rows flagged across 7 genuine pairs; connector-applied |
| 082_staging_type_filter_and_signup_attribution | `20260811022630` | Leads — `p_type` filter so the employer and seeker queues can share `lead_staging` without sharing a screen, plus a `signed_up` flag per row joining `auth.users.raw_user_meta_data->>'ref'` (the `?ref=` attribution loop). DROP+CREATE, not OR REPLACE: a new signature would have created an overload and PostgREST refuses to choose. Grants re-applied; connector-applied |
| 083_seeker_documents_employer_policy_to_authenticated | `20260812103059` | go-live S1/S2 batch — the employer read policy on `seeker_documents` was the only policy on that table without a TO clause, so it was TO PUBLIC and evaluated for `anon` on every anonymous request. Not exploitable: the predicate opens with `get_user_role(auth.uid()) = 'employer'`, which is NULL for anon. `ALTER POLICY … TO authenticated` only — the USING expression is not restated, since retyping a predicate that gates CVs is a chance to get it wrong; connector-applied |
| 084_notify_job_matches_webhook | `20260813120337` | go-live — operator match-alert closing the waitlist promise gap's first half: `on_job_activated_notify_matches` fires on INSERT-as-active or transition-to-active, pg_net → `notify-job-matches` Edge Fn, which emails the operator the matched-seeker list (name/email/phone/region/score from `seeker_contacts` + `match_scores`). Vault triple (supabase_url/service_role_key/WEBHOOK_SECRET) reused from 029 — no new secret. Seeker-facing sends stay manual by design (NEXT-SESSION.md: "discovering the copy beats automating it"); connector-applied |
| 085_employer_self_verification_rpcs | `studio-applied-20260817` | audit F-11 — the trust ladder was unclimbable: 073 revoked `status`/`verified_at` from `authenticated` and four of five client writers still sent them, so every write returned 42501 and no employer ever reached `basic` (which IS email-verified). Adds two SECURITY DEFINER RPCs scoped by `auth.uid()`: `employer_sync_self_verifications` mirrors `auth.users.{email,phone}_confirmed_at`, and `employer_record_farm_photo` self-verifies a farm photo (operator decision — the identity rung stays admin-reviewed, so `fully_verified` cannot be self-asserted). `nzbn`/`document` keep writing directly with `status` omitted so the DEFAULT `pending` stands; Studio-applied because both write-capable connectors had lapsed auth on 2026-08-17 |
| 086_employer_submit_verification_rpc | `20260816223150` | audit F-11 **reopened** — 085 was necessary but not sufficient. Driving the employer flow on live prod 2026-08-17 showed `nzbn` AND `document` both returning 42501 on the **first** submit, not just a resubmit: the payload was fine, the **upsert form** was denied. PostgREST renders `onConflict` as `ON CONFLICT (employer_id, method) DO UPDATE SET …` with the conflict keys in the SET list, and Postgres checks UPDATE privilege at PLAN time, while `authenticated` holds INSERT-only on both (`pg_attribute.attacl`). Proven on prod: identical payload → plain INSERT 201, upsert 42501. Both identity rungs were dead, so `fully_verified` was unreachable from the UI, and 085's claim that `DocumentUpload` "already worked" was wrong. Adds `employer_submit_verification(method, nzbn, document_url)` SECURITY DEFINER, scoped by `auth.uid()`, `status` hard-coded to `'pending'` so the admin queue still rules — the review step is preserved by denying the CALLER a say in `status`, not by keeping the write client-side. Also fixes a stale-verdict bug the direct write could not: a resubmission now clears `verified_at`/`verified_by`/`reviewed_at`/`rejection_reason` and reopens the row, where before a **rejected** employer resubmitting kept `status='rejected'` attached to brand-new evidence. Connector-applied |
| 087_lead_opt_out | `20260817002052` | audit F-21 (Tier 0, **compliance**) — an opt-out could not be recorded for anyone you email. `lead_suppression` had one writer, `admin_lead_reject`, which needs a *staging* row; once a lead was promoted there was no control at all, so `docs/OUTREACH-EMAIL.md:52` documented a procedure that could not be executed. Adds `admin_lead_suppress(lead_id, reason)` — admin-gated, writes the suppression, marks the lead dead, audit-logged. **Also fixes the half an RPC alone would not:** suppression was keyed on `_lead_fingerprint` = name\|region\|type, but `region` is null ~1 row in 11 (081:13), so a farm suppressed with a region sailed through `_lead_intake` when re-harvested without one. New `_lead_suppression_key(name, type)` — dedupe keeps its region-bearing fingerprint, since dedupe wants the opposite bias. Accepted trade-off: same-named farms in different regions now suppress together (false positive beats emailing someone who opted out). Timing deliberate — applied at 0 leads contacted, so the gap cost nothing. Proven on prod: suppressed+region → suppressed, **suppressed+region NULL → suppressed** (the leak), punctuation/case variant → suppressed, different name → inserted, different type → inserted. Connector-applied |
| 088_suspension_actually_gates | `20260817002709` | audit F-01 (Tier 1) — `get_user_role` ignored `is_active`, so suspension gated **nothing**: a suspended admin could call every admin RPC including the one that un-suspends them. Adds `AND is_active`, inherited by 22 RLS policies and, via `_admin_gate`, every admin RPC — no policy DDL. **The trap, and why both functions are in one migration:** the predicate makes a suspended user's role NULL; all 22 policies compare with `=` and fail closed, but `_admin_gate` used `!= 'admin'`, and `NULL != 'admin'` is NULL, which does not fire an `IF`. Verified by scanning every `pg_proc` body for a negated comparison — exactly one hit. Shipping the one-liner alone (as the audit described it) would have opened the gate to **any authenticated user with no `user_roles` row**; demonstrated against a temp replica of the old body, which returned "PASSED (gate open)" for both cases. `_admin_gate` moves to `IS DISTINCT FROM`. Proven on prod: suspended admin REFUSED, reactivated PASSED, **real admin PASSED**, employer REFUSED, no-role REFUSED. Connector-applied |
| 089_employer_profiles_owner_privilege | `20260817003136` | audit F-02 (Tier 1) — the `public marketplace read` policy was `TO anon, authenticated` while `authenticated` held column grants on **38** columns, so every seeker could read `salary_min/max`, `subscription_tier`, `verification_tier`, `rating`, `billing_period` and `user_id` for any employer that had ever posted a job. Re-opened what 040 closed. **The audit's prescription would have broken the marketplace:** it said drop the base policy, but `marketplace_employer_profiles` is `security_invoker=true`, so it runs under the caller's RLS and would have returned zero rows to anon and every logged-in seeker. Revoking the 28 extra columns does not work either — grants are per-role and an employer reading their own dashboard IS `authenticated`. Instead the view flips to owner rights and its own `WHERE` (employer has a job in active/paused/filled/expired) becomes the boundary; base table keeps only `employers manage own`. Also revokes anon's base-table SELECT and the view's stray `arwdDxtm` write grants. Proven on prod with a temporary paused job: anon sees the view, seeker sees `farm_name` via the view, seeker base-table count **0**, employer still reads own row incl. `salary_min`. Connector-applied |
| 090_seeker_contract_type_pref | `20260817112634` | seeker gap G-1 — `seeker_profiles` had **no employment-type field at all**, so a seeker could state the role they want but never the terms. Found by reading six real job-seeker posts saved from NZ farming Facebook groups: **four of the six want relief or part-time work**, which is the shape of this labour market and was not the shape the schema assumed. `jobs` already carried `contract_type`/`hours_min`/`hours_max` and `/jobs` already filtered on it — the asymmetry was one-sided. `role_type_pref` only partly covered it, because 'Relief Milker' is a ROLE and "a permanent job OR relief" is a TERM, which three of the six actually say. Values mirror `jobs_contract_type_check` exactly so preference and listing compare without a mapping layer — the same fork that made every role filter on `/jobs` return zero until F-22. Nullable, no default, matching `sector_pref`/`role_type_pref`: NULL is "not stated", not "wants nothing". **Not wired into `compute_match_score`**, which reads neither `role_type` nor `contract_type` today; this sharpens the profile, it does not yet change ranking. Connector-applied |
| 091_employer_inz_accreditation | `20260817221118` | seeker gap G-13 — across a 23-post corpus of real NZ farming Facebook posts, 30% are visa-touched and the recurring question is **not** the one `jobs.visa_sponsorship` answers. That boolean means "will you sponsor"; **AEWV accreditation is a specific INZ status a farm either holds or does not, and without it a migrant cannot apply at all.** Adds `inz_accredited`, `inz_accreditation_expires` and a reserved `inz_accredited_verified_at`, plus a derived `accredited_employer` on the marketplace view. **Expiry is required by CHECK**: INZ accreditation always lapses (12mo then 24mo renewals) and a lapsed claim is worse than none — a migrant who relies on it loses an application fee and possibly a season — so the projection recomputes per read and a stale claim stops advertising itself with nobody having to switch it off. The raw date never leaves the table; only the derived flag does. **Self-declared** — INZ list-API verification is feasibility-confirmed and NOT built, so this deliberately sits OUTSIDE `employer_verifications`: it is a filterable attribute, not an earned badge, and must not make `fully_verified` self-assertable again (F-11). Note the view is recreated in full because `CREATE OR REPLACE VIEW` drops reloptions and grants — 089's `security_invoker=false` and read-only grants are re-applied, and losing them would take the marketplace dark for anon and every logged-in seeker. Connector-applied |
| 092_seeker_opt_out_holds_across_handles | `20260818003139` | Phase B1 — **compliance**, two halves. (1) The seeker lane had **no opt-out control at all**. 087 closed this for promoted `leads`, but the seeker lane never promotes: outreach state (`sent_at`, `responded_at`, `outreach_status`) lives on `lead_staging` and `AdminSeekerStaging` offered exactly one action, copy-a-link. A reply of "stop" to a DM had nowhere to be recorded. `admin_lead_reject` already accepted `p_suppress`; it had no caller on that screen, so the fix is a UI wiring, not a new RPC. (2) Suppression keys on `display_name`, which for a seeker is a **Facebook handle**. The corpus already holds one person posting the same text under two of them, and the trigram check does not save you — `similarity('Jess M','Jessica Moore')` is well under the 0.6 suspect threshold, so the second handle is not even flagged as a possible duplicate. Adds `_lead_body_key(text)` = md5 of the normalised (lowercased, non-alphanumerics stripped) post body, **NULL under 120 normalised chars** so a short generic post cannot become identity and suppress an innocent stranger; NULL means "fall back to the name key", i.e. exactly the pre-092 behaviour, and every use is guarded on `IS NOT NULL`. `admin_lead_reject` writes a **second** `lead_suppression` row under the body key for seeker rows — two rows rather than one composite, because either alone must be sufficient at read time (same handle + edited text → name key; new handle + same text → body key). **Scoped to the seeker lane on purpose:** employer dedupe works (127 rows, 15 suspects) and syndicated or agency-templated ads genuinely repeat verbatim across distinct farms, so a body key there would merge real leads. Timing deliberate again — prod holds **zero seeker rows**, so like 087 this is applied at the one moment it costs nothing. Proven on prod: two handles with case/punctuation/whitespace variance → identical key, one word changed → different key, 36-char generic post → NULL, NULL input → NULL. `fb_manual_capture` rows (the PasteCapture path the seeker lane uses) have a median 1315-char excerpt, so the floor never bites in practice. **Known gap, not closed here:** `admin_lead_suppress` cannot write a body key because `leads` carries no `raw_excerpt` and no staging reference — correct while only the employer lane promotes; carry the excerpt across if seeker rows ever do. Connector-applied |

| 093_match_score_v3 | `20260818070058` | Phase C — match scoring v3, gates x points. Proposal + reasoning: `.planning/MATCH-SCORE-V3-PROPOSAL.md` (operator-approved). v2 read NEITHER `role_type` NOR `contract_type`, and had three further structural faults. **(a)** Its LARGEST weight was on its least important dimension: `shed_type` 25 -- more than location or skills -- while the role scored **zero**. **(b)** `v_max` was per-pair (55 base, +25 shed, +20 skills, +5 couples), so a sparse listing was scored out of 55 and a thorough one out of 100; a seeker fitting on location, accommodation, salary and visa scored **100% on the EMPTY listing**. Seeker search, the applicant list and the match-alert email all `ORDER BY total_score DESC`, so **the emptier listing won** -- while we ask employers to fill in more. **(c)** Everything was additive, so nothing could be a dealbreaker: a migrant needing sponsorship on a non-sponsoring job lost 5 points out of ~100. **(d)** Entry-level was pure deficit, and ~35% of the 23-post corpus is green. v3 = GATES (multiplicative: visa 0.15, terms 0.40, accommodation 0.25, sector 0.35) x POINTS (fixed denominator 100: role 18, skills 15, location 15, terms 12, accommodation 10, experience 10, salary 8, timing 6, herd 3, shed 3). An unstated field pays **60%** of its weight to both sides -- a vague listing lands mid-pack, cannot beat a real fit, and cannot be gamed by omission. Experience is **two-sided**: it peaks at the requirement and decays both ways, floored at 4 for the over-qualified. Sector is a gate rather than 6 points because `trigger_recompute_job_scores` already filters `WHERE NEW.sector = ANY(sp.sector_pref)`, so points there would be a constant. Adds `get_adjacent_roles()` and `_herd_bucket_overlaps()`. **Proven on prod inside a rolled-back transaction** (4 corpus-shaped seekers x 5 jobs): relief-only seeker scores **92** on the relief job and 21-29 on everything else; 10-year herd manager 85 on the manager role and 15 on entry calf rearing; green backpacker tops out on the **entry calf role at 68**; and the deliberately BARE listing never wins for anyone (25 / 62 / 64). That probe caught a real design error before it shipped: the first draft also gated `working_holiday` and `student`, which scored every backpacker 2-10 against every job -- a working-holiday visa IS the right to work here. Connector-applied, then AMENDED in place by a follow-up `CREATE OR REPLACE` for the visa-gate fix (execute_sql, so no second ledger row -- the disk file and the live `pg_proc` body both carry it; verify by grepping the live body for `needs_sponsorship`). |

| 094_rescore_guard_and_explanation_invalidation | `20260818071443` | Phase C4 — audit **F-15 + F-20 in ONE migration**, as the audit records: both rewrite the same two rescore trigger functions, so applied separately the second silently reverts the first. **F-15:** both guards early-return when "nothing relevant changed", and both column lists had fallen behind the score. The seeker list named **8** columns while `compute_match_score` v3 (093) reads **15** — the audit named `pets` and `family` (the two the accommodation dimension reads), and v3 added five more including **`role_type_pref`, the largest dimension at 18 points**, so after 093 a seeker could change the role they want and their score would not move. The job list was equally stale against v3's `role_type`, `contract_type`, `min_dairy_experience`, `seniority_level`, `start_date` and `herd_size_min/max`. The audit's "do not apply job-side" was correct against v2 — that trigger already had the `IS NOT DISTINCT FROM` pattern, so there was no guard to ADD — but it could not anticipate v3 widening what the job side reads, and leaving it stale is the same defect. `views_count` stays out of both: it moves on every page view and a rescore is a cross join over every seeker. **F-15 second half:** narrowing `sector_pref` orphaned rows the seeker had explicitly excluded, and `match_scores` is what job search reads AND what the operator match-alert emails from — so they were still shown, and still emailed about, that sector. Reconciling DELETE placed in the trigger rather than the cron the audit suggested: the trigger already holds the seeker id and a cron leaves a window in which search is wrong. **F-20:** `explanation` is LLM prose about a specific score and no writer cleared it, so a rescore left last week's reasoning on this week's number — and after 093 every explanation in the table describes v2 weights. Fixed with ONE `BEFORE UPDATE` trigger on `match_scores` rather than three ON CONFLICT clauses: there are three writers and a fourth would not know the rule. `generate-match-explanation` writes `explanation` alone, so it does not trip its own guard. **Proven on prod inside a rolled-back transaction:** changing `role_type_pref` moved the score **82 -> 64** (exactly the 18-point role dimension) and dropped explanations **2 -> 0**; narrowing `sector_pref` took rows 2 -> 1 with **0 sheep_beef orphans**; and a control edit to an irrelevant column KEPT the explanation. Connector-applied |

| 095_job_check_evidence | `20260818073625` | Phase D5 — AEWV Job Check evidence from the applicant pipeline. **The shape came from D1, not from the plan.** The campaign map assumed the compliance artefact was an advertising record (dates + a screenshot). Verified against live INZ 2026-08-18 (`docs/immigration/05-job-check-advertising-verified.md`) it is not: INZ asks the employer to report **counts about the applicant pipeline** — candidates applied, NZ citizens/residents applied, assessed suitable, hired, and at ANZSCO skill level 4-5 why any New Zealander was not suitable. TopFarms already holds every one of those numbers, so **the compliance artefact is a by-product of a board doing its ordinary job** — which is the wedge: a farm needs this document in order to hire the migrant they have already chosen. Adds `job_check_evidence(job_id)`: advertising dates, INZ **listing-content gaps**, and pipeline counts. The content check matters because only `contract_type` is NOT NULL on `jobs`, so a farm can publish a listing that cannot support a Job Check and nothing tells them. **Deliberately does NOT map our statuses onto INZ's category "assessed as suitable"** — that is a legal judgement and belongs to the practising-certificate holder (IALA s 11), not to a SQL function; it returns pipeline facts per status and the answer is composed above it. States no single advertising duration (14 days at ANZSCO 1-3, 21 at 4-5, which is most farm work) and does not imply the Work-and-Income good-faith step is covered, because that happens entirely outside TopFarms. Residency is labelled self-declared. Owner-or-admin only; anon revoked. **Gotcha found on prod:** `v_gaps := v_gaps \|\| 'text'` raises 22P02 — plpgsql parses the right side as an ARRAY LITERAL, so any label containing a comma or parenthesis fails at runtime, only for listings that actually have that gap. Uses `array_append`. Proven in a rolled-back transaction: bare listing -> 5 gaps / rule not met; complete listing -> met; 3 applicants -> 1 NZ citizen/resident, 1 not stated, 1 hired (the migrant), 0 NZ hired. Connector-applied |

| 096_paid_fee_cannot_be_written_off | `20260818074214` | Audit **F-06**, applied AFTER F-05 as the audit records — F-05 stops a placement being invoiced twice, this stops one invoice being counted two ways. `stripe-webhook` records `paid_at` on `invoice.payment_succeeded`; on `payment_failed` / `marked_uncollectible` it wrote `stripe_invoice_status` with **no check on `paid_at`**. Stripe delivers events out of order and retries them, so a late or replayed failure event could set `uncollectible` on an invoice that HAS been paid — the fee then counted as **paid and written off at the same time**, and `admin_revenue_reconciliation` reads both. Webhook fix is `.is('paid_at', null)` on that update: a FILTER, not a raise, so a stale event is a no-op returning 200 rather than making Stripe retry a doomed event forever; it also logs the ignored event, because silently discarding it would hide a genuine ordering problem. This CHECK is the backstop for every OTHER writer (admin waive, a future refund handler, a hand-run reconciliation) — a filter protects one call site, a constraint protects the column. Verified before applying: 0 rows, 0 with `paid_at`, 0 that would violate, so no NOT VALID needed. Proven on prod in a rolled-back transaction: writing off a PAID fee **rejected by the constraint**; the same update filtered on `paid_at IS NULL` a **clean no-op**; control, an UNPAID fee still write-off-able. Connector-applied |

| 097_application_status_state_machine | `20260818074640` | Audit **F-03** — a seeker could promote themselves to `hired`. The live policy `applications: seekers update own` constrained `seeker_id` and **nothing else**, and `hired` is not a label: it is the trigger for the placement/billing path. The employer policy was no better — a USING clause and **no WITH CHECK** — so an employer could move an application anywhere, including backwards. `VALID_TRANSITIONS` in `src/types/domain.ts` was used in exactly ONE place (`ApplicantPanel.tsx:169`) and only to RENDER buttons; nothing had ever enforced it, and `tests/pipeline-transitions.test.ts` was **11 `it.todo` with 0 assertions**, so the gate reported green over an unenforced state machine (also an instance of F-27). Adds `application_status_transitions(from, to, actor)` as DATA plus a `BEFORE UPDATE OF status` trigger. **The actor column is the half VALID_TRANSITIONS cannot express** — without it a seeker could still hire themselves, just along an approved edge. Two things the constant does not describe are encoded from SHIPPED behaviour rather than from the constant: **re-apply** (`declined/withdrawn/hired -> applied`), because the apply path is an UPSERT and `ExpandableCardTabs.tsx:30` deliberately re-enables Apply on terminal states — encoding only the constant would have broken it; and **withdrawal**, because `withdrawn` is modelled, rendered by two components and handled by `useAppliedStatuses` while VALID_TRANSITIONS has no path INTO it, a status nobody could enter. Admin rows are derived by `SELECT DISTINCT` so admin is the union of the two human actors and cannot drift ahead of them — an admin marking someone hired still bills a farm. `auth.uid() IS NULL` passes through, deliberately and visibly: `mark_job_filled` writes `hired` as a definer function (026:74) and re-gating it here would break the pipeline and teach the next person to bypass the trigger. Proven on prod in a rolled-back transaction: **seeker -> hired REJECTED**; seeker withdraw ALLOWED; seeker re-apply ALLOWED; **employer applied -> hired REJECTED** (a pipeline skip the audit did not name); employer walking the real pipeline to hired ALLOWED. Connector-applied |

| 098_placement_follows_the_hire | `20260818075613` | Audit **F-04**, applied AFTER F-03 (097) as the audit records — attaching a placement before 097 would have hung it off a status any seeker could set on themselves. `hired` had **three writers** and `placements` had **one**: `ApplicantDashboard.tsx:470` and `mark_job_filled` (026:74) both set `hired` and touched nothing, while `create-placement-invoice` upserted `placements` only when the invoice flow ran. So a hire recorded through **mark job filled — the natural path for an employer closing a vacancy — produced a `hired` application and NO placement row**, and `placements` is where `started_on`, `employer_confirmed_at` and `seeker_confirmed_at` live, so the record that the hire actually happened did not exist for that path. Fix: the placement follows the STATUS, not the caller — one AFTER trigger on the transition into `hired`, so all three writers converge and a fourth cannot forget (same shape as 094's explanation trigger, same reason: a rule spread across call sites is a rule waiting to be missed). Also fires on INSERT, for an application created directly as hired by an import or a hand-fixed row. The row is created **bare**; `ON CONFLICT DO NOTHING` so `create-placement-invoice`'s upsert still owns `employer_confirmed_at` — inserting it here would claim an employer confirmed something they had not. Leaving `hired` does NOT delete the placement: 097 permits `hired -> applied` because re-apply is real, but a hire that happened is a fact about the past. Verified before applying: 0 hired, 0 placements, 0 divergent — no backfill. Proven on prod in a rolled-back transaction: before 0; mark-filled-style hire **1**; invoice upsert after it **1 row / 1 confirmed**; re-apply still **1**; INSERT-as-hired **1**; control `applied -> review` **0**. Connector-applied |

| 099_storage_path_belongs_to_its_owner | `20260818080451` | Audit **F-24** — `seeker_documents.storage_path` is **seeker-supplied free text** (`DocumentUploader.tsx:126` builds it client-side, `:146` inserts it) and the table had 2 CHECKs and **zero triggers**. Storage RLS does key on the owner — both `seekers view own documents` and `seekers delete own documents` require `(storage.foldername(name))[1] = auth.uid()::text` — but **two SERVICE-ROLE paths read `storage_path` out of the row and act on it, bypassing storage RLS entirely**: `get-applicant-document-url:330` `.createSignedUrl(...)` and `admin-purge:115` `.remove(...)`. So a seeker could point their own document row at ANOTHER seeker file and have it (a) served as a signed URL to an employer they applied to, or (b) deleted. Read and destroy, through a column the client writes. Adds a `BEFORE INSERT OR UPDATE OF storage_path` trigger — **UPDATE matters as much as INSERT**, since writing a valid row and repointing it later is the easier attack. Compares the first segment against `seeker_profiles.user_id`, **not** the `seeker_profiles.id`, because the user id is what the storage policies compare against; a trigger agreeing with the table instead of with storage would accept rows storage then refuses. A NULL path passes (the purge flow nulls it after removing the object), and a seeker with no profile is REJECTED rather than sailing through on a NULL comparison — the same trap as F-01's `!= 'admin'`. Verified before applying: **0 rows**, so nothing to grandfather and the 019 backfill the audit warns about left nothing behind. Proven on prod in a rolled-back transaction: own path ACCEPTED; another seeker path on INSERT **REJECTED**; repointed by UPDATE **REJECTED**; `seeker_profiles.id` used as the prefix **REJECTED**. Connector-applied |

| 100_manawatu_macron | `20260818082739` | **Not in the DSA audit** — found while closing F-17 and confirmed against prod data. The same region was spelled two ways and **both were in production**: `lead_staging` held **6 rows `Manawatū-Whanganui` and 6 rows `Manawatu-Whanganui`**, a 50/50 split of one region into two buckets. That is precisely the defect `leadGeo.ts`'s own header describes fixing BETWEEN the two edge functions — it canonicalised to the macron, and nobody noticed the APP used the other form. **Five sources disagreed:** `src/lib/constants.ts` (no macron), `JobStep1Basics.tsx` and `Step2FarmDetails.tsx` (each with their OWN hand-written copy, no macron), `leadGeo.ts` (macron), migration 061 (macron), and `get_adjacent_regions` from 009 (no macron). It matters because `compute_match_score` compares regions by **exact string equality** and `get_adjacent_regions` returns strings compared the same way — a seeker in one spelling never matches a job in the other, and nothing reports the contradiction. **The macron wins**: correct te reo in a NZ farming product, already what the extractor produces, already what 061 uses. `get_adjacent_regions` rewritten, with a **transitional `WHEN 'Manawatu-Whanganui'` arm** so a stray legacy row resolves to the same neighbours instead of returning an empty array and scoring 0 on location. Existing data normalised across `lead_staging`, `leads`, `jobs`, `seeker_profiles`, `employer_profiles`. The two duplicate app lists are deleted in the same commit and now import `NZ_REGIONS` — two copies is how this diverged, a third would do it again — and `tests/lead-vocab-parity.test.ts` now asserts `constants.NZ_REGIONS` equals `leadGeo.NZ_REGIONS` in order. Verified after applying: **0 no-macron rows remain, 12 macron rows in staging**, adjacency resolves identically for both spellings, and Waikato's neighbours contain the macron form. Connector-applied |
| 101_inz_register_check | `20260818210121` | **D4 Stage 1** — `091` shipped `inz_accredited` as a self-declared claim and reserved `inz_accredited_verified_at` against the day it could be checked. Stage 0 of the D4 work order established that day by opening the register in Chrome and watching the network panel (`docs/immigration/06-inz-register-verification.md`): **it IS keyed on NZBN** — `POST /list-api/getAPIResults/` returns `employerName`/`tradingName`/`nzbn`/`expiryDateOfAccreditation` for a 13-digit query, so the fuzzy name-matching problem `05` feared does not exist — **and INZ's terms of use forbid us querying it**: "scraping… automation, or any similar data gathering, extraction or monitoring method", access "via standard web browsers only, unless we agree otherwise". `robots.txt` not disallowing `/list-api/` is not a permission. So **no Edge Function, no inline widget** (there is also no CORS header and no query-string deep link) — an admin opens the register in their own browser and this migration remembers what they found. Adds `admin_record_inz_register_check(uuid, boolean)`: confirms → `inz_accredited_verified_at = now()`; does-not-confirm → `inz_accredited = false` **and** the timestamp cleared, because a stale "verified on" beside a withdrawn claim reads as our assurance. **The stated expiry is deliberately preserved** and **`jobs` is not touched** — a refusal most often means the employer opted out of publication (INZ's own page says so) or one of 13 hand-typed digits was wrong, and a typo and a genuine miss return the identical HTTP 400; the harm was the claim, so the claim is what is removed. **Nothing here touches `employer_verifications`** — verified on the live `pg_proc` body, not asserted — so accreditation cannot become a trust-ladder rung (F-11) and `tests/trust-ladder.test.ts` holds by construction. `admin_list_verification_queue` gains the three `inz_*` columns plus `inz_register_checked_at` / `inz_register_confirmed`, read back out of `admin_audit_log` through a LATERAL rather than a second timestamp column: a refusal has no natural home on the row (`inz_accredited=false, verified_at=NULL` is byte-identical to an employer who never claimed anything), and the log already carries it with `(target_table, target_id, created_at DESC)` indexed. **Proven on prod inside a rolled-back transaction, and the first probe caught a real defect**: two audit rows written in one transaction share `now()` (the TRANSACTION start time), so `ORDER BY created_at DESC` tied and the queue reported a refusal as `confirmed=true` — fixed with an `al.ctid DESC` tiebreaker. Second probe green end to end: claim-only → `accredited_employer=t`; confirm → `verified_at` set, queue `confirmed=true`; refuse → `inz_accredited=f`, expiry **preserved**, `verified_at` NULL, marketplace flag **f**, queue `confirmed=false`, and the employer's **active job listing still there**; the gate rejects unauthenticated, an employer caller, NULL `p_confirms`, an unknown employer, and confirming a claim nobody made. **No second derived column on `marketplace_employer_profiles`** — nothing renders `accredited_employer` to a seeker yet, so a "verified" flag would have no reader; it lands in the same commit as the badge. **AMENDED in place** by a follow-up `COMMENT ON COLUMN` pair (execute_sql, so no second ledger row): `091`'s comments said "Always NULL today" and "the INZ list API is not built", both false the moment this landed, and a stale comment on a trust column is worse than none because the next reader takes it as the contract. Verify by reading `col_description` on `employer_profiles.inz_accredited_verified_at`. Connector-applied |
| 102_notification_sends | `20260819002425` | Audit **F-19** — **no notification, email or outbox table existed anywhere**, so every sender decided whether to send from DERIVED STATE (a status transition, a due flag) rather than from whether it already sent. Derived state repeats; a send does not un-happen. **The one with a blast radius is `notify-job-filled`**: `handle_job_filled` fires on `OLD.status IS DISTINCT FROM 'filled' AND NEW.status = 'filled'` — verified on the live `pg_proc` body — and the function then emails **every unresolved applicant**, so an employer who fills a job, reopens it because the hire fell through, and fills it again sends all of them "this job has been filled" twice. `handle_job_activated` has the identical shape and `paused` IS employer-reachable, so pause→resume re-sent the match digest; that one goes to the operator alone, so it was noise rather than harm. **Claim-by-insert**: `INSERT … ON CONFLICT DO NOTHING RETURNING id` hands a row to exactly one caller — atomic, no lock, no read-then-write race, which matters because pg_net can deliver the same webhook twice. **The unique index is PARTIAL on `failed_at IS NULL`, and that is the whole design**: claimed and sent rows sit in the index and block a duplicate; a FAILED row leaves it, so a retry can claim again *without deleting the evidence that the first attempt failed*. A plain unique index would have forced a choice between losing the failure record and never retrying — and the audit flagged "Resend failure returns 200" in the same breath as the missing table because they are one problem. `lower(recipient)` because an address differing only in case is the same inbox. **No FK to `jobs`**: the record of having emailed someone must outlive the thing it was about, and `ON DELETE CASCADE` would erase exactly the history this exists to keep. RLS on with **zero policies** (deny-all) — a seeker must not read who else was emailed about a job. **Proven on prod inside a rolled-back transaction, 8/8**: first claim wins; the repeat fill→active→fill is refused; a case-differing address is refused; a second applicant and a different `kind` are each allowed; a SENT row blocks forever; a FAILED row permits a retry and survives as history; and `sent_at`+`failed_at` together is rejected by CHECK. Two senders deliberately UNCHANGED because they already hold a record: `send-followup-emails` (guards `followup_7d_sent`/`followup_14d_sent`) and `send-document-status-email` (admin-pressed; re-sending is the documented operator retry path, so a claim there would break an affordance, not a defect). Note: `service_role` already holds ALL privileges here via Supabase's project-wide default privileges, so the narrower `GRANT SELECT, INSERT, UPDATE` in this file is additive, not a ceiling. No purge cron — one row per email at a scale of tens. Connector-applied |
| 103_inz_columns_readable_by_owner | `20260819023127` | **Not in the DSA audit** — found by reading a Sentry alert (`TOPFARMS-WEB-7`, 2026-08-18) that could not name its own cause. `059` replaced the table-level SELECT grant on `employer_profiles` with an explicit **per-column list**; `091` then added `inz_accredited`, `inz_accreditation_expires` and `inz_accredited_verified_at` with a plain `ALTER TABLE ADD COLUMN`, which inherits nothing from it. INSERT and UPDATE were untouched by 059, so the result is an **asymmetry rather than a lockout**: the onboarding wizard SAVES an accreditation claim and then cannot read it back. `EmployerOnboarding.tsx:96` names both columns in its explicit select list, so a returning employer's resume-load raises **42501**, the Phase 5.6 guard correctly refuses to treat a failed fetch as "no profile", and the wizard stops on an error screen. **Invisible only because prod holds zero employer profiles** — there is no row to fail on, and the first real employer who closes the tab and comes back would have hit it, i.e. step 3 of the pre-launch order. Confirmed on the live catalog with `has_column_privilege('authenticated', …)` before writing anything: exactly **four** columns on the table are unreadable — the three `inz_*` and `stripe_customer_id` — so this is the whole gap, not one instance of it. **`inz_accredited_verified_at` deliberately NOT granted**: `AdminDocumentsQueue` reaches it only through `admin_list_document_queue`, a SECURITY DEFINER RPC behind `_admin_gate()` (verified — every fetch on that page is `supabase.rpc`, no direct `.from()`), and D4 Stage 1 ruled nothing renders verified accreditation to a seeker yet, so granting the timestamp would widen the read surface for a reader that does not exist. **`anon` untouched**: public accreditation is the computed `accredited_employer` boolean on `marketplace_employer_profiles`, so anonymous visitors read the verdict and never the claim. Proven on prod in a rolled-back transaction (before/after `has_column_privilege` in one temp table): both columns `f → t`, `inz_accredited_verified_at` stays `f`. Verified after applying: `authenticated` t/t/f, `anon` f/f/f. Carries a `COMMENT ON TABLE` recording the per-column-grant trap, because 091 will not be the last `ALTER TABLE` on this table. **Ships with the code half**: `EmployerOnboarding.tsx:103` moves from `console.error` to `reportError`, and `reportError` itself coerced non-Errors with `String(error)` — which is `[object Object]` for a PostgrestError, the very reason the alert was unreadable — so `toError()` fixes the coercion in the shared function rather than at the one call site. Connector-applied |

| 104_marketplace_view_stays_definer | `20260821075120` | **UPLIFT-95 Phase 3 item 1 — the proposed fix was NOT applied, and this row is why.** The work order asked for `ALTER VIEW public.marketplace_employer_profiles SET (security_invoker = true)`, backed by the advisor's one ERROR lint and by `059`, which did intend exactly that. Probed on prod in a rolled-back transaction first (§3, and the standing rule that an audit's fix is a hypothesis): the bare ALTER VIEW makes an anon SELECT on the view raise **SQLSTATE 42501, permission denied for table `employer_profiles`** — a hard failure, not a degraded read. Blast radius is the **public jobs board**: `JobSearch`, `V12Roles`, `OpenRolesSection` and `JobDetail` all reach employer data through `marketplace_employer_profiles!inner`, so `/jobs` renders **zero rows** for every anonymous visitor — the exact surface the first outreach recipients land on. The cause is the part the audit did not have: **`059` section 3 is only partly live.** Read off `pg_catalog` 2026-08-21 — the policy `"employer_profiles: public marketplace read"` **does not exist** (the table's only policy is `employers manage own`, TO authenticated); anon holds **no column grants at all** (`has_column_privilege('anon', …, 'farm_name', 'SELECT')` is false, and so is `has_table_privilege`); the view option is back to `false`. Only 059's **authenticated** grants survived (41 columns, `stripe_customer_id` correctly absent). So the lint is a true statement about a view whose scaffolding was reverted, not a report of a leak. A second probe applied the **full 059 restore** (policy + 12 anon column grants — 059's original 10 **plus `inz_accredited` and `inz_accreditation_expires`, which `091` added to the view's computed `accredited_employer` expression after 059 was written** — plus the ALTER VIEW) against a synthetic employer with one ACTIVE job: `before(definer)=1 after(invoker)=1`, visibility preserved. A third probe repeated it with the employer's ONLY job status **`filled`**: `before=1 after=0` — **the employer disappears.** That is a defect in 059's own design, not in the restore: the view's `WHERE EXISTS` accepts `('active','paused','filled','expired')`, but under invoker rights that subquery — and the restored policy's identical subquery — read `jobs` **as the caller**, whose jobs RLS is `status = 'active'` for anon and authenticated alike. The victim is not the marketplace (anon only ever reaches an employer via an active job anyway) but the **seeker's own history**: `MyApplications` and `SeekerDashboard` join this view for the farm name on applications already made, so filling a job — the success case, the outcome the product exists to cause — drops the farm name out of that seeker's record. Plausibly why 059 §3 was reverted originally. Current posture is **safe**: 10 marketing columns + a computed boolean, filtered to employers who chose to publish, no `user_id`, no contact details, no `stripe_customer_id` — definer rights over a narrow projection is the standard shape for a public gateway view. **This migration changes no behaviour**; it writes a `COMMENT ON VIEW` so the next advisor sweep meets the reasoning instead of the lint. Resolving the ERROR properly is an operator decision between three options, recorded in the file header: (a) widen the jobs SELECT policy to all four statuses — makes non-active listings publicly readable; (b) move the view's `EXISTS` into a SECURITY DEFINER helper so status filtering is owner-side, then apply the full 059 restore — keeps behaviour, adds a function; (c) keep definer semantics and suppress the lint. Verified after applying: `obj_description` 846 chars present, `reloptions` still `{security_invoker=false}`, `ep_policies` still 1, anon grant still false. Connector-applied |

| 105_marketplace_view_invoker_via_definer_helper | `20260821083108` | **Resolves what 104 held open — option (b) of the three it recorded, and the only one that neither breaks the page nor widens public reads.** (a) widening the anon `jobs` SELECT policy to all four statuses was REJECTED: it would make paused, filled and expired listings publicly readable through REST, and an employer who PAUSES a listing has deliberately taken it down. (c) was 104's hold. The trap 104 measured: under `security_invoker = true` the view's `WHERE EXISTS (…jobs…)` evaluates **as the caller**, whose jobs RLS is `status = 'active'`, so every filled or expired employer silently dropped out — **including out of `MyApplications`**, where a seeker reads the farm name on applications they already made. Filling a job is the success case; it must not erase the seeker's own history. Fix: `_employer_has_public_listing(uuid)`, `STABLE SECURITY DEFINER`, so the STATUS RULE is evaluated owner-side while the view respects caller privileges for the COLUMNS it returns — the visibility rule is a product decision and belongs to the owner, column access is a permission and belongs to the caller. Definer is also what prevents **42P17 recursion** (incident E8): the helper's read of `jobs` would otherwise re-enter `jobs: employers manage own jobs`, which subqueries `employer_profiles`. Established shape in this repo, not an invention. Also restores the rest of **059 §3**, which 104 found only partly live — the `public marketplace read` policy was absent and anon held **no column grants at all**; only 059's `authenticated` grants had survived. The anon grant is 059's original ten columns **plus `inz_accredited` and `inz_accreditation_expires`**, because `091` added the computed `accredited_employer` expression to the view AFTER 059 was written and an invoker-rights caller must be able to read the columns an expression consumes — granting 059's list verbatim would have failed on exactly that. `stripe_customer_id` stays absent from every grant. **The view keeps its own WHERE as well as the policy, deliberately**: redundant for anon/authenticated, NOT redundant for `service_role`, which bypasses RLS and would otherwise see employers with no public listing. Two enforcement points, one shared helper, no drift. **Proven on prod in a rolled-back transaction, three synthetic employers**: anon ACTIVE `1 -> 1`; anon **FILLED-only `1 -> 1`** (104's regression, fixed); anon NO-job `0 -> 0`; authenticated seeker sees the FILLED employer `1`; anon direct `stripe_customer_id` **blocked 42501**; anon direct `farm_name` readable; anon direct rows for a no-listing employer `0` (the policy holds off-view too). Uses `jobs_employer_id_idx`. **Verified after applying**: `reloptions` `{security_invoker=true}`, `prosecdef` true, 2 policies on `employer_profiles`, anon `farm_name`/`inz_accredited` true and `stripe_customer_id` **false**, anon EXECUTE on the helper true. **Live anon REST on prod**: the exact `JobSearch` query and the view direct both **200**; `select=*` and `select=stripe_customer_id` on the base table both **401/42501**. **`get_advisors(security)` now returns 0 ERROR** (was 1). Connector-applied |
| 106_deny_by_default_is_now_two_layers | `20260821083436` | **UPLIFT-95 Phase 3 item 2 — the audit asked for the intent to be written down, and writing it down required checking it, and checking it found a gap.** Intent CONFIRMED for all twelve: `grep -rn "from('<table>')" src/` returns **0 hits** for every one, so no client code touches any of them and every path is a definer RPC behind `_admin_gate()` or an Edge Function on the service-role key. But **ten of the twelve carried full table grants to `anon` AND `authenticated`** — SELECT, INSERT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER, MAINTAIN, seven privileges per role, read off `pg_class.relacl`. That includes **`leads` and `lead_staging`, holding 132 real farmers' names and contact details**, and `admin_audit_log`. So RLS-enabled-no-policy was **not the second layer — it was the only layer**: one permissive policy added for an unrelated feature, or RLS disabled during a debugging session, and anon could read and WRITE the lead list. Exactly the shape the audit warned twelve identical-looking INFO lints would hide. **`041`'s own header claims the opposite** — 'grants per the CORRECTED 037 pattern (REVOKE ALL FROM PUBLIC, anon; explicit grants only)' for `leads`, `lead_staging`, `lead_suppression` — and the live catalog disagreed. Same class of finding as 059 §3 in 104: a migration's stated intent was never achieved and nothing checked. Two tables prove the pattern works and needed no change: `application_status_transitions` (097) and `notification_sends` (102) already held zero client grants. **Why revoking is safe**: SECURITY DEFINER functions execute as their owner and do not consult the caller's table grants; Edge Functions use the service-role key, granted separately and untouched. **Proven on prod in a rolled-back transaction** rather than reasoned about: anon grants on `leads` `7 -> 0`; a definer RPC reading `leads` as anon **still returned all rows**; anon direct `SELECT FROM leads` **blocked 42501** — and the 42501 arrives at the GRANT layer, before RLS is consulted at all. Ships `COMMENT ON TABLE` for all twelve recording why each has no policy and why one would be unsafe (the audited must not read their own audit trail; the subject of an admin note must not read it; a party to a placement must not edit the fee they owe; deleting a `lead_suppression` row would resurrect someone who opted out). **Verified after applying**: all 12 at `client_privs = 0`, `service_privs = 8` intact, `documented = true`, `rls_on = true`. **Live anon REST on prod**: public jobs board and `jobs` still **200**; `leads`, `lead_staging`, `admin_audit_log`, `placements` all **401/42501**. Connector-applied |
| 107_application_events | `20260829024912` | **The application lifecycle gets a memory, and interview acceptance becomes a real mutation.** The 2026-08-29 journey audit left one product P2 on record — 'interview-accept is still a toast with no backend write; no application status history table' — and inspection made it worse: the seeker's interview DECLINE wrote `status='declined'`, an employer-only edge that 097's trigger has been rejecting since it shipped. Ships `application_events` (append-only; client INSERT revoked, SELECT scoped to the two parties via the applications relationship walks), `applications.interview_accepted_at`, three triggers (log on INSERT, log on `UPDATE OF status`, clear a stale acceptance on terminal→applied re-apply), and `accept_interview(uuid)` — SECURITY DEFINER, ownership via `seeker_profiles.user_id = auth.uid()`, status-gated to `interview`, idempotent, `FOR UPDATE` serialised, anon revoked. **Proven on prod in a rolled-back transaction** (A1–A8): insert event · transition events · wrong-owner rejected `no_data_found` · owner accepts + one event · idempotent re-call returns original stamp · non-interview status rejected `check_violation` · re-apply clears acceptance · seeker→hired still rejected (097 regression guard). Then proven live end-to-end with temp accounts (deleted): apply → review → interview → accept; both sides consistent across refresh. Notifications deliberately NOT built — the events table is the future hook. Connector-applied |

## Ledger rows with no dedicated file (documented duplicates)

Both are the "apply a delta, then amend the source file" pattern. **The content is on disk** — these
are not missing migrations. Verified 2026-07-30 by comparing the live function against the disk file.

| Ledger version | Name | Content lives in | Verification |
|---|---|---|---|
| `20260722232729` | `fix_get_applicants_for_job_joins` | `058_fix_admin_profile_doc_queue_applicants.sql:6-45` | live `get_applicants_for_job` carries `LEFT(a.id::text, 8)` + `v_employer_user_id`, matching 058 |
| `20260730063632` | `list_user_storage_objects` | `076_storage_purge_via_api.sql` §4 | applied as a separate connector call while probing admin-purge, then folded into 076 on disk |
| `20260811034524` | `signed_up_matches_short_ref` | `082_staging_type_filter_and_signup_attribution.sql` (the `signed_up` EXISTS clause) | live function compares `left(raw_user_meta_data->>'ref',8) = left(id::text,8)`, matching 082 on disk |
| `20260729095445` | `leads_list_expose_draft` | `064_lane_a_outreach_worklist.sql` (`admin_leads_list`) | live function exposes `drafted_email/draft_model/contacted_at`, has `follow_up_date`, orders by `status_changed_at DESC` — matching 064 |

This corrects audit finding **P0-8**, which claimed production schema existed outside version
control. It does not. The defect was ledger bookkeeping only — reclassified **P1**.
